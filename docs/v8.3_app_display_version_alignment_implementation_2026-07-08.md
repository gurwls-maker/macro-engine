MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-07-v1
- source_ledger_checked: yes, via current truth and app display version alignment decision
- superseded_docs_checked: yes, v8.2 archive status checked through document status index
- external_anchor_checked: not applicable, no scoring anchor or nutrition formula change

DOCUMENT ROLE
- implementation_log

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview mainline
- preview field storage/UI/Recent/DailyCoach exposure

# v8.3 app display version alignment implementation

Date: 2026-07-08
Type: implementation log

## Purpose

This implementation closes the app display version alignment gate.

The app already uses the v8.3 production scoring identity:

- `MACRO_RANGE_PRODUCTION_SCORING_VERSION = "v8.3_anchor_continuous_macro_score_v1"`
- `ADHERENCE_SCORING_VERSION = MACRO_RANGE_PRODUCTION_SCORING_VERSION`

Before this branch, the user-facing app label still showed `v8.0`. That made the app look like the v8.3 scoring work was not actually active. This branch aligns the visible app version metadata to `v8.3`.

## Implemented

- Updated the HTML document title from `탄단지 다이어리 v8.0` to `탄단지 다이어리 v8.3`.
- Updated the home header app version badge from `v8.0` to `v8.3`.
- Updated `getCurrentAppVersionLabel()` fallback from `v8.0` to `v8.3`.
- Updated full-backup `appVersion` expectations to `v8.3`.
- Updated display-version internal tests to assert `v8.3`.
- Updated the top file comment to state `Version: 8.3` and `Stage: v8.3 display alignment`.

## Deliberately unchanged

- `package.json` package version remains unchanged.
- `FULL_BACKUP_VERSION` remains `1`.
- `MACRO_RANGE_PRODUCTION_SCORING_VERSION` remains `v8.3_anchor_continuous_macro_score_v1`.
- `ADHERENCE_SCORING_VERSION` still aliases `MACRO_RANGE_PRODUCTION_SCORING_VERSION`.
- Backup payload structure remains unchanged.
- Storage/schema, UI layout, Recent, DailyCoach, scoreDeltaPreview, and old-record migration remain closed.
- Historical v8.0/v8.x test fixture names and legacy scenario constants were not globally replaced.

## Minimum scope versus complete implementation

Minimum scope:

- Change the visible title and header badge.
- Change the fallback returned by `getCurrentAppVersionLabel()`.
- Keep scoring, backup schema, and package metadata unchanged.

Complete implementation for this gate:

- Also update tests that protect the title, header badge, version helper, top comment, and full-backup `appVersion`.
- Update current truth and README routing so the gate is no longer pending.

This branch does the complete implementation for the display alignment gate without opening unrelated product work.

## Validation targets

Required:

- `npm run test:docs-policy`
- `npm test`
- `npm run test:core`
- `npm run test:ui`
- `npm run test:macro-policy`
- `npm run test:daily-coach`
- `npm run test:full`
- `git diff --check`

Static checks:

- `index.html` title/header/helper use `v8.3`.
- Full-backup `appVersion` expectations use `v8.3`.
- `FULL_BACKUP_VERSION = 1` remains unchanged.
- `package.json` package version remains unchanged.
- No scoring formula or scoring version change is included in this branch.

## Next step

The next natural step is a v8.3 implementation QA closeout or regression checkpoint.

Do not jump from this branch into score tuning, storage/schema, UI expansion, scoreDeltaPreview, or old-record migration. Those remain separate gates.

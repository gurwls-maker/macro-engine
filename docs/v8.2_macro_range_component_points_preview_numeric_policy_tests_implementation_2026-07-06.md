# v8.2 macro range component pointsPreview numeric policy tests implementation

상태:

```text
작업 성격: component pointsPreview numeric policy test-only guard 구현
index.html 변경: 테스트 suite 추가만 있음
package.json 변경: test:macro-policy suite 연결
tools/render_audit core profile 변경: suite 연결
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
storage/schema 구현: 없음
backup / Recent / UI / DailyCoach production 연결: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_component_points_preview_numeric_policy_test_design_2026-07-06.md`의 후속 test-only 구현 기록이다.

목적은 component `pointsPreview` 숫자화를 여는 것이 아니라, future numeric policy가 아직 production-facing field로 승격되지 않았음을 테스트로 고정하는 것이다.

## 1. 구현 요약

추가:

```text
runMacroRangeComponentPointsPreviewNumericPolicyDesignTests
```

등록:

```text
window.runMacroRangeComponentPointsPreviewNumericPolicyDesignTests
npm run test:macro-policy suite 목록
tools/render_audit/run_internal_tests.cjs core profile
```

suite 성격:

```text
test-only guard
suite 내부 fixture
suite 내부 promoted payload rejection
before/after side-effect comparison
production helper 변경 없음
```

## 2. 테스트 고정 범위

이번 suite는 다음을 고정한다.

```text
component lane inventory
severity lane inventory
fixture category inventory
assertion group inventory
production-facing pointsPreview null
candidateScorePreview null
scoreDeltaPreview null
dataQuality null-only
neutral / explanation no-bonus
old fixed Records no-mutation
promoted pointsPreview payload rejection
backup / Recent / UI / DailyCoach no-impact
ADHERENCE_SCORING_VERSION unchanged
```

## 3. 테스트 케이스

추가된 케이스:

```text
v8.2 component pointsPreview numeric policy test design inventory stays closed
v8.2 component pointsPreview numeric policy keeps eligible component statuses nonnumeric
v8.2 component pointsPreview numeric policy blocks non-full-day and impossible records
v8.2 component pointsPreview numeric policy preserves old fixed Records and no-bonus semantics
v8.2 component pointsPreview numeric policy rejects promoted production-facing payloads
v8.2 component pointsPreview numeric policy has zero production consumer side effects
```

총 6개 케이스다.

## 4. component status guard

테스트에서 확인하는 component status:

```text
kcal matched / high / low
protein satisfied / low
fat below_action_floor / below_recommended / inside_recommended / above_recommended / above_flexible
carbExchange exchange_explained / below_training_support / above_candidate_range
dataQuality full_day_candidate / one_meal_pending / insufficient_record / snapshotless_debug_only / blocked_by_impossible_state
```

확인:

```text
각 component pointsPreview는 null
candidateScorePreview는 null
scoreDeltaPreview는 null
production-facing top-level numeric field 없음
```

## 5. no-bonus guard

neutral / explanation 상태:

```text
kcal matched
protein satisfied
fat inside_recommended
carbExchange exchange_explained
carbExchange inside_candidate_range
dataQuality full_day_candidate
```

확인:

```text
production-facing pointsPreview null
positive bonus 없음
candidateScorePreview 합산 없음
scoreDeltaPreview 비교 없음
```

참고:

```text
이미 존재하는 testLocalNumericPreview는 nested test-local source object로만 남는다.
이번 suite는 그 값을 production-facing pointsPreview로 승격하지 않는다.
```

## 6. blocked / dataQuality guard

blocked fixture:

```text
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
```

확인:

```text
dataQuality.pointsPreview null
nested testLocalCandidateScorePreview null
nested testLocalComponentPoints null
blocked 상태를 sentinel 0으로 표현하지 않음
```

## 7. old fixed Records guard

old fixed detailed fixture를 사용한다.

확인:

```text
stored adherencePercent 유지
stored adherenceScoringVersion 유지
fixed score summary 유지
old fixed record object no-mutation
old fixed record에 pointsPreview / candidateScorePreview / scoreDeltaPreview 추가 없음
```

즉 sufficient detailed old fixed Record는 transient eligibility 후보가 될 수 있지만, 저장값은 fixed score basis로 유지한다.

## 8. promoted payload rejection

suite 내부에서 금지 payload를 만든다.

포함:

```text
top-level pointsPreview
top-level candidateScorePreview
top-level scoreDeltaPreview
rangeScoreCandidateSummary.candidateScorePreview
componentPreviews.kcal.pointsPreview
comparison.scoreDeltaPreview
goalSnapshot.pointsPreview
backupPayloadFragment.pointsPreview
```

확인:

```text
promoted production-facing numeric shape는 guard violation으로 잡는다.
```

## 9. side-effect guard

candidate 생성 전후로 비교한다.

확인:

```text
getDailyAdherenceScore percent unchanged
scoringVersion unchanged
buildFullBackupPayload unchanged
backup string에 pointsPreview / candidateScorePreview / scoreDeltaPreview 없음
getRecentContextComponentCounts unchanged
rendered body text unchanged
generateDailyCoachOutput unchanged
recordsState.items unchanged
ADHERENCE_SCORING_VERSION unchanged
```

## 10. 계속 금지

이번 작업에서 계속 금지:

```text
production component point 계산
candidateScorePreview 숫자화
pointsPreview 숫자화
scoreDeltaPreview 숫자화
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
fixed score weighting 변경
DailyCoach decision matrix 변경
Today score UI 변경
Records score display 변경
Recent aggregation 변경
backup / restore payload 변경
goalSnapshot에 score candidate 저장
macroRangeContract 저장
old fixed Records saved score overwrite
old fixed Records backup/schema migration
```

## 11. 검증 기준

필수:

```text
node tools/render_audit/run_internal_tests.cjs --suite=runMacroRangeComponentPointsPreviewNumericPolicyDesignTests
npm run test:macro-policy
npm run test:core
git diff --check
```

## 12. 최종 판단

현재 결론:

```text
component pointsPreview numeric policy test-only guard: 구현 완료
component pointsPreview numeric readiness: 아직 아님
candidateScorePreview numeric readiness: 아직 아님
scoreDeltaPreview readiness: 아직 아님
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
component pointsPreview numeric policy는 아직 production 숫자화가 아니라, null / no-bonus / no-leak / no-mutation 금지선을 test-only로 고정한 상태다.
```

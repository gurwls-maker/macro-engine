# v8.2 macro range score Records basis / candidate version tests implementation

상태:

```text
작업 성격: Records basis / candidate version decision test-only guard
index.html 변경: 테스트 suite 추가만 있음
package.json 변경: test:macro-policy suite 연결
runner 변경: core profile suite 연결
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
storage/schema 구현: 없음
backup / Recent / UI / DailyCoach 연결: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_score_records_basis_version_decision_2026-07-05.md`의 후속 test-only 구현 기록이다.

목적은 `candidateScorePreview`, component `pointsPreview`, `scoreDeltaPreview`를 숫자로 만드는 것이 아니라, old/new Records score basis와 candidate score version naming decision을 테스트로 고정하는 것이다.

## 1. 추가 suite

추가:

```text
runMacroRangeScoreRecordsBasisVersionDecisionTests
```

등록:

```text
window.runMacroRangeScoreRecordsBasisVersionDecisionTests
tools/render_audit/run_internal_tests.cjs core profile
package.json test:macro-policy
```

## 2. 테스트가 고정한 것

이번 suite는 다음을 테스트한다.

```text
old fixed Records score basis = fixed_adherence
current unknown range field handling = prune
future range-basis Records status = not_current
candidate score preview version은 ADHERENCE_SCORING_VERSION과 분리
old fixed Records fixed target snapshot 유지
old fixed Records adherencePercent / adherenceScoringVersion 의미 유지
unknown macroRangeContract / rangeScoreCandidate / preview fields no-leak
candidateScorePreview null 유지
pointsPreview null 유지
scoreDeltaPreview null 유지
production score / scoring version / snapshot signature / backup / Recent / UI / DailyCoach no-impact
```

## 3. version naming guard

테스트에서 고정한 이름:

```text
productionFixedScoreVersion = ADHERENCE_SCORING_VERSION
reportOnlyCandidateVersion = range_score_candidate_report_only_v0
futureCandidateScorePreviewVersion = range_score_candidate_preview_test_local_v0
futureCandidateScoreBasisLabel = macro_range_candidate_preview_test_local
fixedScoreBasisLabel = fixed_adherence:v6.1_alcohol_penalty_v2
```

현재 helper output에 유지되는 것:

```text
rangeScoreCandidateSummary.candidateVersion = range_score_candidate_report_only_v0
candidateScorePreview = null
scoreDeltaPreview = null
component pointsPreview = null
```

현재 helper output에 없는 것:

```text
candidateScorePreviewVersion
candidateScoreBasisLabel
fixedScoreBasisLabel
```

## 4. 계속 금지

이번 작업에서 계속 금지:

```text
numeric candidate score helper 구현
candidateScorePreview 숫자화
pointsPreview 숫자화
scoreDeltaPreview 숫자화
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
normalizeGoalSnapshot 변경
GOAL_SNAPSHOT_SIGNATURE_KEYS 변경
backup / restore schema 변경
Recent aggregation 변경
DailyCoach action 변경
Today / Records / Recent UI score 표시 변경
macroRangeContract 저장
macroRangeBasisSignature 저장
old fixed Records range score 재해석
```

## 5. 다음 단계

가능한 다음 단계:

```text
1. 외부 감사
2. test-local numeric helper entry decision docs-only
3. numeric helper implementation scope test-only
4. macro range line hold
```

아직 numeric score formula readiness / production score readiness / storage schema readiness / production macro range readiness는 아니다.

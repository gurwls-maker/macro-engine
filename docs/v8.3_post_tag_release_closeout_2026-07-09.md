MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-07-v1 plus v8.3 post-tag state
- source_ledger_checked: yes, through current truth routing and document status index
- superseded_docs_checked: yes, v8.2 macro docs status checked through document status index and archive README
- external_anchor_checked: no new external anchor introduced; this closeout changes no scoring policy

DOCUMENT ROLE
- decision
- docs-only post-tag release closeout
- release state snapshot

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview production-path promotion
- preview field storage/UI/Recent/DailyCoach exposure

# v8.3 post-tag release closeout

작성일: 2026-07-09
문서 성격: docs-only post-tag release closeout
구현 변경: 없음
tag 변경: 없음
score tuning 구현: 없음
UI/storage/schema 변경: 없음

## 1. 목적

이 문서는 `v8.3` merge/tag 작업이 끝난 뒤의 release 상태를 current truth에 반영한다.

이번 작업은 새 기능 구현이 아니다.
이번 작업은 tag 재생성/이동도 아니다.
이번 작업은 score tuning, UI/storage/schema, scoreDeltaPreview, old records migration을 여는 작업이 아니다.

핵심 원칙:

```text
minimal surface, complete post-tag closeout
```

뜻:

```text
- 구현 surface는 열지 않는다.
- tag는 건드리지 않는다.
- v8.3 release 상태, tag target, current truth gate, post-v8.3 backlog는 반쪽으로 남기지 않는다.
```

## 2. release refs

확인한 release refs:

```text
source branch: codex/v8.3-stabilization-tag-readiness-checkpoint-update
source branch HEAD: c4a64aa v8.3 stabilization/tag readiness checkpoint 갱신
target branch: master
merge commit: 0333d9c v8.3 안정화 브랜치 병합
tag name: v8.3
tag type: annotated tag
tag message: v8.3 anchor continuous macro scoring stabilization
tag target commit: 0333d9c
remote master: 0333d9c
remote v8.3 peeled target: 0333d9c
```

판단:

```text
v8.3 merge/tag instruction: completed
tag target is merge commit: yes
source branch commit tag target: no
tag rewrite needed: no
```

## 3. 검증 요약

source branch tag 전 검증:

```text
docs-policy: passed
smoke: 20 suites / 191 cases / failed 0
core: 62 suites / 732 cases / failed 0
ui: 25 suites / 181 cases / failed 0
macro-policy: 29 suites / 235 cases / failed 0
daily-coach: 1 suite / 83 cases / failed 0
mobile: 19 suites / 145 cases / failed 0
full: 138 suites / 1394 cases / failed 0
git diff --check: passed
git diff --check HEAD^ HEAD: passed
```

merge 후 master 검증:

```text
docs-policy: passed
smoke: 20 suites / 191 cases / failed 0
core: 62 suites / 732 cases / failed 0
ui: 25 suites / 181 cases / failed 0
macro-policy: 29 suites / 235 cases / failed 0
daily-coach: 1 suite / 83 cases / failed 0
mobile: 19 suites / 145 cases / failed 0
full: 138 suites / 1394 cases / failed 0
git diff --check: passed
```

검증 해석:

```text
source branch와 merge commit 양쪽에서 v8.3 release block이 같은 test coverage를 통과했다.
docs-policy, macro-policy, DailyCoach/copy, UI/mobile/full regression이 모두 failed 0이다.
```

## 4. v8.3 release 범위

v8.3 included:

```text
- anchor continuous macro scoring
- target/scoring alignment
- Today macro card range display
- source visibility
- DailyCoach/copy regression guard
- WATCH regression guard
- legacy/reference routing
- app display v8.3 alignment
- docs-policy guards
- test profile hardening
```

v8.3 release meaning:

```text
fixed g target / fixed penalty table 중심에서 벗어나
외부근거 anchor와 연속 곡선 기반의 range-aware scoring으로 전환한 release.
```

v8.3 release does not mean:

```text
- package.json version bump
- FULL_BACKUP_VERSION bump
- storage/schema expansion
- scoreDeltaPreview mainline 아님
- old records migration 아님
- all future UI/copy polishing completed 아님
```

## 5. 의도적으로 보류한 항목

post-v8.3 deferred:

```text
- score distribution tuning beyond alignment
- DailyCoach/copy rewrite
- tooltip/glossary
- kcal range display
- alcohol range display
- UI/storage/schema expansion
- scoreDeltaPreview optional audit
- old records migration/recompute/reset
- package version policy
```

중요:

```text
위 항목들은 v8.3 release blocker가 아니다.
v8.3 이후 backlog 또는 v8.3.1 planning 후보로 분리한다.
```

## 6. tag handling decision

폐기:

```text
- v8.3 tag를 다시 만들기
- v8.3 tag를 다른 commit으로 옮기기
- package.json version을 뒤늦게 v8.3으로 맞추기
- FULL_BACKUP_VERSION 변경
- post-tag closeout에 새 기능/튜닝 섞기
```

유지:

```text
- annotated tag v8.3
- tag target commit 0333d9c
- master no-ff merge commit traceability
- source branch commit c4a64aa included through merge commit
```

## 7. 수용 / 폐기 / 통합 / 보류

수용:

```text
- v8.3 annotated tag 유지
- tag target merge commit 유지
- master no-ff merge 유지
- v8.3 release block closeout
- post-v8.3 backlog 분리
```

폐기:

```text
- tag 재작성
- post-tag closeout에 새 기능 섞기
- package/FULL_BACKUP_VERSION 동시 변경
- score tuning/UI/storage/schema를 closeout에 섞기
```

통합:

```text
- merge commit / tag / pushed refs를 release state로 통합
- source branch 검증과 merge 후 검증을 release evidence로 통합
- post-v8.3 deferred items를 release blocker가 아니라 backlog로 통합
```

보류:

```text
- score tuning
- DailyCoach/copy rewrite
- UI/storage/schema
- scoreDeltaPreview optional audit
- old records migration/recompute/reset
- package version policy
```

금지선:

```text
- tag 재생성/이동 금지
- tag 삭제 금지
- v8.2 fixed penalty table production body 부활 금지
- exercise bonus 금지
- v6.1 alcoholImpactPenalty 후처리 부활 금지
- scoreDeltaPreview mainline 재개 금지
- preview field storage/UI/Recent/DailyCoach 노출 금지
- old records hidden recompute / silent mutation 금지
```

## 8. 다음 단계

내 판단 다음 단계:

```text
post-v8.3 backlog triage 또는 v8.3.1 planning
```

하지 말 것:

```text
- 바로 score tuning implementation
- 바로 broad UI redesign
- 바로 storage/schema 확장
- 바로 scoreDeltaPreview 작업
- old records migration/recompute/reset
```

다음 작업은 v8.3 release를 수정하는 작업이 아니라, v8.3 이후 무엇을 v8.3.1로 가져갈지 분류하는 작업이어야 한다.

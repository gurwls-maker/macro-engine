02 macro range current truth
작성일: 2026-07-07
문서 성격: v8.3 macro scoring 현재 기준

이 문서는 짧고 강한 current truth다.
상세 근거는 _source master plan을 보되, 실제 작업 판단은 이 문서를 우선한다.

현재 본류
- v8.3 anchor-based continuous macro scoring으로 간다.
- v8.2 fixed penalty score는 range-aware 전환 실험 / 중간 구현 / superseded 예정 상태다.
- v8.3은 고정 g 목표나 상태별 고정 감점표가 아니라, 외부근거 anchor와 연속 곡선을 사용한다.
- active scoring version은 `v8.3.1_target_authority_continuous_macro_score_v2`, formula는 `anchor_continuous_macro_score_v2_target_authority`다. `v8.3_anchor_continuous_macro_score_v1`은 pre-correction historical identity다.

유지
- fixed g target 문제의식.
- range-aware macro scoring 방향.
- protein anchor.
- carb/fat flexibility.
- dataQuality null gate.
- no sentinel 0.
- preview no-leak.
- scoreDeltaPreview product path rejected. old fixed score 비교는 앱 제품 backlog가 아니다.
- storage/schema/UI/Recent/DailyCoach 확장은 별도 gate.
- TDEE time ownership production 사실.
- 운동 소모는 목표 칼로리/TDEE에 이미 반영된다는 사실.

폐기 / supersede
- high = -10, severe = -16 같은 상태별 고정 감점표를 production body로 쓰는 것.
- candidateScorePreview = clamp(100 + fixed component point sum, 0, 100)를 final policy로 쓰는 것.
- 중간 score cap.
- 즉시 0점 hard threshold 남발.
- v6.1 alcoholImpactPenalty 후처리 부활.
- 운동 보너스.
- scoreDeltaPreview product path.
- old fixed records 보존을 이유로 새 산식 전환을 막는 것.
- kcal range display를 탄단지 range UI처럼 여는 것.

v8.3 산식 방향
finalRawScore =
  100
  - targetEnergyDeviationPenalty
  - tdeeOverloadPenalty
  - proteinShortagePenalty
  - proteinExcessEfficiencyPenalty
  - fatRangePenalty
  - carbExerciseContextPenalty
  - carbFatExchangeFailurePenalty
  - alcoholPhysiologyPenalty
  - dataOutlierPenalty

finalScore = clamp(finalRawScore, 0, 100)

중요:
- 각 penalty는 고정 감점표가 아니다.
- 각 penalty는 중간 cap이 아니다.
- 각 penalty는 외부근거 anchor와 앱 목표를 기반으로 한 연속 곡선이어야 한다.
- 마지막 0~100 clamp는 정책 cap이 아니라 표시 범위다.

외부근거 사용 원칙
- 외부근거는 hard cutoff가 아니라 curve anchor다.
- 외부근거는 점수 자체를 주지 않으므로, 앱 정책 판단은 fixture QA로 제한한다.
- 주관을 완전히 제거할 수는 없지만, 주관이 끼어드는 지점을 external anchor + fixture QA로 제한한다.

Anchor table v1
- NIAAA alcohol:
  - standard drink: pure alcohol 14g.
  - binge drinking: 일반적으로 남성 5잔 이상, 여성 4잔 이상을 약 2시간 안에 마시는 패턴.
  - high-intensity drinking: binge threshold의 약 2배 이상.
  - 사용 방식: alcoholPhysiologyPenalty 곡선 anchor. hard cutoff 아님.

- ISSN protein:
  - 1.4~2.0 g/kg/day: 대부분의 운동자에게 충분한 일반 범위.
  - 2.3~3.1 g/kg/day: hypocaloric period resistance-trained subjects의 제지방 보존 후보.
  - >3.0 g/kg/day: 일부 저항훈련자 체성분에 긍정적 가능성 언급.
  - 사용 방식: shortage는 강하게, excess는 완만하게.

- ISSN nutrient timing / carbohydrate:
  - glycogen storage/recovery 맥락에서 8~12 g/kg/day 고탄수 섭취가 언급된다.
  - 사용 방식: carbExerciseContextPenalty 곡선 anchor. hard zero 아님.

- AMDR / DRI:
  - macronutrient를 total energy 대비 범위로 본다.
  - 사용 방식: fat/carbs/protein을 단일 g target이 아니라 range/risk 관점으로 해석.

알코올
- 알코올 영향을 무시하지 않는다.
- alcohol kcal는 totalKcal와 TDEE overload에 반영한다.
- alcoholG / standard drinks / bodyWeightKg 등은 alcoholPhysiologyPenalty에 반영한다.
- 이것은 이중감점이 아니다. energy 축과 recovery/sleep/metabolic risk 축이 다르기 때문이다.
- v6.1 alcoholImpactPenalty 후처리는 되살리지 않는다.

운동 / 탄수
- 운동은 finalScore bonus가 아니다.
- 운동 소모는 이미 target/TDEE에 반영된다.
- 운동은 carb curve context modifier다.
- 운동 맥락은 탄수 곡선 anchor를 이동시킬 수 있다.
- 고탄수라는 이유만으로 hard zero 처리하지 않는다.
- high-volume context라도 totalKcal/TDEE overload가 크면 curve-mediated collapse는 가능하다.
- target/scoring/card display는 같은 macro range helper를 봐야 한다.
- 8~12g/kg carb anchor는 high-volume flat no-penalty permission이 아니다.
- Today adaptive target이 켜져 있으면 탄수화물과 지방 목표는 같은 kcal 안에서 실시간 교환될 수 있다.
- 반복 고탄수/고지방 흐름은 3일/7일 trend limiter로 adaptive target 이동폭을 줄인다.
- 3일/7일 trend limiter는 count bucket만으로 갑자기 꺾이는 계단식이 아니라, 최근 기록의 초과 정도와 날짜 가까움을 반영하는 continuous pressure 모델이어야 한다.
- 자동 운동 context는 descriptive label이 바뀌는 순간 탄수 범위가 뛰면 안 된다. 실제 carb context는 `continuous_training_load_interpolation`으로 rest -> normal -> high anchor를 보간한다.
- adaptive target OFF이면 Today는 fixed target을 유지한다.
- Today adaptive target은 live display/score interpretation이며 Records goalSnapshot을 덮어쓰지 않는다. snapshot은 selected calculation target을 보존한다.
- Today adaptive target ON/OFF는 persistent setting surface다. 대규모 schema migration은 아니지만 저장되는 설정 field가 추가된 사실은 결과로그에서 명시한다.
- Today macro card의 탄수/지방 range chip은 더 이상 단순 independent marginal range로 열지 않는다. v8.3.1 구현 이후 card는 `jointAllocationModel.conditionalDisplayRanges`를 우선 읽고, 현재 탄수 섭취가 높으면 지방 upper가 내려가며 현재 지방 섭취가 높으면 탄수 upper가 내려간다.
- 탄수/지방 교환은 gram 1:1이 아니라 calorie-equivalent exchange다. 고정 kcal와 protein 조건에서는 carb가 올라가면 fat 여유가 kcal 기준으로 내려가며, fat 1g은 carb 약 2.25g에 해당한다.
- 사용자-facing 설명문으로 산식/제품 의미 결정을 대체하면 안 된다. 앱 내부 모델이 맞게 계산하고 화면은 결과 숫자만 자연스럽게 보여준다.
- carb/fat joint allocation의 채택/구현 모델은 `protein-reserved iso-calorie carb/fat joint allocation with conditional feasible display ranges`다. protein을 먼저 reserve하고, 남은 kcal 안에서 carbs/fat target pair와 display range를 같은 helper로 계산한다.
- card range chip은 `raw physiological range ∩ iso-calorie feasible interval`을 바탕으로 한 conditional feasible range여야 한다. 탄수를 이미 많이 먹으면 fat upper가 내려가고, 지방을 이미 많이 먹으면 carb upper가 내려간다.
- 이 문제는 copy/help/tooltip으로 해결하지 않는다. 사용자에게 내부 calorie-exchange arithmetic을 설명하지 말고, 앱이 맞는 target/range 숫자를 계산한다.
- 핵심 표현: hard-collapse 금지, curve-mediated collapse 허용.

단백질
- 부족은 강하게 본다.
- 초과는 지방/알코올처럼 강하게 벌하지 않는다.
- 초과는 효율 저하, 칼로리 배분 문제, data outlier 후보로 완만하게 본다.
- 단백질은 carb/fat exchange 참여자가 아니라 anchor다.
- Today protein card는 range chip을 보여주지 않고 selected protein target level을 보여준다.
- `proteinTargetLevel`은 persistent setting surface다.
- 사용자 선택지는 `low` / `medium` / `high` 세 단계이며 `medium`이 유일한 기본값이자 mode/context-selected external macro production policy의 자동 권장값이다.
- 예전 저장값/백업의 `default`는 호환 처리를 위해 `medium`으로 normalize한다. UI에 `기본`과 `중간`을 중복 노출하지 않는다.
- `low`와 `high`는 별도 goal 하드코딩 표가 아니라 운동함/운동 안 함 및 lean/advanced 맥락으로 선택된 기존 policy range의 하한/상한에서 파생한다.
- 일반 `high` 설정은 2.3~3.1g/kg FFM contest-prep 예외를 열지 않는다. 이 예외는 lean resistance-trained hypocaloric + 신뢰 가능한 FFM 조건을 별도 gate로 확인하기 전에는 사용하지 않는다.
- protein level은 external macro production candidate 선택 단계에서 적용한다. external policy 적용 후 final protein/carbs/fat을 다시 덮어쓰면 실패다.
- 최종 `protein/carbs/fat`과 `externalMacroActiveProductionApplication` 메타데이터는 같아야 한다. high 선택도 일반 지방 하한과 훈련 탄수 guard를 우회하지 않는다.

지방
- targetFatUpper, fatG/bodyWeightKg, fatKcal/totalKcal, totalKcal/TDEE를 함께 본다.
- fat 1000g 같은 극단은 fatRangePenalty + tdeeOverloadPenalty가 함께 커져 자연스럽게 0점 근처로 가야 한다.
- fat above = -12에서 멈추면 실패다.

데이터 / records
- no meals, one meal, insufficient, snapshotless, range policy missing은 null/blocked다.
- 계산 불가를 sentinel 0으로 쓰지 않는다.
- scoring version과 무관하게 저장된 auto adherencePercent + goalSnapshot은 기록 조회 시 저장값을 우선한다.
- 식사 변경, 기준 갱신, explicit regrade/persistence, Today live score처럼 명시적 이벤트가 있을 때만 current v8.3.1 산식으로 재계산한다.
- v8.3 scoringContext는 body weight와 totalBurn source를 노출해야 한다.
- targetProteinFallback과 missing totalBurn은 authoritative source처럼 숨기지 않는다.
- missing totalBurn/TDEE는 score curve tuning 문제가 아니다.
- full macro + missing TDEE는 target adherence score를 낼 수 있지만, TDEE overload 축이 빠진 source warning 대상으로 본다.
- targetProteinFallback은 score를 막는 null gate가 아니라 inferred weight source warning 대상으로 본다.
- no meals, one meal, insufficient, snapshotless, range policy missing은 source warning이 아니라 null/blocked gate다.
- source visibility 구현은 existing scoringContext를 해석하는 narrow implementation이어야 하며, storage/schema 확장으로 시작하지 않는다.
- old fixed Records no silent mutation은 전환 중 안전장치다.
- old fixed Records는 v8.3 scoring policy를 막는 제품 요구가 아니다.
- dev/local old records는 보존/마이그레이션 기본값이 아니라 explicit cleanup/reset/fallback decision 대상이다.
- 사용자 명시 없이 hidden recompute / silent mutation / silent delete는 금지한다.

테스트/문서 관성 처리
- v8.2 macro range 원문 65개는 docs/archive/v8.2_macro_range/로 이동했다.
- root docs에 v8.2_macro_range_*.md가 다시 생기면 관성 재유입으로 보고 docs-policy 실패로 본다.
- v8.2 fixed-point helper가 남아 있을 수는 있지만 historical guard evidence로만 둔다.
- historical helper는 current v8.3 scoringVersion을 반환하면 안 된다.
- v8.3 scoring correctness는 continuous scoring tests가 담당한다.

현재 구현 gate
- v8.3 fixture direction table은 docs/v8.3_fixture_direction_table_2026-07-07.md로 닫혔다.
- v8.3 continuous scoring test design은 docs/v8.3_continuous_scoring_test_design_2026-07-07.md로 닫혔다.
- v8.3 implementation entry decision은 docs/v8.3_implementation_entry_decision_2026-07-07.md로 닫혔다.
- v8.3 continuous scoring implementation은 docs/v8.3_continuous_scoring_implementation_2026-07-07.md로 구현됐다.
- v8.3 implementation regression audit은 docs/v8.3_implementation_regression_audit_2026-07-07.md로 닫혔다.
- v8.2 root docs archive/link review는 docs/archive/v8.2_macro_range/README.md와 docs-policy guard로 닫혔다.
- v8.3 score distribution tuning audit은 docs/v8.3_score_distribution_tuning_audit_2026-07-07.md로 닫혔다.
- v8.3 score distribution tuning decision은 docs/v8.3_score_distribution_tuning_decision_2026-07-07.md로 닫혔다.
- v8.3 score source visibility decision은 docs/v8.3_score_source_visibility_decision_2026-07-07.md로 닫혔다.
- v8.3 source visibility implementation은 docs/v8.3_source_visibility_implementation_2026-07-07.md로 구현됐다.
- v8.3 source visibility QA profile hardening은 docs/v8.3_source_visibility_qa_profile_hardening_2026-07-07.md로 구현됐다.
- v8.3 target/scoring alignment implementation은 docs/v8.3_target_scoring_alignment_implementation_2026-07-08.md로 구현됐다.
- v8.3 target/scoring alignment QA closeout은 docs/v8.3_target_scoring_alignment_qa_closeout_2026-07-08.md로 닫혔다.
- v8.3.1 scoring tuning protocol decision은 docs/v8.3.1_scoring_tuning_protocol_decision_2026-07-09.md로 닫혔다.
- v8.3.1 scoring tuning evidence pack은 docs/v8.3.1_scoring_tuning_evidence_pack_2026-07-09.md로 닫혔다.
- v8.3.1 scoring tuning objective rubric decision은 docs/v8.3.1_scoring_tuning_objective_rubric_decision_2026-07-09.md로 닫혔다.
- v8.3.1 scoring tuning curve candidate simulation decision은 docs/v8.3.1_scoring_tuning_curve_candidate_simulation_decision_2026-07-09.md로 닫혔다.
- v8.3.1 score tuning은 아직 숫자 구현이 아니다. objective score band와 current-output simulation 기준으로는 `current_curve_with_guarded_outputs`를 유지하고, anchor 값/curve steepness/adaptive limiter 변경은 열지 않는다.
- TDEE/fat/carb/high-volume/UNKNOWN 후보는 현재 numeric tuning 후보가 아니라 guarded-output 유지 상태다. Adaptive limiter는 방향은 유지하되, 나중에 조정하려면 별도 3일/7일 high-carb/high-fat/mixed fixture sampling gate가 필요하다.
- v8.3.1 user-facing range explanation/copy decision은 docs/v8.3.1_user_facing_range_explanation_copy_decision_2026-07-09.md로 닫혔다.
- Today card는 compact range/status surface로 유지한다. DailyCoach는 해석과 행동을 맡는다. Tooltip/glossary는 broad implementation이 아니라 별도 작은 spec 이후에만 연다.
- v8.3.1 DailyCoach/range copy naturalness implementation은 docs/v8.3.1_dailycoach_range_copy_naturalness_implementation_2026-07-09.md로 구현됐다. 알려진 위험이던 "회복 재료" / "성장 재료" 계열 문구와 이를 고정한 test expectation은 DailyCoach/range-adjacent surface에서 제거했다.
- app-wide user-facing copy naturalness inventory / decision은 docs/v8.3.1_app_wide_user_facing_copy_inventory_decision_2026-07-09.md로 닫혔다. 이 작업은 grep-only 치환이 아니라 앱 전체 사용자 문구를 surface / exact current text / issue type / decision / next action으로 분류한 inventory-first gate다.
- v8.3.1 app-wide copy naturalness batch 1 implementation은 docs/v8.3.1_app_wide_copy_naturalness_batch_1_implementation_2026-07-09.md로 구현됐다. source visibility notes, DailyCoach leftover "재료" family, Settings/low-digest copy, backup warning, InBody help/trend phrase, validation spacing/tone, Settings hero를 좁게 자연화했고, 어색한 정확 문구 테스트는 meaning-first expectation으로 갱신했다.
- v8.3.1 carb-fat exchange range consistency audit/design은 docs/v8.3.1_carb_fat_exchange_range_consistency_audit_design_2026-07-10.md로 닫혔다. 이 문서는 기존 card chip이 marginal range였고, 사용자 설명이 아니라 joint allocation model이 필요하다고 정리했다.
- v8.3.1 carb-fat exchange joint allocation model decision/design은 docs/v8.3.1_carb_fat_exchange_joint_allocation_model_decision_2026-07-10.md로 닫혔다.
- v8.3.1 carb-fat joint allocation model implementation은 docs/v8.3.1_carb_fat_joint_allocation_model_implementation_2026-07-10.md로 구현됐다. shared helper / adaptive target / card conditional display range / regression test를 맞췄고, score penalty curve 자체를 과잉 변경하지 않는 경계도 닫았다.
- v8.3.1 carb-fat joint allocation model QA closeout은 docs/v8.3.1_carb_fat_joint_allocation_model_qa_closeout_2026-07-10.md로 닫혔다. real Today summary path에서 card range와 joint allocation은 정렬됐고, target 그대로 섭취 invariant / adaptive overfitting / high-volume no-permission / Records snapshot stability를 통과했다.
- v8.3.1 stabilization/readiness checkpoint update는 docs/v8.3.1_stabilization_readiness_checkpoint_update_2026-07-10.md로 닫혔다. carb/fat joint allocation 구현과 QA closeout은 master에 반영됐고, 다음은 새 기능 자동 진입이 아니라 monitor 또는 실제 이슈 기반 planning이다.
- v8.3.1 macro card adaptive OFF and protein target level implementation은 docs/v8.3.1_macro_card_adaptive_off_and_protein_target_level_implementation_2026-07-10.md로 구현됐다. adaptive ON/OFF card display를 분리했고, protein target level setting을 추가했으며, score formula/curve tuning은 열지 않았다.
- v8.3.1 onboarding / first-run flow decision은 docs/v8.3.1_onboarding_first_run_flow_decision_2026-07-10.md로 닫혔고, implementation과 completion ownership hotfix 및 full-backup snapshot rebase hotfix는 docs/v8.3.1_onboarding_first_run_flow_implementation_2026-07-10.md로 구현됐다. personalized macro calculation 전 Settings ownership을 요구하고, raw storage를 `fresh` / `completed` / `existing_profile_confirmed` / `existing_data_profile_unconfirmed`로 분류한다. Records/InBody/draft 존재만으로 profile을 확인하지 않으며, 체성분 미입력은 `bodyCompositionConfirmed:false`인 null/low-authority 경로를 사용한다. fresh completion만 medium/adaptive ON/neutral Today를 적용하고, re-entry와 profile-unconfirmed existing-data completion은 non-onboarding Settings와 사용자 Today data를 보존한다. 전체 백업 B는 과거 snapshot A를 폐기하며, 미완료 B는 B ownership을 다시 캡처하고 완료 B는 snapshot을 null로 유지한다. exercise profile 변경 시 incompatible routine ownership만 normalize한다. 이 별도 product gate는 v8.3 score formula, anchor, curve, DailyCoach, old Records score를 변경하지 않는다.
- 사용자 요청의 carb/fat adaptive-target Coach 설명은 onboarding에서 구현하지 않고 joint `Today score and guidance surface ownership inventory / decision`으로 넘겼다. 현재 joint allocation은 protein reserve, fixed kcal line, current carb/fat skew, joint bounds, recent excess magnitude와 recency를 반영한 continuous 3-day/7-day pressure를 계산하지만, DailyCoach에는 조정 방향/실제 delta/history confidence/boundary cause를 전달하는 전용 semantic signal이 없다.
- v8.3.1 Today score and guidance surface ownership inventory / decision은 docs/v8.3.1_today_score_guidance_surface_ownership_inventory_decision_2026-07-11.md로 닫혔고, score-card semantic ownership cleanup은 docs/v8.3.1_today_score_card_semantic_ownership_cleanup_implementation_2026-07-11.md로 구현됐다. 최종 score의 9개 `penaltyBreakdown` axis는 탄수/단백질/지방/총칼로리 core aggregate와 carb-fat joint/alcohol physiology/data 조건부 근거에 정확히 한 번 partition되며, raw 합은 `getPenaltyTotal`과 reconcile한다. 현재 production에서는 네 aggregate를 독립 0~100 component score로 바꾸지 않았다.
- available Score의 generic secondary, quick top-2, Coach/recent/InBody/other-kcal extra와 meal-concentration 문구를 제거했다. Score renderer는 Coach output을 받지 않고, 조건부 근거는 실제 nonzero penalty만 직접 보여 준다. missing-TDEE/inferred-weight source validity의 live 경계는 유지한다.
- current record schema에는 `penaltyBreakdown`, `scoringContext`, `componentContributions`가 저장되지 않는다. stored auto score는 v6.1/pre-correction v1/version-null/current 여부와 무관하게 저장된 정수 점수와 version을 유지하고 세부 근거를 현재 값으로 추정하거나 재계산하지 않으며, attribution-limit만 허용한다. storage/schema/backup/Records는 변경하지 않았다.
- adaptive-target stable help는 docs/v8.3.1_adaptive_target_stable_help_implementation_2026-07-11.md로 구현됐다. Settings는 canonical full definition, Today는 같은 source의 mobile/a11y short reference를 사용한다. native button, tooltip id, aria-describedby, role=tooltip, keyboard/tap/Escape/outside-dismiss를 닫았고 계산·저장·점수는 바꾸지 않았다.
- component score architecture feasibility / simulation decision은 docs/component_score_architecture_simulation_decision_2026-07-11.md에 historical input으로 남긴다. 당시 raw multiplicative candidate의 조건부 채택과 구현 next gate는 docs/v8.4_component_score_architecture_falsification_decision_2026-07-11.md의 permanent simulation 결과가 supersede한다.
- raw product는 `95/95/95/95 -> 81.451`, `90/90/90/90 -> 65.610`으로 moderate 대칭 점수의 공통 의미를 무너뜨려 REJECTED다. soft-min과 minimum+bounded-residual은 synthetic fixture만 통과했으며, 익명 실제 54일에서 severe under-intake와 multiple-moderate day를 과도하게 높여 production model로 선택하지 않았다. 현재 선택된 component aggregation model은 없다.
- 현행 thresholded carb-fat joint는 core energy/carb/fat가 설명한 문제를 다시 감점하고 경계에서 불연속적으로 켜지거나 꺼져 REJECTED다. `OPTION_C_JOINT_ALLOCATION_RESIDUAL`은 protein-reserved joint budget의 feasible segment 밖 잔여 거리만 보는 design direction으로만 선택했으며 exact curve는 아직 승인하지 않았다.
- item 51 당시 6 goals x rest/normal/high x low/medium/high의 valid target 54개 중 21개가 exact 100이 아니었다. selected protein target보다 높은 hard floor와 intentional surplus target의 TDEE overload 재감점이 원인이었다.
- docs/v8.3.1_target_scoring_authoritative_reference_correction_implementation_2026-07-12.md가 이 release blocker를 닫았다. current production은 54/54 exact 100이며, protein penalty는 generated target-aligned `proteinRange`, energy overload는 유효 target과 physical TDEE 중 높은 기준을 사용한다. physical `tdeeRatio`와 data-outlier axis는 그대로 유지한다.
- authoritative target은 finite/positive kcal, finite/non-negative macros, macro-kcal closure, live/adaptive/frozen ownership, complete rate guard, energy-availability floor, independent requested protein level, external application/final target 일치를 통과해야 한다. 실패하면 numeric score와 penalty breakdown을 만들지 않는다.
- current component aggregation model은 여전히 없다. 다음 gate는 `v8.4 Option C joint-allocation residual exact-formula simulation`이며 production formula/UI/storage implementation은 열지 않는다.
- `dataOutlierPenalty`는 nutrition domain score로 뒤집지 않는다. missing/non-finite/negative required macro는 null/blocked, extreme unconfirmed numeric input은 provisional + confirmation 상태, confirmed extreme input은 nutrition 평가와 data confidence 상태를 분리한다.
- `balance.adaptiveTarget` causal evidence를 읽는 `adaptiveAllocation` signal의 `DailyCoach semantic v2 phase 1`은 target correction이 닫히고 component path가 선택되거나 폐기될 때까지 보류한다. `3일 연속`/`7일 연속`/`조절 불가`는 explicit consecutive/boundary evidence 없이는 금지한다. selectable Coach voice는 semantic v2 뒤 별도 decision이며 현재 alcohol severity인 `coachTone`을 voice 설정 이름으로 재사용하지 않는다. broad tooltip/glossary는 실제 전문어 필요가 있을 때만 연다.
- alcohol은 kcal + physiology risk를 유지하되, 첫 판단은 copy/product-tone mismatch인지 score curve mismatch인지 분리한다.
- 구현 중에도 UI/storage/schema/scoreDeltaPreview/old records migration은 별도 gate다.

source visibility 현재 기준
- missing total burn / `tdeeAvailable:false` / `totalBurnSource:"missing"`은 score tuning이나 null gate가 아니라 user-facing source note 대상이다.
- `weightSource:"targetProteinFallback"`은 score를 막지 않지만 inferred weight source note 대상이다.
- source note는 Today live score surface에만 붙이고 storage/schema/backup/Recent/DailyCoach field로 열지 않는다.
- stored auto score는 version과 무관하게 hidden recompute하지 않으며, `stored_auto_snapshot`에는 live source warning을 붙이지 않는다. pre-correction/legacy/unknown version에 current formula 이름을 거짓으로 붙이지 않는다.
- no meals / one meal / pending / invalid scoring context는 계속 null/blocked gate다.
- Today source visibility UI regression은 `runAdherenceUiTests`에서 고정하고, 해당 suite는 smoke/core/ui profile에 포함되어야 한다.

MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-07-v1
- source_ledger_checked: yes, through current truth routing and v8.3 implementation logs
- superseded_docs_checked: yes, v8.2 macro docs status checked through document status index and archive README
- external_anchor_checked: no new external anchor introduced; scoring anchors, curves, and penalty values unchanged

DOCUMENT ROLE
- decision
- docs-only stabilization/tag readiness checkpoint

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview mainline
- preview field storage/UI/Recent/DailyCoach exposure

# v8.3 stabilization/tag readiness checkpoint

작성일: 2026-07-08
문서 성격: docs-only stabilization/tag readiness checkpoint
구현 변경: 없음
tag 생성: 없음
score tuning 구현: 없음
UI/storage/schema 변경: 없음

## 1. 목적

이 문서는 v8.3 scoring 구현부터 DailyCoach/copy regression hardening까지 이어진 작업 묶음이 release/tag 후보로 볼 수 있는 상태인지 검산한다.

이번 작업은 새 기능 구현이 아니다.
이번 작업은 실제 tag 생성도 아니다.

핵심 원칙:

```text
minimal surface, complete readiness review
```

뜻:

- 구현 surface는 열지 않는다.
- `index.html`, `package.json`, `tools/render_audit` 코드는 수정하지 않는다.
- readiness review 범위는 반쪽으로 남기지 않는다.
- scoring, source visibility, reference routing, app display, backup metadata, WATCH regression, DailyCoach/copy regression, docs-policy, test profile coverage를 함께 본다.

## 2. HEAD / branch / tag 상태

확인 기준:

```text
HEAD before this checkpoint: 98d4528 v8.3 DailyCoach copy 회귀 테스트 보강
working branch: codex/v8.3-stabilization-tag-readiness
latest existing v8 tag observed: v8.0
```

최근 v8.3 block:

```text
98d4528 v8.3 DailyCoach copy 회귀 테스트 보강
3d442ad v8.3 DailyCoach copy tone 결정 문서 추가
b793082 v8.3 WATCH 회귀 테스트 보강
3f787f0 v8.3 점수 WATCH 튜닝 준비 결정 문서 추가
0f7dc86 v8.3 구현 QA closeout 문서화
e2e5d11 v8.3 앱 표시 버전 정렬 구현
4cca79b v8.3 점수 source visibility 구현
47f9e00 v8.3 연속 점수 산식 구현
```

판단:

- `v8.3` tag는 아직 없다.
- 이번 checkpoint branch에서 tag를 만들지 않는다.
- tag 판단은 이 checkpoint와 테스트 결과를 근거로 다음 별도 merge/tag instruction에서 다룬다.

## 3. 완료된 v8.3 work block

이번 checkpoint가 한 덩어리로 검산하는 완료 블록:

```text
scoring:
- v8.3 anchor continuous macro score implementation
- production scoring version: v8.3_anchor_continuous_macro_score_v1
- formula name: anchor_continuous_macro_score_v1

source visibility:
- missing total burn / targetProteinFallback user-facing source notes
- stored current-version auto snapshot hidden recompute 금지

reference routing:
- root v8.2 macro docs archive 이동
- legacy/reference docs reference folder split
- docs-policy guard active

app display:
- app title/header/helper/full backup appVersion expectation v8.3
- package.json version 유지
- FULL_BACKUP_VERSION 유지

regression hardening:
- WATCH relation tests
- DailyCoach/copy C01~C06 tests
- runAdherenceUiTests and runDailyCoachTestCases profile coverage 보강
```

## 4. scoring / version readiness

확인한 current identity:

```text
MACRO_RANGE_PRODUCTION_SCORING_VERSION = "v8.3_anchor_continuous_macro_score_v1"
ADHERENCE_SCORING_VERSION = MACRO_RANGE_PRODUCTION_SCORING_VERSION
MACRO_RANGE_SCORE_FORMULA_NAME = "anchor_continuous_macro_score_v1"
```

판단:

- production score body는 v8.3 anchor continuous path다.
- v8.2 fixed penalty table은 production body가 아니다.
- candidateScorePreview / pointsPreview / scoreDeltaPreview는 production saved/display field로 열리지 않았다.
- scoreDeltaPreview는 optional audit-only 상태를 유지한다.
- 이번 checkpoint에서 score tuning, anchor 변경, scoring version 변경은 하지 않는다.

## 5. app display / backup metadata readiness

확인한 상태:

```text
document title: v8.3
home header app version badge: v8.3
getCurrentAppVersionLabel fallback: v8.3
full backup appVersion expectation: v8.3
package.json version: 8.0.0 유지
FULL_BACKUP_VERSION: 1 유지
```

판단:

- 사용자가 앱 상단에서 보는 표시 버전은 v8.3으로 정렬됐다.
- package version과 backup schema version은 release/package/schema 의미가 다르므로 이번 v8.3 display alignment에서 유지하는 것이 맞다.
- backup payload structure를 바꾸지 않고 `appVersion` metadata만 현재 app label에 맞춘 상태다.

## 6. docs / reference routing readiness

확인한 상태:

```text
root v8.2 macro docs: 0
archive v8.2 macro docs: 65
routed legacy/reference docs excluding README: 8
references README files: product / external / copy / historical
```

판단:

- root `docs/`에 v8.2 macro 문서가 다시 생긴 상태가 아니다.
- v8.2 문서는 `docs/archive/v8.2_macro_range/` 아래 historical archive로 격하됐다.
- legacy product / external / copy / historical 문서들은 reference folder로 이동되어 current truth보다 우선하지 않는 구조다.
- `docs-policy`는 root v8.2 문서 재생성과 old reference path 부활을 막는 guard로 남아 있다.

주의:

- `tools/render_audit/verify_doc_policy.cjs` 안에는 금지 경로 문자열이 guard pattern으로 남아 있는 것이 정상이다.
- 오래된 감사/QA 문서 안의 old path 언급은 historical reference로 볼 수 있다.
- 실제 current reference routing은 README / status index / reference folder README가 우선한다.

## 7. test / profile readiness

확인한 profile coverage:

```text
runMacroRangeContinuousScoringTests:
- test:macro-policy
- test:core
- test:full exported run*Tests

runAdherenceUiTests:
- smoke
- core
- ui
- full exported run*Tests

runDailyCoachTestCases:
- test:daily-coach
- core
- full exported run*Tests
```

판단:

- v8.3 scoring regression은 macro-policy/core/full에서 잡힌다.
- Today source visibility UI regression은 smoke/core/ui/full에서 잡힌다.
- DailyCoach/copy C01~C06 regression은 daily-coach/core/full에서 잡힌다.
- 중요한 v8.3 회귀가 특정 전용 script에만 갇혀 있지 않다.

## 8. 최종 검증 결과

이번 checkpoint branch에서 최종 실행한 검증:

```text
npm run test:docs-policy
npm test
npm run test:core
npm run test:ui
npm run test:macro-policy
npm run test:daily-coach
npm run test:full
git diff --check
git diff --check HEAD^ HEAD
```

결과:

```text
docs-policy: passed
smoke: 20 suites / 190 cases / failed 0
core: 62 suites / 727 cases / failed 0
ui: 25 suites / 180 cases / failed 0
macro-policy: 29 suites / 232 cases / failed 0
daily-coach: 1 suite / 83 cases / failed 0
full: 138 suites / 1389 cases / failed 0
diff check: passed
```

## 9. remaining pending gates

release/tag readiness 관점에서 blocker로 보지 않는 후속 후보:

```text
score tuning implementation: optional future gate
DailyCoach/copy implementation pass: optional future gate
UI/storage/schema expansion: separate future gate
scoreDeltaPreview optional audit: separate future gate
old records migration/recompute/reset: separate future gate
external anchor inventory split: optional docs cleanup
copy tone pass: optional future UX/copy gate
```

판단:

- 위 항목들은 남아 있지만, 지금 v8.3 scoring implementation block의 release/tag blocker는 아니다.
- 새 기능 확장과 stabilization readiness를 섞지 않는다.
- tag 전 필수 조건은 current v8.3 block의 테스트/문서/라우팅/표시 정합성 확인이다.

## 10. tag readiness decision

선택:

```text
A. tag-ready after PR merge
```

근거:

- v8.3 production scoring version과 formula identity가 맞다.
- app display label은 v8.3으로 정렬됐다.
- backup appVersion metadata는 v8.3이고, backup schema version은 안정적으로 유지됐다.
- root v8.2 macro docs는 0개이고, archive 65개가 유지된다.
- legacy/reference docs는 reference folder로 라우팅됐다.
- WATCH regression과 DailyCoach/copy regression이 core/full coverage로 들어왔다.
- 필수 테스트와 full test가 통과했다.
- 남은 후속 후보는 release blocker가 아니라 별도 future gate다.

주의:

- 이 문서는 tag를 생성하지 않는다.
- 이 문서가 merge된 뒤, 실제 tag 생성 여부는 별도 사용자 승인 또는 merge/tag instruction에서 처리한다.
- tag를 찍는다면 기준은 이 checkpoint가 포함된 commit이어야 한다.

## 11. 수용 / 폐기 / 통합 / 보류

수용:

- v8.3 continuous scoring implementation 유지.
- source visibility implementation 유지.
- legacy reference routing 유지.
- app display version alignment 유지.
- WATCH regression hardening 유지.
- DailyCoach/copy regression hardening 유지.
- stabilization/tag readiness checkpoint 선행.

폐기:

- 테스트 없이 태그 생성.
- WATCH/copy 회귀를 무시하고 태그 생성.
- tag readiness 작업에 score tuning, UI/storage/schema, scoreDeltaPreview, old records migration을 섞는 흐름.
- package.json version이나 FULL_BACKUP_VERSION을 표시 버전과 같은 의미로 강제 변경하는 흐름.

통합:

- v8.3 산식, source visibility, reference routing, app display, WATCH regression, DailyCoach/copy regression을 하나의 stabilization block으로 통합해 검산했다.
- docs-policy와 test profile coverage를 tag readiness 조건으로 통합했다.
- optional future work는 release blocker가 아니라 별도 gate로 분리했다.

보류:

- 실제 tag 생성.
- merge instruction.
- score tuning implementation.
- DailyCoach/copy implementation.
- UI/storage/schema.
- scoreDeltaPreview optional audit.
- old records migration/recompute/reset.
- broader docs rename/consolidation pass.

금지선:

- v8.2 fixed penalty table을 v8.3 production body로 되살리지 않는다.
- 중간 score cap을 정책으로 쓰지 않는다.
- exercise bonus를 추가하지 않는다.
- v6.1 alcoholImpactPenalty post-score subtraction을 되살리지 않는다.
- preview field를 storage/UI/Recent/DailyCoach로 노출하지 않는다.
- old records를 hidden recompute하거나 silent mutation하지 않는다.

## 12. 다음 단계

내 판단 다음 단계:

```text
v8.3 merge/tag instruction
```

조건:

- 이 checkpoint branch가 merge되어야 한다.
- 사용자가 tag 생성을 승인하거나 별도 tag instruction을 줘야 한다.
- 실제 tag 작업은 새 기능 구현이 아니라 release bookkeeping으로 다룬다.

하지 말 것:

- 바로 score tuning implementation.
- 바로 DailyCoach/copy rewrite.
- 바로 UI/storage/schema 확장.
- 바로 scoreDeltaPreview 작업.
- old records migration/recompute/reset.

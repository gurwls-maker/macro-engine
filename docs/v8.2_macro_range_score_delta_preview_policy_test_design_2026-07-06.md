# v8.2 macro range scoreDeltaPreview policy test design

상태:

```text
문서 성격: scoreDeltaPreview policy test design docs-only
작업 범위: future test-only guard 설계
index.html 변경: 없음
package.json 변경: 없음
tools/render_audit 변경: 없음
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
scoreDeltaPreview production field 승격: 없음
scoreDeltaPreview persistence: 없음
goalSnapshot schema 변경: 없음
record schema 변경: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
DailyCoach action 변경: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_score_delta_preview_policy_decision_2026-07-06.md` 이후 test design이다.

목적:

```text
future scoreDeltaPreview policy를 테스트로 고정하기 전에 필요한 test-only guard의 범위, fixture, assertion, 금지선을 문서로 닫는다.
아직 comparison.scoreDeltaPreview 숫자값, score delta formula, sign / rounding, UI / Recent / DailyCoach 연결, production score 연결은 열지 않는다.
```

핵심 결론:

```text
다음 test-only suite 후보는 runMacroRangeScoreDeltaPreviewPolicyDecisionTests다.
테스트 설계의 첫 목적은 scoreDeltaPreview를 계산하는 것이 아니라, scoreDeltaPreview last-gate가 아직 닫혀 있음을 증명하는 것이다.
comparison.scoreDeltaPreview는 계속 null이다.
candidateScorePreview가 null이면 scoreDeltaPreview도 null이다.
candidateScorePreview가 future gate에서 숫자로 열려도 별도 scoreDeltaPreview gate 전까지 delta는 null이다.
componentPreviews.*.pointsPreview는 계속 null이다.
old fixed Records 저장값은 절대 덮어쓰지 않는다.
storage / backup / Recent / UI / DailyCoach 연결은 계속 금지다.
```

## 1. current baseline

이미 닫힌 field decision:

```text
scoreDeltaPreview field name 예약
allowed location 후보: comparison.scoreDeltaPreview
default: null
top-level / rangeScoreCandidateSummary / record / goalSnapshot / backup / Recent / DailyCoach / UI state로 복사 금지
last-gate 성격 명시
candidateScorePreview / pointsPreview 별도 gate 유지
```

이미 test-only로 열린 것:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
testLocalNumericPreview.testLocalCandidateScorePreviewVersion
testLocalNumericPreview.testLocalFixedScoreBasisLabel
testLocalNumericPreview.testLocalCandidateScoreBasisLabel
```

이미 고정한 no-leak:

```text
testLocalNumericPreview nested-only
candidateScorePreview null
component pointsPreview null
comparison.scoreDeltaPreview null
old fixed Records no-mutation
backup / Recent / UI / DailyCoach no-impact
macro-policy / core profile registration coverage
```

중요:

```text
testLocalNumericPreview 숫자 존재 != production scoreDeltaPreview readiness
candidateScorePreview numeric readiness != scoreDeltaPreview readiness
component pointsPreview numeric readiness != scoreDeltaPreview readiness
consumer no-leak 통과 != scoreDeltaPreview visibility readiness
```

## 2. 이번 test design이 정하는 것

정하는 것:

```text
future test-only suite 이름 후보
scoreDeltaPreview policy fixture category
scoreDeltaPreview assertion group
candidateScorePreview null이면 delta null guard
candidateScorePreview numeric gate closed이면 delta null guard
component pointsPreview null이면 delta null guard
old fixed Records no-overwrite guard
storage / backup / Recent / UI / DailyCoach no-leak guard
promoted scoreDeltaPreview payload rejection
last-gate inventory guard
```

정하지 않는 것:

```text
actual scoreDeltaPreview number
scoreDeltaPreview = candidateScorePreview - fixedScore formula
scoreDelta sign convention
scoreDelta rounding / clamp policy
positive / negative display copy
DailyCoach action threshold
Recent aggregation method
storage schema
backup roundtrip
ADHERENCE_SCORING_VERSION bump
```

따라서 이번 문서 이후에도 유지되는 값:

```text
rangeScoreCandidateSummary.candidateScorePreview: null
comparison.candidateScorePreview: null
comparison.scoreDeltaPreview: null
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview: null
```

## 3. future suite candidate

suite 후보:

```text
runMacroRangeScoreDeltaPreviewPolicyDecisionTests
```

suite 성격:

```text
test-only guard
production delta score helper 아님
production scoring helper 아님
UI render test 아님
backup schema test 아님
Recent aggregation test 아님
DailyCoach action test 아님
```

실제 구현 단계 등록 후보:

```text
index.html internal test suite
window.runMacroRangeScoreDeltaPreviewPolicyDecisionTests
package.json test:macro-policy
tools/render_audit core profile
```

주의:

```text
이번 docs-only 단계에서는 위 등록을 하지 않는다.
실제 suite 구현 단계에서만 index.html / package.json / tools/render_audit를 변경한다.
```

## 4. fixture categories

future test-only suite가 다뤄야 할 fixture category:

```text
baseline_fixed_score_only_delta_null
eligible_full_day_candidate_null_delta_null
eligible_full_day_testLocal_candidate_evidence_delta_null
eligible_full_day_anchor_penalty_candidate_delta_null
eligible_full_day_range_evidence_candidate_delta_null
candidateScorePreview_numeric_unapproved_delta_null
component_pointsPreview_numeric_unapproved_delta_null
old_fixed_detailed_sufficient_no_mutation
old_fixed_simple_record_delta_null
old_fixed_weight_only_record_delta_null
snapshotless_debug_only
one_meal_pending
insufficient_record
blocked_by_impossible_state
unsupported_range_basis
invalid_range_basis
range_policy_missing
storage_policy_unapproved
ui_visibility_unapproved
recent_aggregation_unapproved
dailyCoach_action_unapproved
leaky_promoted_scoreDeltaPreview_payload
```

fixture가 검증해야 하는 공통값:

```text
comparison.fixedScore
comparison.candidateScorePreview
comparison.scoreDeltaPreview
rangeScoreCandidateSummary.candidateScorePreview
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview
testLocalNumericPreview presence / nested-only
fixedScoreSummary source / version / score
guards.productionScoreUnchanged
guards.scoringVersionUnchanged
guards.backupRestoreUnaffected
guards.recentUnaffected
guards.uiUnaffected
guards.dailyCoachUnaffected
```

fixture가 검증하지 않는 값:

```text
actual score delta
actual candidate score
actual component point sum
actual sign convention
actual display precision
actual user-facing score copy
actual DailyCoach action
actual Recent trend
```

## 5. assertion groups

future suite assertion group:

```text
1. scoreDeltaPreview field remains null
2. candidateScorePreview null implies scoreDeltaPreview null
3. candidateScorePreview numeric gate closed implies scoreDeltaPreview null
4. component pointsPreview null implies scoreDeltaPreview null
5. comparison.candidateScorePreview null assertion
6. rangeScoreCandidateSummary.candidateScorePreview null assertion
7. no fixed-score fallback delta assertion
8. no sentinel zero delta assertion
9. old fixed Records no-mutation assertion
10. no record / goalSnapshot / backup scoreDeltaPreview assertion
11. Recent no-aggregation assertion
12. UI no-visibility assertion
13. DailyCoach no-action assertion
14. promoted scoreDeltaPreview payload rejection assertion
15. ADHERENCE_SCORING_VERSION unchanged assertion
```

`computed` 관련:

```text
comparison.scoreDeltaPreview number는 future candidateScorePreview numeric policy와 별도 scoreDelta policy가 모두 닫힌 뒤에만 허용한다.
이번 test design에서는 computed delta를 expected output으로 두지 않는다.
```

## 6. candidate null guard

기본 guard:

```text
rangeScoreCandidateSummary.candidateScorePreview: null
comparison.candidateScorePreview: null
comparison.scoreDeltaPreview: null
```

검증 의도:

```text
candidateScorePreview가 null이면 scoreDeltaPreview도 null이다.
fixedScoreSummary.score가 있어도 delta를 계산하지 않는다.
comparison.fixedScore가 있어도 candidateScorePreview fallback을 만들지 않는다.
blocked record에 scoreDeltaPreview 0을 넣지 않는다.
```

금지:

```text
null candidateScorePreview를 fixedScore로 대체
null candidateScorePreview를 0으로 대체
null scoreDeltaPreview를 0 차이로 표시
null scoreDeltaPreview를 no-change action으로 해석
```

## 7. unapproved numeric payload guard

future suite는 suite 내부에서 synthetic unapproved payload를 만들어야 한다.

후보:

```text
candidateScorePreview_numeric_unapproved_delta_null
component_pointsPreview_numeric_unapproved_delta_null
comparison_scoreDeltaPreview_promoted
top_level_scoreDeltaPreview_promoted
record_scoreDeltaPreview_promoted
goalSnapshot_scoreDeltaPreview_promoted
backup_scoreDeltaPreview_promoted
```

검증 의도:

```text
candidateScorePreview 숫자가 payload에 있어도 별도 gate 전에는 scoreDeltaPreview를 계산하지 않는다.
component pointsPreview 숫자가 payload에 있어도 별도 gate 전에는 scoreDeltaPreview를 계산하지 않는다.
promoted scoreDeltaPreview는 violation으로 잡는다.
```

주의:

```text
이 synthetic payload는 future production output이 아니다.
promoted payload rejection을 위한 negative fixture다.
```

## 8. blocked / dataQuality guard

blocked fixture:

```text
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
simple_record_not_numeric_candidate
weight_only_record_not_numeric_candidate
unsupported_range_basis
invalid_range_basis
range_policy_missing
```

확인:

```text
comparison.scoreDeltaPreview null
comparison.candidateScorePreview null
rangeScoreCandidateSummary.candidateScorePreview null
componentPreviews.*.pointsPreview null
dataQuality.pointsPreview null-only
blocked 상태를 sentinel 0 delta로 저장하지 않음
blocked reason은 status / reasonIds / dataQuality로 표현
```

## 9. old fixed Records policy

old fixed Records test design:

```text
sufficient detailed old fixed Record -> transient candidate eligibility 후보로 분류 가능
simple old fixed Record -> scoreDeltaPreview numeric policy 대상 아님
weight_only old fixed Record -> scoreDeltaPreview numeric policy 대상 아님
snapshotless old fixed Record -> blocked / debug-only
insufficient old fixed Record -> blocked
```

계속 금지:

```text
saved adherencePercent overwrite
saved adherenceScoringVersion overwrite
saved score basis 변경
backup payload에 scoreDeltaPreview 추가
Records UI에 scoreDeltaPreview 표시
Recent aggregation에 scoreDeltaPreview 사용
DailyCoach reason/action에 scoreDeltaPreview 사용
```

검증 방향:

```text
old fixed Records는 latest-policy candidate eligibility 후보가 될 수 있지만, 저장 score basis는 fixed로 유지한다.
fixedScoreSummary.score는 comparison input일 수 있지만 scoreDeltaPreview 계산 승인이 아니다.
```

## 10. storage / backup no-leak

future suite가 확인해야 할 no-leak target:

```text
record state
goalSnapshot
backup export
backup import
records-only import
Recent context
Recent summary
Today UI state
Records UI state
DailyCoach payload
DailyCoach action
```

금지 payload:

```text
scoreDeltaPreview
candidateScorePreview
pointsPreview
rangeScoreCandidate
rangeScoreCandidateSummary
macroRangeContract.scoring.scoreDeltaPreview
comparison.scoreDeltaPreview
```

허용 payload:

```text
testLocalNumericPreview source-only object
comparison.fixedScore source number
```

단:

```text
testLocalNumericPreview도 backup / Recent / UI / DailyCoach consumer로 새면 안 된다.
comparison.fixedScore가 있어도 scoreDeltaPreview 계산이나 저장을 의미하지 않는다.
```

## 11. UI / Recent / DailyCoach guard

UI guard:

```text
Today UI에 scoreDeltaPreview 표시 없음
Records UI에 scoreDeltaPreview 표시 없음
Recent UI에 scoreDeltaPreview 표시 없음
score delta badge / chip / reason copy 없음
```

Recent guard:

```text
Recent aggregation에 scoreDeltaPreview 사용 없음
Recent trend에 scoreDeltaPreview 사용 없음
null scoreDeltaPreview를 0 change로 집계하지 않음
```

DailyCoach guard:

```text
DailyCoach reason/action에 scoreDeltaPreview 사용 없음
scoreDeltaPreview positive / negative threshold 없음
null scoreDeltaPreview를 no-change action으로 해석하지 않음
```

## 12. promoted payload rejection

future suite 내부에서 금지 payload를 만든다.

포함:

```text
top-level scoreDeltaPreview
rangeScoreCandidateSummary.scoreDeltaPreview
comparison.scoreDeltaPreview
record.scoreDeltaPreview
goalSnapshot.scoreDeltaPreview
backupPayloadFragment.scoreDeltaPreview
Recent payload scoreDeltaPreview
DailyCoach payload scoreDeltaPreview
UI state scoreDeltaPreview
```

확인:

```text
promoted production-facing delta shape는 guard violation으로 잡는다.
```

## 13. expected future test cases

future suite test case 후보:

```text
v8.2 scoreDeltaPreview policy test design inventory stays closed
v8.2 scoreDeltaPreview policy keeps candidate-null delta null
v8.2 scoreDeltaPreview policy keeps unapproved numeric candidate payload delta null
v8.2 scoreDeltaPreview policy blocks incomplete and impossible records without sentinel delta
v8.2 scoreDeltaPreview policy preserves old fixed Records without delta mutation
v8.2 scoreDeltaPreview policy rejects promoted production-facing delta payloads
v8.2 scoreDeltaPreview policy has zero production consumer side effects
```

예상 케이스 수:

```text
7 cases
```

케이스 수는 구현 단계에서 fixture 중복 제거에 따라 조정 가능하다.

## 14. implementation registration candidate

실제 구현 단계에서 필요한 등록:

```text
index.html: runMacroRangeScoreDeltaPreviewPolicyDecisionTests
index.html: window.runMacroRangeScoreDeltaPreviewPolicyDecisionTests
package.json: test:macro-policy suite 목록
tools/render_audit/run_internal_tests.cjs: core profile
```

구현 단계에서 함께 확인할 것:

```text
단독 suite 실행
npm run test:macro-policy
npm run test:core
git diff --check
```

단:

```text
이번 docs-only test design 단계에서는 위 등록을 하지 않는다.
```

## 15. acceptance criteria

이번 docs-only 단계 통과 조건:

```text
index.html diff 없음
package.json diff 없음
tools/render_audit diff 없음
production code diff 없음
README 등록
scoreDeltaPreview policy decision 후속 기록 추가
future suite 후보 명시
fixture category 명시
assertion group 명시
comparison.scoreDeltaPreview null 유지
candidateScorePreview null이면 scoreDeltaPreview null 유지
component pointsPreview null이면 scoreDeltaPreview null 유지
old fixed Records no-overwrite 유지
storage / backup / Recent / UI / DailyCoach no-leak 유지
promoted payload rejection 설계
git diff --check 통과
```

앱 테스트:

```text
docs-only 변경이므로 생략 가능
```

단:

```text
index.html / package.json / tools/render_audit를 변경했다면 test:macro-policy 실행이 필요하다.
```

## 16. next candidates

이 test design 이후 가능한 안전 후보:

```text
1. runMacroRangeScoreDeltaPreviewPolicyDecisionTests test-only 구현
2. range score UI visibility decision docs-only
3. score persistence gate design docs-only
4. Recent candidate score summary decision docs-only
5. DailyCoach candidate score action decision docs-only
6. line hold / 보류
```

다음 구현 후보를 열더라도:

```text
production score 구현이 아니다.
production scoreDeltaPreview 숫자화가 아니다.
production candidateScorePreview 숫자화가 아니다.
production pointsPreview 숫자화가 아니다.
storage/schema 변경이 아니다.
UI / Recent / DailyCoach 변경이 아니다.
```

## 17. final judgement

현재 결론:

```text
scoreDeltaPreview policy test design: 닫힘
scoreDeltaPreview readiness: 아직 아님
candidateScorePreview numeric readiness: 아직 아님
component pointsPreview numeric readiness: 아직 아님
UI visibility readiness: 아직 아님
Recent readiness: 아직 아님
DailyCoach readiness: 아직 아님
score persistence readiness: 아직 아님
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
이번 단계는 scoreDeltaPreview 숫자화를 여는 단계가 아니라, future test-only suite가 comparison.scoreDeltaPreview null / last-gate / no-leak / no-mutation 금지선을 어떻게 고정해야 하는지 닫는 docs-only gate다.
```

MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt
- docs/00_current_truth/05_required_result_log_format.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-12 target/scoring authoritative-reference correction closeout 이후
- source_ledger_checked: historical source ledger를 production 지시로 다시 열지 않았고 current production geometry와 영구 simulation harness를 직접 확인했다.
- superseded_docs_checked: yes, through docs/00_current_truth/04_document_status_index.txt
- external_anchor_checked: 기존 protein/carb/fat 외부근거 anchor를 읽었으며 새 anchor나 coefficient를 승인하지 않았다.

DOCUMENT ROLE
- decision
- test/docs-only Option C exact-formula simulation result log
- no production implementation

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview mainline
- preview field storage/UI/Recent/DailyCoach exposure

# v8.4 Option C joint residual exact-formula simulation

Date: 2026-07-12

CURRENT CORRECTION NOTICE
- 아래 원래 본문은 `5d8ee0d` 당시 판단과 수치를 historical record로 보존한다.
- 현재 유효한 ownership 분류와 다음 gate는 문서 끝의 `production-authority correction`을 우선한다.
- 원래의 성급한 replacement/removal 보류는 유지되며 production 구현은 열리지 않았다.

Decision summary:

```text
overall outcome: MORE_EVIDENCE_REQUIRED
current joint replacement: NOT APPROVED
joint-axis removal: NOT APPROVED
component aggregation: BLOCKED
production implementation: NOT OPEN
next gate: privacy-safe actual-day joint ownership evidence
```

## 비개발자용 설명

### 무엇을 바꿨는지

앱 화면과 실제 점수 계산은 바꾸지 않았다. 대신 탄수화물과 지방의 조합 문제를 별도 감점으로 남길지 판단하기 위해, 가능한 탄수·지방 조합 범위에서 얼마나 벗어났는지를 계산하는 Option C를 더 촘촘하게 시뮬레이션했다.

이번 검사는 한 가지 예시만 본 것이 아니다. 실제 앱이 만드는 목표 조건 66개에서 탄수·지방 조합을 정해진 grid로 3,058번 움직여 다음을 함께 확인했다. 이 숫자는 실제 사용자에게 얼마나 자주 나타나는지를 뜻하는 prevalence가 아니라, 두 종류가 모두 존재하는지 반증하기 위한 deterministic geometry sample 수다.

- 조합 범위 안에서는 residual이 0인지
- 범위를 조금 벗어날 때 값이 갑자기 뛰지 않는지
- 더 멀리 벗어날수록 값이 한 방향으로 커지는지
- 같은 칼로리 비율을 다른 수학 표현으로 계산해도 같은 결과인지
- 이 residual이 기존 탄수·지방·총칼로리 감점과 겹치는지

결론은 “Option C로 교체해도 된다”도 아니고 “조합 감점을 없애도 된다”도 아니다. 실제 사용 기록에서 무엇을 별도 문제로 볼지 판단할 근거가 더 필요하다.

### 왜 바꿨는지

현재 조합 감점은 경계에서 갑자기 켜졌다가 꺼질 수 있다. 사용자가 실제 화면에서 재현한 값은 약 `20.6점 -> 0점`이었고, 같은 섭취 전환을 고정 fixture와 현재 곡선으로 다시 계산한 값도 `22.449점 -> 0점`으로 같은 절벽을 보였다. 선행 문서의 `21.392점`은 당시 test-only linear interpolation 값이고, 이번 `22.449점`은 production과 같은 smoothstep interpolation으로 바로잡은 exact harness 값이다. 과거 문서를 소급 변경하지 않고 이 문서에서 차이를 명시한다. 이런 구조는 유지하기 어렵다.

Option C의 거리 자체는 그 구간에서 `0.354111`에서 `0.458886`으로 연속적으로 움직였다. 기존 감점 곡선을 시험 삼아 연결하면 `5.702점`에서 `9.147점`이 된다. 하지만 이 숫자는 승인된 새 벌점이 아니다. 연속적인 거리를 만들 수 있다는 것과 그 거리를 실제 점수로 몇 점 반영해야 한다는 것은 서로 다른 판단이다.

3,058개 deterministic grid 표본 중 residual이 양수인 경우는 477개였다. 그중 237개는 기존 탄수·지방·총칼로리 감점이 모두 0인 `core-unique` 사례였고, 240개는 기존 core 감점과 겹치는 `core-overlap` 사례였다. 이 비율은 실제 사용자 분포가 아니라 두 구조가 production geometry에 모두 존재한다는 증거다. 지금 교체하면 중복감점 위험을 남기고 지금 제거하면 별도 조합 신호를 잃을 수 있다.

### 실제 사용자 화면이나 계산 결과가 어떻게 달라지는지

변경 없음.

- Today 점수와 항목별 감점은 현재 production 그대로다.
- 현재 active scoring version `v8.3.1_target_authority_continuous_macro_score_v2`도 그대로다.
- 과거 Records 점수를 다시 계산하지 않는다.
- Option C 거리와 시험 곡선 값은 사용자 화면에 노출하지 않는다.

향후에도 실제일 근거가 나오기 전에는 `5.702점`, `9.147점` 같은 probe 숫자를 사용자 점수로 사용하지 않는다.

### 정책, 산식, 데이터 해석이 바뀌었는지

production 정책·산식·데이터 해석은 변경 없음.

연구 판단만 다음처럼 바뀌었다.

- 기존 thresholded joint는 경계 절벽과 core 중복 문제 때문에 계속 REJECTED다.
- C1의 탄수 에너지 비율 거리와 C2의 kcal 평면 방사 거리 표현은 최대 차이 `1e-15`로 사실상 같은 계산이다.
- C3 legacy normalized-carb 기준은 방향에 따라 비대칭이라 superseded했다.
- 시험한 모든 curve coefficient는 unapproved다.
- joint를 교체하는 안과 joint 축을 제거하는 안 모두 아직 승인하지 않는다.

### 바꾸지 않은 범위와 보류한 내용

- `index.html` production path를 수정하지 않았다.
- scoring formula/version, UI, storage/schema, backup, Records, DailyCoach, v8.3 tag를 바꾸지 않았다.
- component aggregation model을 선택하지 않았다.
- residual에 곱할 coefficient, anchor, curve shape를 선택하지 않았다.
- 실제 private backup은 이 작업공간에 없어서 actual-day 판정은 실행하지 못했다.
- harness에는 `--actual-backup` 선택 경로와 원본 필드를 내보내지 않는 sanitizer만 준비했다.

### 사용자가 큰 틀에서 확인해야 할 판단점

이번 결론은 다음 세 후보 중 하나만 선택한 것이다.

```text
CURRENT_JOINT_REPLACEMENT_READY: 선택하지 않음
DROP_JOINT_AXIS_READY: 선택하지 않음
MORE_EVIDENCE_REQUIRED: 선택
```

다음 작업은 계수를 조금씩 바꿔 테스트를 통과시키는 튜닝이 아니다. 개인정보를 제거한 실제일에서 residual-positive 사례를 다음 두 그룹으로 나눠야 한다.

1. 기존 탄수·지방·총칼로리 감점이 0인데 residual만 남는 core-unique
2. 기존 core 감점과 residual이 함께 생기는 core-overlap

현재 sanitizer는 count/distribution과 익명 대표 표본까지만 만들며 actual-day matched pair를 아직 만들지 않는다. 다음 gate에서 joint를 제외한 core penalty vector와 goal/training/source-validity 맥락을 기준으로 exact match와 tolerance sensitivity를 함께 비교하고, unmatched 비율과 coverage를 숨김없이 내야 한다. rejected current joint가 이미 들어간 현재 최종점수는 독립 정답으로 사용하지 않는다. residual 차이가 별도 점수축으로 설명할 제품 의미가 있는지 그때 판단한다. 이 evidence 전에는 replacement, removal, component aggregation, production 구현을 모두 열지 않는다.

### 테스트에서 무엇을 검증했는지

- 66개 production geometry에서 3,058개 조합을 검사했다.
- residual-positive 477개가 core-unique 237개와 core-overlap 240개로 정확히 나뉘는지 확인했다.
- C1과 C2의 최대 차이가 `1e-15` 이하인지 확인했다.
- C1이 양 방향에서 대칭이고 `[0, 1]` 범위 안에 머무는지 확인했다.
- production 66개 geometry가 모두 joint model이 소유한 `availableCarbFatKcal`을 직접 기준으로 쓰는지 확인했다. 명시된 값이 0·음수·비수치이면 파생값으로 대신하지 않고 fail-closed한다.
- joint carb lower/fat upper와 carb upper/fat lower 두 endpoint가 같은 kcal budget을 닫지 못하면 fail-closed하는지 확인했다.
- 기존 54개 목표와 추가 12개 cross-profile 목표가 모두 exact 100, authoritative, 9개 penalty 0을 유지하는지 확인했다.
- 전체 simulation assertion 39개가 모두 통과했고 deterministic hash는 `54fc8f1d9644a01c78b21cd020dbbfab6160fc28f664d366272929b904f80587`였다.
- actual private backup audit은 입력 파일이 없어 실행하지 않았으며, 이를 통과한 것처럼 기록하지 않았다.

## 기술 검증

### 1. PROMPT_SCOPE_AUDIT

- 요청된 다음 단계: Option C joint-allocation residual exact-formula simulation을 test/docs-only로 닫는다.
- repo 기준 실제 다음 단계: target/scoring correction은 이미 54/54와 version/old-Records 경계까지 닫혔으므로 Option C 반증이 맞다. 단, simulation이 replacement나 removal을 자동 승인한다는 가정은 다시 검토해야 했다.
- 실제 문제: current joint는 절벽과 중복 위험이 있지만, 연속 residual이 존재한다는 사실만으로 별도 사용자 벌점을 정당화할 수 없다.
- 문서 밖에서 확인한 증거: production joint geometry, 66개 production/cross-profile target, 3,058개 sweep, 사용자 재현 전환, current 9-axis core penalty, optional private-backup sanitizer 경로.
- 검토한 대안: current threshold 유지, C1/C2 residual로 교체, C3 legacy baseline, joint axis 제거, coefficient tuning, actual-day ownership evidence 선행.
- 선택한 작업이 실제 문제를 푸는 이유: replacement와 removal을 성급히 고르지 않고, 실제로 판정이 갈리는 core-unique/core-overlap 소유권 질문을 다음 evidence gate로 좁힌다.
- 이 선택이 틀렸다고 볼 조건: C1/C2가 서로 다른 의미를 내거나, production joint endpoints가 같은 kcal budget을 닫지 못하거나, target invariant가 깨지거나, 이미 사용 가능한 독립 actual-day/제품 의미 증거가 replacement 또는 removal 한쪽을 충분히 지지하는 경우다. 현재 repo에는 마지막 증거가 없고 앞의 수학·authority invariant는 통과했다.
- 최소 변경 표면: test-only harness와 결과/current-routing/docs-policy 문서.
- 이 작업 안에서 완결한 범위: residual geometry 정의, C1/C2 동치, C3 폐기, boundary/monotonic/symmetry/boundedness, production sweep, target invariant, outcome/next gate.
- 더 넓히지 않은 이유: actual-day 사용자 결과와 privacy-safe evidence가 없으므로 coefficient, aggregation, production 의미를 승인할 수 없다.

### 2. formula와 authority

```text
joint budget K = production joint model의 availableCarbFatKcal
actual carb energy share s = 4C / (4C + 9F)
feasible share segment = [s_min, s_max]

C1 residual = segment 밖 share 거리 / 해당 방향에서 가능한 최대 거리
C2 residual = 같은 kcal 평면에서 feasible segment까지의 방사 거리 정규화
```

C1과 C2는 66개 geometry와 3,058개 deterministic grid sample에서 최대 절대차 `1e-15`였다. 두 표현을 서로 다른 후보처럼 집계하지 않는다. harness는 evidence 수치만 내고 `outcome: null`을 유지하며, `MORE_EVIDENCE_REQUIRED`는 이 결정문과 current truth가 소유한다.

### 3. production geometry evidence

```text
production geometry: 54
cross-profile geometry: 12
combined geometry: 66
samples: 3,058
residual-positive: 477
core-unique: 237
core-overlap: 240
```

`237 + 240 = 477`로 ownership accounting은 완결되지만 어느 쪽도 0이 아니다. 이 grid는 존재 반증용이며 실제일 prevalence를 추정하지 않는다. 따라서 replacement와 removal 중 하나를 geometry만으로 선택할 수 없다.

### 4. target invariant

```text
production target matrix: 54/54 exact 100
production authority: 54/54 pass
production 9 penalty axes: 54/54 zero

cross-profile smoke: 12/12 exact 100
cross-profile authority: 12/12 pass
cross-profile 9 penalty axes: 12/12 zero
```

### 5. privacy-safe actual-day evidence 계약

다음 gate의 입력은 사용자가 명시적으로 제공한 backup이어야 한다. 현재 sanitizer는 raw meal name, memo, body metrics, date 같은 원본 필드를 결과 JSON/CSV에 내보내지 않고 extraction/count/distribution과 익명 대표 표본까지만 만든다.

- residual-positive / core-unique / core-overlap count
- residual 방향과 크기
- current score와 aggregation probe의 익명 요약
- 표본 부족/범주 누락 표시

actual-day matched-pair grouping은 아직 구현하지 않았다. 다음 gate에서는 다음 계약을 먼저 닫아야 한다.

- joint를 제외한 energy/carb/fat core penalty vector, goal, training context, source-validity 상태를 matching 축으로 사용한다.
- exact match와 여러 tolerance sensitivity를 함께 보고하며, 한 tolerance만 골라 결론을 만들지 않는다.
- unmatched count, coverage, 각 residual 방향의 표본 수를 모두 공개한다.
- rejected current joint가 포함된 현재 final score나 aggregation probe를 독립 정답으로 사용하지 않는다.
- core-matched residual contrast가 tolerance sensitivity에서 안정성을 잃고 actual-day residual-positive가 기존 core 설명으로만 남으면 `DROP_JOINT_AXIS_READY` 후보로 보낸다.
- core-unique가 여러 goal/training 맥락에서 반복되고, current score를 참조하지 않아도 joint-feasibility라는 별도 제품 의미를 설명하며, overlap 사례를 0 또는 deduplicated 처리할 수 있을 때만 `CURRENT_JOINT_REPLACEMENT_READY` 후보로 보낸다. curve coefficient 승인은 그 뒤 별도다.
- 두 구조가 계속 섞이거나 표본/coverage가 부족하면 production을 열지 않고 `MORE_EVIDENCE_REQUIRED`를 유지한다. coefficient만 바꾸는 반복 loop는 허용하지 않는다.

실제 backup이 없으면 `evidence unavailable`로 끝내며 합성 자료로 사용자 결과를 대신하지 않는다.

### 6. 작업 결과

- 브랜치: `codex/v8.4-option-c-joint-residual-formula-simulation`
- 기능 커밋: `cd1ee2a` (`v8.4 탄수 지방 잔여 감점 산식 시뮬레이션`)
- branch push: `origin/codex/v8.4-option-c-joint-residual-formula-simulation` 완료
- master no-ff merge: `997c14d` (`v8.4 탄수 지방 잔여 감점 시뮬레이션 병합`)
- master push: merge와 이 결과로그 마감 커밋까지 완료
- production `index.html`: 변경 없음
- scoring version/UI/storage/schema/Records/DailyCoach/tag: 변경 없음

### 7. 읽은 기준

- 최상위 기준문서: `docs/00_current_truth/00_READ_FIRST.txt`, `02_macro_range_current_truth.txt`, `04_document_status_index.txt`, `05_required_result_log_format.txt`
- 직접 선행 결정: `docs/v8.4_component_score_architecture_falsification_decision_2026-07-11.md`
- target correction closeout: `docs/v8.3.1_target_scoring_authoritative_reference_correction_implementation_2026-07-12.md`
- v8.2 문서 상태 확인: historical archive로만 확인했고 production body로 사용하지 않았다.
- 외부근거 anchor: 기존 anchor 유지, 변경 없음.

### 8. 판단

- 수용: production-owned joint budget, C1/C2 동치 geometry, continuous/monotonic/symmetric/bounded residual, core ownership 분리, privacy-safe actual-day evidence gate.
- 폐기: current threshold cliff, C3 asymmetric legacy baseline, simulation-only replacement 승인, 근거 없는 joint 제거, coefficient tuning next gate.
- 통합: 54 production target과 12 cross-profile target invariant를 Option C 반증 harness에 통합했다.
- 보류: current joint replacement, joint-axis removal, 모든 curve coefficient, component aggregation/selection, production implementation, DailyCoach semantic v2.
- 금지선: 특정 coefficient/hash의 영구 정책 고정, actual backup 없이 actual-day 결론 주장, hidden modifier, core/joint 중복감점, UI/storage/schema/Records 혼합.
- 내 판단 다음 단계: privacy-safe actual-day joint ownership evidence. 먼저 위 matching/tolerance/coverage 계약을 구현하고 residual-positive를 core-unique/core-overlap으로 나눈다. current score를 순환 oracle로 쓰지 않고, residual 차이를 별도 점수축으로 소유할 제품 의미가 있는지 판단한다.

### 9. 검증

- `npm run test:component-score-simulation`: 39 assertions / 39 pass / 0 fail.
- schema: `component_score_architecture_falsification_v2`.
- deterministic hash: `54fc8f1d9644a01c78b21cd020dbbfab6160fc28f664d366272929b904f80587`.
- `npm run test:docs-policy`: pass.
- `npm run test:macro-policy`: 31 suites / 306 cases / 0 fail, console/page error 0.
- `npm run test:full`: 143 suites / 1,491 cases / 0 fail, console/page error 0.
- post-merge `master@997c14d`에서도 component simulation, docs-policy, 전체 143 suites / 1,491 cases를 다시 통과했다.
- `git diff --check`: pass.
- 독립 최종 감사 2회: P0/P1/P2 없음, MERGE_READY.
- production diff guard: `index.html`, `package.json`, `AGENTS.md`, result-log contract, 이전 falsification decision 모두 unchanged.
- v8.3 peeled tag: 기존 `0333d9c73956fe58d2cd1284279751a6f507c1b8`을 그대로 유지했다.
- actual private-backup audit: 실행하지 못함. workspace에 명시적으로 제공된 backup이 없으며 optional sanitizer만 준비됨.

## production-authority correction — 비개발자용 설명

Date: 2026-07-12

### 무엇을 바꿨는지

앱의 점수나 화면은 바꾸지 않았다. 대신 앞선 시뮬레이션이 탄수·지방 조합 신호를 기존 감점과 비교할 때 사용한 기준을 바로잡았다.

앞선 `237 core-unique / 240 core-overlap`은 실제 앱의 전체 감점 내역이 아니라 총칼로리·탄수·지방만 단순화한 test-local helper 결과였다. 이번 correction은 같은 66개 조건과 같은 3,058개 표본을 실제 production 점수 경로에 통과시켜, 조합 감점 하나만 제외한 production 8축으로 다시 분류한다.

실제 backup을 나중에 분석할 선택 경로도 고쳤다. 이제 full-backup 형식과 기록 당시 raw goalSnapshot의 목표·체중·운동 출처를 점수 계산 전에 검사한다. snapshotless 기록이나 현재 Settings가 필요한 기록은 점수를 새로 만들지 않고 제외 사유만 집계한다.

### 왜 바꿨는지

기존 helper에는 production의 다음 동작이 빠져 있었다.

- 감량 중 목표 초과와 증량 중 목표 부족의 가중치
- 목표와 총소비량 중 높은 기준을 보는 TDEE overload 축
- 지방을 줄인 만큼 탄수를 늘린 정상 same-kcal exchange 예외
- 단백질·술·데이터 이상 축
- 실제 앱이 축별 감점을 0.001 단위로 반올림하는 동작

또한 기존 backup 경로는 snapshot이 없는 과거 식사를 현재 Settings 목표와 섞어 재평가할 수 있었다. 실제 기록을 넣기 전에 이 두 권한 문제를 먼저 닫지 않으면, 정밀해 보이는 결과가 잘못된 기준에서 나올 수 있다.

### 실제 사용자 화면이나 계산 결과가 어떻게 달라지는지

변경 없음.

- Today 점수, 항목별 감점, 탄수·지방 목표와 화면 문구는 그대로다.
- 과거 Records 점수를 다시 계산하거나 저장하지 않는다.
- scoring version과 production formula를 바꾸지 않는다.
- 이번 숫자는 test-only evidence이며 사용자 점수로 노출하지 않는다.

### 정책, 산식, 데이터 해석이 바뀌었는지

production 정책과 산식은 변경 없음. evidence 해석만 교정했다.

```text
같은 residual-positive 477개

5d8ee0d historical test-local helper:
237 unique / 240 overlap

현재 production joint 제외 8축:
242 unique / 235 overlap
```

5개가 overlap에서 unique로 이동했다. 모두 helper가 `0.000059~0.000389점`의 극소 에너지 감점을 남겼지만 production breakdown은 0.001 단위 반올림 뒤 0이 되는 경우였다. goal multiplier, TDEE, same-kcal 예외 차이도 실제로 존재하지만 이번 제한된 grid의 0/양수 분류를 추가로 바꾸지는 않았다.

따라서 기존 `237/240`은 historical 잠정치로만 보존한다. 새 `242/235`도 실제 사용자 빈도나 별도 감점의 필요성을 증명하는 숫자는 아니다. C1/C2는 두 독립 후보가 아니라 동일 residual의 두 수학 표현이다.

### 바꾸지 않은 범위와 보류한 내용

- `index.html`, production scoring version/formula, UI, storage/schema, backup format, Records, DailyCoach, v8.3 tag는 바꾸지 않았다.
- current joint replacement와 joint-axis removal은 모두 미승인이다.
- residual coefficient와 component aggregation도 열지 않았다.
- 사용자가 explicit backup 경로를 제공하지 않았으므로 실제 기록은 읽지 않았다.
- actual-day prevalence와 blinded product-meaning 실제 판단은 보류했다.

### 사용자가 큰 틀에서 확인해야 할 판단점

production-authority correction outcome: AUTHORITY_CORRECTED_AWAITING_EXPLICIT_BACKUP

이 outcome은 새 감점 후보를 승인했다는 뜻이 아니다. production 기준 분류와 안전한 기록 필터를 준비했지만, 실제 사용 기록이 없으므로 replacement와 removal 어느 쪽도 고르지 않았다는 뜻이다.

다음에는 사용자가 `--actual-backup=<path>`를 명시한 경우에만 source-safe 기록을 분석한다. 그 결과에서도 현재 최종점수나 current joint penalty를 정답으로 쓰지 않는다. 같은 production 8축·goal·training·source-validity끼리 exact와 여러 tolerance로 비교하고, 점수 숫자를 가린 blinded product-meaning 사례에서 다음 중 하나를 판단한다.

```text
별도 joint 의미가 있음
기존 core 설명이면 충분
판단 불가
```

### 테스트에서 무엇을 검증했는지

- 같은 3,058개 표본 ID와 좌표가 production 평가 결과와 모두 1:1 대응하는지 확인했다.
- 3,058개 모두 production target authority와 joint 제외 8축이 완전한지 확인했다.
- historical `237/240`과 production `242/235` 사이의 5개 이동을 sample ID와 사유로 남겼다.
- 합성 full-backup 17건에서 출처가 완전한 휴식일 1건과 유산소-only 운동일 1건만 포함하고, snapshotless·target·weight·training·meal·full-day·비정상/중복 날짜 문제 15건을 점수 계산 전에 제외하는지 확인했다.
- snapshotless 기록은 점수 함수를 호출하기 전에 차단하고, 포함 기록은 `frozen_goal_snapshot`과 `goalSnapshot` 체중 source를 쓰며 currentResult fallback을 쓰지 않는지 확인했다.
- `null`/빈 문자열이 숫자 0으로 강제변환되어 target·training 출처 검사를 우회하지 않는지, 중복 날짜의 식사와 다른 snapshot이 섞이지 않는지 확인했다.
- raw snapshot의 유산소 정보를 production helper로 에너지화하여 유산소-only 기록을 휴식일로 오분류하지 않는지 확인했다.
- exact, tolerance 0.25, tolerance 1.0을 모두 실행하고 unmatched/coverage를 계산하는지 확인했다.
- blinded output에 current final score, current joint penalty, candidate penalty, 날짜, 음식명, 메모, 식사 ID, 절대 섭취량과 backup 경로가 없는지 확인했다.

## production-authority correction — 기술 검증

### 10. PROMPT_SCOPE_AUDIT

- 요청된 다음 단계: 실제 backup 분석 전에 production-authoritative ownership과 snapshot source safety를 교정한다.
- repo 기준 실제 다음 단계: 맞다. 기존 current truth의 actual-day gate를 그대로 실행하면 test-local ownership과 snapshotless current-Settings fallback을 실제 근거로 오인할 수 있었다.
- 실제 문제: residual 공식이 아니라 residual을 기존 감점과 비교하는 source와 과거 기록의 목표 소유권이 부정확했다.
- 문서 밖에서 확인한 증거: production `getMacroRangeProductionScoreSummary`의 9축 breakdown, goal multiplier, TDEE reference, same-kcal carb 예외, 0.001 rounding, `getMacroBalanceForDate`의 live target fallback, `getDailyAdherenceScore`의 stored-score 우회 조건을 직접 확인했다.
- 검토한 대안: 기존 helper에 production 동작을 계속 복제, 곧바로 실제 backup 분석, production code 변경, browser에서 production 함수를 직접 호출하는 test-only correction.
- 선택한 작업이 실제 문제를 푸는 이유: helper 복제 대신 production 함수가 만든 반올림 breakdown을 primary로 사용하고, raw snapshot 권한이 부족하면 score 호출 전에 차단한다.
- 이 선택이 틀렸다고 볼 조건: sample identity가 달라지거나 production 8축/authority가 불완전하거나, snapshotless가 score 호출에 도달하거나, blinded output에 숨겨야 할 score/private field가 나타나는 경우다.
- 최소 변경 표면: permanent simulation harness, 기존 결과로그 append-only correction, current truth/status/README, docs-policy 의미 guard.
- 이 작업 안에서 완결한 범위: geometry ownership 교정, historical drift, full-backup/source prefilter, exact/복수 tolerance matcher, blinded review contract, 실제 backup 부재 상태.
- 더 넓히지 않은 이유: 실제 기록과 장기 outcome label이 없으므로 제품 의미·coefficient·production implementation을 판단할 수 없다.

### 11. production-exact ownership

primary vector는 production의 반올림된 다음 8축이다.

```text
targetEnergyDeviationPenalty
tdeeOverloadPenalty
proteinShortagePenalty
proteinExcessEfficiencyPenalty
fatRangePenalty
carbExerciseContextPenalty
alcoholPhysiologyPenalty
dataOutlierPenalty
```

`carbFatExchangeFailurePenalty`만 제외한다. test-local energy/carb/fat helper는 historical 비교 전용으로 이름과 역할을 격하했으며 current ownership이나 matcher signature에 사용하지 않는다.

```text
geometry: 66/66 valid
samples: 3,058/3,058 source·coordinate·authority·8축 complete
residual-positive: 477
production-core-unique: 242
production-core-overlap: 235
unclassified: 0
historical simplified: 237 / 240
drift: 5, 모두 overlap -> unique
C1/C2 max delta: 1e-15
```

drift sample은 다음 5개다.

- `diet_rest_low::raw_4_0`
- `diet_rest_medium::raw_4_0`
- `baseline_male_diet_rest::raw_4_0`
- `small_female_diet_rest::raw_4_0`
- `large_older_male_diet_rest::raw_4_0`

### 12. actual-day source safety

- full-backup envelope와 non-empty raw Settings/records만 허용한다. root records 또는 `payload.data` fallback은 폐기했다.
- raw goalSnapshot target/macro closure/goal/weight/training provenance와 raw meal macro/full-day 조건을 normalize 전에 검사한다.
- snapshotless와 prefilter 실패 기록은 `scoreEvaluationAttempted:false`로 남기며 score 함수에 넣지 않는다.
- `null`/빈 문자열 numeric coercion, 실제 달력에 없는 날짜, 같은 날짜 중복 기록은 score 호출 전에 차단한다. 평가 대상 record state에는 통과한 고유 날짜 기록만 넣어 식사/snapshot 교차 혼합을 막는다.
- 평가 가능한 기록은 snapshot-owned result/options를 사용한다.
- 웨이트·유산소 energy는 raw snapshot 필드와 production `calcCardio`/`calcWeightTrainingKcal` 경로로 재구성한다. 유산소 source가 불완전하면 제외하며 `cardioKcal:0`으로 숨겨 휴식일을 만들지 않는다.
- postcondition은 `targetAuthority.source=frozen_goal_snapshot`, `weightSource=goalSnapshot`, `totalBurnSource`가 `currentResult.*`가 아님, snapshot goal 소유, complete 9-axis breakdown과 joint model이다.
- TDEE를 snapshot에서 소유할 수 없으면 현재 TDEE를 빌리지 않고 `snapshot_basis_unavailable` source-validity로 분리한다.

합성 source-safety fixture 결과는 다음과 같다.

```text
source records: 17
included: 2
excluded: 15
snapshotless: 2
invalid snapshot authority: 1
missing weight provenance: 1
missing training provenance: 4
null/blank target provenance: 2
invalid meal macros: 1
insufficient full day: 1
invalid calendar date: 1
duplicate record date: 2
invalid envelope/settings: fail-closed
```

### 13. matched evidence와 blinded review

matching context는 joint 제외 production 8축, goal, training context, source-validity다. `exact`, `tolerance_0_25`, `tolerance_1_00`을 모두 내며 한 tolerance만 골라 결론을 만들지 않는다. 각 결과는 pair/matched/unmatched/coverage를 함께 낸다.

blinded case는 goal/training, 상대 target ratio band, non-joint vector 최대 차이, residual 방향과 small/moderate/large만 보여 준다. current final score, current joint/candidate penalty와 private/absolute field는 내보내지 않는다.

### 14. 판단과 다음 gate

- 수용: production direct 8-axis ownership, rounded breakdown contract, raw snapshot prefilter, explicit full-backup only, exact+multi-tolerance, blinded product meaning.
- 폐기: simplified helper를 current ownership으로 사용, snapshotless hidden recompute, current Settings/currentResult fallback, final score/current joint를 oracle로 사용, actual 빈도만으로 penalty 정당화.
- 통합: 기존 Option C geometry harness에 authority/source-safety/matcher/blind contract를 통합했다.
- 보류: actual-day result, replacement/removal, coefficient, aggregation, production scoring version, DailyCoach semantic v2.
- 금지선: backup 임의 탐색, raw private evidence commit, 한 tolerance cherry-pick, candidate outcome을 production 승인으로 해석, UI/storage/schema/Records 변경.
- 내 판단 다음 단계: 사용자가 경로를 명시한 explicit backup이 있을 때만 item 55 actual-day evidence를 실행한다. source-safe coverage가 부족하면 `MORE_EVIDENCE_REQUIRED`로 끝내며 합성 fixture로 실제 결론을 대신하지 않는다.

### 15. correction 검증

- branch: `codex/v8.4-joint-ownership-evidence-authority-correction`
- production `index.html`: 변경 없음
- actual backup 사용: 없음
- simulation: 50 assertions / 50 pass / 0 fail, two-run same hash
- deterministic hash: `1e2bc3cab6f8cee17085f77503dc6e70bdc9b247f76c7859d4e9aa0b9d3eb07e`
- C1/C2와 target 54/54 + cross-profile 12/12 invariant: pass
- production penalty key set: 3,058/3,058 exact 9축, 그중 joint 제외 ownership 8축
- source-safety synthetic fixture: 17 records / 2 included / 15 pre-score excluded; cardio-only snapshot context owned
- `npm run test:docs-policy`: pass
- `npm run test:macro-policy`: 31 suites / 306 cases / 0 fail
- `npm run test:full`: 143 suites / 1,491 cases / 0 fail
- `git diff --check`: pass
- 독립 최종 감사 3개 축(math/production authority, backup privacy/source safety, docs-policy falsifiability): P0/P1/P2 없음, `MERGE_READY`
- 실제 backup analysis와 blinded 사용자 판단: 실행하지 않음
- feature commit: `144d68ea574aac3c2fe85dfef0126309b30517d8` (`v8.4 조합 감점 근거 분류를 production 기준으로 교정`)
- feature push: `origin/codex/v8.4-joint-ownership-evidence-authority-correction`에 동일 commit 반영
- no-ff merge commit: `052cac0089faf65e56259eac8a45ec5bdaf85e30` (`v8.4 조합 감점 근거 교정 병합`)
- first master push: `origin/master`에 merge commit 반영
- post-merge `master@052cac0`: simulation 50/50·동일 hash, docs-policy, full 143 suites / 1,491 cases / 0 fail
- 이 Git closeout 기록만 docs-only follow-up commit으로 마감하며 production 파일은 계속 unchanged다.

## 16. actual-day evidence preflight hardening — 비개발자용 설명

### 무엇을 바꿨는지

실제 백업을 분석하기 직전의 검사 도구를 두 축으로 보강했다.

첫째, 식사에 `alcoholKcal` 또는 `otherKcal` 항목이 아예 없는 과거 기록은 0으로 호환하되, 항목이 실제로 존재하면 정상적인 0 이상 숫자인지 원본 상태에서 검사한다. `null`, 빈칸, 문자, 음수, 무한대처럼 잘못된 값은 0으로 조용히 바꾸지 않고 점수 계산 전에 제외한다. 개별값은 정상 숫자여도 두 식사의 합이 너무 커 무한대가 되는 경우도 별도 제외한다. 운동 routine도 simple 모드의 공통 선택지 또는 advanced 운동 profile에서 앱이 실제로 제공하는 선택지와 일치해야 한다.

둘째, 제품 의미를 판단하는 자료와 판단 뒤 답을 확인하는 자료를 물리적으로 나눴다. 먼저 보는 review packet에는 탄수·지방 비율 구간과 맥락만 있고 residual 방향·크기와 점수는 없다. A/B 배치와 사례 제시 순서도 고정 seed로 섞는다. residual은 사용자가 판단을 고정한 뒤 별도 reveal 파일에서만 확인할 수 있다. reveal을 만들 때는 앞서 판단에 사용한 review 파일이 같은 백업에서 나온 잠긴 packet인지 검사하며 그 파일을 덮어쓰지 않는다.

### 왜 바꿨는지

기존 도구는 잘못된 술·기타 열량이 앱 정규화 과정에서 0으로 바뀌어, 실제로는 존재하는 alcohol/energy 감점을 없는 것처럼 분류할 수 있었다. 또한 기존 “blinded” 사례는 점수는 숨겼지만 residual 방향과 크기를 먼저 보여 줬기 때문에 어느 쪽을 문제 사례로 볼지 암시했다. 이 상태로 실제 기록을 판단하면 데이터 오류와 판단 유도가 동시에 섞일 수 있다.

검토 중 문서에 적힌 두 항목만 기계적으로 고치지 않고 production 경로를 다시 비교했다. 그 결과 하루 기록 30% 기준도 기존 도구는 탄수·단백질·지방 열량만 사용하지만 production은 술·기타 열량을 포함한 scoring kcal를 쓴다는 차이를 찾았다. 값 차단만 고치고 이 차이를 남기면 정상적인 alcohol/other 기록이 표본에서 빠질 수 있으므로 함께 정렬했다.

### 실제 사용자 화면이나 계산 결과가 어떻게 달라지는지

앱 화면과 production 점수 계산 결과는 달라지지 않는다. 이번 변경은 나중에 사용자가 명시적으로 제공한 백업에서 어떤 날짜를 근거로 인정할지와, 어떤 순서로 판단 자료를 보여 줄지만 바꾼다.

예를 들어 목표가 2,200kcal이고 탄수·단백질·지방에서 550kcal를 기록했다면 macro만으로는 25%라 하루 판단을 보류한다. 그러나 같은 날 alcohol 196kcal와 other 24kcal가 정상적으로 기록돼 있으면 production scoring kcal는 770kcal, 즉 35%이므로 분석 대상이 될 수 있다. 반대로 `alcoholKcal: "abc"`처럼 잘못된 값이 있으면 이를 0으로 고쳐 포함하지 않고 그 날짜를 score 호출 전에 제외한다.

### 정책, 산식, 데이터 해석이 바뀌었는지

production 영양 정책과 점수 산식은 변경 없음이다. `index.html`, scoring version, penalty 계수와 joint 산식은 그대로다.

바뀐 것은 test-only actual evidence의 데이터 해석 계약이다. 선택 열량 key 부재는 과거 기록의 legacy zero로 해석하지만, key가 존재하는 잘못된 값은 “0 섭취”가 아니라 “근거로 사용할 수 없는 원본”으로 해석한다. 하루 표본 충족 여부는 production과 같은 total scoring kcal로 해석한다. 또한 기존 packet은 정확히 score-blinded였고, 이번 standalone review artifact부터 residual 가설까지 숨긴 hypothesis-blind로 본다.

### 바꾸지 않은 범위와 보류한 내용

실제 백업 경로는 제공되지 않았고 임의 검색도 하지 않았다. 따라서 실제 사용자의 포함 기록 수, residual-positive 빈도, matched coverage와 제품 의미 결론은 아직 없다. 현재 상태 `AUTHORITY_CORRECTED_AWAITING_EXPLICIT_BACKUP`은 그대로이며, 이는 `ACTUAL_EVIDENCE_INSUFFICIENT`가 아니다. 후자는 명시적인 백업을 실제로 검사했지만 표본이 부족할 때만 사용한다.

current joint 교체·삭제, coefficient/curve 선택, component aggregation, production score/version, UI, storage/schema/backup 형식, Records, DailyCoach semantic v2와 v8.3 tag도 모두 보류했고 변경하지 않았다.

### 사용자가 큰 틀에서 확인해야 할 판단점

지금 추가 판단은 필요 없다. 이번 단계의 큰 판단은 “실제 기록을 보기 전에 출처 오류와 판단 유도를 닫는다”이며 구현과 검증으로 마감한다.

나중에 명시적 백업으로 review packet이 만들어지면 사용자는 exact score나 계수를 고르는 것이 아니라, A/B 두 사례가 `별도 joint 의미가 있다`, `기존 core 설명이면 충분하다`, `판단 불가` 중 어디에 해당하는지만 먼저 판단한다. 그 판단을 고정한 뒤에만 같은 packet hash의 reveal을 확인한다.

### 테스트에서 무엇을 검증했는지

합성 기록에서 key가 없는 과거 식사는 정상 포함되고, 정상적인 alcohol/other 열량은 30% production 기준과 표준잔 계산에 실제로 남는지 확인했다. 두 선택 field 각각의 `null`·빈 문자열·문자·음수, 추가 극단값과 합계 overflow, profile에 없는 routine은 모두 score 호출 전에 제외되는지 확인했다.

blind 검증에서는 review packet이 exact allowlist 밖의 필드와 residual/score/private 정보를 전혀 갖지 않는지, reveal만 residual을 갖는지, A/B source 대응이 유지되는지 확인했다. 같은 입력을 두 번 실행하거나 입력 순서를 뒤집어도 packet/hash가 같고, A/B 양 방향이 실제로 나타나며, residual 크기순 사례 번호를 피하도록 제시 순서가 seed로 결정되는지도 확인했다. 변경된 packet으로 reveal을 시도하면 실패하고 기존 locked review가 보존되는 계약, raw backup과 output 경로 충돌 차단도 확인했다.

## 17. actual-day evidence preflight hardening — 기술 검증

### 작업 결과

- 브랜치: `codex/v8.4-joint-actual-day-evidence-preflight`
- production `index.html`: 변경 없음
- 실제 backup 사용: 없음; 명시적 경로를 임의 탐색하지 않음
- 새 decision 문서: 없음; 이 기존 결과로그에 append-only로 통합
- commit / push / merge: 기능 검증 뒤 closeout subsection에 기록

### PROMPT_SCOPE_AUDIT

- 요청된 다음 단계: `1f6dcd0`을 유지하고, 실제 백업 전에 present-invalid non-macro kcal 차단과 진짜 blind review/reveal 분리를 test/docs-only로 닫는다.
- repo 기준 실제 다음 단계: 맞다. item 55 actual evidence는 명시적 backup이 있어야 열리므로, 지금은 그 입력 전 preflight가 실제 substantive gate다.
- 실제 문제: production normalizer가 잘못된 meal 값을 0으로 만들 수 있고 기존 packet이 residual 가설을 노출해 actual evidence의 분류와 사람 판단을 각각 오염시킬 수 있다.
- 문서 밖에서 확인한 증거: production `normalizeMeal`/`normalizeMacroValue`, `calculateMealEnergy`, `getMealTotalsForDate`, `getMacroBalanceForDate`, pending 30% gate와 profile별 routine 선택지를 직접 추적했다. 기존 harness prefilter가 macro 3개만 검사하고 macro kcal만 합산하며, 기존 case가 residual 방향/크기와 residual 차이순 case order를 내보내는 것도 코드에서 확인했다.
- 검토한 대안: production normalizer 변경, optional key 부재까지 전부 제외, residual label만 삭제하고 같은 JSON에 reveal 유지, A/B만 섞고 residual 차이순 case order 유지, 문서 문구만 정정.
- 선택한 작업이 실제 문제를 푸는 이유: production 화면·계산을 건드리지 않고 실제 근거 입력 경계에서만 invalid/coercion/overflow를 fail-closed하고, 판단 전달물은 residual summary/reveal과 별도 파일로 분리한다. production scoring kcal와 routine vocabulary를 직접 대조해 표본 편향도 줄인다.
- 이 선택이 틀렸다고 볼 조건: legacy key 부재 기록이 빠지거나 present-invalid/overflow/corrupt routine이 score에 도달하는 경우, 같은 input의 packet/hash가 달라지는 경우, review에 residual/score/private field가 나타나는 경우, changed backup이 locked review 검증을 통과하는 경우, production 파일 diff가 생기는 경우다.
- 최소 변경 표면: simulation harness, 관련 assertion, current truth/status/README/docs-policy와 이 append-only 결과로그.
- 이 작업 안에서 완결한 범위: strict source value/derived-total/routine validation, production full-day threshold 정렬, canonical deterministic A/B와 presentation order, exact review/reveal allowlist, hash linkage, locked review와 separate post-judgment reveal, ignored-debug/distinct-path gate.
- 더 넓히지 않은 이유: 실제 backup과 사람의 product-meaning 판단이 없으므로 actual prevalence, joint 유지·삭제, coefficient, aggregation과 production 구현을 승인할 근거가 없다.

### 구현 세부사항

- `isExplicitRawFiniteNumber`는 숫자처럼 강제변환 가능한 문자열·boolean·array가 아니라 native finite number만 허용한다.
- `isValidOptionalRawNonMacroKcal`는 `hasOwnProperty`로 key 부재와 present-invalid를 분리한다.
- `getRawActualRecordExclusionReason`은 `invalid_meal_non_macro_kcal`, `non_finite_meal_energy_total`, profile별 routine provenance를 score 전에 판정하고 alcohol/other를 포함한 scoring kcal로 30%를 계산한다.
- `buildHypothesisBlindProductMeaningReview`는 sample id로 canonical pair를 만든 뒤 seed의 side hash로 A/B를, 별도 order hash로 presentation을 정한다. 선택은 가장 좁은 tolerance가 pair를 먼저 소유하고 presentation 뒤 case id를 붙인다.
- review artifact는 ratio band/context/non-joint similarity/세 판단 선택지만 갖는다. reveal artifact는 같은 `reviewPacketHash`, case id, randomized A/B의 `source_1|source_2`, residual direction/magnitude만 갖는다.
- 실제 artifact write 전 exact key/value enum, case id uniqueness, hash와 case linkage를 다시 검사한다. pre-judgment review는 기존 파일을 덮어쓰지 않고, post-judgment reveal은 기존 locked review를 읽어 같은 generated packet인지 확인한 뒤 review를 수정하지 않는다.
- actual backup, audit ledger, review와 reveal 출력은 서로 다른 경로여야 하고 모두 ignored `tools/render_audit/_debug/` 아래에 있어야 한다.

### 읽은 기준

- 최상위 기준문서: `AGENTS.md`, `docs/00_current_truth/00_READ_FIRST.txt`, `02_macro_range_current_truth.txt`, `04_document_status_index.txt`, `05_required_result_log_format.txt`
- 관련 current 문서/코드: 이 Option C 결과로그 전체, `docs/00_current_truth/templates/new_doc_preamble.txt`, `docs/README.md`, `tools/render_audit/README.md`, simulation harness, docs-policy, production `index.html`의 backup/meal/score/routine 경로
- v8.2 문서 상태 확인: `04_document_status_index.txt`에서 historical archive임을 확인했고 직접 기준으로 사용하지 않음
- 외부근거 anchor: nutrition/scoring policy 수치를 바꾸지 않아 새 외부 anchor는 사용하지 않음

### 판단

- 수용: `1f6dcd0`, production 8축 ownership 교정, historical 수치 보존, explicit-backup-only, legacy optional-key absence zero, strict present-value/derived-total gate, production scoring-kcal threshold, standalone hypothesis-blind 판단 절차.
- 폐기: present-invalid를 0으로 정규화해 actual 근거로 사용, score-blinded packet을 hypothesis-blinded로 부르기, residual label을 review에 제공, residual 차이순 case 번호, input-order A/B, review/reveal 동시 전달, reveal 때 review 덮어쓰기.
- 통합: 요청된 두 보강과 코드 재검토에서 찾은 scoring-kcal threshold, aggregate overflow, routine provenance, locked-packet/path-collision 방어를 같은 actual evidence preflight에 통합.
- 보류: explicit backup actual result, 제품 의미 판단, replacement/removal, coefficient, aggregation, production formula/version/UI/storage/Records/DailyCoach.
- 금지선: backup 임의 탐색, raw/private/generated artifact commit, 현재 점수·joint penalty를 oracle로 사용, 한 tolerance cherry-pick, actual evidence 없이 정책 outcome 변경.
- 내 판단 다음 단계: 사용자가 full-backup 경로를 명시할 때만 source-safe actual audit과 standalone review를 생성한다. review 판단을 고정한 뒤 같은 locked packet으로 reveal한다. 표본이나 matched pair가 부족할 때만 `ACTUAL_EVIDENCE_INSUFFICIENT`로 끝낸다.

### 현재 검증

- component simulation: 61 assertions / 61 pass / 0 fail
- deterministic hash: `e0e77831fa514348b1296de67685a71a938bfe6bbb731eae12caf29860b202eb`
- source-safety fixture: 30 records / 4 included / 26 pre-score excluded
- invalid non-macro fixture: alcohol/other 각각 null·blank·text·negative 8건 모두 `invalid_meal_non_macro_kcal`, score 미호출
- derived total fixture: finite `Number.MAX_VALUE` 두 건의 day sum overflow를 `non_finite_meal_energy_total`로 score 전 차단
- full-day threshold fixture: macro 550 + alcohol 196 + other 24 = 770kcal, 2,200kcal의 35%, standard drinks 2로 production 결과와 정렬
- legacy fixture: optional key 실제 부재, energy ratio 1, standard drinks 0으로 포함
- routine vocabulary: simple 공통 `REST|PUSH`와 advanced profile별 set이 production selectable set 전체와 일치; non-bodybuilding simple `PUSH`는 포함하고 임의 routine은 score 전 차단
- blind fixture: exact/0.25/1.0, narrowest-tolerance dedup, same/reversed input deterministic, both A/B orientations, exact allowlists, side/hash/case linkage, changed locked packet과 input/output collision 거부
- 나머지 docs/macro/full/Git 검증은 아래 closeout에 append한다.

### Git / 검증 closeout

- feature commit: `a700d5115ae22595dbe0cddb14497558437bfdd0` (`v8.4 실제 기록 조합 근거의 blind/source 안전성 보강`)
- feature push: `origin/codex/v8.4-joint-actual-day-evidence-preflight`에 동일 commit 반영
- no-ff merge commit: `c89e313c3ece1bda0e846116e5d9f93e48f138b5` (`v8.4 실제 기록 조합 근거 사전검증 병합`)
- first master push: `origin/master`에 merge commit 반영
- pre-merge component simulation two-run: 각각 61/61 pass, 동일 hash `e0e77831fa514348b1296de67685a71a938bfe6bbb731eae12caf29860b202eb`
- pre-merge docs-policy: pass
- pre-merge macro-policy: 31 suites / 306 cases / 0 fail
- pre-merge full: 143 suites / 1,491 cases / 0 fail
- post-merge `master@c89e313`: component 61/61·동일 hash, docs-policy pass, full 143 suites / 1,491 cases / 0 fail
- `git diff --check`, `git diff --check HEAD^ HEAD`, Node syntax check: pass
- production static QA: base/current `index.html` blob가 모두 `23714d9067cc7276d1fbb919c60d8664c8513904`; production scoring path와 `package.json` diff 없음
- result-log append-only QA: historical/current outcome 줄은 각각 정확히 1개이며 기존 본문 삭제 없음
- 독립 최종 감사: blind artifact, non-macro/source/routine, docs-policy/result-log 세 축 모두 P0/P1/P2 없음, `MERGE_READY`
- actual backup analysis와 product-meaning judgment: 실행하지 않음; 명시적 경로가 없어 현재 awaiting 상태 유지
- 이 closeout 기록만 docs-only follow-up commit으로 master에 추가하며 production 파일은 계속 unchanged다.

## 18. explicit-backup actual-day evidence closeout — 비개발자용 설명

actual-day evidence outcome: ACTUAL_EVIDENCE_INSUFFICIENT

### 무엇을 바꿨는지

사용자가 명시적으로 제공한 전체 백업을 실제로 검사했다. 처음 실행에서 판단 도구가 과거 기록 55개를 현행 운동 profile 항목이 없다는 이유만으로 제외하고, 같은 날짜를 여러 비교에 재사용하며, 사용자가 보기에는 에너지·단백질 차이와 탄수·지방 차이가 섞인 사례까지 고르는 문제를 발견했다. 이 결과는 판단에 사용하지 않고 폐기했다.

과거 profile 항목이 생기기 전의 기록은 당시 production 계약에 따라 다시 해석했다. 휴식일은 profile과 무관한 과거 기록으로, 알려진 단일 운동 routine은 당시 기본 bodybuilding 상세 routine으로 복구했다. 출처를 단정할 수 없는 복합 routine 2개만 점수 호출 전에 제외했다. 그 결과 전체 98개 중 96개를 실제 근거 후보로 인정했다.

그다음 비교 가능한 날짜와 사용자가 의미를 판단할 수 있는 날짜를 분리했다. 같은 목표·운동 맥락·기록 출처이고 기존 조합 항목을 제외한 8개 점수 축이 같은 범위에 있는 날짜는 base pair로 집계했다. 실제 판단 카드는 에너지와 단백질이 같은 5% 구간이고 탄수와 지방이 모두 다른 경우만 허용하며, 같은 날짜 재사용과 화면상 중복을 막고, residual 차이가 있는 비교 사례와 같은 residual 분류의 대조 사례가 같은 맥락에 함께 있어야만 열도록 했다.

### 왜 바꿨는지

이 작업의 목적은 새 감점을 만들기 전에 “기존 8개 축이 같은 두 날에서도 탄수·지방의 공동 배분 차이가 사용자에게 별도 의미로 보이는가”를 확인하는 것이다. 에너지나 단백질 차이가 함께 보이면 사용자가 그 차이를 보고 판단했는지 탄수·지방 조합을 보고 판단했는지 분리할 수 없다. 같은 날짜를 여러 번 쓰거나 residual 차이가 있는 사례만 고르면 표본 수와 결론이 부풀려진다.

따라서 96개가 포함됐다는 사실만으로 충분하다고 보지 않았다. source-safe record 수, base match 수, 사람이 읽을 수 있는 독립 비교 수를 각각 따로 계산했고, 마지막 조건을 통과하지 못하면 빈 review packet으로 끝내도록 했다.

### 실제 사용자 화면이나 계산 결과가 어떻게 달라지는지

앱 화면과 production 계산 결과는 변경 없음이다. `index.html`, 현재 점수, 목표 열량과 탄수·단백질·지방 범위, Records, 백업 형식, DailyCoach는 그대로다.

이번에 달라진 것은 test-only 판단 도구의 근거 선택 방식뿐이다. 예를 들어 A와 B의 에너지가 각각 목표의 95~99%로 같은 구간이고 단백질도 같은 구간이면서, 탄수와 지방은 양쪽 모두 다른 구간이어야 탄수·지방 공동 배분을 비교할 수 있다. 에너지가 90~94%와 100~104%처럼 서로 다르거나 탄수·지방 중 한쪽만 다르면 base match로는 집계될 수 있어도 사용자 판단 카드에서는 제외한다.

### 정책, 산식, 데이터 해석이 바뀌었는지

production 정책과 산식은 변경 없음이다. joint 항목을 유지할지, 연속 residual로 교체할지, 삭제할지 어느 쪽도 승인하지 않았다.

변경된 데이터 해석은 audit 전용이다. 현행 profile 필드가 없는 55개 과거 snapshot을 current Settings로 채우지 않고 historical production 계약으로 복구했다. 이는 앱 schema migration이나 오래된 Records 재계산이 아니다. 또한 base pair가 존재한다는 것과 사용자 판단에 충분한 balanced pair가 존재한다는 것을 다른 의미로 분리했다.

### 바꾸지 않은 범위와 보류한 내용

production formula/version, coefficient, component aggregation, UI, storage/schema, backup format, Records migration, DailyCoach semantic v2와 v8.3 tag는 모두 변경하지 않았다. 첫 진단 packet은 판단이나 reveal에 사용하지 않았고, 최종 review packet은 빈 배열이다. residual reveal도 생성하지 않았으며 도구가 실행 자체를 거부하는지 확인했다.

component-score actual-day aggregation 재평가와 최종 후보 선택·폐기는 계속 막혀 있다. 근거 부족은 “joint 의미가 없다”는 증명이 아니므로 joint 제거로 바로 넘어가지 않는다.

### 사용자가 큰 틀에서 확인해야 할 판단점

지금 사용자가 A/B 사례를 판단할 단계가 아니다. 최종 상태는 `NO_BALANCED_JUDGEABLE_BLOCK`이고 `reviewForUser: false`다. 따라서 `별도 joint 의미가 있음`, `기존 core 설명이면 충분`, `판단 불가` 중 하나를 지금 고르도록 요청하지 않는다.

같은 백업을 다시 실행하는 것도 다음 작업이 아니다. 나중에 사용자가 실질적으로 더 넓어진 전체 백업을 새로 명시한 경우에만 이 evidence gate를 다시 열 수 있다. 그 전까지 joint 교체·삭제, coefficient와 aggregation 구현은 모두 보류한다.

### 테스트에서 무엇을 검증했는지

- 과거 profile 없는 단일 routine과 휴식 기록은 historical source로 복구되고, 복합 routine 2개와 잘못된 source marker는 score 호출 전에 막히는지 확인했다.
- base matching이 residual 차이가 있는 사례만 세지 않고 대조 사례도 포함하며, exact·0.25·1.0 세 tolerance의 전체 pair·matched record·coverage·최대 독립 pair를 각각 계산하는지 확인했다.
- 일반 그래프 최대 matching이 삼각형·경로·홀수 cycle과 입력 순서 변경에서 같은 최대 독립 pair 수를 내는지 확인했다.
- 백업의 Records 배열 순서를 뒤집어도 익명 sample identity와 판단 결과가 같고, 같은 백업을 두 번 실행한 audit/review 파일이 byte 단위로 같은지 확인했다.
- review에 residual, 방향, 크기, 점수, 날짜, 음식, 메모, 식사 ID, 절대 g/kcal, 백업 경로가 없고, 같은 날짜 재사용과 화면상 중복이 없는지 확인했다.
- 판단 가능한 balanced block이 없으면 review가 비고 post-judgment reveal 파일이 생성되지 않는지 확인했다.

## 19. explicit-backup actual-day evidence closeout — 기술 검증

### 작업 결과

- 브랜치: `codex/v8.4-joint-actual-day-evidence`
- production `index.html`: 변경 없음
- 새 decision 문서: 없음; 이 기존 결과로그에 append-only 통합
- raw backup은 repo 밖의 명시 경로에서 읽기만 했고, generated audit/review만 ignored debug에 존재하며 어느 것도 commit하지 않음
- 최종 outcome: `ACTUAL_EVIDENCE_INSUFFICIENT`
- 근거 상태: `NO_BALANCED_JUDGEABLE_BLOCK`
- review / reveal: `reviewForUser: false`, `reviewPacket: []`, reveal 미생성·실행 차단
- commit / push / merge: 검증 뒤 closeout subsection에 기록

### PROMPT_SCOPE_AUDIT

- 요청된 다음 단계: 명시된 full-backup으로 source-safe actual audit과 standalone hypothesis-blind review를 실행한다.
- repo 기준 실제 다음 단계: actual evidence 실행은 맞았다. 다만 첫 출력에서 과거 profile 기록 누락, residual-selected case, sample 재사용과 화면상 confound가 발견돼 그 packet을 사용자에게 전달하지 않고 evidence harness부터 바로잡아야 했다.
- 실제 문제: joint residual이 기존 production 8축과 별개로 사용자가 구분할 제품 의미를 갖는지, private 기록과 rejected current score를 oracle로 쓰지 않고 확인할 수 있어야 한다.
- 문서 밖에서 확인한 증거: production의 profile 도입 전후 Git 이력, historical record-basis routine 해석, raw snapshot/meal/score 경로, 실제 source cohort와 base/judgeable pair 분포, 첫 packet의 사용자 판별 가능성을 직접 대조했다.
- 검토한 대안: 첫 packet 그대로 전달, 55개 legacy 기록 계속 제외, residual 차이가 있는 pair만 coverage로 사용, energy/protein confound 또는 한 macro 차이를 허용해 case 수 늘리기, balanced control 없이 judgment 요청, reveal 선실행, production formula 변경.
- 선택한 작업이 실제 문제를 푸는 이유: current Settings를 섞지 않은 historical source 복구 뒤 residual-free base graph를 만들고, 5% 표시 구간·전역 sample 독립성·화면상 dedup·같은 source stratum의 contrast/control block을 별도 gate로 두어 “match가 있음”과 “사람이 joint 의미를 판단할 수 있음”을 구분한다.
- 이 선택이 틀렸다고 볼 조건: raw record 순서가 packet/hash를 바꾸거나, current/legacy source cohort가 교차 match되거나, base coverage에서 control이 사라지거나, review에 숨긴 가설/private field가 나타나거나, balanced block이 있는데 빈 packet이 만들어지거나, review가 비었는데 reveal이 생성되는 경우다.
- 최소 변경 표면: test-only component simulation harness, 합성 assertion, current truth/status/README/docs-policy와 이 append-only 결과로그.
- 이 작업 안에서 완결한 범위: legacy source recovery, residual-free base matching, 최대 독립 pair capacity, 5% judgeability, 화면상 dedup, disjoint balanced selection, empty-packet/reveal fail-closed, explicit backup actual outcome.
- 더 넓히지 않은 이유: 판단 가능한 contrast/control block이 없으므로 joint replacement/removal, coefficient, aggregation과 production 구현을 승인할 실제 근거가 없다.

### actual evidence

- source records: 98
- source-safe included: 96
- pre-score excluded: 2, 모두 `ambiguous_legacy_compound_routine`
- historical profile-source recovery: 55
- exact base: 8 pairs / 10 matched records / coverage `0.104167` / maximum disjoint 4
- tolerance 0.25 base: 12 pairs / 13 matched records / coverage `0.135417` / maximum disjoint 6
- tolerance 1.0 base: 30 pairs / 30 matched records / coverage `0.3125` / maximum disjoint 13
- judgeable: exact 0 / tolerance 0.25 0 / tolerance 1.0 1
- hidden balanced roles at widest tolerance: contrast 1 / control 0
- balanced block candidates: 0
- selected cases / samples: 0 / 0
- 2.5 percentage-point band-offset sensitivity flip: 0
- 모든 included 기록의 total-burn source는 `snapshot_basis_unavailable`이므로 이 백업만으로 일반화하는 데 추가 한계가 있음

### 구현 세부사항

- `resolveActualTrainingProfileProvenance`는 explicit current-schema profile, profile-independent legacy REST, legacy expert bodybuilding routine을 서로 다른 source provenance로 유지한다. markerless 여부도 source key에 남겨 서로 다른 근거 cohort를 교차 match하지 않는다.
- valid unique record date에서 만든 내부 salted digest를 sample identity로 사용해 raw array order가 case selection을 바꾸지 않게 했다. 날짜나 digest는 review에 출력하지 않는다.
- `buildActualMatchedEvidence`는 residual을 보지 않고 goal/training/source와 production joint 제외 8축 tolerance로 base graph를 만든다. judgeable subset과 hidden contrast/control capacity는 그 다음 단계다.
- hidden control은 residual 방향과 기존 magnitude band가 모두 같은 pair다. 둘 중 하나라도 다르면 contrast다. 이 label은 review에 출력하지 않는다.
- review 적격 pair는 energy/protein 5% band가 각각 같고 carb/fat band가 모두 달라야 한다. emitted field만으로 visible signature를 만들고 source가 달라도 화면상 같으면 한 사례로 dedup한다.
- maximum-cardinality general matching은 base/role별 독립 capacity를 보고한다. 최종 packet은 같은 hidden stratum 안에서 서로 다른 4개 sample의 contrast 1개와 control 1개를 한 balanced block으로 묶고 전역 sample 재사용을 막는다.
- 한 balanced block은 제한된 blind review를 구성할 논리적 최소일 뿐 production 정책을 승인하지 않는다. review artifact는 항상 `policyDecisionAuthorized: false`를 유지한다.

### 읽은 기준

- 최상위 기준문서: `AGENTS.md`, `docs/00_current_truth/00_READ_FIRST.txt`, `02_macro_range_current_truth.txt`, `04_document_status_index.txt`, `05_required_result_log_format.txt`
- 관련 current 문서/코드: 이 Option C 결과로그 전체, `docs/00_current_truth/templates/new_doc_preamble.txt`, `docs/README.md`, `tools/render_audit/README.md`, simulation harness, docs-policy, production `index.html`의 backup/record-basis/profile/routine/score 경로와 관련 Git 이력
- v8.2 문서 상태 확인: `04_document_status_index.txt`에서 historical archive임을 확인했고 직접 기준으로 사용하지 않음
- 외부근거 anchor: nutrition/scoring policy 수치를 바꾸지 않아 새 외부 anchor는 사용하지 않음

### 판단

- 수용: explicit-backup actual 실행, 55개 historical source 복구, residual-free base graph, exact/복수 tolerance, maximum disjoint capacity, 엄격한 judgeability, hypothesis-blind disjoint balanced contract, `ACTUAL_EVIDENCE_INSUFFICIENT`.
- 폐기: 첫 진단 packet과 그 10개 case, residual-selected coverage, sample 재사용, 화면상 duplicate, energy/protein confounded pair, control 없는 사용자 판단, 판단 전 reveal.
- 통합: source false-negative 교정, base/control/contrast 분리, raw-order-independent identity, 5% band와 offset sensitivity, general matching, review/reveal fail-closed를 같은 actual evidence closeout에 통합.
- 보류: joint replacement/removal, residual coefficient, component aggregation, production scoring version/formula, UI/storage/schema/Records/DailyCoach.
- 금지선: 첫 진단 artifact 재사용, raw/private/generated artifact commit, 같은 백업 반복을 새 evidence로 가장하기, 표본 수를 늘리려고 judgeability 완화, 현재 final score/current joint를 oracle로 사용, item 56/57 또는 DailyCoach 구현 선행.
- 내 판단 다음 단계: 즉시 열 실질 구현 없음. 사용자가 실질적으로 더 넓어진 full-backup을 새로 명시한 경우에만 item 55 evidence를 재개한다. 그 전까지 item 56/57은 blocked, DailyCoach semantic v2는 paused다.

### 현재 검증

- component simulation: 67 assertions / 67 pass / 0 fail
- deterministic hash: `fafd0207833ffb9e80bc053db9e85ed29e54d1b8161a7517145fef3e5d12215a`
- source-safety fixture: 36 records / 6 included / 30 pre-score excluded
- actual same-input two-run: audit/review 모두 byte-identical
- final standalone review: exact allowlist pass, `reviewPacket: []`
- blocked reveal probe: nonzero exit, reveal file 미생성
- production static diff: `index.html` 변경 없음
- docs-policy, macro-policy, full, Git 검증과 commit/push/merge는 아래 closeout에 append한다.

### Git / 검증 closeout

- feature commit: `bcac0796aa48e9a376bef9d38c9f00a8488b2cb2` (`v8.4 실제 기록 조합 근거 부족을 확정`)
- feature push: `origin/codex/v8.4-joint-actual-day-evidence`에 동일 commit 반영
- no-ff merge commit: `4d4a1abb7a86dd128108c58538095202b2f14261` (`v8.4 실제 기록 조합 근거 감사 병합`)
- first master push: `origin/master`에 merge commit 반영
- pre-merge component simulation: 67/67 pass, deterministic hash `fafd0207833ffb9e80bc053db9e85ed29e54d1b8161a7517145fef3e5d12215a`
- pre-merge docs-policy: pass
- pre-merge macro-policy: 31 suites / 306 cases / 0 fail
- pre-merge full: 143 suites / 1,491 cases / 0 fail
- post-merge `master@4d4a1ab`: component 67/67·동일 deterministic hash, docs-policy pass, full 143 suites / 1,491 cases / 0 fail
- actual same-input two-run: audit/review 각각 byte-identical, final packet empty, reveal 없음
- blocked reveal probe: nonzero exit, reveal file 미생성
- `git diff --check`, `git diff --cached --check`, Node syntax check: pass
- production static QA: base/current `index.html` blob가 모두 `23714d9067cc7276d1fbb919c60d8664c8513904`; production scoring path diff 없음
- privacy QA: raw backup 경로·파일명·hash와 generated actual artifact/hash는 commit하지 않음
- result-log append-only QA: historical overall/correction outcome과 current actual-day outcome 줄은 각각 정확히 1개이며 기존 본문 삭제 없음
- 독립 최종 감사: actual evidence, harness/privacy, docs/result-log 세 축의 발견사항을 모두 교정한 뒤 P0/P1/P2 없음, `MERGE_READY`
- 이 Git closeout 기록만 docs-only follow-up commit으로 master에 추가하며 production 파일은 계속 unchanged다.

## 20. actual-context-anchored counterfactual closeout — 비개발자용 설명

actual-context counterfactual evidence outcome: COUNTERFACTUAL_PACKET_INSUFFICIENT

### 무엇을 바꿨는지

실제 기록 두 날짜를 억지로 짝짓는 대신, 한 실제 기록의 목표·운동 맥락·총열량·단백질을 그대로 둔 채 탄수화물과 지방 총량만 서로 바꾼 통제 시나리오를 만드는 test-only 경로를 추가했다. A와 B는 모두 생성한 시나리오이며 어느 한쪽도 실제로 먹은 식사나 실제 날짜가 아니다.

교환 단위는 탄수화물 `+9g`와 지방 `-4g`, 또는 그 반대다. 탄수 9g과 지방 4g은 각각 36kcal이므로 총열량은 같다. 예를 들어 탄수화물을 18g 늘리면 지방은 8g 줄이는 식이다. 이 교환을 실제 기록에서 가능한 범위 안에서 대칭으로 미리 만들고, 그 뒤에야 별도 joint residual 분류를 계산했다.

### 왜 바꿨는지

item 55의 실제 날짜 비교는 96개 source-safe 기록이 있어도 사람이 읽을 수 있는 독립 contrast/control 묶음을 만들지 못했다. 그렇다고 관찰 기록을 복제하거나 조건을 느슨하게 해 사례 수를 부풀릴 수는 없다. 이번 경로의 목적은 실제 빈도가 아니라, 같은 현실적 맥락에서 탄수·지방 공동 배분만 달라졌을 때 사용자가 별도 제품 의미를 느끼는지 판단할 수 있는 통제 비교가 가능한지 확인하는 것이었다.

생성기는 residual, 현재 final score, 현재 joint penalty나 원하는 hidden role을 입력으로 받지 않는다. 모든 후보를 고정 교환표로 먼저 만든 뒤, 총열량·단백질·비매크로 열량·목표·운동 맥락·target authority와 joint를 제외한 production 8축이 정확히 같은 경우만 남겼다.

### 실제 사용자 화면이나 계산 결과가 어떻게 달라지는지

앱 화면과 production 계산 결과는 변경 없음이다. `index.html`, 점수 산식과 version, 목표 범위, Today, Records, 저장·백업 형식, DailyCoach는 그대로다.

판단 카드가 충분했다면 사용자는 절대 g/kcal, 날짜, 음식, 점수, residual 정답 없이 목표 대비 5% 구간으로 표현된 A/B만 봤을 것이다. 그러나 실제 결과는 지방 쪽으로 벗어나는 대비 사례가 0이어서 한 방향만 보여 주는 편향된 packet이 될 수 있었다. 따라서 카드 0개로 닫았고 사용자 화면에는 아무것도 추가하지 않았다.

### 정책, 산식, 데이터 해석이 바뀌었는지

production 정책과 산식은 변경 없음이다. item 55의 관찰 결과 `ACTUAL_EVIDENCE_INSUFFICIENT`도 그대로다. 이번 생성 시나리오는 실제 사용자가 그런 식단을 먹었다거나 그 패턴이 자주 발생한다는 근거가 아니다.

audit 전용으로만 “관찰 evidence”와 “실제 맥락에 고정한 controlled counterfactual”을 별도 증거 유형으로 구분했다. 전자는 발생 빈도와 실제 pair를 다루고, 후자는 다른 조건을 고정한 생성 비교의 제품 의미만 제한적으로 다룬다.

### 바꾸지 않은 범위와 보류한 내용

joint replacement/removal, residual coefficient, component aggregation, production formula/version, UI, storage/schema, backup, Records migration, DailyCoach semantic v2와 v8.3 tag는 모두 보류했다. 생성 packet 부족은 “joint 의미가 없다”는 증명이 아니다.

결과를 맞추기 위해 지방 방향 사례가 나오도록 교환 폭을 사후 확대하거나 실제 marginal support, core-zero, 5% 표시 구간, 양방향 3:3 균형을 낮추지 않았다. raw backup과 generated audit/review는 ignored debug 밖으로 내보내거나 commit하지 않았다.

### 사용자가 큰 틀에서 확인해야 할 판단점

지금은 A/B를 평가할 단계가 아니다. source-safe 96개 맥락에서 null-control 12개와 carb-heavy contrast 6개는 만들 수 있었지만 fat-heavy contrast가 0이었다. 요구한 null 6개, carb 방향 3개, fat 방향 3개의 12-case packet이 아니므로 `reviewForUser: false`다.

정확한 해석은 “실제 맥락에 고정한 생성 비교 자체는 가능했지만, 양 방향 대조를 갖춘 사람 판단 묶음은 만들 수 없었다”이다. actual prevalence, policy, coefficient, aggregation이나 production 구현으로 넘어갈 근거는 생기지 않았다.

### 테스트에서 무엇을 검증했는지

- 탄수 `9g`/지방 `4g`의 36kcal 교환 뒤 총열량, derived scoring kcal와 단백질이 그대로이고 두 개의 synthetic meal 합계가 생성 총량과 닫히는지 확인했다.
- 원본 기록 순서를 뒤집거나 원본에 residual/final-score 유도 메타데이터를 넣어도 생성 후보와 선택 결과가 같은지 확인했다.
- 양방향 contrast와 null-control, 서로 다른 12개 anchor, 두 개 이상 public context가 모두 있어야만 review를 여는지 확인했다.
- 가능한 6-block 조합을 순차 선택 때문에 놓치지 않도록 deterministic backtracking으로 완전한 균형 조합을 찾고, 한 anchor의 첫 표시 pair가 충돌하면 같은 anchor의 다른 유효 pair까지 탐색하는지 확인했다.
- 대비 사례의 outside 정답 위치가 A/B에 3개씩 균형 배치되고, review에서 본 A/B와 post-judgment reveal의 A/B가 같은 시나리오로 결속되는지 확인했다.
- 한 방향 contrast만 있거나 malformed/부분 packet이면 review와 reveal이 정확히 비고 예외 없이 fail closed하는지 확인했다.
- 같은 명시 backup을 두 번 실행한 audit/review가 byte-identical인지 확인했다.

## 21. actual-context-anchored counterfactual closeout — 기술 검증

### 작업 결과

- 브랜치: `codex/v8.4-joint-actual-anchored-counterfactual`
- production `index.html`: 변경 없음
- 새 standalone decision 문서: 없음; 이 기존 결과로그에 append-only 통합
- evidence kind: `controlled_counterfactual_not_observed_prevalence`
- 최종 상태: `COUNTERFACTUAL_PACKET_INSUFFICIENT`
- review / reveal: `reviewForUser: false`, review packet empty, reveal map empty, reveal 파일 미생성
- private input/output: repo 밖의 명시 backup은 read-only, generated audit/review는 ignored debug 전용이며 commit하지 않음
- commit / push / merge: 최종 검증 뒤 closeout subsection에 기록

### PROMPT_SCOPE_AUDIT

- 요청된 다음 단계: 실제 데이터가 부족하면 같은 실제 맥락에서 통제 시나리오를 만들어 판단 근거를 구성한다.
- 실제 문제: 관찰 빈도를 꾸미지 않으면서 탄수·지방 공동 배분만의 별도 제품 의미를 사람이 판단할 수 있는 양방향 contrast/control 비교를 만들 수 있는가다.
- 문서 밖에서 확인한 증거: 실제 source-safe 96개 기록의 target/goal/training/source shape, production score 9축과 joint 제외 8축, meal·non-macro kcal closure, 고정 교환 grid의 feasible variant, 표시 구간과 hidden role 분포를 직접 검사했다.
- 검토한 대안: item 55 actual pair를 재사용, 한 관찰 쪽과 한 생성 쪽 비교, residual이 큰 시나리오부터 생성, carb 방향만으로 판단 요청, 부족 결과 뒤 exchange 폭·support·judgeability 완화, production 구현 선행.
- 선택한 작업이 실제 문제를 푸는 이유: A/B 모두를 같은 실제 anchor에서 생성하고 에너지·단백질·비매크로 열량·권한·운동·8축을 고정하면 탄수·지방 joint 차이 외 설명을 제한할 수 있다. 생성과 hidden selection을 분리하고 양방향 3:3 + null 6을 요구해 한 방향 결론 유도를 막는다.
- 이 선택이 틀렸다고 볼 조건: 교환 에너지가 닫히지 않거나, residual/current score가 후보 생성에 영향을 주거나, raw 순서가 packet을 바꾸거나, 8축/authority가 달라지거나, 실제 marginal support 밖 시나리오가 섞이거나, 가능한 balanced 조합을 선택기가 놓치거나, A/B 위치가 불균형하거나, review와 reveal side가 어긋나는 경우다.
- 최소 변경 표면: test-only component simulation harness, assertions, 이 append-only 결과로그와 current truth/status/README/docs-policy.
- 완결 범위: actual-context generator, production browser 재평가, hidden-role-independent generation, exact balanced selection, privacy allowlist, review/reveal lock과 실제 two-run closeout.
- 넓히지 않은 이유: fat-heavy 적격 contrast가 0이어서 어떤 선택 방식으로도 요구한 양방향 packet을 만들 수 없었다.

### actual-context counterfactual evidence

- source records: 98
- source-safe included / pre-score excluded: 96 / 2
- excluded: `ambiguous_legacy_compound_routine` 2
- anchor candidates: 96
- fixed exchange steps: `t = -8..-1, 1..8`; carb `+9t g`, fat `-4t g`, exchange energy gap 0
- assigned roles: null-control 51 / carb-heavy contrast 25 / fat-heavy contrast 20
- eligible roles after all invariants: null-control 12 / carb-heavy contrast 6 / fat-heavy contrast 0
- block candidates: carb-heavy 8 / fat-heavy 0
- required packet: null 6 + carb 3 + fat 3, 6 blocks / 12 distinct anchors, at least 2 public contexts, no visible duplicate
- selected balanced blocks / cases: 0 / 0
- current actual-day outcome preserved: `ACTUAL_EVIDENCE_INSUFFICIENT`
- actual prevalence / policy / coefficient authorization: 모두 false

### 구현 세부사항

- `extractAnonymizedActualDaysFromPayload(..., { counterfactualRequested: true })`만 고정 exchange family를 만든다. 기본 observed lane의 출력 shape와 의미는 유지한다.
- browser production helper가 각 variant의 target authority, 9개 penalty, macro ratios와 closure invariant를 다시 평가한다. generator 입력에는 residual, current final score, current joint penalty나 desired role이 없다.
- `buildActualAnchoredCounterfactualEvidence`는 같은 goal/training의 actual marginal support, anchor/variant core-zero, joint 제외 production 8축 exact equality, 5% display judgeability와 2.5 percentage-point offset 안정성을 요구한다.
- hidden role은 source stratum 안의 canonical anchor 순서와 고정 pattern으로 residual 평가 전에 배정한다. null은 inside/inside, contrast는 inside/outside이며 carb/fat 방향을 구분한다.
- selection은 방향별 3개씩의 고정 schedule과 각 anchor의 모든 유효 display-pair option을 deterministic backtracking으로 탐색하고 anchor/visible signature 재사용을 금지한다. 완전한 6-block/12-case와 두 context가 없으면 부분 packet을 버린다.
- ready packet에서 contrast outside는 A/B 각각 3개로 deterministic balanced allocation한다. review는 5% ratio band와 공통 context만 포함하고 reveal은 별도 파일에서 같은 A/B band binding, role/inside/outside coherence를 검증한다.
- review/reveal strict allowlist는 날짜, 음식, meal/memo, absolute g/kcal, raw/current/final score, residual 정답, sample/anchor id, backup path/hash를 차단한다.

### 읽은 기준

- `AGENTS.md`
- `docs/00_current_truth/00_READ_FIRST.txt`
- `docs/00_current_truth/02_macro_range_current_truth.txt`
- `docs/00_current_truth/04_document_status_index.txt`
- `docs/00_current_truth/05_required_result_log_format.txt`
- 이 Option C 결과로그, `docs/README.md`, production `index.html`의 target/score/meal/backup 경로, simulation harness와 docs-policy
- v8.2 status: historical archive임을 status index에서 확인했고 직접 구현 기준으로 사용하지 않음
- 외부근거: nutrition 정책값이나 제품 산식을 바꾸지 않아 새 외부 anchor를 사용하지 않음

### 판단

- 수용: actual-context anchor, A/B 모두 generated, fixed symmetric isoenergetic exchange, production 8-axis exact equality, actual marginal support, balanced two-direction/null review gate, `COUNTERFACTUAL_PACKET_INSUFFICIENT`.
- 폐기: actual occurrence로 가장하기, observed/generated 혼합 pair, residual-directed generation, carb-only packet, partial review, 판단 전 reveal, 부족 결과 뒤 조건 완화.
- 통합: generator/evaluator separation, deterministic backtracking, A/B outside balance, review/reveal side binding, malformed artifact fail-closed를 기존 actual evidence harness에 별도 lane으로 통합.
- 보류: 사람 product-meaning judgment, joint replacement/removal, coefficient, component aggregation, production scoring/UI/storage/schema/Records/DailyCoach.
- 금지선: raw/private/generated artifact commit, backup 임의 탐색, current score/joint oracle, counterfactual을 actual prevalence로 해석, 결과를 맞추는 사후 grid 확대나 invariant 완화, item 56/57 또는 DailyCoach 구현 선행.
- 내 판단 다음 단계: 이 packet에서는 사용자 판단이나 reveal을 열지 않는다. item 55와 item 56/57의 blocked 상태도 유지한다. 새 실질 근거 없이 추가 구현을 지어내지 않는다.

### 현재 검증

- component simulation: 77 assertions / 77 pass / 0 fail
- deterministic hash: `fdf3f1ad2c0131f526c91cb8068026eb34e763a841fabfd0f86a025792eb5598`
- actual same-input two-run: audit/review 각각 byte-identical
- actual status: `COUNTERFACTUAL_PACKET_INSUFFICIENT`, review/reveal case 0/0
- observed-lane regression: source 98 / included 96 / excluded 2, base exact `8/10/max4`, tolerance 0.25 `12/13/max6`, tolerance 1.0 `30/30/max13`, judgeable `0/0/1`, 기존 outcome 보존
- blocked reveal probe: nonzero exit, reveal 파일 미생성
- docs-policy: pass
- macro-policy: 31 suites / 306 cases / 0 fail
- full: 143 suites / 1,491 cases / 0 fail
- Node syntax / `git diff --check`: pass
- production static diff: `index.html` 변경 없음
- 독립 최종 감사: counterfactual selection/privacy/reveal과 docs/result-log 모두 P0/P1/P2 없음, `MERGE_READY`
- Git commit/push/merge는 아래 closeout에 append한다.

### Git / 검증 closeout

- feature commit: `7ca593af0f9dbee6940da79be4229f913390a2ef` (`v8.4 실제 맥락 반사실 근거 부족을 확정`)
- feature push: `origin/codex/v8.4-joint-actual-anchored-counterfactual`에 동일 commit 반영
- no-ff merge commit: `71ca3162fdb2a0b01eecdedd810cabce2fac51a2` (`v8.4 실제 맥락 반사실 근거 감사 병합`)
- first master push: `origin/master`에 merge commit 반영
- pre-merge: component 77/77, docs-policy pass, macro-policy 31 suites/306 cases, full 143 suites/1,491 cases, 모두 0 fail
- post-merge `master@71ca316`: component 77/77·동일 deterministic hash, docs-policy pass
- actual final two-run: audit/review 각각 byte-identical, `COUNTERFACTUAL_PACKET_INSUFFICIENT`, review/reveal case 0/0
- blocked reveal probe: nonzero exit, reveal 파일 미생성
- production static QA: base/current `index.html` blob가 모두 `23714d9067cc7276d1fbb919c60d8664c8513904`
- privacy QA: raw backup 경로·파일명·hash와 generated actual artifact/hash는 commit하지 않음
- result-log QA: historical overall/correction/actual-day outcome 줄을 보존하고 counterfactual outcome 줄을 정확히 1개 추가
- 독립 최종 감사: selection completeness, A/B balance, reveal binding, privacy, docs/status 전부 P0/P1/P2 없음, `MERGE_READY`

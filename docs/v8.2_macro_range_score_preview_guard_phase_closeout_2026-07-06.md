# v8.2 macro range score preview guard phase closeout

상태:

```text
문서 성격: score preview guard phase closeout / readiness checkpoint docs-only
작업 범위: docs-only
index.html 변경: 없음
package.json 변경: 없음
tools/render_audit 변경: 없음
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
score formula / sign / rounding 결정: 없음
goalSnapshot schema 변경: 없음
record schema 변경: 없음
storage/schema 구현: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
DailyCoach action 변경: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_score_delta_preview_policy_tests_implementation_2026-07-06.md` 이후 phase closeout이다.

목적:

```text
component pointsPreview, candidateScorePreview, scoreDeltaPreview guard 라인이 무엇을 증명했는지 정리한다.
동시에 production-facing preview field가 shape / test-only guard까지 닫혔다는 사실을 production numeric readiness, storage readiness, UI readiness로 오해하지 않도록 금지선을 재고정한다.
```

최종 판단:

```text
score preview guard phase: closeout 가능
component pointsPreview guard: test-only 완료
candidateScorePreview guard: test-only 완료
scoreDeltaPreview guard: test-only 완료
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
UI / Recent / DailyCoach readiness: 아직 아님
production macro range readiness: 아직 아님
```

## 1. closeout 범위

이번 closeout에 포함되는 라인:

```text
score/storage separation decision
candidateScorePreview field decision
component pointsPreview shape decision
component pointsPreview numeric policy test design / tests
candidateScorePreview numeric policy test design / tests
scoreDeltaPreview policy decision
scoreDeltaPreview policy test design / tests
README registration
macro-policy suite registration
tools/render_audit core profile registration
```

이번 closeout의 기반이 되는 이전 phase:

```text
report-only numeric preview phase closeout
testLocalNumericPreview nested-only
consumer no-leak guard
Records basis / version no-overwrite guard
macro-policy / core profile registration coverage
```

이번 closeout에 포함되지 않는 것:

```text
actual component pointsPreview number
actual candidateScorePreview number
actual scoreDeltaPreview number
candidate score sum formula
score delta sign / rounding / clamp
getDailyAdherenceScore replacement
ADHERENCE_SCORING_VERSION bump
goalSnapshot schema 변경
record schema 변경
backup / restore schema 변경
Recent range-aware score aggregation
Today / Records / Recent UI range score 표시
DailyCoach range score action
production macro range 구현
```

## 2. 승인 가능한 것

현재까지 승인 가능한 것:

```text
candidateScorePreview라는 future field 이름을 예약
candidateScorePreview 허용 위치 후보를 rangeScoreCandidateSummary.candidateScorePreview로 제한
component pointsPreview라는 future field 이름을 예약
component pointsPreview 허용 위치 후보를 rangeScoreCandidateSummary.componentPreviews.<component>.pointsPreview로 제한
scoreDeltaPreview라는 future field 이름을 예약
scoreDeltaPreview 허용 위치 후보를 comparison.scoreDeltaPreview로 제한
세 production-facing preview field의 기본값을 null로 유지
testLocalNumericPreview는 nested test-local evidence로만 유지
old fixed Records 저장값 no-overwrite 유지
storage / backup / Recent / UI / DailyCoach no-leak 유지
promoted production-facing payload rejection을 test-only guard로 고정
macro-policy suite와 core profile에 guard suite 등록
```

승인 가능한 작업 성격:

```text
docs-only decision
docs-only test design
test-only guard
suite 내부 fixture
suite 내부 synthetic unapproved payload
suite 내부 promoted payload rejection
before/after no-impact assertion
registration audit
phase closeout / readiness checkpoint
```

## 3. 승인되지 않은 것

아직 승인되지 않은 것:

```text
rangeScoreCandidateSummary.candidateScorePreview 숫자화
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview 숫자화
comparison.scoreDeltaPreview 숫자화
component point sum formula
candidate score clamp / rounding policy
score delta sign convention
score delta display precision
fixed score fallback candidate
sentinel zero score / delta
getDailyAdherenceScore range-aware 변경
ADHERENCE_SCORING_VERSION bump
rangeScoreCandidateSummary 저장
macroRangeContract 저장
backup / restore roundtrip
Recent aggregation
Today / Records / Recent UI 표시
DailyCoach reason/action 연결
```

특히 아래 오해를 금지한다.

```text
testLocalNumericPreview 숫자 존재 == candidateScorePreview production 숫자화 승인
component pointsPreview shape 결정 == component points 숫자화 readiness
candidateScorePreview guard 통과 == candidateScorePreview numeric readiness
scoreDeltaPreview guard 통과 == scoreDeltaPreview numeric readiness
consumer no-leak 통과 == production consumer 연결 승인
macro-policy/core registration 통과 == production score readiness
old fixed Records eligibility 후보 == old fixed Records 저장값 변경 허용
comparison.fixedScore 존재 == scoreDeltaPreview 계산 승인
```

## 4. 현재 production 계약

현재 source of truth:

```text
fixed target goalSnapshot
targetCal
targetProtein
targetCarbs
targetFat
getDailyAdherenceScore fixed scoring path
ADHERENCE_SCORING_VERSION = v6.1_alcohol_penalty_v2
stored adherencePercent / adherenceScoringVersion fixed basis
backup / restore fixed record normalize path
Recent fixed target component counts
DailyCoach fixed signal basis
```

현재 정상 동작:

```text
rangeScoreCandidateSummary.candidateScorePreview는 null이다.
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview는 null이다.
comparison.candidateScorePreview는 null이다.
comparison.scoreDeltaPreview는 null이다.
testLocalNumericPreview는 report-only source object 안에만 있다.
comparison.fixedScore는 fixed score source를 참조할 수 있지만 delta 계산을 의미하지 않는다.
backup payload는 candidateScorePreview / pointsPreview / scoreDeltaPreview를 포함하지 않는다.
Recent는 preview score / component point / delta를 읽지 않는다.
DailyCoach는 preview score / component point / delta를 action source로 쓰지 않는다.
UI text는 preview score / component point / delta를 표시하지 않는다.
recordsState.items는 report-only candidate 생성으로 변하지 않는다.
```

이 상태는 score preview 미완성의 결함이 아니라 현재 phase의 안전 계약이다.

## 5. 고정된 guard

고정된 guard:

```text
candidateScorePreview null
component pointsPreview null
scoreDeltaPreview null
candidateScorePreviewStatus not computed
dataQuality.pointsPreview null-only
neutral / explanation no-bonus
testLocalNumericPreview nested-only
candidate null이면 scoreDeltaPreview null
component pointsPreview null이면 scoreDeltaPreview null
unapproved numeric payload가 있어도 scoreDeltaPreview null
fixed-score fallback 금지
sentinel zero score / delta 금지
old fixed Records no-mutation
saved adherencePercent / adherenceScoringVersion no-overwrite
record / goalSnapshot / backup no preview fields
Recent no-aggregation
UI no-visibility
DailyCoach no-action
ADHERENCE_SCORING_VERSION unchanged
promoted candidateScorePreview payload rejection
promoted pointsPreview payload rejection
promoted scoreDeltaPreview payload rejection
macro-policy suite export coverage
core profile registration coverage
```

guard가 막는 실패:

```text
production-facing preview field가 조용히 숫자로 승격되는 것
test-local numeric evidence가 저장/표시/행동 근거로 새는 것
candidateScorePreview가 fixed score fallback으로 채워지는 것
scoreDeltaPreview가 fixed score와 candidate score의 production 비교처럼 보이는 것
blocked state가 0점 또는 0 차이로 저장되는 것
old fixed Records 저장값이 report-only preview로 덮이는 것
backup/restore가 preview field를 roundtrip하는 것
Recent가 fixed/range score basis를 섞는 것
DailyCoach가 range score preview를 action 근거로 쓰는 것
UI/debug text가 preview field를 사용자에게 노출하는 것
QA profile에서 macro-policy suite가 빠지는 것
```

## 6. 문서별 상태

| 문서 | 상태 | production 영향 |
| --- | --- | --- |
| `v8.2_macro_range_score_report_only_numeric_preview_phase_closeout_2026-07-05.md` | 이전 phase closeout | production score 변경 없음 |
| `v8.2_macro_range_score_storage_separation_decision_2026-07-05.md` | 통과 | storage 구현 없음 |
| `v8.2_macro_range_candidate_score_preview_field_decision_2026-07-05.md` | 통과 | candidateScorePreview 숫자화 없음 |
| `v8.2_macro_range_component_points_preview_shape_decision_2026-07-05.md` | 통과 | pointsPreview 숫자화 없음 |
| `v8.2_macro_range_component_points_preview_numeric_policy_test_design_2026-07-06.md` | 통과 | 없음 |
| `v8.2_macro_range_component_points_preview_numeric_policy_tests_implementation_2026-07-06.md` | test-only 통과 | production score 변경 없음 |
| `v8.2_macro_range_candidate_score_preview_numeric_policy_test_design_2026-07-06.md` | 통과 | 없음 |
| `v8.2_macro_range_candidate_score_preview_numeric_policy_tests_implementation_2026-07-06.md` | test-only 통과 | candidateScorePreview production 숫자화 없음 |
| `v8.2_macro_range_score_delta_preview_policy_decision_2026-07-06.md` | 통과 | scoreDeltaPreview 숫자화 없음 |
| `v8.2_macro_range_score_delta_preview_policy_test_design_2026-07-06.md` | 통과 | 없음 |
| `v8.2_macro_range_score_delta_preview_policy_tests_implementation_2026-07-06.md` | test-only 통과 | scoreDeltaPreview production 숫자화 없음 |
| `v8.2_macro_range_score_preview_guard_phase_closeout_2026-07-06.md` | closeout | docs-only |

## 7. 현재 테스트 기준

최신 guard implementation 기준:

```text
runMacroRangeComponentPointsPreviewNumericPolicyDesignTests: 6/6
runMacroRangeCandidateScorePreviewNumericPolicyDesignTests: 6/6
runMacroRangeScoreDeltaPreviewPolicyDecisionTests: 7/7
test:macro-policy: 24 suites / 184 cases / failed 0
test:core: 55 suites / 586 cases / failed 0
git diff --check: 통과
```

registration 기준:

```text
window export 등록: component / candidate / scoreDelta 모두 있음
package.json test:macro-policy 등록: component / candidate / scoreDelta 모두 있음
tools/render_audit core profile 등록: component / candidate / scoreDelta 모두 있음
macro-policy suite 누락: 0
core profile macro-policy 누락: 0
```

`test:full`:

```text
이번 score preview guard phase에서는 새로 실행하지 않았다.
test-only guard와 docs-only decision 중심이므로 merge blocker는 아니다.
release gate 직전에는 별도로 실행한다.
```

## 8. readiness matrix

현재 readiness:

| 항목 | 상태 | 이유 |
| --- | --- | --- |
| component pointsPreview shape | 닫힘 | 위치 / nullability / no-leak decision 완료 |
| component pointsPreview numeric policy | 아직 아님 | actual point number / weight / deduction formula 없음 |
| candidateScorePreview field shape | 닫힘 | 위치 / nullability / no-leak decision 완료 |
| candidateScorePreview numeric policy | 아직 아님 | actual candidate score / sum / clamp / version formula 없음 |
| scoreDeltaPreview field shape | 닫힘 | 위치 / nullability / last-gate decision 완료 |
| scoreDeltaPreview numeric policy | 아직 아님 | candidate total numeric policy 이후 별도 delta formula 필요 |
| storage/schema | 아직 아님 | record / goalSnapshot / backup persistence gate 없음 |
| Recent aggregation | 아직 아님 | preview score / delta aggregation policy 없음 |
| UI visibility | 아직 아님 | Today / Records / Recent display policy 없음 |
| DailyCoach action | 아직 아님 | reason/action threshold policy 없음 |
| production score | 아직 아님 | getDailyAdherenceScore / version / old Records policy gate 없음 |
| production macro range | 아직 아님 | storage / score / visibility gate 미완료 |

## 9. 계속 금지

다음은 계속 금지다.

```text
production consumer 연결
candidateScorePreview 숫자화
pointsPreview 숫자화
scoreDeltaPreview 숫자화
score delta formula / sign / rounding 결정
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
normalizeGoalSnapshot schema 변경
normalizeRecord schema 변경
buildFullBackupPayload schema 변경
normalizeFullBackupPayload schema 변경
Recent aggregation 변경
DailyCoach action 변경
Today / Records / Recent UI range score 표시
Records state 저장 field 추가
old fixed Records saved score overwrite
rangeScoreCandidate 저장
macroRangeContract 저장
production macro range 구현
```

이 금지선의 의미:

```text
영구 금지가 아니다.
production score/storage/UI gate가 별도로 닫히기 전까지 열지 않는 순서 통제다.
```

## 10. 다음 gate가 열리려면

production-facing score preview 숫자화를 열려면 최소한 아래가 별도 승인되어야 한다.

```text
1. component pointsPreview numeric formula decision
2. component weight / deduction / no-bonus policy decision
3. candidateScorePreview sum / clamp / rounding policy decision
4. scoreDeltaPreview formula / sign / rounding policy decision
5. candidateScorePreviewVersion / scoreDeltaPreviewVersion naming decision
6. fixedScoreBasisLabel / candidateScoreBasisLabel production metadata decision
7. old fixed Records display / no-overwrite policy decision
8. score persistence gate decision
9. backup / restore roundtrip policy decision
10. Recent aggregation policy decision
11. DailyCoach action/copy policy decision
12. Today / Records / Recent UI display policy decision
13. getDailyAdherenceScore relation decision
14. ADHERENCE_SCORING_VERSION bump decision
```

하나라도 빠지면 production score 구현을 열지 않는다.

## 11. 다음 단계 후보

이 closeout 이후 가능한 안전 후보:

```text
1. line hold / 보류
2. component pointsPreview numeric formula decision docs-only
3. candidateScorePreview numeric formula decision docs-only
4. scoreDeltaPreview formula decision docs-only
5. range score UI visibility decision docs-only
6. score persistence gate design docs-only
7. Recent candidate score summary decision docs-only
8. DailyCoach candidate score action decision docs-only
```

다음도 production 구현이 아니라 gate decision docs-only부터 시작해야 한다.

## 12. closeout 판단

최종 판단:

```text
score preview guard phase closeout: 가능
hotfix 필요: 없음
문서 보강 필요: 없음
앱 테스트 필요: 없음, docs-only
production score 구현: 아직 금지
storage/schema 구현: 아직 금지
production macro range 구현: 계속 금지
```

이번 phase의 성과:

```text
production-facing preview field 이름과 허용 위치를 제한했다.
candidateScorePreview / pointsPreview / scoreDeltaPreview를 계속 null로 고정했다.
testLocalNumericPreview와 production-facing preview field를 분리했다.
old fixed Records 저장값과 latest-policy eligibility를 분리했다.
blocked state의 sentinel 0 score/delta를 금지했다.
consumer no-leak를 backup / Recent / UI / DailyCoach / Records state 기준으로 테스트했다.
promoted payload rejection을 component / candidate / delta 별로 고정했다.
macro-policy / core profile registration 누락을 방지했다.
```

이번 phase의 종료 조건:

```text
이제 더 많은 null/no-leak guard를 쌓기보다,
다음에 실제 숫자 정책을 열지 말지 또는 UI/storage gate를 계속 미룰지의 제품/저장/표시 decision을 별도 문서로 판단해야 한다.
```

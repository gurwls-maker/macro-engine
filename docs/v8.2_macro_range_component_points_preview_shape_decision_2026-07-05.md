# v8.2 macro range component pointsPreview shape decision

상태:

```text
문서 성격: component pointsPreview shape decision docs-only
작업 범위: docs-only
index.html 변경: 없음
package.json 변경: 없음
tools/render_audit 변경: 없음
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
goalSnapshot schema 변경: 없음
record schema 변경: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
DailyCoach action 변경: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_candidate_score_preview_field_decision_2026-07-05.md` 이후 gate decision이다.

목적:

```text
future component pointsPreview field의 이름 / 위치 / nullability / status / reasonIds / blocked behavior / visibility 기본값을 문서로 닫는다.
아직 component pointsPreview를 숫자화하거나 candidateScorePreview / scoreDeltaPreview / production score로 연결하지 않는다.
```

핵심 결론:

```text
pointsPreview라는 이름은 future transient component contribution preview field로 예약한다.
허용 위치 후보는 rangeScoreCandidateSummary.componentPreviews.<component>.pointsPreview 내부 한 곳이다.
기본값은 null이다.
top-level pointsPreview, record pointsPreview, goalSnapshot pointsPreview, backup pointsPreview는 금지다.
dataQuality는 scoring component가 아니라 eligibility gate이므로 pointsPreview 기본값은 null이다.
candidateScorePreview와 scoreDeltaPreview는 이번 decision으로 열지 않는다.
```

## 1. current baseline

직전 decision에서 열린 것:

```text
candidateScorePreview field name 예약
allowed location: rangeScoreCandidateSummary.candidateScorePreview
candidateScorePreview default null
candidateScorePreview transient-only 기본값
blocked behavior: null + status/reason
old fixed Records saved score no-overwrite
```

직전 decision에서 닫힌 것:

```text
componentPreviews.*.pointsPreview
top-level pointsPreview
record pointsPreview
UI component points
DailyCoach component severity from pointsPreview
scoreDeltaPreview
```

이번 decision에서 다루는 것:

```text
componentPreviews.<component>.pointsPreview 이름과 위치
componentPreviews.<component>.pointsPreview nullability
componentPreviews.<component>.pointsPreviewStatus 후보
componentPreviews.<component>.reasonIds 후보
componentPreviews.<component>.severityLane 후보
componentPreviews.<component>.componentLane 후보
blocked / unresolved 처리
storage / Recent / UI / DailyCoach visibility 기본값
```

이번 decision에서 다루지 않는 것:

```text
actual pointsPreview number
actual candidateScorePreview number
actual scoreDeltaPreview number
component weights
deduction amount
component point sum formula
getDailyAdherenceScore replacement
ADHERENCE_SCORING_VERSION bump
```

## 2. allowed location decision

허용 위치 후보:

```text
rangeScoreCandidateSummary.componentPreviews.kcal.pointsPreview
rangeScoreCandidateSummary.componentPreviews.protein.pointsPreview
rangeScoreCandidateSummary.componentPreviews.fat.pointsPreview
rangeScoreCandidateSummary.componentPreviews.carbExchange.pointsPreview
rangeScoreCandidateSummary.componentPreviews.dataQuality.pointsPreview
```

금지 위치:

```text
top-level pointsPreview
rangeScoreCandidateSummary.pointsPreview
record.pointsPreview
record.rangeScoreCandidate.pointsPreview
record.goalSnapshot.pointsPreview
backup.data.records[].pointsPreview
backup.data.records[].rangeScoreCandidateSummary.componentPreviews.*.pointsPreview
Recent component counts
DailyCoach reason/action
UI component card state
```

이유:

```text
pointsPreview는 component-local transient candidate field다.
저장 field가 아니다.
Recent aggregation component count가 아니다.
DailyCoach action severity가 아니다.
UI 표시 field가 아니다.
fixed score component를 대체하지 않는다.
```

## 3. shape candidate

future transient object 후보:

```text
rangeScoreCandidateSummary: {
  candidateVersion: "range_score_candidate_report_only_v0",
  candidateScorePreview: number | null,
  candidateScorePreviewStatus: "computed" | "blocked" | "not_computed_policy_unresolved",
  candidateScorePreviewVersion: "range_score_candidate_preview_transient_v0",
  candidateScoreBasisLabel: "macro_range_candidate_preview_transient",
  componentPreviews: {
    kcal: {
      componentLane: "anchor",
      status: "matched" | "high" | "low" | "unresolved" | "blocked",
      pointsPreview: number | null,
      pointsPreviewStatus: "computed_candidate" | "blocked" | "not_computed_policy_unresolved",
      severityLane: "neutral" | "material" | "blocked" | null,
      reasonIds: string[]
    },
    protein: {
      componentLane: "anchor",
      status: "satisfied" | "low" | "unresolved" | "blocked",
      pointsPreview: number | null,
      pointsPreviewStatus: "computed_candidate" | "blocked" | "not_computed_policy_unresolved",
      severityLane: "neutral" | "strong" | "blocked" | null,
      reasonIds: string[]
    },
    fat: {
      componentLane: "range_evidence",
      status: "below_action_floor" | "below_recommended" | "inside_recommended" | "above_recommended" | "above_flexible" | "unresolved" | "blocked",
      pointsPreview: number | null,
      pointsPreviewStatus: "computed_candidate" | "blocked" | "not_computed_policy_unresolved",
      severityLane: "neutral" | "observation" | "strong" | "blocked" | null,
      reasonIds: string[]
    },
    carbExchange: {
      componentLane: "range_evidence",
      status: "exchange_explained" | "inside_candidate_range" | "below_training_support" | "above_candidate_range" | "unresolved" | "blocked",
      pointsPreview: number | null,
      pointsPreviewStatus: "computed_candidate" | "blocked" | "not_computed_policy_unresolved",
      severityLane: "neutral" | "material" | "blocked" | null,
      reasonIds: string[]
    },
    dataQuality: {
      componentLane: "eligibility",
      status: "full_day_candidate" | "one_meal_pending" | "insufficient_record" | "snapshotless_debug_only" | "blocked_by_impossible_state" | "unresolved" | "blocked",
      pointsPreview: null,
      pointsPreviewStatus: "blocked" | "not_computed_policy_unresolved",
      severityLane: "blocked" | null,
      reasonIds: string[]
    }
  },
  unresolved,
  needsDecision
}
```

중요:

```text
위 shape는 future 후보이며 이번 문서에서 구현하지 않는다.
number | null 표기는 future numeric gate가 열릴 가능성을 표현할 뿐이다.
현재 결정의 기본값은 모든 component pointsPreview null이다.
dataQuality.pointsPreview는 이번 decision에서 number 후보로 열지 않는다.
```

## 4. nullability decision

기본값:

```text
componentPreviews.*.pointsPreview: null
```

null이어야 하는 경우:

```text
candidateScorePreview numeric policy unresolved
component points numeric policy unresolved
candidateScorePreview가 null인 상태
scoreDeltaPreview policy unresolved
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
simple record
weight_only record
unsupported range basis
invalid range basis
old fixed Records display/storage policy unresolved
UI visibility unresolved
Recent aggregation unresolved
DailyCoach visibility unresolved
```

금지:

```text
blocked record에 pointsPreview 0 저장
blocked record에 fixed component score 복사
blocked record에 이전 pointsPreview 재사용
null을 Recent component count 0으로 해석
null을 DailyCoach severity none으로 해석
null을 UI에서 0점으로 표시
```

## 5. pointsPreviewStatus decision

허용 후보:

```text
not_computed_policy_unresolved
blocked
computed_candidate
```

현재 기본값:

```text
pointsPreviewStatus: "not_computed_policy_unresolved"
pointsPreview: null
```

blocked 상태:

```text
pointsPreviewStatus: "blocked"
pointsPreview: null
reasonIds: non-empty when known
```

`computed_candidate`는 이번 decision에서 열지 않는다.

조건:

```text
computed_candidate는 별도 numeric policy gate가 닫힌 뒤에만 가능하다.
computed_candidate가 열려도 record / goalSnapshot / backup 저장을 의미하지 않는다.
computed_candidate가 열려도 Recent / UI / DailyCoach visibility를 의미하지 않는다.
```

## 6. reasonIds decision

`reasonIds` 목적:

```text
component status를 test / audit / future explainability 후보로 추적한다.
blocked 이유를 numeric field 대신 status field로 표현한다.
```

`reasonIds`가 아닌 것:

```text
DailyCoach action id가 아니다.
UI copy id가 아니다.
backup schema field가 아니다.
Recent aggregation basis가 아니다.
stored score explanation이 아니다.
```

후보 reasonIds:

```text
kcal_matched
kcal_above_target
kcal_below_target
protein_satisfied
protein_below_anchor
fat_below_action_floor
fat_below_recommended
fat_inside_recommended
fat_above_recommended
fat_above_flexible
carb_exchange_explained
carb_inside_candidate_range
carb_below_training_support
carb_above_candidate_range
data_quality_full_day_candidate
data_quality_one_meal_pending
data_quality_insufficient_record
data_quality_snapshotless_debug_only
data_quality_blocked_by_impossible_state
range_basis_invalid
range_basis_unsupported
policy_unresolved
```

정책:

```text
reasonIds는 machine-readable 후보다.
reasonIds는 user-facing copy로 바로 노출하지 않는다.
reasonIds가 있어도 pointsPreview 숫자 계산을 의미하지 않는다.
```

## 7. component lane decision

문서상 lane:

```text
kcal: anchor
protein: anchor
fat: range_evidence
carbExchange: range_evidence
dataQuality: eligibility
```

원칙:

```text
anchor lane은 fixed target source of truth를 흔들지 않는다.
range_evidence lane은 fat/carb range 설명 후보이며 보너스가 아니다.
eligibility lane은 score component가 아니라 계산 가능성 gate다.
```

아직 닫지 않는 것:

```text
anchor lane과 range_evidence lane의 numeric ratio
range_evidence가 actual deduction에 들어가는지 여부
dataQuality를 sentinel 0으로 둘지, null-only gate로 둘지
component lane별 UI grouping
```

현재 결정:

```text
dataQuality.pointsPreview는 null-only로 둔다.
sentinel 0이 필요하면 별도 numeric policy gate에서 결정한다.
```

## 8. severity lane relationship

이전 weight / severity decision과 맞춘 severity 후보:

```text
blocked:
- one_meal_pending
- insufficient_record
- snapshotless_debug_only
- blocked_by_impossible_state

strong:
- protein low
- fat below_action_floor
- fat above_flexible

material:
- kcal high
- kcal low
- carb below_training_support
- carb above_candidate_range

observation:
- fat below_recommended
- fat above_recommended

neutral:
- kcal matched
- protein satisfied
- fat inside_recommended
- carb exchange_explained
- carb inside_candidate_range
- full_day_candidate eligibility
```

주의:

```text
severityLane은 pointsPreview number가 아니다.
severityLane은 DailyCoach severity가 아니다.
severityLane은 UI color / copy를 열지 않는다.
neutral은 bonus를 의미하지 않는다.
observation은 actual deduction 확정을 의미하지 않는다.
```

## 9. candidateScorePreview relationship

이번 decision으로 열지 않는 것:

```text
rangeScoreCandidateSummary.candidateScorePreview 숫자화
candidateScorePreview = sum(componentPreviews.*.pointsPreview)
candidateScorePreview fallback from fixed score
candidateScorePreview persistence
```

관계:

```text
candidateScorePreview는 future total candidate preview field다.
pointsPreview는 future component-local contribution preview field다.
component pointsPreview shape가 정리되어도 candidateScorePreview numeric formula는 아직 미결정이다.
```

따라서:

```text
pointsPreview shape decision 완료 != candidateScorePreview 숫자화 readiness
pointsPreview shape decision 완료 != production score readiness
```

## 10. scoreDeltaPreview relationship

이번 decision으로 열지 않는 것:

```text
comparison.scoreDeltaPreview
top-level scoreDeltaPreview
record scoreDeltaPreview
UI improvement copy
Recent improvement trend
```

이유:

```text
scoreDeltaPreview는 fixed score와 candidate score의 직접 비교다.
component pointsPreview가 존재하면 scoreDeltaPreview를 계산할 수 있다고 오해할 수 있다.
old fixed Records의 saved score 의미를 흔들 수 있다.
```

따라서:

```text
scoreDeltaPreview는 별도 policy decision 전까지 null이다.
pointsPreview shape decision은 scoreDeltaPreview gate를 열지 않는다.
```

## 11. old fixed Records behavior

old fixed Records는:

```text
stored adherencePercent 유지
stored adherenceScoringVersion 유지
fixed target goalSnapshot 유지
candidateScorePreview 저장 없음
pointsPreview 저장 없음
scoreDeltaPreview 저장 없음
backup payload 변경 없음
Recent fixed aggregation 유지
Records UI saved score 의미 유지
```

허용 후보:

```text
sufficient detailed old fixed Record를 transient pointsPreview eligibility 후보로 분류
test-only fixture에서 component status / reasonIds 관찰
no-mutation comparison
```

금지:

```text
old fixed Records saved adherencePercent overwrite
old fixed Records adherenceScoringVersion overwrite
old fixed Records backup에 pointsPreview 추가
old fixed Records UI에 pointsPreview를 saved component score처럼 표시
old fixed Records Recent 집계에 pointsPreview 사용
old fixed Records DailyCoach reason에 pointsPreview 사용
```

## 12. storage / backup / Recent / UI / DailyCoach visibility

기본값:

```text
persisted: false
backupVisible: false
recentVisible: false
uiVisible: false
dailyCoachVisible: false
```

계속 닫힌 것:

```text
record componentPreviews 저장
goalSnapshot componentPreviews 저장
backup componentPreviews 저장
Recent component points trend
DailyCoach component severity from pointsPreview
Today component points UI
Records component points UI
Recent component points UI
```

별도 gate가 필요한 항목:

```text
score persistence gate
backup schema gate
Recent candidate score summary decision
range score UI visibility decision
DailyCoach candidate score action decision
```

## 13. version / basis labels

future 후보:

```text
pointsPreviewVersion
pointsPreviewBasisLabel
componentPolicyVersion
```

현재 결정:

```text
pointsPreviewVersion은 ADHERENCE_SCORING_VERSION이 아니다.
pointsPreviewVersion은 saved record adherenceScoringVersion에 저장하지 않는다.
pointsPreviewBasisLabel은 comparison metadata 후보일 뿐이다.
componentPolicyVersion은 production score version이 아니다.
```

현재 닫힌 것:

```text
ADHERENCE_SCORING_VERSION bump
pointsPreviewVersion persistence
range score production version
componentPolicyVersion backup roundtrip
```

## 14. example blocked shape

one-meal pending 후보:

```text
rangeScoreCandidateSummary: {
  candidateScorePreview: null,
  candidateScorePreviewStatus: "blocked",
  componentPreviews: {
    kcal: {
      componentLane: "anchor",
      status: "blocked",
      pointsPreview: null,
      pointsPreviewStatus: "blocked",
      severityLane: "blocked",
      reasonIds: ["data_quality_one_meal_pending"]
    },
    protein: {
      componentLane: "anchor",
      status: "blocked",
      pointsPreview: null,
      pointsPreviewStatus: "blocked",
      severityLane: "blocked",
      reasonIds: ["data_quality_one_meal_pending"]
    },
    fat: {
      componentLane: "range_evidence",
      status: "blocked",
      pointsPreview: null,
      pointsPreviewStatus: "blocked",
      severityLane: "blocked",
      reasonIds: ["data_quality_one_meal_pending"]
    },
    carbExchange: {
      componentLane: "range_evidence",
      status: "blocked",
      pointsPreview: null,
      pointsPreviewStatus: "blocked",
      severityLane: "blocked",
      reasonIds: ["data_quality_one_meal_pending"]
    },
    dataQuality: {
      componentLane: "eligibility",
      status: "one_meal_pending",
      pointsPreview: null,
      pointsPreviewStatus: "blocked",
      severityLane: "blocked",
      reasonIds: ["data_quality_one_meal_pending"]
    }
  }
}
```

중요:

```text
blocked shape는 0점 저장이 아니다.
blocked shape는 DailyCoach action이 아니다.
blocked shape는 UI component warning이 아니다.
blocked shape는 backup payload가 아니다.
```

## 15. example eligible-but-uncomputed shape

full-day candidate이지만 numeric policy가 아직 닫히지 않은 후보:

```text
rangeScoreCandidateSummary: {
  candidateScorePreview: null,
  candidateScorePreviewStatus: "not_computed_policy_unresolved",
  componentPreviews: {
    kcal: {
      componentLane: "anchor",
      status: "matched",
      pointsPreview: null,
      pointsPreviewStatus: "not_computed_policy_unresolved",
      severityLane: "neutral",
      reasonIds: ["kcal_matched", "policy_unresolved"]
    },
    protein: {
      componentLane: "anchor",
      status: "satisfied",
      pointsPreview: null,
      pointsPreviewStatus: "not_computed_policy_unresolved",
      severityLane: "neutral",
      reasonIds: ["protein_satisfied", "policy_unresolved"]
    },
    fat: {
      componentLane: "range_evidence",
      status: "inside_recommended",
      pointsPreview: null,
      pointsPreviewStatus: "not_computed_policy_unresolved",
      severityLane: "neutral",
      reasonIds: ["fat_inside_recommended", "policy_unresolved"]
    },
    carbExchange: {
      componentLane: "range_evidence",
      status: "exchange_explained",
      pointsPreview: null,
      pointsPreviewStatus: "not_computed_policy_unresolved",
      severityLane: "neutral",
      reasonIds: ["carb_exchange_explained", "policy_unresolved"]
    },
    dataQuality: {
      componentLane: "eligibility",
      status: "full_day_candidate",
      pointsPreview: null,
      pointsPreviewStatus: "not_computed_policy_unresolved",
      severityLane: null,
      reasonIds: ["data_quality_full_day_candidate", "policy_unresolved"]
    }
  }
}
```

중요:

```text
eligible-but-uncomputed shape는 pointsPreview 숫자화가 아니다.
neutral status는 bonus가 아니다.
inside_recommended / exchange_explained는 positive points가 아니다.
```

## 16. future numeric policy constraints

future numeric policy gate가 열린다면 지켜야 할 방향:

```text
pointsPreview는 component-local candidate contribution이다.
pointsPreview는 saved score component가 아니다.
pointsPreview는 penalty-only 방향을 기본으로 검토한다.
positive bonus는 별도 decision 전까지 금지한다.
inside_recommended / exchange_explained는 bonus로 계산하지 않는다.
dataQuality는 score component가 아니라 eligibility gate다.
```

아직 결정하지 않는 것:

```text
0 vs null for neutral components
exact negative point values
component weight ratio
clamp policy
sum policy
rounding policy
candidateScorePreview formula
scoreDeltaPreview formula
```

## 17. still forbidden

계속 금지:

```text
index.html production score 변경
candidateScorePreview 숫자화 구현
pointsPreview 숫자화 구현
scoreDeltaPreview 숫자화 구현
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
goalSnapshot schema 변경
record schema 변경
backup schema 변경
Recent aggregation 변경
DailyCoach action 변경
UI 표시 변경
pointsPreview persistence
candidateScorePreview persistence
rangeScoreCandidate 저장
macroRangeContract 저장
production macro range 구현
```

## 18. acceptance criteria

이번 docs-only 단계 통과 조건:

```text
index.html diff 없음
package.json diff 없음
tools/render_audit diff 없음
production code diff 없음
README 등록
candidateScorePreview field decision 후속 기록 추가
pointsPreview allowed location을 rangeScoreCandidateSummary.componentPreviews.<component>.pointsPreview로 제한
top-level / record / goalSnapshot / backup pointsPreview 금지 명시
default null 명시
blocked behavior 명시
dataQuality null-only eligibility gate 명시
old fixed Records no-overwrite 유지
scoreDeltaPreview 별도 gate 유지
UI / Recent / DailyCoach visibility 기본 false 유지
storage persistence 별도 gate 유지
git diff --check 통과
```

앱 테스트:

```text
docs-only 변경이므로 생략 가능
```

## 19. next candidates

이 decision 이후 가능한 안전 후보:

```text
1. component pointsPreview numeric policy test design docs-only
2. candidateScorePreview numeric policy test design docs-only
3. scoreDeltaPreview policy decision docs-only
4. range score UI visibility decision docs-only
5. score persistence gate design docs-only
6. line hold / 보류
```

다음 gate도 production 구현이 아니다.

## 20. final judgement

현재 결론:

```text
component pointsPreview shape decision: 닫힘
pointsPreview numeric readiness: 아직 아님
candidateScorePreview numeric readiness: 아직 아님
scoreDeltaPreview readiness: 아직 아님
UI visibility readiness: 아직 아님
Recent readiness: 아직 아님
DailyCoach readiness: 아직 아님
score persistence readiness: 아직 아님
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
pointsPreview는 future transient component-local field 이름과 위치만 예약하고, 모든 numeric value / storage / UI / Recent / DailyCoach 연결은 별도 gate 전까지 계속 닫는다.
```

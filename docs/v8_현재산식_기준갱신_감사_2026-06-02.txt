# v8 현재 산식 및 Today-Records 기준갱신 감사 리포트

작성일: 2026-06-02
대상 브랜치: codex/v8-formula-audit
대상 구현: index.html
기준 문서: docs/v8_운동프로필_산식_정밀설계.txt 9-1

이 문서는 산식 수정 제안서가 아니다.
v8 기준문서가 지시한 1단계, 즉 현재 index.html의 산식과 Today-Records 기준갱신 판단 흐름을 감사한 결과이다.
최초 감사 리포트 작성 중에는 index.html 구현을 변경하지 않았다.
이후 v8.0-A / v8.0-B / v8.0-C 기준갱신 핫픽스, v8.0-D 시나리오 러너, v8.0-D1 근거 등급 재감사 결과는 이 문서의 진행 메모와 검증 메모에 누적한다.

2026-06-03 v8.0-AU 문서 계층 정리 이후 이 문서는 긴 감사 ledger로만 읽는다. 현재 기준 지도는 `docs/README.md`가 맡고, 장시간 `/GOAL` 재개 방식은 폐기한다. 과거 stage의 "현재" 표현은 해당 stage 당시 현재값일 수 있으므로, 최신 production 상태로 쓰기 전에 반드시 최신 `index.html`, runner, 테스트와 대조한다.

## 처음 보는 작업자용 사용법

이 문서는 단독 기준문서가 아니다.
처음 이 브랜치를 여는 작업자는 아래 순서로 읽어야 한다.

1. `docs/README.md`
2. `docs/내가-바라는-앱의-성격.txt`
3. `docs/개발정책.txt`
4. `docs/v8_운동프로필_산식_정밀설계.txt`
5. 이 문서
6. `index.html`

이 문서를 읽은 뒤 바로 할 수 있는 작업은 "현재 산식 감사 결과를 바탕으로 다음 단계 설계"이다.
아직 바로 하면 안 되는 작업은 "산식 수정", "UI 수정", "점수/Coach/Recent Flow/InBody/backup schema 변경"이다.

다음 작업자는 이 문서를 다음처럼 사용한다.

- 현재 앱이 어떤 입력을 어디서 모으는지 확인하려면 1장을 본다.
- 유지 칼로리 계산식을 확인하려면 2장을 본다.
- 목표 칼로리와 energy availability floor를 확인하려면 3장을 본다.
- 단백질 180.5g 원인을 확인하려면 4장과 5장을 본다.
- Today-Records 기준갱신 문제를 확인하려면 7장과 8장을 본다.
- 다음에 무엇을 고칠지 결정하려면 9장과 10장을 본다.

이 문서만 보고 임의로 수치를 고치면 안 된다.
특히 단백질 180.5g은 "높아 보인다"는 감각으로 낮추는 대상이 아니라, 원래 의도와 부작용을 분리한 뒤 candidate formula와 scenario runner에서 비교해야 한다.

## 근거 등급 규칙

v8.0-D1부터 모든 산식 / 테스트 / 문서 근거는 아래 중 하나로 표시한다.

1. 외부 운동생리학 / 스포츠영양 근거
   - 논문, 공식 가이드, position stand처럼 외부 출처가 명시된 근거다.
   - 출처가 있다고 끝난 것이 아니라 대상군, 목표, 훈련 수준, 앱의 사용자 흐름에 맞는지까지 검토해야 한다.
2. 내부 코드 근거
   - `index.html` 경로, snapshot, 테스트, 리포트 출력처럼 현재 구현이 어떻게 동작하는지 보여주는 근거다.
   - 현재 동작을 증명할 뿐, 생리학적 타당성을 증명하지 않는다.
3. 제품 정책 근거
   - 사용자 의도, 기준문서, UX 책임, Records / Today / Coach 책임 분리처럼 앱이 어떻게 행동해야 하는지에 대한 근거다.
   - 앱 정책으로는 사용할 수 있지만 운동생리학 근거처럼 말하면 안 된다.
4. 미검증 생리학 후보
   - 그럴듯한 내부 산식 또는 의도지만 외부 근거와 앱 적합성 검토가 끝나지 않은 항목이다.

외부 운동생리학 근거라고 명시되어 있지 않은 기존 근거는 기본적으로 내부 코드 근거 또는 제품 정책 근거로 본다.
이런 항목은 후속 산식 변경에서 검토대상으로 남긴다.

## 0. 감사 결론

현재 계산은 다음 순서로 진행된다.

1. Today 계산값과 Settings 기본값을 합쳐 입력 상태를 만든다.
2. 입력 근거에 따라 내부 런타임 설정을 만든다.
3. BMR, 일상 활동, 업무 보정, 주간 웨이트 보정, 오늘 웨이트, 유산소, EPOC를 합쳐 총소비량을 만든다.
4. 목표별 주간 체중 변화율 정책과 안전선을 적용해 목표 칼로리를 만든다.
5. 1차 탄단지를 계산한다.
6. guide/activity dual basis를 다시 만들고, 선택된 basis의 탄단지를 최종 화면/저장 기준으로 사용한다.

중요한 점은 최종 탄단지가 1차 `calculateMacroTargets()` 결과가 아니라는 것이다.
최종 화면값과 goalSnapshot 저장값은 `buildDualBasisProduction()` 이후 선택된 basis 값이다.

1913kcal / 탄수 196.3g / 단백질 180.5g / 지방 45.1g 케이스는 현재 구현에서 재현된다.
직접 원인은 다음 두 가지다.

1. 목표 칼로리는 감량 deficit 결과가 아니라 energy availability floor에 의해 올라간 값이다.
2. 단백질과 지방은 guide basis의 diet 규칙인 단백질 2.4g/kg, 지방 0.6g/kg에서 나온다.

이 값은 "단백질이 남은 칼로리를 먹었다"가 아니다.
현재 guide basis는 단백질을 goal range의 mid 값으로 먼저 잡고, 지방을 min 값으로 잡은 뒤, 남은 칼로리를 탄수화물에 배정한다.

## 1. 입력값 수집 경로

확인 위치:

- `collectState()` - index.html:18980
- `getEffectiveTodayCalculationValues()` - index.html:17594
- `resolveModeFreeRuntimeConfig()` - index.html:18489
- `calculate()` - index.html:19121

`collectState()`는 계산 입력을 직접 DOM/상태에서 모으지 않고 먼저 Today 계산값을 만든다.
`getEffectiveTodayCalculationValues()`는 Settings 기본값, Today draft, 최신 InBody 자동 기본값, cardio preset 기본값을 합친다.

Today 소유 입력:

- 계산용 체중
- 체지방률
- 체지방량
- 골격근량
- 오늘 활동량
- 오늘 업무 유형
- 오늘 루틴/세션
- 오늘 훈련 강도 보정
- 오늘 웨이트 시간
- 오늘 유산소 종류/시간/속도/경사

Settings 소유 입력:

- 키
- 나이
- 성별
- 목표
- 업무 보정
- homeInbody 여부
- expertLbmAlpha
- 주간 웨이트일수
- generalAdvancedSettings
- generalLowDigestCarbs
- routinePlan

Records는 현재 계산 입력 자체를 직접 만들지 않는다.
Records의 `goalSnapshot`은 저장 당시 기준을 보관하고, Records 점수/상세 보기/Today 기준갱신 비교에서 사용된다.

내부 런타임 설정은 사용자에게 노출되는 예전 모드가 아니라 입력 근거 기반으로 만들어진다.
체성분 입력이 있으면 `bodyCompReliability`가 `partial`, `home`, `inbody` 중 하나가 되고, `appliedAlpha`가 적용된다.
체성분 입력이 있으면 BMR은 Katch-McArdle, 없으면 Mifflin-St Jeor가 된다.

주의점:

- 현재 `resolveTrainingEvidence()`는 주간 웨이트일수, 오늘 웨이트 시간, 강도 보정, 유산소 시간/속도를 함께 본다.
- 그러나 목표 rate용 `getTargetRateTrainingBand()`는 주간 웨이트일수 중심으로 beginner/intermediate/advanced를 정한다.
- v8 기준의 "주간 웨이트일수 단독 판단 금지"와 충돌 가능성이 가장 큰 지점이다.

## 2. 유지 칼로리 분해

확인 위치:

- `calcBMR()` - index.html:18592
- `calcCardio()` - index.html:18620
- `calcWeightTrainingKcal()` - index.html:18632
- `getWeeklyTrainingBaselineKcal()` - index.html:18639
- `calculate()` - index.html:19121

현재 총소비량은 다음 합이다.

```
totalBurn =
  baseDaily
  + trainingBaselineKcal
  + weightKcal
  + cardio.kcal
  + epocKcal
```

BMR:

- 체성분 입력이 있고 `appliedAlpha > 0`이면 Katch-McArdle:
  - `370 + 21.6 * lbmEffective`
- 체성분 입력이 없으면 Mifflin-St Jeor:
  - 남성: `10 * weight + 6.25 * height - 5 * age + 5`
  - 여성: `10 * weight + 6.25 * height - 5 * age - 161`

일상/업무:

```
workloadY = clamp(WORK_TYPE_Y[workType] + workAdj, 0, 1)
rawActCoeff = ACTIVITY_COEFF[activityLevel] + workloadY * 0.06
actCoeff = clamp(rawActCoeff, 1.08, 1.62)
baseDaily = bmr * actCoeff
```

주간 웨이트 보정:

```
frequencyRatio = clamp(weeklyTrainingDays / 7, 0, 1)
recoveryFactor = clamp(0.012 + frequencyRatio * 0.053, 0.015, 0.05)
trainingBaselineKcal = bmr * recoveryFactor * multiplier
```

오늘 웨이트가 있는 날은 multiplier가 0.40이다.
휴식일은 multiplier가 1이다.
즉 휴식일에도 주간 웨이트일수가 있으면 회복/유지 수요로 `trainingBaselineKcal`이 붙는다.

오늘 웨이트:

```
met = 2.5 + xw * 2.5
weightKcal = met * weight * durationHours
```

유산소:

- 트레드밀 워킹/러닝, 실외 러닝별 VO2 기반 MET 계산
- `cardio.kcal = met * weight * durationHours`
- EPOC는 MET 기준 0, 4%, 7%, 10% 중 하나

## 3. 목표 칼로리 분해

확인 위치:

- `GOAL_RATE_POLICIES` - index.html:15875
- `getTargetRateTrainingBand()` - index.html:15916
- `selectGoalWeeklyRate()` - index.html:15940
- `buildGoalTargetRateContext()` - index.html:15969
- `calculate()` - index.html:19121

현재 목표 칼로리는 단순 `totalBurn * factor`가 아니다.
실제 적용 경로는 다음과 같다.

1. cardio reflection으로 목표 계산용 소비 기준을 만든다.

```
targetBurnBase = totalBurn - cardioKcal + cardioKcal * CARDIO_TARGET_REFLECTION_RATE[goal]
```

2. 목표별 주간 체중 변화율을 선택한다.

```
selectedWeeklyRate = selectGoalWeeklyRate(goal, s, modeCfg)
weeklyBodyWeightDeltaKg = weight * selectedWeeklyRate
rawGoalDelta = weeklyBodyWeightDeltaKg * 7700 / 7
clampedGoalDelta = clamp(rawGoalDelta, policy.deltaMin, policy.deltaMax)
targetCalBeforeFloor = targetBurnBase + clampedGoalDelta
```

3. 안전선을 적용한다.

```
energyAvailabilityFloorTarget =
  30 * actualLbm + exerciseKcal

minimumTargetCal =
  max(BMR if diet/cut, energyAvailabilityFloorTarget)

targetCal =
  max(targetCalBeforeFloor, minimumTargetCal)
```

여기서 `actualLbm`은 adjusted LBM이 아니라 실제 체중/체지방률 또는 체지방량 기반 LBM이다.
이 부분은 v8 기준문서의 "안전선은 adjusted LBM이 아니라 actual LBM"과 맞는다.

위험 지점:

- `getTargetRateTrainingBand()`는 주간 웨이트일수 3/5 기준으로 intermediate/advanced를 나눈다.
- 몸 상태, 수행, 운동 프로필, 최근 기록 신뢰도 없이 목표 rate 보정에 trainingBand가 들어간다.
- 이 지점은 v8의 사용자 타입/운동 수준 판단 기준과 충돌 가능성이 높다.

## 4. 탄단지 분해

확인 위치:

- `calculateMacroTargets()` - index.html:18838
- `applyPlausibilityLayer()` - index.html:18763
- `getDualBasisProteinRange()` - index.html:54080
- `getDualBasisFatRange()` - index.html:54088
- `calculateGuideBasisMacrosWithinTarget()` - index.html:54355
- `buildDualBasisProduction()` - index.html:54519

1차 탄단지:

```
proteinBasisWeight = getProteinBasisWeight(...)
proteinRaw = GOALS[goal].proteinKg * proteinBasisWeight
defaultProteinCap = targetCal * 0.35 / 4
protein = min(proteinRaw, defaultProteinCap)
fat = fatTargetPreferredG
carbs = (targetCal - protein * 4 - fat * 9) / 4
```

그 뒤 plausibility layer가 단백질/탄수/지방 상한을 적용한다.

하지만 최종 화면값은 여기서 끝나지 않는다.
`calculate()`는 1차 결과를 `legacyA58`에 넣고, 다시 dual basis를 만든다.
그리고 `dualBasis.selected`의 target/protein/carbs/fat을 최종 `targetCal`, `protein`, `carbs`, `fat`으로 반환한다.

guide basis의 diet 범위:

```
protein: 2.2 ~ 2.6 g/kg, mid 2.4 g/kg
fat: floor/min 0.6 g/kg, mid 0.8 g/kg, max 1.0 g/kg
carbs: low_moderate load 기준 3 ~ 5 g/kg
```

guide basis within target 계산:

1. 단백질을 `protein.mid * weight`로 잡는다.
2. 지방을 `fat.min * weight`로 잡는다.
3. 남은 칼로리를 탄수화물로 채운다.
4. 목표 안에 들어가지 않으면 지방, 단백질 순서로 낮춘다.
5. 탄수화물이 range max를 넘으면 지방 flexible range로 이동한다.

현재 1913 케이스에서는 단백질과 지방만 먼저 고정되고 탄수화물이 잔여값이 된다.
그 결과 탄수화물은 guide minimum 3g/kg보다 낮은 `2.61g/kg`가 된다.
코드는 이를 `carbsBelowGuideMin: true`로 표시한다.

주의점:

- 최종 반환의 `proteinBasisWeight`는 실제 1차 protein basis가 아니라 bodyweight로 다시 기록된다.
- v8 기준문서가 요구한 `protein_g_per_kg_bodyweight`, `protein_g_per_kg_lbm`, `protein_rule_source`, cap/floor hit 같은 리포트 필드는 현재 한 번에 구조화되어 있지 않다.
- 다음 단계에서 시나리오 러너를 만들 때 이 필드는 별도 리포트 객체로 명시해야 한다.

## 5. 실제 케이스: 1913 / 196.3 / 180.5 / 45.1

브라우저에서 현재 index.html을 로드해 실제 `calculate()` 결과로 재현했다.

재현 입력:

```
goal: diet
selectedMacroBasis: guide
weight: 75.2083 kg
bodyFat: 15.2 %
skeletal: 35 kg
gender: male
height: 173 cm
age: 32
activityLevel: sedentary
workType: office
weeklyTrainingDays: 4
routine: REST
weightDuration: 0
cardioDuration: 0
cardioSpeed: 0
cardioIncline: 0
```

유지 칼로리:

```
lbmBase = 63.7766 kg
lbmEffective = 66.4249 kg
bmr = 1804.7785 kcal
baseDaily = 2057.4474 kcal
trainingBaselineKcal = 76.3146 kcal
weightKcal = 0
cardioKcal = 0
epocKcal = 0
totalBurn = 2133.7158 kcal
```

목표 칼로리:

```
selectedWeeklyRate = -0.005
rawGoalDelta = -413.6457 kcal
clampedGoalDelta = -413.6457 kcal
targetCalBeforeFloor = 1720.0701 kcal
actualLbm = 63.7766 kg
energyAvailabilityFloorTarget = 63.7766 * 30 = 1913.2992 kcal
minimumTargetCal = 1913.2992 kcal
targetCal = 1913.2992 kcal
display targetCal = 1913 kcal
```

탄단지:

```
protein = 75.2083 * 2.4 = 180.4999 g -> 180.5 g
fat = 75.2083 * 0.6 = 45.1250 g -> 45.1 g
carbs = (1913.2992 - 180.4999 * 4 - 45.1250 * 9) / 4
      = 196.2937 g -> 196.3 g
```

따라서 이 케이스의 단백질 180.5g은 다음 의미다.

- 원래 의도: diet 상태에서 근손실 방지를 위해 높은 단백질 범위의 중간값을 사용한다.
- 내부 정책상 보존할 만한 구조 후보: 단백질을 남은 칼로리 처리로 두지 않고 목표/체중 기준으로 먼저 정한다. 단, 이것은 현재 앱 산식 구조의 장점 후보이지 외부 스포츠영양 근거로 확정한 결론은 아니다.
- 과한 부분 후보: 모든 diet 사용자에게 bodyweight 2.4g/kg이 기본 중간값으로 적용된다.
- 부작용: energy availability floor로 목표 칼로리가 낮게 고정될 때 탄수화물이 guide minimum 아래로 내려간다.
- 수정 판단은 아직 금지: 현재 단계에서는 "높아 보이니 낮춘다"가 아니라, 보존할 의도와 줄여야 할 부작용을 분리해 후보안을 설계해야 한다.

비교: 1차 legacyA58 탄단지

같은 케이스에서 dual basis 적용 전 1차 macroPlan은 다음과 같다.

```
protein = 150.9275 g
carbs = 207.8161 g
fat = 53.1472 g
proteinBasisWeight = 68.6034 kg
proteinKgApplied = 2.2 g/kg
```

즉 180.5g은 1차 diet protein rule의 직접 결과가 아니라, final guide basis에서 다시 올라간 값이다.

## 6. 주간 웨이트일수 영향 확인

같은 1913 케이스에서 주간 웨이트일수를 0 / 2 / 4 / 6으로 바꿔 확인했다.

결과:

- 주 0일: targetCal 1913.0735, totalBurn 2057.4011, trainingBand beginner
- 주 2일: targetCal 1913.0735, totalBurn 2106.3869, trainingBand beginner
- 주 4일: targetCal 1913.0735, totalBurn 2133.7158, trainingBand intermediate
- 주 6일: targetCal 1913.0735, totalBurn 2147.6380, trainingBand advanced

이 케이스에서는 targetCal이 주간 웨이트일수에 따라 바뀌지 않는다.
이유는 `targetCalBeforeFloor`가 모두 energy availability floor보다 낮아서 최종 targetCal이 floor에 고정되기 때문이다.

그러나 totalBurn, trainingBaselineKcal, trainingBand는 바뀐다.
다른 체성분/활동/운동 케이스에서는 floor가 지배하지 않으면 주간 웨이트일수의 영향이 targetCal에 드러날 수 있다.
따라서 문서의 1844 / 1907 / 2025류 케이스는 별도 시나리오 그리드에서 계속 확인해야 한다.

## 7. Today-Records 기준갱신 판단 흐름

확인 위치:

- `normalizeGoalSnapshot()` - index.html:20861
- `buildGoalSnapshotFromCurrentState()` - index.html:23018
- `getGoalSnapshotSignature()` - index.html:23055
- `areGoalSnapshotsEquivalent()` - index.html:23099
- `getTodayRecordConfirmationState()` - index.html:26280
- `applyCurrentTodayBasisToTodayRecord()` - index.html:26395
- `persistAutoAdherenceForDate()` - index.html:26207

Records의 계산 기준은 `goalSnapshot`에 저장된다.
Today 기준갱신 패널은 오늘 Records가 detailed record일 때, 저장된 `goalSnapshot`과 현재 Today 기준에서 새로 만든 snapshot의 signature를 비교한다.

signature 비교 키:

```
targetCal, protein, carbs, fat,
mode, goal, weight, height, age, gender,
bodyFat, bodyFatMass, skeletal,
activityLevel, workType, weeklyTrainingDays,
routinePlan, routine, intensityOverride, weightDuration,
cardioType, cardioDuration, cardioSpeed, cardioIncline,
workAdj, homeInbody, expertLbmAlpha,
generalAdvancedSettings, generalLowDigestCarbs
```

`snapshotSource`는 signature에 들어가지 않는다.
숫자는 4자리 반올림 후 비교된다.

상태 판단:

- 오늘 기록 없음: missing
- 오늘 기록이 detailed가 아님: current
- 현재 계산 불가: stale, action none
- detailed이고 snapshot이 현재 기준과 같음: current
- detailed이고 snapshot이 현재 기준과 다름: stale, action update

수동 갱신 버튼은 다음만 갱신한다.

- `goalSnapshot`
- `snapshotSource`
- auto adherence score 관련 값

식사, 공복 체중, 메모는 유지한다.

## 8. 기준갱신 시나리오 확인 결과

브라우저에서 현재 앱을 로드해 직접 상태를 눌렀다.

기준 케이스:

```
오늘 detailed 기록 있음
goalSnapshot 있음
식사 2개 있음
공복 체중 있음
Today 기준과 snapshot 일치
```

결과:

- base current: current, 패널 숨김
- meal add only: current, 패널 숨김, snapshot 유지
- record note only: current, 패널 숨김, snapshot 유지
- Today 계산용 체중 변경: stale
- Today 계산용 체중 원복: current, 패널 숨김
- legacy snapshot에서 REST/cardio 0 필드 누락: normalize 후 current

문서 기준과 맞는 부분:

- 식사만 추가해도 기존 snapshot은 유지된다.
- 메모만 바꿔도 기준갱신은 뜨지 않는다.
- Today 값을 바꿨다가 저장 당시 값으로 되돌리면 기준갱신이 사라진다.
- REST 0, cardio 0 계열은 normalize 후 같은 값으로 취급된다.
- 수동 갱신은 식사/공복 체중/메모를 유지한다.

검토 필요한 부분:

1. snapshot 없는 detailed 오늘 기록

브라우저 확인 결과:

```
status = stale
mismatchKeys = ["unavailable"]
패널 표시 = true
```

v8 기준문서는 "저장된 오늘 Records 계산 snapshot과 현재 Today 계산 기준이 의미 있게 다를 때" 표시하라고 한다.
snapshot이 없는 detailed 기록은 비교할 저장 기준이 없으므로, "갱신 필요"보다 "당시 계산 기준 없음/기준 저장 필요"로 분리하는 편이 문서 의도에 더 가깝다.
다만 현재 일반 상세 저장 흐름은 대부분 snapshot을 생성하므로 실제 노출은 legacy/import/snapshotless edge에 가까울 수 있다.

2026-06-02 v8.0-A 반영:

- `getTodayRecordConfirmationState()`에서 snapshot 없는 detailed 오늘 기록을 `status = "basis-missing"`으로 분리했다.
- 사용자 화면 문구는 "갱신 필요"가 아니라 "계산 기준 저장 필요" / "현재 기준 저장"으로 표시한다.
- 수동 저장 액션은 기존 `applyCurrentTodayBasisToTodayRecord()`를 사용하되, 식사 / 공복 체중 / 메모를 유지하고 현재 Today 기준의 `goalSnapshot`과 자동 점수 기준만 저장한다.
- `runTodayRecordConfirmationTests`에 snapshotless 상태가 stale mismatch처럼 보이지 않는지, 저장 후 current가 되는지 확인하는 v8.0-A 회귀 테스트를 추가했다.
- desktop / mobile viewport에서 같은 suite를 통과했다.

2. guide/activity basis 토글

브라우저 확인 결과:

```
guide -> activity:
status = stale
mismatchKeys = ["carbs", "fat"]

activity -> guide 복귀:
status = current
```

targetCal은 바뀌지 않지만 selected basis의 탄수/지방이 달라지므로 goalSnapshot과 현재 Today macro basis가 달라진다.
현재 구현은 이를 기준갱신 대상으로 본다.

2026-06-02 v8.0-B 반영:

- guide/activity basis 선택은 Today target macro / remaining / score 기준에 영향을 주므로, 선택 basis가 탄수/단백질/지방 출력값을 바꾸는 경우 Today-Records 기준갱신 대상으로 유지한다.
- `selectedMacroBasis` 문자열 자체는 UI/session 상태로 남긴다. records/export/localStorage schema에는 저장하지 않는다.
- Records의 `goalSnapshot`에는 선택 basis의 결과값인 `targetCal`, `protein`, `carbs`, `fat`만 저장한다.
- 사용자가 "오늘 기준으로 기록 갱신"을 누르면 현재 선택 basis의 탄단지 산출값이 snapshot에 저장되고, 식사 / 공복 체중 / 메모는 유지된다.
- guide/activity 산출 탄단지가 우연히 같으면 저장된 계산 기준이 달라지지 않은 것이므로 stale을 띄울 이유가 없다.
- `runTodayRecordConfirmationTests`에 선택 basis 변경이 stale을 띄우되 UI-only 상태를 저장하지 않고, 수동 갱신 후 저장 basis로 돌아오면 current가 되는 v8.0-B 회귀 테스트를 추가했다.

3. `suppressTodayRecordAutoSync`

검색 결과 이 플래그는 몇몇 render 호출 주변에서 true/false로 바뀌지만, 실제 auto sync 실행 여부를 막는 조건으로는 쓰이지 않는다.
현재 기준갱신 흐름은 자동 저장보다 표시/수동 갱신 중심이라 즉시 버그로 단정하지 않는다.
하지만 이름과 역할이 어긋나 있을 가능성이 있으므로 기준갱신 핫픽스 단계에서 확인해야 한다.

2026-06-02 v8.0-C 반영:

- `render()`는 자동 Today snapshot sync를 호출하지 않는다.
- 이 플래그는 실제 분기에서 읽히지 않는 잔재 상태였으므로 제거했다.
- 관련 InBody / 기록 체중 적용 흐름은 기존처럼 `render()`를 호출한다. 산식, score formula, Coach, recentContext, InBody gate, backup schema는 바꾸지 않았다.

4. `record_detail_edit` source

`syncTodayRecordGoalSnapshot()`은 `snapshotSource === "record_detail_edit"`이면 자동 sync를 막는다.
그러나 `getTodayRecordConfirmationState()`는 해당 source여도 현재 기준과 다르면 stale 패널을 보여준다.
이건 "사용자가 Records에서 당시 기준을 직접 편집한 기록"과 "Today 기준으로 다시 덮어쓸 수 있는 기록"의 UX 경계가 더 필요할 수 있다.

2026-06-02 v8.0-C 반영:

- `record_detail_edit` snapshot은 자동 Today sync가 덮지 않는 보호 기준으로 유지한다.
- Today 기준과 다르면 stale 패널은 계속 보여준다. 이는 사용자가 명시적으로 현재 Today 기준으로 다시 저장할 수 있게 하는 경계다.
- 사용자가 기준갱신 버튼을 누르면 `today_manual_confirm`으로 snapshot source가 바뀌고, 식사 / 공복 체중 / 메모는 유지된다.
- `runTodayRecordConfirmationTests`에 자동 sync 차단과 수동 confirm 허용을 동시에 검증하는 v8.0-C 회귀 테스트를 추가했다.

## 9. 위험 지점 목록

P0 후보:

- 아직 확정 P0는 없다.
- diet targetCal > totalBurn 문제는 재현 케이스에서 확인되지 않았다.
- 1913 케이스는 targetCal 1913.3, totalBurn 2133.7로 diet targetCal <= totalBurn을 만족한다.

P1 후보:

1. 운동 수준 판단이 주간 웨이트일수 중심이다.
   - 위치: `getTargetRateTrainingBand()`
   - v8 기준의 "주간 웨이트일수 단독 기준 금지"와 직접 충돌 가능성이 있다.

2. final guide basis가 diet 단백질을 bodyweight 2.4g/kg으로 기본 적용한다.
   - 위치: `getDualBasisProteinRange()`, `calculateGuideBasisMacrosWithinTarget()`
   - 근손실 방지라는 내부 의도는 이해되지만, 외부 근거와 앱 적용범위 검토 없이 모든 diet 사용자에게 bodyweight 2.4g/kg 중간값을 확정할 수는 없다. 저칼로리 floor 지배 상황에서는 탄수화물을 guide minimum 아래로 밀 수 있으므로 미검증 생리학 후보로 남긴다.

3. snapshot 없는 detailed 오늘 기록이 "갱신 필요"로 보인다. [v8.0-A 처리 완료]
   - 위치: `getTodayRecordConfirmationState()`
   - `basis-missing` 상태와 "계산 기준 저장 필요" 문구로 분리했다.
   - desktop / mobile viewport 기준 `runTodayRecordConfirmationTests`는 통과했다.

P2 후보:

1. final result의 protein basis report가 부정확하게 읽힐 수 있다.
   - `calculate()` 최종 반환에서 `proteinBasisWeight`가 bodyweight로 덮인다.
   - v8 리포트 필드에는 bodyweight 기준과 LBM 기준을 모두 명확히 분리해야 한다.

2. selected macro basis 토글이 기준갱신을 띄우는 정책이 불명확하다. [v8.0-B 처리 완료]
   - 선택 basis가 macro 기준을 바꾸면 기준갱신 대상으로 본다.
   - `selectedMacroBasis` 문자열은 UI-only 상태로 유지하고, 저장 snapshot에는 선택 basis의 산출 macro 값만 보관한다.

3. `suppressTodayRecordAutoSync`가 실제 조건 분기에 쓰이지 않는다. [v8.0-C 처리 완료]
   - 실제 auto sync 분기가 빠진 것이 아니라, 더 이상 읽히지 않는 잔재 플래그였다.
   - 플래그를 제거하고 관련 흐름은 기존 `render()` 호출로 단순화했다.

## 10. 다음 작업 기준

현재 단계에서 하면 안 되는 것:

- 단백질 180.5g을 바로 낮추기
- guide/activity basis를 바로 제거하기
- score, Coach, Recent Flow, InBody gate, backup schema를 함께 건드리기
- 주간 웨이트일수 보정을 임의로 삭제하기
- snapshotless stale 표시를 CSS로 숨기기

다음 단계에서 해야 하는 것:

1. 현재 감사 결과를 기준으로 산식 리포트 객체를 설계한다.
2. 필수 필드를 포함한다.
   - protein_g
   - protein_g_per_kg_bodyweight
   - protein_g_per_kg_lbm
   - protein_rule_source
   - protein_soft_cap_hit
   - protein_hard_cap_hit
   - fat_g
   - fat_percent_kcal
   - fat_floor_hit
   - carb_g
   - carb_min_hit
   - redistribution_reason
3. candidate formula를 만들기 전에 현재/candidate side-by-side 시나리오 러너를 준비한다. [v8.0-D 1차 처리 완료]
   - `runV8ScenarioRunner()`는 8-2 필수축 category sweep과 8-3의 18개 human-review 케이스를 별도 레이어로 출력한다.
   - human-review 레이어에는 필수 `required_user_rest_diet_1913` 케이스를 포함한다.
   - current production과 기존 report-only `candidate-no-weekly-xw`를 나란히 보여준다.
   - `candidate-no-weekly-xw`는 비교 기준일 뿐이며, v8 승인 산식 또는 production 후보로 확정된 것이 아니다.
   - 8-2 목표 축의 `유지`는 보존한다. v8.0-D/I 당시에는 production `GOALS.maintain`이 없었으므로 유지 축 케이스를 `axisGoal=maintain`, `productionGoalFallback=recomp`로 명시했다. v8.0-J 이후 현재는 production maintain goal 1차가 들어가 `axisGoal=maintain`, `goal=maintain`, `productionGoalFallback=null`로 확인한다.
4. 사용자 타입/운동 프로필 판단은 주간 웨이트일수 단독이 아니라 체성분, SMM, FFMI, 세션 길이, 강도, 운동 프로필, cardio, 최근 기록 신뢰도까지 포함한 후보 모델로 문서화한다.
5. Today-Records 기준갱신의 사용자 노출 정책은 v8.0-A / v8.0-B에서 1차 고정했다.
   - snapshotless detailed 상태 분리는 v8.0-A에서 처리했다.
   - selected basis 토글 정책은 v8.0-B에서 처리했다.
   - `suppressTodayRecordAutoSync` 잔재 정리와 `record_detail_edit` UX 경계 확인은 v8.0-C에서 처리했다.
   - 9-4 현재/candidate side-by-side 시나리오 러너 1차본은 v8.0-D에서 처리했다.
   - 다음 큰 작업은 러너 출력으로 9-3 단백질/지방/탄수 감사를 수행하고, 그 결과를 바탕으로 9-5/9-6 candidate 설계를 좁히는 것이다.

## 11. 2026-06-02 추가 보강: 감사 한계와 다음 검증 요구

이 문서는 현재 구현을 추적한 감사 리포트이지, v8 산식이나 운동 프로필 구현 승인서가 아니다.
다음 작업자가 이 문서를 읽고 바로 산식을 고치거나 기능을 추가하면 안 된다.

오해하면 안 되는 점:

- 1913 / 196.3 / 180.5 / 45.1 케이스는 원인 재현 케이스이지 대표 검증 전체가 아니다.
- 단백질 180.5g은 "남은 칼로리를 단백질이 가져간 결과"가 아니라 guide basis의 bodyweight 2.4g/kg 규칙에서 나온 값이다.
- 주간 웨이트일수는 현재 target rate band의 입력이지만, v8 사용자 운동 수준 판단의 충분조건이 아니다.
- guide/activity basis 토글은 단순 표시 토글이 아니라 target macro / remaining / score 기준과 연결된다.
  - 2026-06-02 v8.0-B에서 이 항목은 "macro 출력값이 달라지면 기준갱신 대상, `selectedMacroBasis` 문자열은 UI-only"로 정책을 고정했다.
- snapshot 없는 detailed 기록이 "갱신 필요"로 보이는 현상은 CSS로 숨길 문제가 아니라 상태 분류와 사용자 행동 경로를 정해야 하는 문제다.
  - 2026-06-02 v8.0-A에서 이 항목은 `basis-missing` / "계산 기준 저장 필요"로 1차 처리했다.

현재 시나리오 검증의 부족한 부분:

- 브라우저에서 직접 확인한 기준갱신 시나리오는 핵심 흐름 위주다.
- v8.0-D에서 8-2 필수축 category sweep과 18개 human-review scenario 1차 그리드는 생겼지만, 전체 80,621,568,000 cross-product 실행과 각 케이스 수치의 human review / candidate formula 승인 검증은 아직 없다.
- smoke 테스트는 현재 구현의 기본 회귀 확인이지, v8 candidate 산식의 타당성 검증이 아니다.
- 현재 문서는 여성 / 고체지방 / 저체지방 운동인 / 러닝 중심 / 스트렝스 중심 / 기록 신뢰도 낮음 / 고유산소 / REST day / InBody 부재 또는 의심 기록 같은 케이스를 v8.0-D 러너에 포함했지만, 아직 수치별 해석/수정 판단을 완료하지 않았다.

v8.0-D 러너와 후속 시나리오 검증은 최소한 아래 축을 교차해야 한다.

- 목표: 유지 / diet / cut / lean_bulk / bulk
- 성별, 키, 체중, 골격근량, 체지방률, LBM 추정 신뢰도
- 활동량, 업무 유형, 주간 웨이트일수, 세션 길이, 세션 강도
- 운동 프로필: 보디빌딩, 러닝 중심, 스트렝스 중심, 혼합 운동, 운동 거의 없음
- 오늘 세션: REST / 보디빌딩 세션 / 파워빌딩 세션 / 스트렝스 세션 / 러닝 세션 / 혼합형 세션
- 유산소: 없음, 저강도, 중강도, 장시간, 고경사/고속, 웨이트와 병행
- 기록 상태: 오늘 상세 기록 있음, snapshot 없음, import/legacy, 식사만 있음, 체중만 있음, InBody 최신/과거/의심
- basis: guide / activity 선택 상태와 선택 변경 전후

v8.0-D 러너 해석 주의:

- 러너는 report-only다. 산식 / score formula / Coach / recentContext / InBody gate / backup schema를 바꾸지 않는다.
- 운동 프로필은 수치표의 축으로만 포함했다. 프로필별 세션 후보, UI 입력, snapshot 저장, Today/Records 연동은 아직 설계/구현 완료가 아니다.
- v8.0-D/H 당시 운동 프로필별 report-only session과 current production routine fallback은 분리해서 읽어야 했다. v8.0-AG 이후 현재 runner는 비보디빌딩 profile session을 보디빌딩 fallback으로 계산하지 않고 profile-owned routine/session으로 계산한다.
- 8-2 필수축을 모두 곱한 80,621,568,000개 조합은 v8.0-D에서 전수 실행하지 않았다. v8.0-D는 모든 필수축 카테고리를 한 번씩 계산하는 category sweep과 18개 human-review 표를 분리한 1차 러너다.
- 80,621,568,000개 full cross-product는 단일 브라우저 내부 테스트로 돌릴 대상이 아니다. 1ms/케이스여도 약 933일이 걸리므로, 후속 검증은 full 축 정의를 보존한 상태에서 pairwise/orthogonal coverage, targeted stress, shard runner를 설계해야 한다.
- `유지` 목표는 8-2 필수축에서 삭제하거나 리컴프로 확정하지 않는다. v8.0-D/I는 당시 앱에 production 유지 키가 없다는 사실만 `productionGoalFallback=recomp`로 드러냈다. v8.0-J 현재는 production maintain goal 1차가 들어갔고, rate / cardio reflection / target floor / macro soft range / fat target / score / Coach / snapshot / backup-export-import 계약을 함께 닫았는지 확인한다. 단, profile-specific macro formula와 전체 v8 산식 승인은 아직 아니다.
- 업무 강도 보정축은 `office` 기준으로 검증하지 않는다.
  - 현재 산식은 `workloadY = clamp(WORK_TYPE_Y[workType] + workAdj, 0, 1)`이고 `WORK_TYPE_Y.office`는 0이라서 low 보정이 clamp에 막혀 base와 겹친다.
  - v8.0-D category sweep은 업무 보정축의 low/base/high가 앱 산식 안에서 실제로 분리되는지 보기 위해 `active` 업무 유형을 기준으로 삼는다.
  - 이 근거는 내부 코드 근거다. `WORK_TYPE_Y` 계수, 업무활동 계수, `0.06` 업무 가산폭, 실제 직업군별 활동량 모델이 외부 운동생리학적으로 타당하다는 뜻이 아니다.
  - v8.0-D1부터 report의 `axisCoverage.evidenceLevel`은 `internal_formula_coverage_only`로 표시하고, `impactScope`는 `report_only_axis_coverage_case_fixture`로 제한한다.
- candidate 열은 `candidate-no-weekly-xw` 하나만 비교한다. 이는 "주간 웨이트일수 단독 영향 제거 시 차이"를 보기 위한 기존 후보일 뿐, 다각적 v8 운동 프로필 산식 전체가 아니다.

다각적 기능 구현 기준:

- 기능을 많이 추가하는 것이 v8 목표가 아니다. 기능이 Today 계산, Records 저장, Recent 해석, InBody 근거, Settings 기본값, Coach 행동 문구 안에서 어떻게 이어지는지 먼저 정해야 한다.
- 새 기능은 입력 주체, 저장 위치, snapshot 포함 여부, Today/Records 갱신 영향, 최근 흐름 반영 여부, 사용자가 되돌릴 수 있는 경로를 함께 가져야 한다.
- 운동 프로필은 이름만 늘리면 안 된다. 프로필마다 세션 후보, 루틴 기본값, 칼로리 보정, 단백질/지방/탄수 기준, 기록 snapshot, Settings 기본값, Today 수정 흐름이 맞아야 한다.
- 충분히 설계하지 못한 프로필은 active 기능으로 열지 말고 disabled / 후속 후보로 둔다. 사용자가 세팅 단계에서 앱의 깊이를 의심하게 만드는 반쪽 기능은 금지한다.
- 사용자에게 실질적으로 도움이 되는 기준은 "화면에 기능이 있다"가 아니라 "사용자가 자기 상황을 넣었을 때 결과와 다음 행동을 믿을 수 있다"이다.

## 11-1. 2026-06-02 v8.0-D1 근거 등급 재감사

현재까지의 v8 작업을 근거 등급 기준으로 다시 보면 아래와 같다.

- v8.0-A snapshotless detailed 기록 분리
  - 근거 등급: 제품 정책 근거 + 내부 코드 근거
  - 의미: "갱신 필요"와 "저장 당시 계산 기준 없음"을 UX 상태로 분리한 것이다.
  - 외부 운동생리학 근거가 필요한 산식 변경은 아니다.
- v8.0-B selected macro basis 기준갱신 정책
  - 근거 등급: 제품 정책 근거 + 내부 코드 근거
  - 의미: 선택 basis가 실제 `targetCal/protein/carbs/fat` 저장값을 바꾸면 Records 기준갱신 대상으로 본다는 계약이다.
  - 영양학적으로 guide/activity 중 하나가 더 타당하다는 결론이 아니다.
- v8.0-C `record_detail_edit` 보호와 `suppressTodayRecordAutoSync` 잔재 정리
  - 근거 등급: 내부 코드 근거 + 제품 정책 근거
  - 의미: 자동 sync가 사용자의 Records 편집 기준을 덮지 않게 하는 저장 책임 정리다.
  - 운동 / 영양 산식 근거로 사용하지 않는다.
- v8.0-D 시나리오 러너 1차본
  - 근거 등급: 내부 코드 근거
  - 의미: 8-2 필수축 category sweep과 8-3 human-review 18개를 분리했고, 전체 80,621,568,000개 cross-product를 전수 실행하지 않았음을 명시한다.
  - 각 케이스의 수치 타당성, candidate formula 승인, profile별 세션 산식 적합성은 아직 완료되지 않았다.
- v8.0-D `유지` 목표 fallback
  - 근거 등급: 내부 코드 근거
  - 의미: v8.0-D 당시 production `GOALS`에 유지 키가 없다는 사실을 드러내기 위해 `axisGoal=maintain`, `productionGoalFallback=recomp`로 기록한 것이다.
  - 유지 목표를 리컴프로 확정한 것이 아니며, 실제 maintain 추가는 별도 production 변경이다.
  - v8.0-J 이후 이 항목은 히스토리다. 현재 구현은 maintain production goal 1차를 추가했으므로, 더 이상 현재 상태를 `maintain -> recomp`로 설명하면 안 된다.
- v8.0-D / D1 업무 강도 보정축 `active` fixture
  - 근거 등급: 내부 코드 근거
  - 의미: `office + low`가 clamp로 base와 겹치기 때문에, report-only category sweep에서 low/base/high가 실제로 분리되는지 보기 위한 fixture다.
  - `WORK_TYPE_Y` 계수나 실제 직업군별 활동량 모델을 외부적으로 검증한 근거가 아니다.
- 1913 / 196.3 / 180.5 / 45.1 단백질 회귀 케이스
  - 근거 등급: 내부 코드 근거 + 미검증 생리학 후보
  - 의미: AT 전 guide basis에서 diet 단백질이 bodyweight 2.4g/kg까지 올라가는 구현을 재현한 것이다.
  - "단백질을 먼저 정하고 남은 칼로리를 탄수에 배분한다"는 구조는 내부 정책상 보존 후보지만, 모든 diet 사용자에게 2.4g/kg 기본 중간값을 적용해도 된다는 외부 근거 검토는 아직 끝나지 않았다.

재검토 결과, v8.0-A/B/C는 저장 / 기준갱신 / UX 책임 정리라서 외부 운동생리학 근거가 필요한 산식 변경으로 보지 않는다.
다만 1차 감사문서의 단백질 항목과 v8.0-D 업무 보정축 설명에는 내부 근거를 외부 타당성처럼 오해할 수 있는 표현이 남아 있었으므로 v8.0-D1에서 문서와 report metadata를 보강한다.

## 11-2. 2026-06-03 v8.0-E 9-3 현재 탄단지 감사 1차

v8.0-E에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `macroAudit` report-only 레이어를 추가했다.

근거 분리:

- ISSN protein position stand 2017
  - URL: https://jissn.biomedcentral.com/articles/10.1186/s12970-017-0177-8
  - 앱 사용: protein bodyweight g/kg이 일반 운동인 범위 상단을 넘는지 검토하는 외부 review threshold.
  - 이 근거만으로 모든 diet 사용자의 기본 단백질을 확정하지 않는다.
- Helms / Aragon / Fitschen natural bodybuilding contest prep review 2014
  - URL: https://pubmed.ncbi.nlm.nih.gov/24864135/
  - 앱 사용: 2.3~3.1g/kg LBM 단백질과 15~30% kcal 지방을 contest prep 맥락 후보로 검토한다.
  - 이 범위에 들어간다고 일반 diet 케이스가 자동 통과되는 것은 아니다.
- 현재 `index.html` macro decomposition
  - 근거 등급: 내부 코드 근거.
  - 앱 사용: 현재 산식이 어떤 값을 내는지 보여준다. 외부 생리학 타당성은 증명하지 않는다.

감사 규칙:

- `protein_bodyweight_above_general_issn_range`: 단백질이 bodyweight 2.0g/kg을 넘으면 일반 운동인 범위 상단 초과로 검토한다.
- `diet_default_near_2_4g_bodyweight_requires_candidate_review`: diet 기본값이 bodyweight 약 2.4g/kg에 가까우면 high-priority candidate review로 본다.
- `protein_lbm_in_contest_prep_range_but_context_unverified`: 단백질이 2.3~3.1g/kg LBM 범위에 들어가도, 해당 케이스가 contest prep 또는 그에 준하는 맥락인지 확인되지 않았으면 검토대상으로 둔다.
- `fat_percent_below_contest_prep_reference_floor`: 지방이 15% kcal 아래면 외부 contest-prep 하한 후보보다도 낮은 경고로 본다.
- `fat_at_guide_floor`, `carb_below_guide_min`: 내부 guide basis의 floor/min 충돌 신호다. 외부 근거가 아니라 현재 산식의 배분 순서 문제를 보여주는 내부 근거다.
- `exercise_profile_macro_formula_pending`: 운동 프로필은 report-only 축이고 아직 profile-specific macro formula가 없음을 표시한다.

v8.0-E runner 결과:

- `macroAudit` 전체 case count: 91
- 8-2 axis category fixture: 73
- 8-3 human-review case: 18
- 전체 finding count:
  - `protein_bodyweight_above_general_issn_range`: 77
  - `diet_default_near_2_4g_bodyweight_requires_candidate_review`: 77
  - `protein_lbm_in_contest_prep_range_but_context_unverified`: 76
  - `fat_at_guide_floor`: 85
  - `carb_below_guide_min`: 27
  - `fat_percent_below_contest_prep_reference_floor`: 4
  - `exercise_profile_macro_formula_pending`: 13
- human-review 18건 기준 finding count:
  - `protein_bodyweight_above_general_issn_range`: 8
  - `diet_default_near_2_4g_bodyweight_requires_candidate_review`: 8
  - `protein_lbm_in_contest_prep_range_but_context_unverified`: 8
  - `fat_at_guide_floor`: 15
  - `carb_below_guide_min`: 10
  - `fat_percent_below_contest_prep_reference_floor`: 1
  - `exercise_profile_macro_formula_pending`: 5
- 필수 `required_user_rest_diet_1913`:
  - protein: 2.4g/kg bodyweight
  - protein: 2.83g/kg LBM
  - fat: 21.23% kcal
  - findings: `protein_bodyweight_above_general_issn_range`, `diet_default_near_2_4g_bodyweight_requires_candidate_review`, `protein_lbm_in_contest_prep_range_but_context_unverified`, `fat_at_guide_floor`, `carb_below_guide_min`

해석 주의:

- 91건 count는 사용자 prevalence가 아니다. 73건은 axis category fixture라 기본 diet 케이스가 반복된다.
- 그럼에도 반복 신호가 크므로, 9-5/9-6 candidate 설계에서는 단백질 선택 기준을 목표 / 운동 프로필 / 체지방률 / LBM / 칼로리 제한 정도와 분리해야 한다.
- v8.0-E 당시 결론은 "단백질을 낮춰라"가 아니라 "일반 diet 기본값이 bodyweight 2.4g/kg으로 고정되는 구조를 candidate review 없이 승인하지 말라"이다.
- carb guide min 하회와 fat floor hit가 동시에 나타나는 케이스는 단백질-지방-탄수 배분 순서와 재분배 기준을 함께 설계해야 한다.

## 11-3. 2026-06-03 v8.0-F 9-5/9-6 candidate 설계 레이어 1차

v8.0-F에서는 v8.0-E의 `macroAudit` 결과를 `candidateDesign` report-only 레이어로 연결했다.

candidateDesign의 성격:

- 산식 적용이 아니다.
- candidate formula 승인도 아니다.
- 9-7 candidate 비교를 시작하기 전에 필요한 입력, 보류 조건, 외부근거 상태, production 계약을 명시하는 설계 레이어다.
- `candidateDesign.meta.productionFormulaChanged`는 false이고, `candidateFormulaApproved`도 false다.

입력 후보:

- `exerciseProfile`
  - Settings + Today report axis 소유.
  - non-bodybuilding profile은 아직 active production formula가 아니다.
- `profileSession`
  - Today 소유.
  - 러닝 세션을 `UPPER` 같은 production fallback과 섞어 말하지 않기 위한 필수 입력이다.
- `performanceLevel`
  - Settings + Today 소유.
  - 주간 웨이트일수 단독 판단을 피하기 위한 축이다.
- `bodyCompReliability`
  - Today calculation input 소유.
  - bodyweight / LBM / adjusted basis 단백질 기준을 분리하기 위해 필요하다.
- `recent14RecordCoverage`
  - Records / recentContext 소유.
  - 즉시 산식 변경이 아니라 기록 충분성 판단에 사용한다.
- `recent28Trend`
  - Records / recentContext 소유.
  - 목표 rate feedback 후보에 사용한다.

candidateDesign에서 남긴 필수 결정:

- `protein_context_gate`
  - 일반 diet 기본값이 bodyweight 2.4g/kg으로 고정되는 구조를 목표, 체지방률, LBM 신뢰도, 운동 프로필, 수행 수준, deficit 맥락으로 분리해야 한다.
- `fat_carb_joint_redistribution`
  - fat floor와 carb guide min 하회가 동시에 나타나는 케이스를 단일 영양소 수정으로 처리하지 않고 함께 재분배해야 한다.
- `profile_specific_carbohydrate_policy`
  - 러닝 / 스트렝스 / 파워빌딩 / 보디빌딩 / 혼합형은 세션별 탄수와 회복 규칙이 필요하다.
- `bodyweight_lbm_adjusted_basis_policy`
  - 체중 기준, LBM 기준, 보정 기준 단백질을 report와 candidate formula에서 분리해야 한다.
- `maintain_goal_mapping`
  - v8.0-F 당시 `maintain`은 축에는 남아 있지만 production `GOALS` key가 없으므로 리컴프로 확정하지 않았다.
  - v8.0-J 이후 `maintain`은 production key와 cross-app 계약이 함께 들어간 1차 production goal이다. 이 변경은 maintain goal mapping만 닫은 것이며, profile-specific macro formula 승인은 아니다.
- `profile_session_input_contract`
  - Today / Settings / Records snapshot / export-import에 새 profile/session 입력이 어떻게 들어가는지 정해야 한다.

미해결 production 계약:

- `maintain contract regression guard`
- Records `goalSnapshot` schema review for new profile inputs
- Today / Settings profile input ownership
- score / remaining / Coach / recentContext copy gates after formula approval
- export/import compatibility

해석 주의:

- v8.0-F는 "무슨 산식으로 바꿀지"를 확정하지 않았다.
- 대신 "무엇을 확정해야만 산식을 바꿀 수 있는지"를 report 객체로 고정했다.
- 다음 단계는 이 설계 계약을 기준으로 9-7 candidate formula 비교를 만들거나, maintain/profile input contract를 먼저 세분화하는 것이다.

## 11-4. 2026-06-03 v8.0-G 9-7 context macro candidate-v0 비교 1차

v8.0-G에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `candidateFormulaComparison` report-only 레이어를 추가했다.

candidate-v0의 성격:

- model: `candidate-v8-context-macro-v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `targetChanged`: false
- `profileSpecificCarbohydratePolicyApplied`: false
- v8.0-G 당시 `maintainGoalMappingApplied`: false
- v8.0-J 이후 `maintainGoalMappingApplied`: true
  - 의미: candidate-v0 report가 production maintain mapping을 볼 수 있다는 뜻이다.
  - 비의미: candidate-v0가 maintain macro formula를 새로 승인했거나 전체 v8 formula를 승인했다는 뜻이 아니다.

candidate-v0가 하는 일:

- current targetCal을 그대로 유지한다.
- non-contest-prep 일반 diet 맥락에서는 단백질을 ISSN 2017 general exercising-individual review upper range인 bodyweight 2.0g/kg까지 낮추는 비교를 수행한다.
- 지방은 현재값을 유지한다. 단, 외부 contest-prep lower reference보다 낮은 경우에만 report-only 비교에서 하한을 올린다.
- 남은 target kcal은 탄수로 재배분한다.

candidate-v0가 하지 않는 일:

- profile-specific carbohydrate formula를 적용하지 않는다.
- running / strength / powerbuilding / mixed 세션별 carbohydrate periodization을 적용하지 않는다.
- v8.0-G 당시에는 maintain goal production key를 만들지 않았다.
- v8.0-J 이후에는 maintain goal production key가 존재한다. 다만 candidate-v0는 current production maintain 계산을 비교에 포함할 뿐, maintain 전용 macro formula를 별도로 승인하지 않는다.
- score, remaining, Coach, recentContext, InBody gate, backup schema를 바꾸지 않는다.
- candidate-v0를 최종 v8 production formula로 승인하지 않는다.

근거 검토:

- ISSN protein 2017은 "대부분의 운동하는 개인"에 대한 1.4-2.0g/kg/day 범위를 제공한다. 따라서 v8.0-G에서는 2.0g/kg을 일반 diet 기본값의 검토 상한으로만 사용했다.
- Helms / Aragon / Fitschen 2014는 자연 보디빌딩 contest prep 맥락에서 LBM 2.3-3.1g/kg protein과 fat 15-30% kcal을 제시한다. 따라서 일반 diet 사용자에게 곧장 2.4g/kg BW를 승인하는 근거로 쓰지 않는다.
- Academy / Dietitians of Canada / ACSM 2016은 탄수화물이 training load와 session quality 맥락에서 조정되어야 한다는 근거다. v8.0-G에서는 이 근거를 profile-specific carbohydrate policy의 보류 근거로만 유지하고, candidate-v0에 적용하지 않았다.
- `currentFormulaReport`는 내부 코드 근거일 뿐 외부 운동생리학 타당성 근거가 아니다.

필수 1913 케이스 runner 결과:

- current: target 1913.3kcal / protein 180.5g / carbs 196.3g / fat 45.1g
- candidate-v0: target 1913.3kcal / protein 150.4g / carbs 226.4g / fat 45.1g
- delta: target 0 / protein -30.1g / carbs +30.1g / fat 0
- resolved findings: `protein_bodyweight_above_general_issn_range`, `diet_default_near_2_4g_bodyweight_requires_candidate_review`, `carb_below_guide_min`
- remaining findings: `protein_lbm_in_contest_prep_range_but_context_unverified`, `fat_at_guide_floor`

전체 91 케이스 runner 요약:

- targetChangedCount: 0
- proteinReducedCount: 77
- carbsIncreasedCount: 77
- candidateHighPriorityCaseCount: 0
- remainingFindingCounts에는 `fat_at_guide_floor`, `protein_lbm_in_contest_prep_range_but_context_unverified`, `carb_below_guide_min`, `exercise_profile_macro_formula_pending`이 남는다.
- newFindingCounts에 `protein_lbm_in_contest_prep_range_but_context_unverified` 6건이 생긴다. 이는 단백질을 2.0g/kg BW로 낮추어도 일부 체성분에서는 LBM 기준 contest-prep range에 들어갈 수 있음을 뜻하며, candidate-v0가 최종 승인 산식이 아니라는 추가 근거다.

해석 주의:

- v8.0-G는 "단백질을 낮추면 모든 문제가 해결된다"는 결론이 아니다.
- v8.0-G는 "AT 전 diet 2.4g/kg BW 기본값이 일반 사용자 기본값으로는 과한 신호를 만든다"는 당시 감사 결과를 후보 비교 수치로 확인한 단계다.
- profile-specific carbohydrate와 profile input의 Records snapshot / export-import contract는 다음 단계에서 별도로 다뤄야 한다. maintain production goal의 1차 계약은 v8.0-J에서 닫았지만, 이를 profile-specific macro formula 승인으로 읽으면 안 된다.

## 11-5. 2026-06-03 v8.0-H 9-7 profileSession carbohydrate policy review 1차

v8.0-H에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `profileSessionCarbPolicy` report-only 레이어를 추가했다.

이 레이어의 목적:

- 운동 프로필별 세션을 production routine fallback과 분리해서 보는 것이 v8.0-H의 원래 목적이었다. v8.0-AG 이후 현재 기준은 한 단계 더 나아가, profile-owned routine/session을 실제 계산 입력으로 넣는 것이다.
- current와 `candidate-v8-context-macro-v0`의 탄수 g/kg를 profileSession별 검토범위와 비교한다.
- running / mixed의 endurance-carried load와 bodybuilding / strength / powerbuilding의 resistance load를 같은 규칙으로 뭉개지 않는다.
- profile-specific carbohydrate formula를 승인하거나 적용하지 않는다.

근거 검토:

- Academy / Dietitians of Canada / ACSM 2016 position stand는 운동 수행과 회복이 훈련/경기 시나리오별 nutrition strategy에 의해 개선될 수 있고, 식품/영양소의 양과 timing이 scenario-specific이어야 한다고 제시한다. 이 문서는 v8.0-H에서 running/mixed training-load carbohydrate review reference로만 사용한다.
- Escobar / VanDusseldorp / Kerksick 2016 BJN review는 resistance-based exercise의 carbohydrate 필요량은 endurance보다 직접 확립되어 있지 않으며, strength/power/bodybuilding 맥락에는 별도 주의가 필요하다는 근거로 사용한다.
  - v8.0-H 작성 당시 Henselmans / Schoenfeld로 표기한 것은 출처 오기다. v8.0-K에서 코드와 문서를 정정했다.
- 따라서 v8.0-H는 endurance carb range를 strength / powerbuilding / bodybuilding에 그대로 씌우지 않고, `resistance_carb_evidence_limited_do_not_overapply_endurance_range` finding을 남긴다.
- `currentFormulaReport`는 내부 구현 근거다. v8.0-H 당시에는 production routine fallback이 actual profile session과 다르다는 사실을 드러내는 데 썼고, v8.0-AG 이후에는 fallback이 사라졌는지와 profile-specific macro policy가 아직 별도 승인 대상인지를 구분하는 데 쓴다.

v8.0-H runner summary:

- 전체 profileSessionCarbPolicy case count: 91
- profileFormulaPendingCount: 13
- productionFallbackCount: 13
- currentBelowReviewFloorCount: 56
- candidateBelowReviewFloorCount: 50
- highPriorityCaseCount: 3

human-review summary:

- case count: 18
- profileFormulaPendingCount: 5
- productionFallbackCount: 5
- currentBelowReviewFloorCount: 4
- candidateBelowReviewFloorCount: 3
- highPriorityCaseCount: 1

대표 확인:

- `running_focused`
  - profileSession: `running_tempo_50m_support_upper`
  - productionRoutineFallback: `UPPER`
  - current/candidate carbs: 6.83g/kg
  - review range: 5-7g/kg
  - interpretation: 탄수량 자체는 around-one-hour endurance review range 안이지만, `UPPER`는 보조 상체 fallback일 뿐 actual running session이 아니며 cardio-carried running load가 아직 macro policy에 연결되지 않았다.
  - findings: `profile_formula_pending_non_bodybuilding`, `production_routine_fallback_not_profile_session`, `running_load_carried_by_cardio_not_macro_policy`
- `strength_focused`
  - profileSession: `strength_heavy_lower_90m`
  - productionRoutineFallback: `LOWER`
  - current/candidate carbs: 5.67g/kg
  - resistance review range: 3-5g/kg
  - interpretation: high carb 자체를 오류로 단정하지 않는다. 다만 strength profile에는 endurance range를 그대로 적용하지 않고 resistance-specific policy를 별도로 설계해야 한다.
  - findings: `profile_formula_pending_non_bodybuilding`, `production_routine_fallback_not_profile_session`, `resistance_carb_evidence_limited_do_not_overapply_endurance_range`
- `low_reliability_user` mixed case
  - profileSession: `mixed_strength_cardio_70m`
  - current carbs: 5.52g/kg
  - candidate-v0 carbs: 5.88g/kg
  - mixed review floor: 6g/kg
  - interpretation: v8.0-G의 단백질 context 후보만으로는 mixed profile carbohydrate floor 문제가 해결되지 않는다.
- `required_user_rest_diet_1913`
  - rest-day case이므로 performance carbohydrate floor를 적용하지 않는다.

해석 주의:

- v8.0-H는 "profile-specific carbohydrate formula"를 구현한 것이 아니다.
- v8.0-H는 세션별 탄수 정책을 앱에 적용하기 전에, 어떤 프로필이 production routine fallback 때문에 오해될 수 있는지와 어떤 케이스가 current/candidate-v0 모두에서 여전히 세션 부하와 충돌하는지를 드러낸 역사적 감사 레이어다. v8.0-AG 이후 현재 runner에서는 fallback count가 0이어야 한다.
- 다음 단계에서는 이 결과를 바탕으로 maintain goal mapping 또는 profile-specific macro candidate-v1 중 어느 것을 production contract와 먼저 연결할지 결정해야 한다.

## 11-6. 2026-06-03 v8.0-I 9-6 maintain production contract review 1차
v8.0-I에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `maintainGoalContract` report-only 레이어를 추가했다.

이 레이어의 목적:

- 8-2 목표 축의 `maintain`을 `recomp` fallback으로 계산해 본 사실을 유지 목표 승인으로 오해하지 않게 막는다.
- `GOALS.maintain` 하나만 추가하는 변경이 production-safe하지 않음을 코드 계약 단위로 드러낸다.
- v8.0-I 당시 scenario runner의 명시적 `maintain -> recomp` fallback과 production 함수들의 잠재적 `lean_bulk` silent fallback이 서로 다른 문제임을 분리한다.

확인된 production 계약:

- `GOALS.maintain`
- `CARDIO_TARGET_REFLECTION_RATES.maintain`
- `GOAL_RATE_POLICIES.maintain`
- `GOAL_TARGET_FLOOR_RULES.maintain`
- `GOAL_AWARE_MACRO_SOFT_RANGE_KG.maintain`
- `FAT_TARGET_PROFILES.maintain`
- `MACRO_SCORE_PROFILES.maintain`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.maintain`
- `DAILY_COACH_DECISION_PROFILES.maintain`
- `getDailyCoachGoalWording.maintain`
- `normalizeGoalSnapshot.goal=maintain`
- `Settings/Today goal chip integration`
- `backup/export/import maintain compatibility`

v8.0-I runner summary:

- `axisMaintainCaseCount`: 1
- `productionGoalExists`: false
- `missingContractCount`: 13
- `productionBlockerCount`: 13
- `silentFallbackRiskCount`: 6
- `autoExposureRiskCount`: 1
- `currentAxisFallbackGoal`: `recomp`
- `currentTargetRateFallback`: `lean_bulk`
- `currentFatTargetFallback`: `lean_bulk`
- `currentMacroScoreFallback`: `lean_bulk`
- `currentCoachDecisionFallback`: `lean_bulk`

해석:

- `maintain` 축 case는 현재 production `GOALS`에 유지 키가 없어서 `recomp`로 계산 fallback을 사용한다. 이것은 유지 목표를 리컴프로 확정했다는 뜻이 아니다.
- production에 `GOALS.maintain`만 추가하면 goal chip UI는 자동 노출될 수 있지만 rate/fat/score/Coach/copy/snapshot/export-import 계약이 비어 있어 실제 사용자는 유지 목표라고 눌렀는데 gain-phase류 fallback 안내를 볼 수 있다.
- 이 근거는 내부 코드 계약 + 제품 정책 감사 근거이며, 외부 운동생리학 근거가 아니다. 유지 목표의 실제 kcal delta, cardio 반영률, macro/score/Coach 기준을 정하려면 별도 제품 정책과 필요 시 외부 근거 검토가 필요하다.
- v8.0-I는 production 산식, score, Coach, recentContext, InBody gate, backup schema를 변경하지 않는다.

다음 production-safe 조건:

- `GOALS.maintain`을 추가하기 전에 rate, cardio reflection, target floor, macro soft range, fat target, score, Coach threshold/decision/copy, Records snapshot, export/import round-trip 테스트를 함께 준비한다.
- 유지 목표의 수치 정책이 internal-only이면 문서에 internal-only로 남기고, 외부 운동생리학 근거처럼 통과시키지 않는다.

## 11-7. 2026-06-03 v8.0-J maintain production goal 1차

v8.0-J에서는 v8.0-I에서 막아 둔 `GOALS.maintain` 단독 추가 위험을 피하기 위해 maintain production goal과 연결 계약을 함께 추가했다.

변경 범위:

- `GOALS.maintain`
- `CARDIO_TARGET_REFLECTION_RATES.maintain`
- `GOAL_RATE_POLICIES.maintain`
- `GOAL_TARGET_FLOOR_RULES.maintain`
- `GOAL_AWARE_MACRO_SOFT_RANGE_KG.maintain`
- `FAT_TARGET_PROFILES.maintain`
- `MACRO_SCORE_PROFILES.maintain`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.maintain`
- `DAILY_COACH_DECISION_PROFILES.maintain`
- maintain 전용 Coach goal / decision / InBody copy
- `normalizeGoalSnapshot.goal=maintain`
- Settings / Today goal chip 자동 노출 확인
- backup / export / import maintain round-trip 확인

runner 직접 추출:

- `maintainGoalContract.meta.stage`: `9-6_maintain_production_contract_review_v1`
- `productionMaintainGoalAvailable`: true
- `maintainProductionApproved`: true
- `axisMaintainCaseCount`: 1
- `productionGoalExists`: true
- `missingContractCount`: 0
- `productionBlockerCount`: 0
- `silentFallbackRiskCount`: 0
- `autoExposureRiskCount`: 0
- `currentAxisProductionGoal`: `maintain`
- `currentAxisFallbackGoal`: null
- `currentTargetRateFallback`: `maintain`
- `currentFatTargetFallback`: `maintain`
- `currentMacroScoreFallback`: `maintain`
- `currentCoachDecisionFallback`: `maintain`

maintain axis case 직접 추출:

- `axisGoal`: `maintain`
- `goal`: `maintain`
- `goalAxisStatus`: `production_goal`
- `productionGoalFallback`: null
- `profileSession`: `bodybuilding_push`
- `selectedWeeklyRate`: 0
- `policyKind`: `maintain`
- `targetBurnBase`: 2537.7748276923076
- `finalTargetCal`: 2537.7748276923076
- `cardioTargetReflectionRate`: 0.50
- `goalTargetFloor`: 2417.7748276923076
- `goalTargetFloorApplied`: false

maintain macro 직접 추출:

- protein: 135g / 1.8g/kg BW / 21.28% kcal
- fat: 60g / 0.8g/kg BW / 21.28% kcal
- carbs: 364.44g / 4.859g/kg BW / 57.44% kcal
- macro gap: 0
- range context: protein 1.6 / 1.8 / 2.0g/kg, carbs 3 / 4 / 5g/kg low-moderate, fat floor 0.8g/kg, mid 1.2g/kg, max 1.5g/kg, flexibleMax 2.0g/kg

근거 분리:

- ISSN protein position stand 2017
  - URL: https://jissn.biomedcentral.com/articles/10.1186/s12970-017-0177-8
  - 앱 사용: maintain protein range가 일반 운동인 sports nutrition guard 밖으로 벗어나지 않는지 확인한다.
  - 비의미: maintain의 모든 macro 수치나 kcal 중립 범위를 외부 근거로 확정한다는 뜻이 아니다.
- IOM Dietary Reference Intakes Table C-5 AMDR
  - URL: https://www.ncbi.nlm.nih.gov/books/NBK208874/
  - 앱 사용: maintain macro distribution이 broad population macro distribution guard 밖으로 벗어나지 않는지 확인한다.
  - 비의미: 운동 프로필별 탄수 periodization 또는 유지 목표의 모든 세부 산식을 확정한다는 뜻이 아니다.
- maintain product policy
  - 앱 사용: 체중을 크게 움직이지 않는 중립 목표라서 `selectedWeeklyRate=0`, kcal guard ±120, target floor `totalBurn - 120`, cardio reflection 0.50, score/Coach neutral tolerance를 둔다.
  - 근거 등급: 내부 제품 정책이다. 외부 운동생리학 근거로 말하면 안 된다.

해석:

- v8.0-J는 maintain goal을 production에서 누를 수 있게 하는 1차 계약을 닫았다.
- v8.0-I의 `maintain -> recomp` / `lean_bulk` fallback 위험은 현재 상태가 아니라 히스토리와 회귀 보호 대상으로 남긴다.
- `maintainProductionApproved=true`는 maintain production goal 계약 승인이지, profile-specific macro formula 또는 전체 v8 완료가 아니다.
- 다음 작업에서 maintain 수치를 다시 조정하려면 external reference / product policy / score / Coach / Records / backup 영향을 다시 함께 검토한다.

## 11-8. 2026-06-03 v8.0-K profile macro candidate-v1 1차

v8.0-K에서는 v8.0-H가 남긴 profile-specific carbohydrate 미해결을 실제 candidate-v1 report로 연결했다.

candidate-v1의 성격:

- model: `candidate-v8-profile-macro-v1`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `targetChanged`: false
- `profileSpecificCarbohydratePolicyApplied`: true
- score / Coach / recentContext / InBody gate / backup schema 변경 없음

candidate-v1가 하는 일:

- `candidate-v8-context-macro-v0`를 출발점으로 사용한다.
- running / mixed 세션에서 profile/session 탄수 review floor를 targetCal 변경 없이 맞출 수 있는지 검토한다.
- fat은 guide/external lower guard 아래로 낮추지 않는다.
- protein은 context candidate의 보호선 아래로 더 낮추지 않는다.
- resistance-only profile에는 탄수 floor를 강제하지 않는다.

candidate-v1가 하지 않는 일:

- profile-specific macro formula를 production에 적용하지 않는다.
- target/rate를 올려서 탄수 floor를 억지로 맞추지 않는다.
- resistance profile에 endurance 탄수 range를 그대로 씌우지 않는다.
- Today / Settings profile-session 입력, Records snapshot, export/import, score/Coach 계약을 닫지 않는다.

근거 검토:

- Academy / Dietitians of Canada / ACSM 2016 position stand는 running/mixed의 training-load carbohydrate review floor 근거로만 사용한다.
- Escobar / VanDusseldorp / Kerksick 2016 BJN review는 resistance-based exercise의 carbohydrate 필요량이 endurance만큼 직접 확정되어 있지 않다는 제한적 직접성 근거로 사용한다.
- ISSN protein 2017과 Helms contest-prep 2014는 protein/fat 보호선과 candidate-v0 감사 맥락을 유지하기 위한 reference다.
- `currentFormulaReport`는 내부 코드 근거다. profile candidate-v1의 결과가 생리학적으로 승인되었다는 뜻이 아니다.

직접 추출 summary:

- `caseCount`: 91
- `targetChangedCount`: 0
- `profileCarbFloorAttemptedCount`: 3
- `profileCarbFloorMetCount`: 0
- `profileCarbFloorUnresolvedCount`: 3
- `carbsIncreasedFromContextCount`: 0
- `fatReducedFromContextCount`: 0
- `proteinReducedFromContextCount`: 0
- `resistanceReviewOnlyCount`: 79
- `highPriorityCaseCount`: 3

대표 확인:

- `running_focused`
  - profileSession: `running_tempo_50m_support_upper`
  - candidate-v0 carbs: 6.8294g/kg
  - candidate-v1 carbs: 6.8294g/kg
  - review range: 5-7g/kg
  - 해석: 탄수 floor 자체는 이미 range 안에 있으므로 재분배를 시도하지 않는다. 하지만 production `UPPER` fallback과 actual running load 분리는 여전히 production formula 전환 전 계약으로 남는다.
- `low_reliability_user` mixed case
  - profileSession: `mixed_strength_cardio_70m`
  - candidate-v0 carbs: 5.8758g/kg
  - candidate-v1 carbs: 5.8758g/kg
  - mixed review floor: 6g/kg
  - finding: `profile_carb_floor_attempted`, `profile_carb_floor_unresolved_by_target_constraints`, `profile_macro_candidate_requires_production_contract`
  - 해석: targetCal을 유지하고 protein/fat 보호선을 깨지 않으면 mixed floor를 맞출 수 없다. 이 케이스는 "탄수만 올리는 후보"로 닫을 수 없고 target/rate, protein/fat floor, Recent/Coach 계약을 함께 재설계해야 한다.
- `strength_focused`
  - profileSession: `strength_heavy_lower_90m`
  - finding: `resistance_profile_carb_floor_not_applied_limited_directness`
  - 해석: strength profile에 endurance floor를 강제하지 않는다.
- `required_user_rest_diet_1913`
  - rest-day case이므로 performance carbohydrate floor를 적용하지 않는다.

해석:

- v8.0-K는 profile-specific macro formula의 production 구현이 아니라 candidate feasibility audit이다.
- 단백질 context 후보와 maintain production goal만으로는 mixed profile 탄수 문제가 해결되지 않는다.
- v8.0-S 이후 다음 단계는 candidate-v2 production contract readiness를 실제 formula approval / production `targetCal` wiring / 사용자 관점 QA로 옮길지 판단하는 것이다. Settings/Today input, Today draft persistence, Records snapshot/signature, backup/import roundtrip은 v8.0-P에서 닫혔고 target-rate metadata / recentContext gate는 v8.0-Q에서 닫혔으며 score/Coach candidate-v2 계약은 v8.0-R에서 닫혔다. v8.0-S는 protein guard blocker까지 닫았지만 candidate-v2는 아직 production formula가 아니다.

## 11-9. 2026-06-03 v8.0-L 8-2 pairwise / targeted stress 1차

v8.0-L에서는 80,621,568,000개 full cross-product를 실행했다고 주장하지 않으면서도, category sweep보다 강한 축 상호작용 검증을 추가했다.

pairwiseStress의 성격:

- stage: `8-2_pairwise_targeted_stress_v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `fullCartesianRun`: false
- nominal full Cartesian count: 80,621,568,000
- coverage mode: constrained pairwise covering array plus calculated stress cases

제약:

- `profileSession`은 `exerciseProfile`과 맞아야 한다. 단 `rest`는 모든 프로필의 휴식일로 허용한다.
- `rest` session은 positive `weightDuration`을 가질 수 없다.
- non-rest training session은 `weightDuration=0`을 가질 수 없다.
- `weeklyTrainingDays=0`은 non-rest training session 또는 positive `weightDuration`과 묶지 않는다.
- `performanceLevel=none`은 non-rest training session, weekly training, positive `weightDuration`과 묶지 않는다.
- running/mixed cardio session은 `cardio=none`과 묶지 않는다.

직접 추출 summary:

- `axisCount`: 20
- `includedPairRequirementCount`: 2472
- `excludedPairRequirementCount`: 49
- `generatedTupleCount`: 50
- `calculatedCaseCount`: 50
- `coveredPairRequirementCount`: 2472
- `missingPairRequirementCount`: 0
- `effectiveTupleMismatchCaseCount`: 0
- `derivedTupleMismatchCaseCount`: 31
- `inactiveTupleMismatchCaseCount`: 13
- `highPriorityJudgmentCaseCount`: 0

excluded pair 사유:

- `exercise_profile_session_mismatch`: 20
- `no_performance_with_training_session`: 5
- `training_session_without_weight_duration`: 5
- `weekly_zero_with_training_session`: 5
- `no_performance_with_weekly_training`: 3
- `no_performance_with_weight_duration`: 3
- `rest_session_with_weight_duration`: 3
- `weekly_zero_with_weight_duration`: 3
- `running_session_without_cardio_axis`: 1
- `mixed_session_without_cardio_axis`: 1

해석:

- 50개 pairwise stress case는 18개 human-review case를 대체하지 않는다.
- 2,472개 valid pair를 빠짐없이 덮지만, 80,621,568,000개 full cross-product 전체를 실행한 것은 아니다.
- `effectiveTupleMismatchCaseCount=0`이므로 직접 제어 가능한 pairwise tuple 값은 계산 입력에 보존된다.
- `derivedTupleMismatchCaseCount=31`은 FFMI가 height/weight/bodyFat과 동시에 독립적으로 맞기 어려운 파생축이라는 사실을 드러낸다. 이것은 숨길 문제가 아니라, FFMI 축을 production input으로 쓸 때 제약/우선순위를 정해야 한다는 근거다.
- `inactiveTupleMismatchCaseCount=13`은 rest day에서 intensity가 비활성화되는 것처럼 계산상 축이 꺼지는 케이스다.
- v8.0-M targeted stress는 derived FFMI conflict, rest-day inactive intensity, mixed carb unresolved, maintain + non-bodybuilding profile을 묶어 사람 눈 수치 검토를 확대하는 방향이 맞다.

## 11-10. 2026-06-03 v8.0-M targeted stress review 1차

v8.0-M에서는 v8.0-L pairwise stress가 남긴 residual signal을 그냥 메모로 두지 않고, 계산 가능한 targeted case와 계산하면 안 되는 constraint-only case로 분리했다.

targetedStress의 성격:

- stage: `8-2_targeted_stress_review_v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `fullCartesianRun`: false
- source signals: `pairwiseStress.derivedTupleMismatches`, `pairwiseStress.inactiveTupleMismatches`, `profileMacroCandidateComparison.profileCarbFloorUnresolved`, `maintainGoalContract.plus.nonBodybuildingProfileFormulaPending`, `pairwiseStress.excludedPairRequirements`

직접 추출 summary:

- `caseCount`: 8
- `calculatedCaseCount`: 5
- `constraintOnlyCaseCount`: 3
- `derivedFfmiConflictCount`: 1
- `inactiveIntensityCaseCount`: 1
- `mixedCarbUnresolvedCaseCount`: 2
- `maintainNonBodybuildingCaseCount`: 2
- `highPriorityCaseCount`: 3

focus count:

- `derived_ffmi`: 1
- `inactive_intensity`: 1
- `mixed_carb_unresolved`: 2
- `maintain_non_bodybuilding`: 2
- `excluded_constraint`: 3

계산 케이스:

- `targeted_derived_ffmi_high_conflict`: 의도 tuple은 `ffmiBand=low`지만 height/weight/bodyFat/skeletal에서 파생된 실제 FFMI는 다르다. `derived_ffmi_axis_conflict_requires_priority_rule`로 표시한다.
- `targeted_rest_day_intensity_inactive`: rest day, `todayWeightDuration=0`에서 intensity 축은 숨은 training load가 아니며 `rest_day_intensity_axis_inactive_not_training_load`로 표시한다.
- `targeted_mixed_carb_unresolved`: mixed strength/cardio 고부하에서 candidate-v1이 `profile_carb_floor_unresolved_by_target_constraints`를 유지한다. 이는 target/rate와 protein/fat guard를 함께 여는 candidate-v2 필요 근거다.
- `targeted_maintain_running_contract`: maintain goal + running profile/session은 production maintain goal이 존재해도 running-specific macro formula와 UI/Records/Coach 계약이 별도 pending임을 표시한다.
- `targeted_maintain_mixed_contract`: maintain goal + mixed profile/session은 non-bodybuilding profile formula pending이다. v8.0-M~AL 당시 AG+ profile-owned 입력 기준으로는 이 케이스도 candidate-v1 `profile_carb_floor_unresolved_by_target_constraints`를 가지므로, maintain 계약 risk와 mixed carb-floor unresolved risk가 함께 남았다. v8.0-AT 이후 일반 diet protein recovery로 diet mixed case는 carb floor를 맞추지만, maintain mixed 1건은 여전히 unresolved다.

constraint-only 케이스:

- `constraint_running_session_without_cardio_axis`: `running_session_without_cardio_axis`
- `constraint_profile_session_mismatch`: `exercise_profile_session_mismatch`
- `constraint_performance_none_training`: `no_performance_with_training_session`, `no_performance_with_weekly_training`, `no_performance_with_weight_duration`

해석:

- 8개 targeted stress는 80,621,568,000개 full Cartesian 실행이 아니다.
- 5개 calculated case는 18개 human-review case를 대체하지 않는다.
- 3개 constraint-only case는 앱이 계산해도 되는 유저 세션이 아니라, incoherent tuple을 계산하지 않도록 남기는 감사 기록이다.
- 외부 운동생리학 근거는 carbohydrate/protein/fat review threshold로만 쓰며, `currentFormulaReport`는 내부 구현 근거다.
- v8.0-M은 candidate-v8-profile-macro-v1을 production formula로 승인하지 않는다.

## 11-11. 2026-06-03 v8.0-N profile macro candidate-v2 1차

v8.0-N에서는 v8.0-M의 `targeted_mixed_carb_unresolved`를 candidate-v2 report-only 레이어로 연결했다. 핵심은 "탄수만 올리면 해결"이 아니라, target/rate delta와 protein/fat guard conflict를 같이 기록하는 것이다.

profileMacroCandidateV2Comparison의 성격:

- stage: `9-7_profile_macro_candidate_v2_linked_target_v0`
- model: `candidate-v8-profile-macro-v2-linked-target-v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- source: `candidate-v8-context-macro-v0`, `candidate-v8-profile-macro-v1`, `8-2_targeted_stress_review_v0`

직접 추출 summary:

- `caseCount`: 96
- `targetChangedCount`: 5
- `profileCarbFloorAttemptedCount`: 5
- `profileCarbFloorMetCount`: 5
- `profileCarbFloorUnresolvedCount`: 0
- `targetRateContractRequiredCount`: 5
- `proteinGuardConflictCount`: 4
- `fatGuardConflictCount`: 0
- `nonBodybuildingContractCount`: 16
- `highPriorityCaseCount`: 4

targeted calculated 5건 summary:

- `targetChangedCount`: 2
- `profileCarbFloorAttemptedCount`: 2
- `profileCarbFloorMetCount`: 2
- `profileCarbFloorUnresolvedCount`: 0
- `targetRateContractRequiredCount`: 2
- `proteinGuardConflictCount`: 1
- `nonBodybuildingContractCount`: 3
- `highPriorityCaseCount`: 1

`targeted_mixed_carb_unresolved` 직접 값:

- `targetDeltaKcal`: 43.72915555555528
- `targetRateDeltaEquivalentKgPerWeek`: 0.03975377777777753
- `carbsGPerKgBodyweight`: 6
- finding:
  - `profile_carb_floor_attempted_v2`
  - `profile_carb_floor_requires_target_rate_contract`
  - `profile_carb_floor_met_by_target_delta_report_only`
  - `profile_protein_guard_conflict_unresolved`
  - `profile_macro_candidate_v2_requires_production_contract`

해석:

- candidate-v2는 mixed carb floor feasibility를 target delta로 맞출 수 있음을 보여준다.
- 그러나 이 target delta는 diet/cut/recent28/score/Coach/Records 계약을 바꾸는 문제이므로 production 적용이 아니다.
- protein guard conflict는 모든 케이스에 무차별 적용하지 않고, profile carb floor를 실제 시도한 케이스에 한정했다. 이는 v2 목적보다 넓은 high finding을 만들지 않기 위한 수정이다.
- `profileCarbFloorUnresolvedCount=0`은 feasibility layer의 unresolved가 0이라는 뜻이지, profile-specific macro formula 전체가 끝났다는 뜻이 아니다.
- AG 이후 현재 profile-owned runner 기준으로는 위 `43.72915555555528` 값이 역사값이다. 현재 직접 추출값은 `targeted_mixed_carb_unresolved.targetDeltaKcal=128.996060400435`, `targetRateDeltaEquivalentKgPerWeek=0.11726914581857727`, `carbsGPerKgBodyweight=6`이며, stage/source 없이 과거 숫자를 현재 기대값으로 재사용하면 안 된다.
- score, Coach, Settings/Today profile input, Records snapshot, export/import 계약은 아직 production 적용 전 필수 검토로 남는다.

## 11-12. 2026-06-03 v8.0-O profile macro candidate-v2 production contract review 1차

v8.0-O에서는 v8.0-N candidate-v2를 production에 붙일 수 있는지 바로 판단하지 않고, 먼저 `profileMacroCandidateV2Contract` report-only 레이어를 추가했다. 핵심은 candidate-v2 계산 가능성과 앱 전체 운영 가능성을 분리하는 것이다.

profileMacroCandidateV2Contract의 성격:

- stage: `9-8_profile_macro_candidate_v2_production_contract_review_v0`
- reportOnly: true
- productionFormulaChanged: false
- candidateFormulaApproved: false
- productionReady: false
- sourceCandidateModel: `candidate-v8-profile-macro-v2-linked-target-v0`
- evidencePolicy: 내부 code-contract review. 외부 sports-nutrition reference는 threshold background일 뿐 앱 전체 production 승인 근거가 아니다.

직접 추출 summary:

- `contractCount`: 14
- `presentCount`: 3
- `missingContractCount`: 11
- `productionBlockerCount`: 12
- `reportOnlyGuardCount`: 2
- `settingsTodayContractMissingCount`: 3
- `snapshotFieldMissingCount`: 12
- `targetRateContractMissingCount`: 1
- `scoreCoachContractMissingCount`: 3
- `recentContextContractMissingCount`: 1
- `backupImportContractMissingCount`: 1
- `unresolvedProteinGuardConflictCount`: 4
- `productionReady`: false

missing contract keys:

- `Settings/Today exerciseProfile input`
- `Settings/Today profileSession derived field`
- `Today calculation draft profile/session persistence`
- `buildGoalTargetRateContext.profileCandidateDelta`
- `normalizeGoalSnapshot.profileCandidateFields`
- `getGoalSnapshotSignature.profileCandidateFields`
- `MACRO_SCORE_PROFILES.profileCandidateV2`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.profileCandidateV2`
- `DAILY_COACH_DECISION_PROFILES.profileCandidateV2`
- `recentContext.profileCandidateV2TargetDeltaGate`
- `backup/export/import profileCandidateV2 roundtrip`

production blocker keys:

- 위 missing contract 11개 전체
- `profileMacroCandidateV2Comparison.proteinGuardConflict`

present guard keys:

- `profileMacroCandidateV2Comparison.reportOnlyGuard`
- `targetedStress.constraintOnlyTupleGuard`

probe 해석:

- snapshot probe는 `exerciseProfile`, `profileSession`, candidate model/stage, target delta, weekly-rate delta가 `normalizeGoalSnapshot()`에 보존되지 않음을 확인했다.
- signature probe도 같은 필드들이 `getGoalSnapshotSignature()` equivalence 대상에 포함되지 않음을 확인했다.
- target-rate probe는 candidate-v2 target delta 입력을 넣어도 `buildGoalTargetRateContext()` 출력에 candidate model, target delta, weekly-rate delta가 보존되지 않음을 확인했다.
- 따라서 candidate-v2가 pre-AG `targeted_mixed_carb_unresolved`에서 +43.729kcal로 6g/kg carb floor를 맞출 수 있었다는 사실은 production 적용 가능성을 뜻하지 않는다. AG 이후 현재 profile-owned baseline에서는 같은 케이스가 +128.996kcal로 재추출되므로, 과거 숫자를 현재 기대값으로 쓰지 않는다.

해석:

- `missingContractCount=11`은 현재 운영 연결부가 실제로 비어 있는 항목이다.
- `productionBlockerCount=12`가 11보다 큰 이유는 `profileMacroCandidateV2Comparison.proteinGuardConflict`가 "존재하는 guard"이면서 unresolved production blocker이기 때문이다.
- Settings/Today input이 없으므로 사용자는 운동 프로필과 오늘 세션을 실제로 선택할 수 없다.
- Records snapshot/signature가 없으므로 저장된 target/protein/carbs/fat가 어떤 profile/session/candidate-delta에서 나온 값인지 보존되지 않는다.
- score/Coach/recentContext/backup 계약이 없으므로 target/rate delta를 production에 넣으면 사용자 피드백, 기록 보존, 복원 경로가 서로 다른 기준을 말할 수 있다.
- 이 레이어는 v8 full completion, candidate-v2 formula approval, 80,621,568,000개 full Cartesian 실행을 주장하지 않는다.

## 11-13. 2026-06-03 v8.0-P profile/session snapshot contract 1차, AG 정정

v8.0-P에서는 v8.0-O가 missing으로 잡은 Settings/Today input, Today draft persistence, Records snapshot/signature, backup/import roundtrip 계약만 production data path로 닫았다. 산식 자체, score, Coach, recentContext, target-rate 계약은 건드리지 않았다.

구현한 계약:

- Settings에 `exerciseProfile` 기본값과 `profileSession` 기본값을 추가했다.
- Today quick edit에 운동 프로필 / 오늘 세션 선택을 추가했다.
- `profileSession`은 `exerciseProfile`과 맞아야 한다. 단 `rest`는 모든 프로필의 휴식일로 허용한다.
- 러닝 프로필에 `bodybuilding_push` 같은 불일치 세션이 들어오면 `running_tempo`로 정규화한다.
- `TODAY_CALCULATION_DRAFT_FIELDS`에 `todayExerciseProfile`, `todayProfileSession`을 추가했다.
- `collectState()`는 Today effective values의 `exerciseProfile`, `profileSession`을 계산 state에 싣는다.
- `normalizeGoalSnapshot()`과 `getGoalSnapshotSignature()`는 `exerciseProfile`, `profileSession`, `profileMacroCandidateModel`, `profileMacroCandidateStage`, `profileTargetDeltaKcal`, `profileTargetRateDeltaEquivalentKgPerWeek`를 보존한다.
- Records 상세 화면은 저장 당시 운동 프로필 / 오늘 세션을 보여준다.
- full backup settings와 todayCalculationValues는 프로필/세션을 보존한다.

직접 추출 summary:

- stage: `9-8_profile_macro_candidate_v2_production_contract_review_v1_input_snapshot`
- `contractCount`: 14
- `presentCount`: 9
- `missingContractCount`: 5
- `productionBlockerCount`: 6
- `reportOnlyGuardCount`: 2
- `settingsTodayContractMissingCount`: 0
- `snapshotFieldMissingCount`: 0
- `targetRateContractMissingCount`: 1
- `scoreCoachContractMissingCount`: 3
- `recentContextContractMissingCount`: 1
- `backupImportContractMissingCount`: 0
- `unresolvedProteinGuardConflictCount`: 4
- `productionReady`: false

남은 missing contract keys:

- `buildGoalTargetRateContext.profileCandidateDelta`
- `MACRO_SCORE_PROFILES.profileCandidateV2`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.profileCandidateV2`
- `DAILY_COACH_DECISION_PROFILES.profileCandidateV2`
- `recentContext.profileCandidateV2TargetDeltaGate`

남은 production blocker keys:

- 위 missing contract 5개
- `profileMacroCandidateV2Comparison.proteinGuardConflict`

probe 해석:

- snapshot probe의 `missingNormalizedFields`와 `missingSignatureFields`는 모두 빈 배열이다.
- target-rate probe는 여전히 candidate-v2 model, target delta, target-rate delta를 `buildGoalTargetRateContext()` 출력에 보존하지 않는다.
- 따라서 Records/backup이 candidate-v2 필드를 저장할 수 있게 되었어도, target/rate delta가 production에 적용될 수 있다는 뜻은 아니다.

해석:

- v8.0-P는 사용자가 운동 프로필과 오늘 세션을 실제로 선택하고, 그 선택이 기록과 백업까지 보존되는 최소 운영 계약을 닫은 단계다.
- 이 작업은 사용자에게 "운동 프로필을 고를 수 있는데 기록에는 이유가 안 남는다"는 앱 신뢰도 문제를 줄인다.
- 반대로 target-rate, score, Coach, recentContext, protein guard conflict는 아직 남아 있으므로 candidate-v2 formula approval로 읽으면 안 된다.
- v8.0-P 추가 테스트는 러닝 프로필에 bodybuilding 세션이 들어왔을 때 `running_tempo`로 정규화되고, production `targetCal`은 baseline과 동일하게 유지되는지 확인한다.

## 11-14. 2026-06-03 v8.0-Q target-rate recentContext candidate-delta contract 1차

v8.0-Q에서는 v8.0-P가 missing으로 남긴 target-rate candidate delta 계약과 recentContext target-delta gate 계약을 닫았다. 이 단계는 candidate-v2 target/rate delta를 앱 경로에서 추적하고 recent28 gate로 검토 가능성을 분류하기 위한 계약이다. production `targetCal`, score formula, Daily Coach decision/copy는 바꾸지 않았다.

구현한 계약:

- `buildGoalTargetRateContext()`는 candidate-v2 metadata로 `profileMacroCandidateModel`, `profileMacroCandidateStage`, `profileTargetDeltaKcal`, `profileTargetRateDeltaEquivalentKgPerWeek`를 반환한다.
- 같은 context에 `profileCandidateDeltaContract`를 추가했다.
- `profileCandidateDeltaContract`는 `requested`, `active`, `supportedModel`을 기록하지만 `appliedToTarget=false`, `targetCalUnchangedByProfileCandidateDelta=true`, `targetRateChangedByProfileCandidateDelta=false`를 명시한다.
- `calculate()` 출력에도 target-rate context의 candidate metadata를 싣는다.
- `buildRecentContext()`는 `profileCandidateV2TargetDeltaGate`를 반환한다.
- gate는 recent28을 review window로 쓰고, low goalSnapshot coverage, recent goal change, no usable diet day, insufficient weight trend, record/window/context fit below medium을 block한다.
- gate의 eligible 상태도 `canApplyAutomatically=false`다. 즉 "검토 가능"은 "자동 적용"이 아니다.

직접 추출 summary:

- stage: `9-8_profile_macro_candidate_v2_production_contract_review_v2_target_recent_gate`
- `contractCount`: 14
- `presentCount`: 11
- `missingContractCount`: 3
- `productionBlockerCount`: 4
- `reportOnlyGuardCount`: 2
- `settingsTodayContractMissingCount`: 0
- `snapshotFieldMissingCount`: 0
- `targetRateContractMissingCount`: 0
- `scoreCoachContractMissingCount`: 3
- `recentContextContractMissingCount`: 0
- `backupImportContractMissingCount`: 0
- `unresolvedProteinGuardConflictCount`: 4
- `productionReady`: false

남은 missing contract keys:

- `MACRO_SCORE_PROFILES.profileCandidateV2`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.profileCandidateV2`
- `DAILY_COACH_DECISION_PROFILES.profileCandidateV2`

남은 production blocker keys:

- 위 missing contract 3개
- `profileMacroCandidateV2Comparison.proteinGuardConflict`

probe 해석:

- target-rate probe는 candidate-v2 model, target delta, target-rate delta를 `buildGoalTargetRateContext()` 출력에 보존한다.
- 같은 probe에서 `profileCandidateDeltaContract.appliedToTarget=false`, `targetCalUnchangedByProfileCandidateDelta=true`가 확인된다.
- recentContext gate probe는 high-confidence recent28 fixture에서 `eligible`을 반환하지만 `eligibleCanApplyAutomatically=false`다.
- recentContext gate probe는 goalSnapshot coverage 0.50 fixture를 `recent28_goal_snapshot_coverage_below_0_70`로 block한다.
- recentContext gate probe는 recent28 내부 goal change fixture를 `recent28_goal_change_detected`로 block한다.
- candidate delta가 없으면 gate는 `inactive`이다.

해석:

- v8.0-Q는 target/rate delta가 앱 내부에서 사라지지 않고 target-rate context와 recentContext에 남도록 만든다.
- 이 작업은 candidate-v2를 production에 적용하기 위한 traceability/gate 계약이지, candidate-v2 formula approval이 아니다.
- 남은 핵심 위험은 score/Coach가 candidate-v2 target delta와 protein guard conflict를 어떻게 판단하고 설명할지 아직 정의하지 않았다는 점이다.
- `profileMacroCandidateV2Comparison.proteinGuardConflict`는 여전히 unresolved production blocker다. 단순히 target delta metadata와 recentContext gate가 생겼다는 이유로 단백질 guard 충돌을 통과 처리하면 안 된다.

## 11-15. 2026-06-03 v8.0-R score Coach candidate-v2 contract 1차

v8.0-R에서는 v8.0-Q가 missing으로 남긴 score / Daily Coach 계약 3개를 닫았다. 단, 이 작업은 candidate-v2 target delta를 production `targetCal`에 적용하는 변경이 아니고, candidate-v2 formula approval도 아니다. 핵심은 candidate metadata가 있을 때 score/Coach가 goal-only fallback으로 조용히 흘러가지 않도록 명시적인 후보 정책 키를 두는 것이다.

구현한 계약:

- `profileCandidateV2` policy key를 추가했다. 이 키는 `candidate-v8-profile-macro-v2-linked-target-v0` metadata가 있는 경우에만 score/Coach 후보 계약 경로를 선택한다.
- `MACRO_SCORE_PROFILES.profileCandidateV2`는 diet-equivalent 제품 정책을 명시적으로 재사용한다. 임의 tolerance를 새로 만든 것이 아니며, 외부 운동생리학 근거라고 주장하지 않는다.
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.profileCandidateV2`는 diet macro threshold를 재사용하고, candidate target-delta review와 protein/fat guard conflict review 정책을 별도로 둔다.
- `DAILY_COACH_DECISION_PROFILES.profileCandidateV2`는 guard conflict를 target-delta review보다 먼저 노출한다.
- Coach copy는 사용자에게 내부 candidate model 이름을 노출하지 않고, 자동 적용 금지와 protein/fat guard 재검토를 말한다.

직접 추출 summary:

- stage: `9-8_profile_macro_candidate_v2_production_contract_review_v3_score_coach`
- `contractCount`: 14
- `presentCount`: 14
- `missingContractCount`: 0
- `productionBlockerCount`: 1
- `reportOnlyGuardCount`: 2
- `settingsTodayContractMissingCount`: 0
- `snapshotFieldMissingCount`: 0
- `targetRateContractMissingCount`: 0
- `scoreCoachContractMissingCount`: 0
- `recentContextContractMissingCount`: 0
- `backupImportContractMissingCount`: 0
- `unresolvedProteinGuardConflictCount`: 4
- `productionReady`: false

남은 production blocker keys:

- `profileMacroCandidateV2Comparison.proteinGuardConflict`

probe 해석:

- score/Coach probe는 `policyKey=profileCandidateV2`를 반환한다.
- `scoreProfileUsesDietProductPolicy=true`이고 `coachThresholdUsesDietMacroThresholds=true`다. 즉 새로운 score/Coach threshold를 감각적으로 만든 것이 아니라 기존 diet 제품 정책을 candidate-v2 계약 키에 명시적으로 연결했다.
- `hasProfileTargetDeltaSignal=true`, `hasProfileGuardConflictSignal=true`다.
- Coach decision 우선순위는 `profile_guard_conflict`가 `profile_target_delta_review`보다 앞선다.
- rendered Coach output의 첫 decision도 `profile_guard_conflict`이고, `outputMentionsInternalCandidateName=false`다.

해석:

- v8.0-R은 candidate-v2 score/Coach 계약 누락을 닫았지만, profile-specific macro formula를 승인하지 않는다.
- 내부 제품 정책을 외부 운동생리학 근거처럼 통과시키지 않기 위해, score profile은 diet-equivalent product policy reuse로 기록했다.
- 남은 핵심 위험은 protein/fat guard behavior다. target/rate delta, recent gate, score/Coach copy가 준비되었더라도 protein/fat guard 충돌을 해소하거나 명시적으로 정당화하기 전에는 production `targetCal`에 candidate delta를 적용하면 안 된다.

## 11-16. 2026-06-03 v8.0-S protein guard candidate-v2 resolution contract 1차

v8.0-S에서는 v8.0-R 이후 남은 `profileMacroCandidateV2Comparison.proteinGuardConflict` blocker를 재검토했다. 직접 추출 결과 conflict 4건은 모두 candidate-v2 protein 2.0g/kg BW와 legacy diet guide floor 2.2g/kg BW 사이의 충돌이었다. fat conflict는 0건이었다.

핵심 판단:

- ISSN protein 2017은 일반 운동인의 단백질 범위를 1.4-2.0g/kg/day로 다룬다. 따라서 non-contest mixed/running candidate-v2에서 2.0g/kg BW는 외부 근거 범위 안이다.
- Helms / Aragon / Fitschen 2014의 2.3-3.1g/kg LBM protein 맥락은 natural bodybuilding contest-prep / caloric restriction context다. 현재 resolved 4건은 `cut + bodybuilding + high performance + bodyFat <= 15` 조건의 contest-prep-like 케이스가 아니다.
- 기존 2.2g/kg BW diet floor는 앱 내부 guide floor이지, 모든 mixed/running 사용자의 외부 안전선으로 주장하면 안 된다.
- 따라서 v8.0-S는 legacy guide-floor conflict를 숨기지 않고 `profile_protein_guard_resolved_by_general_issn_range` finding으로 남긴다.

근거 링크:

- ISSN position stand: protein and exercise, 2017: `https://jissn.biomedcentral.com/articles/10.1186/s12970-017-0177-8`
- Helms, Aragon, Fitschen natural bodybuilding contest preparation review, 2014: `https://pubmed.ncbi.nlm.nih.gov/24864135/`

구현한 계약:

- `V8_MACRO_AUDIT_RULES.proteinGeneralExerciserMinGKg=1.4`를 추가해 ISSN general range를 1.4-2.0g/kg BW로 명시했다.
- `buildV8ProfileMacroCandidateProteinGuardReview()`를 추가해 contest-prep-like 케이스와 general exercising range 케이스를 분리한다.
- `buildV8ProfileMacroCandidateFatGuardReview()`를 추가해 fat guard도 같은 구조로 평가하도록 했다.
- candidate-v2 finding은 unresolved conflict가 있으면 `profile_protein_guard_conflict_unresolved`를 유지하고, 이번 케이스처럼 외부 범위 안에서 legacy floor만 충돌하면 `profile_protein_guard_resolved_by_general_issn_range`로 기록한다.
- `profileMacroCandidateV2Contract`의 guard 항목은 guard behavior contract present로 유지하되, unresolved count가 0이면 productionBlocker=false가 된다.

직접 추출 summary:

- `profileMacroCandidateV2Comparison.summary.proteinGuardConflictCount`: 0
- `proteinGuardResolvedCount`: 4
- `proteinLegacyGuideConflictCount`: 4
- `fatGuardConflictCount`: 0
- `fatGuardResolvedCount`: 5
- `profileMacroCandidateV2Contract.summary.contractCount`: 14
- `presentCount`: 14
- `missingContractCount`: 0
- `productionBlockerCount`: 0
- `reportOnlyGuardCount`: 2
- `resolvedProteinGuardPolicyCount`: 4
- `productionReady`: true

해석:

- v8.0-S는 candidate-v2 production contract blocker를 닫는다.
- 이것은 candidate-v2 formula approval이 아니다. `candidateFormulaApproved=false`, `productionFormulaChanged=false`, target-rate probe의 `appliedToTarget=false`는 계속 유지된다.
- 다음 단계에서 실제 production formula 적용을 검토하려면, candidate-v2 target delta를 production `targetCal`에 연결하는 범위, UI/Today 표시, Records 기준갱신, score/Coach 문구가 실제 사용자에게 납득 가능한지 확인해야 한다.

## 11-17. 2026-06-03 v8.0-T candidate-v2 runtime target-delta proposal wiring 1차

v8.0-T에서는 v8.0-S의 `productionReady=true`를 곧바로 targetCal 적용으로 읽지 않고, 먼저 실제 `calculate()` Today 경로에서 candidate-v2 target-delta proposal metadata가 생성되고 보존되는지 구현했다. 이 단계는 formula approval이 아니며 production targetCal을 바꾸지 않는다.

구현 계약:

- `calculate()`는 `profileMacroCandidateV2RuntimeProposal`을 반환한다.
- runtime proposal은 현재 Today `exerciseProfile/profileSession`, 선택된 production target/macro, `dualBasis.rangeContext`로 candidate-v2 feasibility를 다시 계산한다.
- candidate-v2 target delta가 필요한 경우 `targetRateContext.profileMacroCandidateModel`, `profileMacroCandidateStage`, `profileTargetDeltaKcal`, `profileTargetRateDeltaEquivalentKgPerWeek`가 채워진다.
- `profileCandidateDeltaContract.appliedToTarget=false`, `targetCalUnchangedByProfileCandidateDelta=true`, `targetRateChangedByProfileCandidateDelta=false`를 유지한다.
- `recentContext.profileCandidateV2TargetDeltaGate`는 runtime delta를 볼 수 있지만 `canApplyAutomatically=false`를 유지한다.

직접 확인한 runtime mixed high-load probe:

- production `targetCal`: `3283.4951111111113`
- proposed candidate-v2 targetCal: `3308.524266666667`
- `targetDeltaKcal`: `25.029155555555462`
- `targetRateDeltaEquivalentKgPerWeek`: `0.022753777777777692`
- `profileCarbFloorAttempted=true`
- `profileCarbFloorMet=true`
- `profileCandidateGuardConflict.proteinGuardConflict=false`
- `profileCandidateGuardConflict.fatGuardConflict=false`

해석:

- v8.0-T는 report-only candidate-v2를 실제 Today calculation metadata path에 연결했다.
- 이것은 `candidate-v8-profile-macro-v2-linked-target-v0`의 production formula approval이 아니다.
- production `targetCal`에는 candidate delta를 적용하지 않는다.
- 다음 단계에서 production 적용을 검토하려면 recent28 gate의 자동 적용 조건, 사용자 승인/표시 방식, Records 기준갱신, score/Coach copy가 실제 사용자에게 납득 가능한지 별도로 QA해야 한다.

## 11-18. 2026-06-03 v8.0-U candidate-v2 runtime user-facing QA 1차

v8.0-U에서는 v8.0-T runtime candidate-v2 target-delta proposal을 production targetCal에 적용하지 않고, 사용자가 실제로 보게 되는 Coach / Records 표면이 안전한지 먼저 확인하는 report-only QA 레이어를 추가했다. 이 단계는 `candidate-v8-profile-macro-v2-linked-target-v0` formula approval이 아니며 production formula wiring도 아니다.

추가된 코드 경로:

- `runV8ScenarioRunner()` 출력에 `profileFormulaUserFacingQa` 추가
- `profileFormulaUserFacingQa.meta.stage=9-9_profile_formula_user_facing_qa_v0`
- runtime mixed high-load fixture로 `calculate()` Today profile/session 경로를 직접 실행
- Coach output decision과 Records detail text에 내부 candidate/report/schema 용어가 노출되는지 검사
- Records `goalSnapshot` / detail text가 `profileTargetDeltaKcal`, `profileTargetRateDeltaEquivalentKgPerWeek`를 보존하는지 검사

직접 추출 기준:

- `version=8.0-U`
- `runtime.exerciseProfile=mixed`
- `runtime.profileSession=mixed_strength_cardio`
- production `targetCal=3283.4951111111113`
- proposed candidate-v2 targetCal `3308.524266666667`
- `targetDeltaKcal=25.029155555555462`
- `targetRateDeltaEquivalentKgPerWeek=0.022753777777777692`
- `profileCarbFloorAttempted=true`
- `profileCarbFloorMet=true`
- `proteinGuardConflict=false`
- `fatGuardConflict=false`
- `recentGateStatus=eligible`
- `coachPolicyKey=profileCandidateV2`
- `coachHasProfileTargetDeltaSignal=true`
- `coachHasProfileTargetDeltaDecision=true`
- `recordDetailHasProfileTargetDelta=true`
- `recordDetailHasProfileTargetRateDelta=true`
- `coachForbiddenTermCount=0`
- `recordDetailForbiddenTermCount=0`
- `surfaceForbiddenTermCount=0`
- `targetDeltaApplied=false`
- `canApplyAutomatically=false`

해석:

- v8.0-U는 candidate-v2 target delta가 사용자 표면에서 "검토 대상"으로 설명될 수 있는지 확인한 단계다.
- `surfaceForbiddenTermCount=0`은 Coach / Records 표면이 내부 candidate 식별자, schema, draft, selectedBasis, policy 같은 내부 용어를 드러내지 않는다는 최소 계약이다.
- 이 QA는 모바일 screenshot audit이나 실제 production formula approval을 대체하지 않는다.
- production `targetCal` 적용은 여전히 별도 formula approval / production wiring / visual QA가 필요하다.

## 11-19. 2026-06-03 v8.0-V mobile screenshot audit analyzer 1차

v8.0-V에서는 v8.0-U가 남긴 mobile screenshot audit / production visual QA 경계를 분리했다. 이번 단계는 최신 앱 상태를 `tools/render_audit/capture_render_audit.cjs`로 캡처하고, `tools/render_audit/analyze_render_audit.cjs`로 manifest와 PNG를 자동 분석하는 1차 시각 감사 근거다. 이 단계는 candidate-v2 formula approval, production `targetCal` application, 또는 formula wiring 이후의 production visual QA가 아니다.

추가된 도구:

- `tools/render_audit/analyze_render_audit.cjs`
- `capture_render_audit.cjs`가 생성한 ignored `manifest.json`과 `screenshots/*.png`를 읽는다.
- 모바일 Today / Records / Recent / InBody / Settings / backup-import / edit modal 표면이 모두 캡처됐는지 확인한다.
- 각 PNG가 존재하고 viewport width와 맞으며, viewport height 이상이고, sampled PNG pixel statistics 기준으로 비어 있지 않은지 확인한다.
- 모바일 캡처는 full-page expanded 상태와 PNG height / capture height 일치를 확인한다.

최신 실행 근거:

- `capture_render_audit.cjs`: 47 captures 생성
- desktop captures: 33
- mobile captures: 14
- required mobile captures: 14
- `analyze_render_audit.cjs`: `failedCount=0`
- `minMobileImageHeight=1601`
- `maxMobileImageHeight=4909`
- `minUniqueSampleColorCount=547`
- `minLuminanceStdDev=24.244`
- 직접 눈으로 확인한 모바일 스크린샷: `34_mobile_today_rich.png`, `36_mobile_records.png`, `40_mobile_settings.png`

해석:

- v8.0-V는 "모바일 캡처를 돌렸다"가 아니라, 캡처 manifest와 PNG 자체를 자동 검증하는 도구를 추가한 단계다.
- 이 단계로 first-pass mobile/desktop screenshot audit evidence gate는 닫을 수 있다.
- 단, candidate-v2 target delta가 production `targetCal`에 실제 적용된 뒤의 production visual QA는 아직 실행할 수 없으므로 별도 open gate로 유지한다.

## 11-20. 2026-06-03 v8.0-W human-review numerical approval gate 1차

v8.0-W에서는 8-3의 18개 human-review candidate-v2 케이스를 별도 numerical approval gate로 분리했다. 이 단계는 report-only다. owner signoff, formula approval, production `targetCal` application, production formula wiring으로 읽으면 안 된다.

추가된 출력:

- `runV8ScenarioRunner().humanReviewNumericalApproval`
- `humanReviewNumericalApproval.meta.stage=8-3_human_review_numerical_approval_v0`
- `humanReviewNumericalApproval.meta.reportOnly=true`
- `humanReviewNumericalApproval.meta.candidateFormulaApproved=false`
- `humanReviewNumericalApproval.meta.productionFormulaChanged=false`

직접 확인된 요약:

- `humanReviewCaseCount=18`
- `numericApprovedCount=17`
- `blockedCount=1`
- `productionFormulaApprovalReady=false`
- `blockedCaseIds=["low_reliability_user"]`
- `requiredRegressionCaseId=required_user_rest_diet_1913`
- `requiredRegressionNumericApproved=true`

막힌 케이스:

- `low_reliability_user`
- goal `diet`, exerciseProfile `mixed`, profileSession `mixed_strength_cardio_70m`
- pre-AG candidate-v2 target delta는 당시 기준으로 유지됐다: `targetDeltaKcal` 약 43.729kcal, `targetRateDeltaEquivalentKgPerWeek` 약 0.0398kg/week. AG 이후 현재 profile-owned mixed baseline의 기대값으로는 이 숫자를 쓰지 않는다.
- `profile_carb_floor_requires_target_rate_contract`는 v8.0-Q/R/U의 target-rate/recentContext/score/Coach/user-facing QA 계약으로 numerical gate 안에서는 resolved 처리한다.
- 하지만 `fat_percent_below_contest_prep_reference_floor`는 외부 Helms contest-prep reference 기반 high finding이므로 unresolved blocker로 유지한다. 앱 내부 계약이 닫혔다는 이유만으로 외부 운동생리학 근거 기반 blocker를 통과 처리하면 안 된다.

해석:

- v8.0-W는 "18개 중 17개 수치가 owner formula review 후보로 통과했고, 1개는 blocker로 남았다"는 상태를 코드와 테스트에 고정한 단계다.
- `required_user_rest_diet_1913`는 수치상 통과했지만, 이것도 production formula approval이 아니라 owner formula review 후보 통과일 뿐이다.
- candidate-v2 formula approval, production wiring, production wiring 이후 visual QA는 계속 별도 open gate다.

## 11-21. 2026-06-03 v8.0-X candidate-v2 fat floor linked-target consistency 1차

v8.0-X에서는 v8.0-W가 남긴 `low_reliability_user` fat-percent blocker를 다시 추적했다. 원인은 "감각적으로 지방을 더 올려야 한다"가 아니라, candidate-v2 내부 guard와 candidateAudit가 서로 다른 target을 보고 있었다는 불일치였다.

문제:

- candidate-v2는 mixed/running carb floor를 맞추기 위해 report-only target delta를 연다.
- v8.0-W 기준 `low_reliability_user`는 base target `3261.4951111111113`에서 candidate target `3305.2242666666666`으로 올라갔다.
- 하지만 fat guard review는 base/context target 기준으로 15% OK를 기록했고, candidateAudit는 증가한 candidate target 기준으로 `fatPercentKcal=14.80154528697236`을 계산해 `fat_percent_below_contest_prep_reference_floor` high finding을 남겼다.
- 따라서 "앱 계약은 닫혔으니 통과"가 아니라, candidate target 기준의 fat guard consistency를 다시 계산해야 했다.

v8.0-X 변경:

- `buildV8ProfileMacroCandidateFatGuardReview()`가 candidate target override를 받을 수 있게 했다.
- candidate-v2 linked-target 계산이 carb floor와 15% kcal fat reference를 같은 candidate target 방정식 안에서 풀도록 수정했다.
- 이 변경은 report-only candidate-v2 내부 feasibility 계산이다. production `targetCal`은 그대로 유지하고, candidate formula approval도 하지 않는다.

직접 확인된 `low_reliability_user` 결과:

- `targetCal=3312.9411744928284`
- `baseTargetCal=3261.4951111111113`
- `targetDeltaKcal=51.44606338171707`
- `targetRateDeltaEquivalentKgPerWeek=0.046769148528833696`
- `proteinG=176`
- `carbsG=528`
- `fatG=55.215686054758734`
- `proteinGPerKgBodyweight=2`
- `carbsGPerKgBodyweight=6`
- `fatGPerKgBodyweight=0.6274509778949856`
- `fatPercentKcal≈15`
- `candidateAudit.findingIds` no longer includes `fat_percent_below_contest_prep_reference_floor`

직접 확인된 요약:

- `profileMacroCandidateV2Comparison.summary.fatFloorRaisedForTargetDeltaCount=4`
- `profileMacroCandidateV2Comparison.summary.fatGuardConflictCount=0`
- `profileMacroCandidateV2Comparison.summary.fatGuardResolvedCount=5`
- `humanReviewNumericalApproval.summary.numericApprovedCount=18`
- `humanReviewNumericalApproval.summary.blockedCount=0`
- `humanReviewNumericalApproval.summary.productionFormulaApprovalReady=true`
- `humanReviewNumericalApproval.summary.candidateFormulaApproved=false`
- `humanReviewNumericalApproval.summary.productionFormulaChanged=false`

runtime user-facing QA 재확인:

- production `targetCal=3283.4951111111113`
- proposed candidate target `3312.9411753385825`
- `targetDeltaKcal=29.446064227471197`
- `targetRateDeltaEquivalentKgPerWeek=0.02676914929770109`
- `targetDeltaApplied=false`
- `canApplyAutomatically=false`

해석:

- v8.0-X로 W의 numerical blocker는 report-only candidate-v2 안에서 해소됐다.
- `productionFormulaApprovalReady=true`는 "owner formula review에 올릴 수 있는 수치 상태"라는 뜻이지, owner signoff / formula approval / production wiring / production visual QA 완료가 아니다.
- candidate-v2 target delta를 production `targetCal`에 적용하려면 별도 승인, production wiring, production wiring 이후 visual QA가 필요하다.

## 11-22. 2026-06-03 v8.0-Y profile formula approval preflight 1차

v8.0-Y에서는 `productionFormulaApprovalReady=true`가 formula approval이나 production targetCal wiring으로 오독되지 않도록 `profileFormulaApprovalPreflight`를 추가했다. 이 레이어는 이미 닫힌 candidate-v2 code contract, 18개 human-review numerical gate, runtime user-facing QA, report-only boundary, target-delta 미적용 경계를 하나로 묶는 report-only preflight다.

직접 추출:

- `profileFormulaApprovalPreflight.meta.stage=9-10_profile_formula_approval_preflight_v0`
- `profileFormulaApprovalPreflight.meta.approvalStatus=ready_for_explicit_formula_approval_decision_not_approved`
- `profileFormulaApprovalPreflight.summary.checkCount=5`
- `profileFormulaApprovalPreflight.summary.readyCheckCount=5`
- `profileFormulaApprovalPreflight.summary.blockedCount=0`
- `profileFormulaApprovalPreflight.summary.approvalPreflightReady=true`
- `profileFormulaApprovalPreflight.summary.readyForExplicitFormulaApprovalDecision=true`
- `profileFormulaApprovalPreflight.summary.candidateFormulaApproved=false`
- `profileFormulaApprovalPreflight.summary.productionFormulaChanged=false`
- `profileFormulaApprovalPreflight.summary.productionWiringAllowed=false`
- `profileFormulaApprovalPreflight.summary.productionTargetCalApplied=false`
- `profileFormulaApprovalPreflight.summary.productionVisualQaCompleted=false`

준비 완료 check:

- `candidate_v2_code_contracts_closed`
- `human_review_numerical_gate_18_of_18`
- `runtime_user_surfaces_review_only_ready`
- `report_only_boundary_preserved`
- `candidate_target_delta_not_applied`

해석:

- v8.0-Y는 명시적 formula approval decision을 위한 preflight지 decision 자체가 아니다.
- candidate-v2 산식은 아직 production-approved 상태가 아니다.
- production targetCal wiring과 post-wiring production visual QA는 별도 gate로 남는다.
- 내부 앱 계약 closure는 외부 운동생리학 근거가 아니라 앱 전체 구현 근거로만 취급한다.

## 11-23. 2026-06-03 v8.0-Z profile formula approval decision 1차

v8.0-Z에서는 Y preflight 5/5 ready 결과를 근거로 `profileFormulaApprovalDecision`을 추가하고, `candidate-v8-profile-macro-v2-linked-target-v0`을 다음 production wiring 단계에 한정해 승인했다. 이 결정은 산식 선택 승인이지 production targetCal 적용이 아니다.

직접 추출:

- `profileFormulaApprovalDecision.meta.stage=9-11_profile_formula_approval_decision_v0`
- `profileFormulaApprovalDecision.summary.decisionStatus=approved_for_production_wiring_not_applied`
- `profileFormulaApprovalDecision.summary.candidateFormulaApproved=true`
- `profileFormulaApprovalDecision.summary.approvedCandidateModel=candidate-v8-profile-macro-v2-linked-target-v0`
- `profileFormulaApprovalDecision.summary.productionFormulaChanged=false`
- `profileFormulaApprovalDecision.summary.productionWiringAllowed=true`
- `profileFormulaApprovalDecision.summary.productionTargetCalApplied=false`
- `profileFormulaApprovalDecision.summary.productionVisualQaCompleted=false`
- `profileFormulaApprovalDecision.summary.approvalPreflightReady=true`
- `profileFormulaApprovalDecision.summary.preflightBlockedCount=0`
- `profileFormulaApprovalDecision.summary.fullV8Complete=false`

아직 승인/완료되지 않은 범위:

- `production_targetCal_application`
- `post_wiring_production_visual_qa`
- `full_8_2_cartesian_execution`
- `full_v8_completion`

해석:

- v8.0-Z부터 candidate-v2는 production wiring을 진행할 수 있는 승인 후보가 아니라 승인된 산식 선택이다.
- 그러나 production targetCal은 아직 바뀌지 않았다.
- post-wiring production visual QA가 끝나기 전까지 사용자 표면에서 최종 production formula 완료로 주장하면 안 된다.
- 내부 코드 계약과 18개 human-review numerical gate를 근거로 한 implementation decision이며, 새 외부 운동생리학 근거를 추가한 단계가 아니다.

## 11-24. 2026-06-03 v8.0-AA production candidate-v2 target wiring 1차

v8.0-AA에서는 v8.0-Z에서 승인된 `candidate-v8-profile-macro-v2-linked-target-v0`를 실제 `calculate()` production 경로에 연결했다. 적용 범위는 runtime Today 계산에서 profile target-delta가 active이고, candidate-v2 carb floor가 충족되며, protein/fat guard conflict가 없는 경우다.

직접 추출 기준 mixed high-load runtime QA는 `productionTargetCal=3312.9411753385825`, `proposedCandidateTargetCal=3312.9411753385825`, `targetDeltaKcal=29.446064227471197`, `targetDeltaApplied=true`, `productionTargetCalApplied=true`, `recentGateStatus=applied`, `coachHasProfileTargetDeltaAppliedDecision=true`다. `targetRateContext.profileCandidateDeltaContract.appliedToTarget=true`가 되어 Records snapshot / recentContext / Coach가 같은 적용 상태를 본다.

AA 구현 중 중요한 충돌을 발견했다. production wiring을 무조건 `calculate()`에 넣으면 `runV8ScenarioRunner()`의 과거 report-only baseline까지 AA 이후 production target으로 다시 계산되어 v8.0-D~X 감사 논리가 오염된다. 따라서 scenario runner의 baseline 산출은 `profileCandidateV2ProductionWiring=false`로 고정했고, 실제 app runtime QA와 production calculation은 기본값으로 AA wiring을 켠다. 이 구분은 80,621,568,000개 full Cartesian을 실행했다는 주장을 만들지 않기 위한 경계이기도 하다.

또한 V8 runtime QA가 `exerciseProfile/profileSession` DOM/state 값을 `mixed / mixed_strength_cardio`로 남겨 이후 full suite의 golden scenario가 잘못된 운동 프로필을 읽는 sandbox 복구 누수를 발견했다. `withRecordTestSandbox()`와 `withCalibrationReportSandbox()`에 exercise profile/session DOM/state 복구를 추가했다. 이 수정은 산식 근거가 아니라 테스트/감사 격리 계약이며, 다른 suite가 이전 V8 fixture에 끌려가지 않도록 하는 안전장치다.

검증 기준은 `runV8ScenarioRunnerTests` 1 suite / 24 cases / failed 0, calibration profile 14 suites / 132 cases / failed 0, full profile 99 suites / 1052 cases / failed 0이다.

이 단계에서 닫힌 것은 `production_targetCal_application`이다. 아직 닫히지 않은 것은 `post_wiring_production_visual_qa`, `full_8_2_cartesian_execution`, `full_v8_completion`이다. AA는 새 외부 운동생리학 근거를 추가하지 않고, 이미 분리된 외부 근거 / 내부 제품 정책 / 내부 코드 계약을 production 경로에 연결한 구현 단계로 기록한다.

## 11-25. 2026-06-03 v8.0-AB post-wiring production visual QA 1차

v8.0-AB에서는 AA에서 실제 production `calculate()` 경로에 연결된 candidate-v2 target/macro가 사용자 화면에서도 깨지지 않고 보이는지 확인했다. 기존 v8.0-V screenshot audit은 rich bodybuilding fixture 중심이라 AA production target-delta 적용 케이스를 직접 보지 못했다. 따라서 이번 단계에서는 `profileCandidateV2` payload를 새로 만들고, analyzer가 단순 캡처 수가 아니라 runtime 적용 상태를 확인하도록 확장했다.

구현/검증 내용:

- `capture_render_audit.cjs`에 AA 직접 테스트와 같은 mixed / mixed_strength_cardio / treadmill_run 70분 high-load fixture를 추가했다.
- Today applied, Today quick-open, Records detail, Records basis-open을 desktop/mobile에서 캡처한다.
- `analyze_render_audit.cjs`는 55 captures = desktop 37 + mobile 18을 요구한다.
- 8개 post-wiring profile-candidate 캡처는 manifest runtime meta에서 `exerciseProfile=mixed`, `profileSession=mixed_strength_cardio`, `selectedMacroBasis=profile_candidate_v2`, `productionTargetCalApplied=true`, `recentGateStatus=applied`, `recentGateTargetDeltaApplied=true`, `recentGateCanApplyAutomatically=false`를 모두 통과해야 한다.
- `runV8ScenarioRunner()`는 version `8.0-AB`로 올라갔고, approval decision에서 `productionVisualQaCompleted=true`를 보고하며 `notApprovedYet`에서 `post_wiring_production_visual_qa`를 제거한다.
- 직접 runtime 추출 기준 8개 캡처 모두 `targetCal=3312.9411753385825`, `protein=176`, `carbs=528`, `fat=55.21568614873139`, `targetDeltaKcal=29.446064227471197`다.
- 수동 screenshot spot-check는 `52_mobile_today_profile_candidate_v2_applied.png`, `54_mobile_records_profile_candidate_v2_detail.png`, `55_desktop_records_profile_candidate_v2_basis_open.png`, `56_mobile_records_profile_candidate_v2_basis_open.png`를 확인했다. basis-open 화면에서 `프로필 목표 보정 29 kcal`, `주간 변화 보정 0.027 kg/주`, 운동 프로필/오늘 세션/유산소 입력이 보인다.

이 단계에서 닫힌 것은 `post_wiring_production_visual_qa`다. 아직 닫히지 않은 것은 `full_8_2_cartesian_execution`, `full_v8_completion`이다. AB는 새 외부 운동생리학 근거를 추가하지 않고, AA 적용이 실제 사용자-facing 화면과 Records 당시 계산 기준 화면에 도달하는지 확인한 QA evidence 단계로 기록한다.

## 11-26. 2026-06-03 v8.0-AC full Cartesian execution contract 1차

v8.0-AC에서는 사용자가 지적한 8-2 필수축 전체 곱 `80,621,568,000`개를 18개 human-review 케이스나 50개 pairwise 케이스로 대체했다고 읽히지 않도록, `runV8ScenarioRunner()`에 전수 실행 계약 레이어를 추가했다. 이 단계는 실제 806억 조합 계산 완료가 아니라, 앞으로 전수 shard를 실행할 때 모든 agent가 같은 축 순서와 같은 인덱스 해석을 쓰게 만드는 감사/실행 계약이다.

구현/검증 내용:

- `runV8ScenarioRunner()` version은 `8.0-AC`로 올라갔다.
- 새 report 객체 `fullCartesianExecutionContract`는 stage `8-2_full_cartesian_execution_contract_v0`를 사용한다.
- 20개 필수축의 count를 다시 곱해 `axisProductCheck=80,621,568,000`이 `nominalFullCartesianCaseCount`와 일치하는지 고정한다.
- 축 stride table과 `decodeV8FullCartesianTupleIndex()`를 추가해 row index 하나가 어떤 축 값 조합인지 결정적으로 복원된다.
- pilot tuple은 index `0`, `1`, middle, `80,621,567,998`, `80,621,567,999`를 decode한다.
- default shard size는 `100,000`이고, planned shard count는 `806,216`, 마지막 shard는 `68,000`건이다.
- `fullCartesianRun=false`, `full8_2CartesianExecutionClosed=false`, `fullV8CompletionClosed=false`를 유지한다.

판단:

- AC는 감각적 산식 조정이나 새 운동생리학 근거가 아니다. 순수하게 축 곱/인덱스/샤드 경계를 고정하는 실행 계약이다.
- AC는 `full_8_2_cartesian_execution`을 닫지 않는다. 다음 단계에서 실제 shard executor와 per-shard manifest evidence가 생겨야 전수 실행 여부를 논할 수 있다.
- 18개 human-review, pairwise/targeted stress, full Cartesian execution gate는 계속 별도 레이어다.

## 11-27. 2026-06-03 v8.0-AD deterministic full Cartesian shard pilot 1차

v8.0-AD에서는 AC의 축 index/stride 계약을 실제 bounded shard 실행으로 연결했다. 새 함수 `runV8FullCartesianShard()`와 CLI `tools/render_audit/run_v8_full_cartesian_shard.cjs`는 지정된 full Cartesian row range만 브라우저에서 실행하고, 결과를 ignored JSON evidence로 남긴다.

구현/검증 내용:

- `runV8FullCartesianShard()` stage는 `8-2_full_cartesian_shard_execution_v0`다.
- coherent tuple은 `buildV8ScenarioCase()` 경로로 실제 current/candidate 산출값을 계산한다.
- incoherent tuple은 계산하지 않고 constraint-only case로 분리한다. 이 경계는 profile/session/rest/cardio 제약을 무시한 가짜 사용자 세션 계산을 막기 위한 것이다.
- CLI output은 `tools/render_audit/v8_full_cartesian_shards/` 아래에 저장하며, `.gitignore`에 추가했다.
- Pilot A: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=0 --limit=8 --max-cases=8` → decoded 8, calculated 8, constraint-only 0.
- Pilot B: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=1152 --limit=3 --max-cases=3` → decoded 3, calculated 0, constraint-only 3.
- Pilot B의 constraint 이유는 `training_session_without_weight_duration`, `weekly_zero_with_training_session`, `no_performance_with_training_session`다.
- 첫 CLI pilot에서 `--end` 생략 시 빈 문자열이 `0`으로 해석되는 parser bug를 발견했고, 빈 값은 fallback을 쓰도록 수정했다.

판단:

- AD는 실제 shard evidence를 만들 수 있는 실행 경로를 열었지만, 전체 `80,621,568,000` row를 실행한 것은 아니다.
- 따라서 `full_8_2_cartesian_execution`과 `full_v8_completion`은 계속 open이다.
- 다음 단계는 shard manifest schema/hash/failure summary를 더 엄격히 만들고, 여러 shard를 배치로 이어 실행하는 orchestration을 설계하는 것이다.

## 11-28. 2026-06-03 v8.0-AE shard manifest integrity and analyzer 1차

v8.0-AE에서는 AD의 bounded shard JSON을 검증 가능한 evidence manifest로 강화했다. 단순히 파일이 생겼다는 사실만으로는 전수 실행 증거가 될 수 없으므로, schema version, hash, source hash, decoded accounting, calculation issue count, non-claim flag를 analyzer가 다시 계산하도록 만들었다.

구현/검증 내용:

- `runV8FullCartesianShard()` report는 schema `v8_full_cartesian_shard_report_v1`를 포함한다.
- report meta는 `decodedEqualsCalculatedPlusConstraintOnly`를 기록한다.
- report summary는 `calculationIssueCount`와 최대 20개 `calculationIssues`를 기록한다.
- `run_v8_full_cartesian_shard.cjs` output은 manifest schema `v8_full_cartesian_shard_manifest_v1` wrapper로 바뀌었다.
- manifest에는 `reportSha256`, `manifestSha256`, source file hash, git head/branch/status, command args, shard summary가 포함된다.
- `--shard-index` / `--shard-size` 옵션을 추가해 future batch shard 실행이 손계산 start/end에 의존하지 않게 했다.
- 새 analyzer `analyze_v8_full_cartesian_shards.cjs`는 schema, hash, source hash, decoded accounting, zero calculation issue, `fullCartesianRun=false`, `full8_2CartesianExecutionClosed=false`, `fullV8CompletionClosed=false`를 검증한다.
- AE manifest analyzer 결과: 2 manifests, `failedCount=0`, decoded 11, calculated 8, constraint-only 3, truncated 0.

판단:

- AE는 evidence 무결성 단계다. 새 운동생리학 근거도 아니고 산식 조정도 아니다.
- AE analyzer가 통과했다는 뜻은 "bounded shard evidence가 조작/오독 방지 필드를 갖추고 검증된다"는 뜻이지, 전체 806억 row 실행 완료가 아니다.
- `full_8_2_cartesian_execution`과 `full_v8_completion`은 계속 open이다.

## 11-29. 2026-06-03 v8.0-AF shard batch orchestration pilot 1차

v8.0-AF에서는 단일 shard evidence를 여러 개 묶어 실행하고 검증하는 batch orchestration pilot을 추가했다. 이 단계의 목적은 전체 806억 row를 실행했다는 주장이 아니라, 나중에 다수 shard를 실행할 때 각 shard manifest 생성과 analyzer 검증이 같은 폴더 단위로 묶이는지 확인하는 것이다.

구현/검증 내용:

- 새 도구 `tools/render_audit/run_v8_full_cartesian_batch.cjs`를 추가했다.
- batch schema는 `v8_full_cartesian_batch_manifest_v1`다.
- explicit non-contiguous shard list `--shards=0,144`와 contiguous mode `--start-shard` / `--shard-count`를 지원한다.
- 각 shard는 기존 `run_v8_full_cartesian_shard.cjs`를 통해 manifest schema/hash/source hash를 포함한 ignored JSON으로 생성된다.
- batch runner는 생성된 batch folder를 `analyze_v8_full_cartesian_shards.cjs`로 검증한 뒤 `batch_summary.json`을 작성한다.
- analyzer는 `shard_*.json`만 읽도록 갱신해 `batch_summary.json`을 shard manifest로 오인하지 않는다.
- AF pilot: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=af_pilot` → 2 shard runs, analyzer failed 0, decoded 16, calculated 8, constraint-only 8, truncated 0.
- batch folder 직접 analyzer 재검증도 2 manifests / failed 0이다.

판단:

- AF는 batch orchestration evidence 단계다. 전체 806억 row 실행 완료가 아니다.
- `full_8_2_cartesian_execution`과 `full_v8_completion`은 계속 open이다.
- 다음 단계는 batch 실행 결과를 누적 관리하는 ledger/checkpoint와 shard coverage gap summary를 설계하는 것이다.

## 11-30. 2026-06-03 v8.0-AG 운동 프로필 루틴 소유권 재감사 1차

v8.0-P의 profile/session snapshot contract는 "프로필/세션 필드를 보존한다"는 코드 계약을 닫았지만, `profileSession`을 별도 사용자 입력처럼 다루면 기존 루틴/요일 계획을 우회하는 기준통과용 작업이 된다. AG 재감사 기준은 다음과 같다.

판단:

- 기존 기능 기준선은 v8 이전 운동 루틴 동작이다. 고급설정 OFF 화면은 `휴식 / 운동`이며 내부 저장값만 `REST / PUSH`다. 여기서 `PUSH`는 보디빌딩 PUSH 분할이 아니라 단순 운동일 내부값이고, 운동일 강도는 `0.70 + weeklyTrainingDays` 자동 보정으로 계산됐다.
- 고급설정 ON의 보디빌딩은 기존 `PPL-UL / 2분할 / 3분할 / 4분할 / 5분할` 루틴/세션을 그대로 유지해야 한다.
- 운동 프로필을 추가할 때 사용자 선택의 소유권은 `exerciseProfile + routinePlan + routine`에 있다. `profileSession`은 Today draft, Records snapshot/signature, backup, v8 report/candidate contract에서 쓰는 파생 필드일 뿐이다.
- 러닝, 스트렝스, 파워빌딩, 혼합형은 보디빌딩 루틴으로 fallback 매핑하지 않는다. 각 프로필의 전용 routine plan/session이 실제 `routine`, `intensityOverride`, `weightDuration`, cardio defaults로 계산에 들어가야 한다.
- "profile/session 입력은 production targetCal을 바꾸면 안 된다"는 v8.0-P 테스트 전제는 기존 기능이 아니다. 루틴/세션, 강도, 웨이트 시간, 유산소 기본값은 기존 production workout input이므로 프로필 전용 세션은 기존 계산 경로로 `targetCal`을 바꿀 수 있다.

구현/검증 현재 상태:

- `profileSession` user-facing select는 숨기고, 설정/Today의 실제 선택은 운동 프로필과 루틴/세션 select가 맡도록 재정렬했다.
- 보디빌딩은 기존 루틴 플랜 목록을 유지하고, 비보디빌딩 프로필은 프로필 전용 routine plan/session을 노출한다.
- 러닝 세션은 기본적으로 웨이트 시간 0, 러닝 cardio defaults를 갖고 계산에 들어간다.
- 2026-06-03 AG 1차 확인: `runTodayQuickEditTests` 1 suite / 27 cases / failed 0, `runV8ScenarioRunnerTests` 1 suite / 26 cases / failed 0.
- 아직 full internal suite, browser visual QA, V8 전 단계 전체 "문서통과용 우회" 재감사는 완료되지 않았다.

## 11-31. 2026-06-03 v8.0-AG V8 전체 단계 정밀 재감사 1차

이번 재감사는 "운동 프로필/운동 세션"이라는 단어 자체를 다른 단계에 기계적으로 대입하지 않는다. 사용자 지적의 근본 원칙은 다음이다.

- 기존 기능의 소유권을 먼저 확인한다. 새 필드나 새 축이 기존 사용자 흐름을 우회하면 기준 통과용 작업이다.
- 보고용/후보용/production 적용용 경계를 섞지 않는다.
- 내부 코드 근거, 제품 정책 근거, 외부 운동생리학 근거를 분리한다.
- 테스트는 숫자만 맞추는 방패가 아니라 실제 사용자 입력과 계산 입력이 같은 대상을 가리키는지 검증해야 한다.
- 18개 human-review, pairwise, targeted, full Cartesian shard는 서로 대체물이 아니다.

재감사 결과:

- v8.0-A/B/C Today-Records 기준갱신 단계: 현재 재검토 기준으로는 기존 feature ownership을 우회한 흔적이 낮다. 이 단계들은 산식 변경보다 snapshot/current/stale 상태 책임을 나눈 단계이므로 외부 운동생리학 근거 대상이 아니다.
- v8.0-D/D1 scenario runner와 근거 등급: 80,621,568,000개 전체 실행을 하지 않았음을 meta와 문서에 남긴 방향은 맞다. 다만 D/H 계열의 profile session fixture가 한때 `UPPER`/`LOWER`/`PUSH` fallback을 실제 세션처럼 보호했으므로, AG에서 runner 입력 자체를 profile-owned routine/session으로 재정렬했다.
- v8.0-E/F/G current macro audit과 context candidate-v0: report-only 경계는 유지된다. 현재 결론은 "단백질을 감각적으로 낮춘다"가 아니라, 기존 diet guide floor와 외부 ISSN general range의 충돌을 후보 비교로 드러낸 것이다.
- v8.0-H/K/N profile macro 계열: 재개방/정정 대상이었다. AG 이전의 `productionFallbackCount>0`, running `UPPER`, strength `LOWER`, mixed `PUSH`는 현재 기준으로는 사용자 세션이 아니라 역사적 fallback 감사 신호다. AG 현재 runner는 `productionFallbackCount=0`, running `running_tempo`, strength `strength_heavy_lower`, powerbuilding `powerbuilding_lower`, mixed `mixed_strength_cardio`를 실제 입력으로 계산한다.
- v8.0-I/J maintain: `maintain -> recomp`는 히스토리로만 남겨야 하며, J 이후 현재 상태는 production maintain goal 1차 계약이 들어간 상태다. 이 승인은 maintain goal 계약에 한정되고 profile-specific macro formula 승인이 아니다.
- v8.0-L/M pairwise/targeted stress: full Cartesian 실행, 18개 human-review, targeted residual queue를 서로 대체하지 않는 구조는 맞다. AG에서 targeted mixed의 intended/effective tuple도 `mixed_strength_cardio` 45분 기준으로 정정했다.
- v8.0-O/P contract: P의 "profile/session 보존"은 별도 `profileSession` 입력 보존이 아니라 `exerciseProfile + routinePlan + routine`에서 파생된 snapshot/draft/report 필드 보존으로 정정됐다.
- v8.0-Q/R/S score/Coach/protein guard: score/Coach가 diet-equivalent product policy를 재사용하는 것은 외부 근거가 아니라 내부 제품 정책으로만 읽어야 한다. ISSN/Helms 근거는 protein/fat guard의 검토 배경이지 앱 전체 자동 승인 근거가 아니다.
- v8.0-T/U/V/AB runtime/visual QA: AG가 profile-owned session 입력과 candidate delta 숫자를 바꿨으므로, 이전 screenshot/runtime 숫자 `25.029`, `29.446`, `43.729`, `51.446`은 current runner/runtime 재실행 근거 없이 현재 AG 기대값으로 쓰면 안 된다. 단, `29.446`처럼 AA 이후 current runtime에서 다시 확인된 숫자는 stage/source를 붙여 별도로 읽는다.
- v8.0-W/X/Y/Z/AA approval/wiring: candidate-v2 approval/wiring 경계는 유지된다. 다만 AG 현재 scenario baseline의 mixed target delta는 profile-owned 입력 때문에 `targeted_mixed_carb_unresolved≈128.996kcal`, `low_reliability_user≈130.646kcal`로 바뀌었다. 이 값은 88kg mixed case의 6g/kg carb floor 528g과 15% fat reference를 동시에 맞춘 결과다.
- v8.0-AC/AD/AE/AF full Cartesian: index/shard/manifest/batch는 bounded evidence와 실행 계약을 만든 것이지 전체 806억 row 실행 완료가 아니다. 이 경계는 유지한다.

AG에서 실제 수정한 충돌:

- `runV8ScenarioRunner()`의 profile-specific human-review/targeted cases가 더 이상 bodybuilding routine fallback으로 계산되지 않는다.
- scenario calculation 직전 DOM/state와 Today draft를 definition의 `exerciseProfile`, `routinePlan`, `routine`, intensity, duration, cardio 값으로 임시 정렬한다. 보고서 input과 `calculate()` 입력이 분리되는 우회 위험을 제거하기 위한 조치다.
- `runV8ScenarioRunnerTests`는 running/strength가 profile-owned routine으로 계산되는지, `profileSessionCarbPolicy.summary.productionFallbackCount=0`인지, mixed candidate-v2가 88kg × 6g/kg = 528g carb floor를 근거로 target delta를 여는지 검증한다.

현재 AG 직접 확인:

- `runTodayQuickEditTests`: 1 suite / 27 cases / failed 0
- `runV8ScenarioRunnerTests`: 1 suite / 26 cases / failed 0
- 직접 추출: `running_focused.input.todayRoutine=running_tempo`, `strength_focused.input.todayRoutine=strength_heavy_lower`, `bulk_user.input.todayRoutine=powerbuilding_lower`, `low_reliability_user.input.todayRoutine=mixed_strength_cardio`
- 직접 추출: `profileSessionCarbPolicy.summary.productionFallbackCount=0`
- 직접 추출: `targeted_mixed_carb_unresolved.candidate.targetDeltaKcal=128.996060400435`, `low_reliability_user.candidate.targetDeltaKcal=130.64606033700375`

AG 추가 검증:

- full internal suite: 99 suites / 1056 cases / failed 0
- render audit capture/analyzer: 55 captures / failed 0
- screenshot spot-check: desktop Settings groups open, desktop Today profile-candidate quick-open, mobile Today profile-candidate quick-open, mobile Settings groups open

아직 열려 있는 항목:

- 프로필 전용 세션의 xw/default duration/cardio default는 현재 제품 설계값이다. 외부 운동생리학 근거로 과장하지 말고, 향후 별도 근거/사용자 테스트/정책 문서로 보강해야 한다.
- AG visual QA는 first-pass render audit와 spot-check를 통과했지만, 모든 프로필별 실제 사용자 설정 조합을 사람이 훑은 완료 선언은 아니다.

## 11-32. 2026-06-03 v8.0-AH profile-owned visual QA evidence 재감사 1차

AG 재감사 원칙을 AA/AB runtime and render-audit evidence에도 적용했다. 발견된 문제는 앱 계산 경로 자체보다 evidence fixture의 소유권이었다.

발견한 충돌:

- candidate-v2 runtime QA와 `profileCandidateV2` render payload가 `exerciseProfile=mixed`, `profileSession=mixed_strength_cardio`를 쓰면서도 시작 routine input은 `routinePlan=ppl_ul`, `routine=PUSH`, `todayRoutineSession=PUSH`였다.
- `getEffectiveTodayCalculationValues()`가 이 값을 mixed 기본 세션으로 정규화할 수 있더라도, 그 증거는 사용자가 실제로 mixed routine plan/session을 선택한 상황을 검증한 것이 아니다.
- 따라서 이는 AG에서 지적한 “profileSession만 맞추고 실제 루틴/세션 소유권은 우회하는” 문제와 같은 계열이다.

보강한 내용:

- `buildV8RuntimeCandidateV2UserFacingFixture()`는 이제 `routinePlan=mixed_balanced`, `routine=mixed_strength_cardio`, `todayRoutineSession=mixed_strength_cardio`를 직접 넣는다.
- `capture_render_audit.cjs`의 `profileCandidateV2` settings / Today draft / saved snapshot도 같은 profile-owned routine input으로 갱신했다.
- `getRuntimeMeta()`는 runtime metadata에 `routinePlan`과 `routine`을 포함한다.
- `analyze_render_audit.cjs`는 8개 post-wiring candidate-v2 captures에서 `routinePlan=mixed_balanced`와 `routine=mixed_strength_cardio`를 필수로 검사한다.
- `runV8ScenarioRunner()` version은 `8.0-AH`로 올라갔다.

실행한 확인:

- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` → 1 suite / 26 cases / failed 0
- `node .\tools\render_audit\capture_render_audit.cjs` → 55 captures
- `node .\tools\render_audit\analyze_render_audit.cjs` → 55 captures / 8 post-wiring candidate-v2 captures / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=full` → 99 suites / 1056 cases / failed 0

판단:

- AH는 post-wiring visual/runtime evidence의 정합성을 높인 단계다.
- AH는 새 외부 운동생리학 근거가 아니며, candidate-v2 공식 승인 범위를 넓히지 않는다.
- AH는 `full_8_2_cartesian_execution` 또는 `full_v8_completion`을 닫지 않는다.
- 다음 full-axis 작업은 shard 실행 결과를 누적 관리하는 ledger/checkpoint, gap summary, dirty-source/truncated/overlap evidence 분리다.

## 11-33. 2026-06-03 v8.0-AI full Cartesian shard coverage ledger 1차

v8.0-AI에서는 AF 이후 남긴 다음 작업인 shard coverage ledger/checkpoint를 구현했다. 목적은 더 많은 shard를 실제로 돌렸다고 주장하는 것이 아니라, 이미 생성된 shard/batch evidence가 full Cartesian product에서 어느 interval을 실제로 실행했는지, 어디가 gap/overlap/truncated/dirty evidence인지 기계적으로 분리하는 것이다.

구현한 내용:

- 새 도구 `tools/render_audit/build_v8_full_cartesian_ledger.cjs`를 추가했다.
- ledger schema는 `v8_full_cartesian_coverage_ledger_v1`이다.
- shard manifest schema/hash/report hash/source hash/non-claim flag를 다시 검증한다.
- batch summary schema/hash/non-claim flag도 검증한다.
- coverage는 `executedEndExclusive` 기준으로만 병합한다. `endExclusive`는 계획된 shard boundary일 뿐이며 `--max-cases` 또는 truncation으로 실행이 멈춘 경우 coverage로 세면 안 된다.
- summary는 `uniqueExecutedCaseCount`, `contiguousCoveredFromZero`, `gapCount`, `overlapCount`, `dirtySourceManifestCount`, `truncatedManifestCount`, `closureBlockers`를 보고한다.
- `run_v8_full_cartesian_batch.cjs`는 batch summary 작성 후 같은 batch folder에 ignored `coverage_ledger.json` checkpoint를 생성한다.
- `runV8ScenarioRunner()` version은 `8.0-AI`로 올라갔다.

실행한 확인:

- `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_af_pilot` → shard manifests 2 / batch summaries 1 / failed 0 / unique executed 16 / gap 2 / dirty-source manifests 2 / fullCoverageCandidate false
- `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=ai_ledger_pilot` → shard runs 2 / analyzer failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0 / ledger unique 16 / ledger gap 2 / ledger dirty-source manifests 2 / fullCoverageCandidate false
- post-commit clean-source 재확인: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=ai_clean_ledger_pilot` → shard runs 2 / analyzer failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0 / ledger unique 16 / ledger gap 2 / ledger dirty-source manifests 0 / fullCoverageCandidate false
- `node .\tools\render_audit\analyze_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_ledger_pilot` → manifests 2 / failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0
- `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_ledger_pilot` → manifests 2 / failed 0 / unique executed 16 / contiguous from zero 8 / first gap 8~1152 / gap 2 / overlap 0 / dirty-source manifests 2 / fullCoverageCandidate false
- `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0 --shard-size=100000 --max-cases=8 --label=ai_truncation_probe` → shard runs 1 / analyzer failed 0 / decoded 8 / calculated 8 / truncated 1 / ledger unique 8 / ledger gap 1 / ledger dirty-source manifests 1 / fullCoverageCandidate false
- `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_truncation_probe` → manifests 1 / failed 0 / unique executed 8 / truncated manifests 1 / first gap 8~80621568000 / fullCoverageCandidate false
- ledger code review 중 contiguous intervals가 zero-length overlap으로 기록될 수 있는 조건을 발견해 수정했다. `startIndex === previousEndExclusive`는 gap도 overlap도 아니므로 merge만 해야 한다.
- `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,1 --shard-size=8 --max-cases=8 --label=ai_contiguous_probe_after_fix` → shard runs 2 / analyzer failed 0 / decoded 16 / calculated 16 / truncated 0 / ledger unique 16 / ledger gap 1 / ledger overlap 0 / fullCoverageCandidate false
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` → 1 suite / 26 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=full` → 99 suites / 1056 cases / failed 0

판단:

- AI는 full Cartesian 실행 증거를 부풀리는 단계가 아니라, 실행 증거를 더 엄격히 축소해서 읽는 단계다.
- 기존 AF batch pilot처럼 2 shard / 16 decoded evidence는 여전히 80,621,568,000개 중 16개 실행 evidence일 뿐이다.
- dirty-source manifest가 있으면 full closure blocker로 남긴다. dirty evidence를 폐기하지는 않지만, clean full-run evidence와 같은 등급으로 읽으면 안 된다.
- AI는 `full_8_2_cartesian_execution` 또는 `full_v8_completion`을 닫지 않는다.

## 11-34. 2026-06-03 v8.0-AJ full Cartesian next-shard planner 1차

v8.0-AJ에서는 AI ledger를 바탕으로 다음에 실행할 shard/range를 결정적으로 뽑는 planner를 추가했다. 목적은 실행 완료 주장이 아니라, 여러 Codex가 각자 임의 shard를 고르거나 부분 실행된 shard를 완료로 오독하는 일을 줄이는 것이다.

구현한 내용:

- 새 도구 `tools/render_audit/plan_v8_full_cartesian_shards.cjs`를 추가했다.
- planner schema는 `v8_full_cartesian_shard_plan_v1`이다.
- `--ledger`로 기존 ledger 파일을 읽거나, `--dir`로 shard evidence folder를 주면 내부에서 `build_v8_full_cartesian_ledger.cjs --detail-limit=-1`를 실행해 전체 gap을 읽는다.
- output은 `plannedRanges`와 `plannedShardIndexes`를 분리한다.
- `plannedRanges`는 exact uncovered range다. 예: 기존에 0~8만 실행된 100,000-row shard는 `8~100000`이 exact range다.
- `plannedShardIndexes`는 batch runner에 넘길 shard index다. 이 방식은 편하지만 부분 coverage가 있는 shard에서는 중복 재실행이 생길 수 있으므로 `wouldRerunCaseCountIfUsingShardIndexes`를 함께 보고한다.
- code review 중 같은 shard 안에 여러 gap이 있는 경우 range별 중복량 합산이 과대계산될 수 있음을 발견했고, summary 중복량은 shard 단위로 묶어 계산하도록 보정했다.
- exact range가 필요한 경우 `exactRangeExecutionRequiredForNoDuplicateCoverage=true`를 표시한다.
- `runV8ScenarioRunner()` version은 `8.0-AJ`로 올라갔다.

실행한 확인:

- `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-shards=4 --label=aj_plan_clean_pilot` → ledger gap 2 / dirty-source manifests 0 / planned ranges 4 / planned shard indexes 4 / planned uncovered 32 / duplicate-if-batch 0 / first planned range 8~16 / full execution closed false
- `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_truncation_probe --shard-size=100000 --max-shards=2 --label=aj_plan_truncation_probe` → ledger gap 1 / dirty-source manifests 1 / truncated manifests 1 / planned ranges 2 / planned shard indexes 2 / planned uncovered 199,992 / duplicate-if-batch 8 / first planned exact range 8~100000 / exact range required true / full execution closed false
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` → 1 suite / 26 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=full` → 99 suites / 1056 cases / failed 0

판단:

- AJ는 다음 실행 대상을 고르는 planner다. 계획된 range는 아직 실행된 evidence가 아니다.
- batch shard index 명령은 편하지만 partial shard에서는 중복 재실행을 일으킬 수 있다. planner가 exact range command를 따로 내는 이유가 이것이다.
- AJ는 `full_8_2_cartesian_execution` 또는 `full_v8_completion`을 닫지 않는다.

## 11-35. 2026-06-03 v8.0-AK full Cartesian exact-range plan executor 1차

v8.0-AK에서는 AJ planner가 만든 exact uncovered range를 실제 shard manifest evidence로 실행하는 도구를 추가했다. 목적은 "다음 범위가 무엇인지"에서 "계획된 exact range를 중복 없이 실행하고 analyzer/ledger로 다시 검증한다"로 한 단계 전진하는 것이다.

구현한 내용:

- 새 도구 `tools/render_audit/run_v8_full_cartesian_plan.cjs`를 추가했다.
- execution schema는 `v8_full_cartesian_plan_execution_v1`이다.
- `--plan`으로 기존 plan JSON을 읽거나, `--dir/--ledger/--shard-size/--max-ranges`로 내부에서 AJ planner를 실행한다.
- 실행은 `plannedShardIndexes`가 아니라 `plannedRanges`의 `exactStartIndex~exactEndExclusive`만 사용한다.
- 각 range는 기존 `run_v8_full_cartesian_shard.cjs` manifest wrapper를 통해 실행된다.
- 실행 폴더에는 ignored `plan_execution_summary.json`과 `coverage_ledger.json`이 생성된다.
- `runV8ScenarioRunner()` version은 `8.0-AK`로 올라갔다.

실행한 확인:

- `node .\tools\render_audit\run_v8_full_cartesian_plan.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-ranges=2 --label=ak_exact_range_pilot` → executed ranges 2 / executed cases 16 / analyzer failed 0 / execution-folder ledger unique 16 / gap 2 / dirty-source manifests 2 / full execution closed false
- explicit cumulative ledger: source clean pilot `0~8`, AK exact `8~16`, AK exact `16~24`, source clean pilot `1152~1160` → shard manifests 4 / failed 0 / batch summaries 0 / dirty-source manifests 2 / unique executed 32 / contiguous from zero 24 / first gap 24~1152 / fullCoverageCandidate false
- 검증 중 `build_v8_full_cartesian_ledger.cjs --file=...`가 선택한 shard manifest만 읽지 않고 default folder batch summaries까지 같이 수집하는 버그를 발견했다. explicit file cumulative 검증의 의미가 흐려지므로 `--file/--manifest` 모드에서는 batch summary 자동 수집을 끄도록 수정했다.
- 수정 후 같은 explicit cumulative ledger가 shard manifests 4 / batch summaries 0 / unique executed 32 / contiguous from zero 24 / first gap 24~1152를 보고했다.
- 재검토 중 `run_v8_full_cartesian_plan.cjs --max-ranges=0`이 `0 || plannedRanges.length` fallback으로 모든 planned range 실행처럼 넓어질 수 있는 위험도 발견했다. 사용자 지정 실행 bound가 조용히 넓어지는 것은 full Cartesian 증거를 과장할 수 있으므로, 0범위 입력은 `noRangesSelected=true`, executed ranges 0 / cases 0으로 기록하고 analyzer/ledger 실행 claim을 만들지 않게 수정했다.
- post-push clean-source rerun: `--label=ak_clean_exact_range_pilot` → executed ranges 2 / executed cases 16 / analyzer failed 0 / execution-folder ledger unique 16 / dirty-source manifests 0 / full execution closed false
- clean-source explicit cumulative ledger: source clean pilot `0~8`, AK clean exact `8~16`, AK clean exact `16~24`, source clean pilot `1152~1160` → shard manifests 4 / failed 0 / batch summaries 0 / dirty-source manifests 0 / unique executed 32 / contiguous from zero 24 / first gap 24~1152 / fullCoverageCandidate false
- `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-shards=2 --label=ak_plan_recheck` → planned ranges 2 / planned uncovered 16 / duplicate-if-batch 0 / first planned range 8~16 / full execution closed false
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` → 1 suite / 26 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=full` → 99 suites / 1056 cases / failed 0

판단:

- AK는 bounded exact-range execution bridge다. 실행된 것은 16 case pilot이며 80,621,568,000개 full Cartesian 실행 완료가 아니다.
- 최초 AK pilot은 편집 중 실행했으므로 dirty-source manifests 2를 closure blocker로 남겼다. post-push clean-source rerun은 같은 2개 exact range에서 dirty-source manifests 0을 확인했지만, cumulative ledger에는 여전히 gap 24~1152와 full Cartesian incomplete blocker가 남는다.
- AK는 `full_8_2_cartesian_execution` 또는 `full_v8_completion`을 닫지 않는다.

## 11-36. 2026-06-03 v8.0-AL targeted stress mixed maintain 재감사

사용자 지적을 운동프로필/세션 외 다른 단계에도 적용해 재검토하던 중, v8.0-M targeted stress 문서와 테스트가 AT 전 AG+ profile-owned runner 상태를 느슨하게 읽는 문제를 발견했다.

발견:

- 문서 일부는 `mixedCarbUnresolvedCaseCount=1`, `highPriorityCaseCount=2`, maintain mixed case는 carb-floor-met이라고 설명했다.
- v8.0-AL 당시 runner 직접 추출 기준은 `mixedCarbUnresolvedCaseCount=2`, `highPriorityCaseCount=3`이다.
- `targeted_mixed_carb_unresolved`와 `targeted_maintain_mixed_contract`가 모두 candidate-v1 `profile_carb_floor_unresolved_by_target_constraints`를 가진다.
- `targeted_maintain_mixed_contract`는 maintain/non-bodybuilding profile formula contract pending이면서 동시에 mixed candidate-v1 carb-floor unresolved case다.
- v8.0-AT 이후 current runner에서는 general diet protein production recovery 때문에 diet mixed case가 carb floor를 맞추며, maintain mixed 1건만 mixed carb unresolved로 남는다.

수정:

- `runV8ScenarioRunnerTests`의 targeted stress 기대값을 `>=1`에서 정확한 current count로 강화했다.
- maintain mixed case가 mixed carb-floor unresolved와 maintain profile-contract risk를 모두 갖는지 test assertion을 추가했다.
- `index.html` stage/version을 `8.0-AL`로 올렸다.
- `docs/README.md`, `docs/v8_운동프로필_산식_정밀설계.txt`, 이 감사 문서의 stale targeted stress 해석을 현재 runner 기준으로 갱신했다.

검증:

- 직접 runner 추출: `version=8.0-AL`, targeted 8 cases / calculated 5 / constraint-only 3 / mixed unresolved 2 / maintain non-bodybuilding 2 / high priority 3.
- `targeted_maintain_mixed_contract`: `goal=maintain`, `exerciseProfile=mixed`, `profileSession=mixed_strength_cardio`, targeted findings에 `mixed_profile_carb_floor_unresolved_requires_candidate_v2`와 `maintain_non_bodybuilding_profile_formula_contract_pending` 포함.
- `runV8ScenarioRunnerTests`: 1 suite / 26 cases / failed 0.
- full internal suite: 99 suites / 1056 cases / failed 0.

판단:

- 이것은 production formula 변경이 아니라 감사/테스트 고정이다.
- maintain goal production availability를 mixed profile formula completion으로 읽으면 안 된다.
- AL도 full Cartesian 실행 완료, 18개 human-review 대체, full V8 completion을 닫지 않는다.

## 11-37. 2026-06-03 AG 이후 candidate-v2 숫자 역사값 재감사

AL 재감사 이후 같은 원리로 candidate-v2 숫자 문서를 다시 훑었다. 발견한 문제는 `targeted_mixed_carb_unresolved`의 pre-AG `targetDeltaKcal=43.72915555555528` 값이 일부 README/요약 문맥에서 AG~AL profile-owned 기대값처럼 읽힐 수 있다는 점이다.

AG~AL 당시 직접 runner 추출 기준:

- `targeted_mixed_carb_unresolved`: `targetDeltaKcal=128.996060400435`, `targetRateDeltaEquivalentKgPerWeek=0.11726914581857727`, `carbsGPerKgBodyweight=6`.
- `targeted_maintain_mixed_contract`: `targetDeltaKcal=131.76560244395614`, `targetRateDeltaEquivalentKgPerWeek=0.1197869113126874`, `carbsGPerKgBodyweight=6`.
- `low_reliability_user`: `targetDeltaKcal=130.64606033700375`, `targetRateDeltaEquivalentKgPerWeek=0.11876914576091249`, `carbsGPerKgBodyweight=6`.

수정:

- README의 v8.0-N note를 AG-corrected로 바꿔 `43.729`가 pre-AG 역사값임을 명시했다.
- 이 감사 문서의 v8.0-N 본문, v8.0-N 검증 로그, 하단 단계 요약에서 `43.729`를 pre-AG historical 값으로 표시했다.
- `29.446`처럼 같은 숫자가 AA 이후 current runtime에서 다시 확인된 경우에는 숫자 자체가 아니라 stage/source를 기준으로 읽어야 한다고 명시했다.

검증:

- 직접 runner 추출로 위 세 케이스의 AG~AL target delta 값을 확인했다.
- `runV8ScenarioRunnerTests`: 1 suite / 26 cases / failed 0.

판단:

- 이것은 production formula 변경이 아니라 문서/감사 경계 수정이다.
- pre-AG 숫자를 AG~AL profile-owned runner 기대값처럼 재사용하거나, AG~AL 숫자를 AT 이후 current production evidence처럼 재사용하는 것은 운동프로필/세션 우회 문제와 같은 계열의 문서통과 위험이다.
- 이 재감사도 full Cartesian 실행 완료, full V8 completion, 새 외부 운동생리학 근거를 뜻하지 않는다.

## 11-38. 2026-06-03 v8.0-AM full Cartesian campaign runner 재감사

AM에서는 기존 AK exact-range executor 이후 바로 원래 개발 단계로 돌아가지 않고, 사용자가 지적한 "기준 통과용/우회성 증거" 위험을 full Cartesian 증거 운영에도 적용해 재검토했다.

문제:

- AK까지는 exact range를 실행할 수 있었지만, 여러 clean/dirty/truncated pilot을 어떤 기준으로 누적할지 관리하는 campaign layer가 없었다.
- 이 상태에서는 다음 작업자가 clean seed와 dirty pilot을 손으로 섞거나, plan-only 결과를 실행 증거처럼 읽거나, overlap/gap이 있는 ledger를 full closure 근거처럼 과장할 위험이 남았다.

수정:

- `tools/render_audit/run_v8_full_cartesian_campaign.cjs`를 추가했다.
- seed manifest는 `build_v8_full_cartesian_ledger.cjs --file=...` 검증을 통과하고, dirty-source/truncation/overlap blocker가 없을 때만 campaign의 `accepted_seed_manifests/` 아래 `shard_seed_...json` 이름으로 복사한다.
- campaign은 before ledger, next exact-range plan, optional execution, after ledger, `campaign_state.json`을 같은 ignored campaign 폴더에 남긴다.
- 재검토 중 plan-only 실행이 실제 실행 evidence처럼 읽힐 수 있는 출력 문제를 발견했다. 이제 `executedRangeCount=0`이면 `hasExecutionEvidence=false`, `executionEvidenceClean=false`로 기록한다.

검증:

- `node --check .\tools\render_audit\run_v8_full_cartesian_campaign.cjs`
- `node .\tools\render_audit\run_v8_full_cartesian_campaign.cjs --label=am_reaudit_plan_only_probe_after_evidence_flag --seed-dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot,tools\render_audit\v8_full_cartesian_shards\batch_ak_clean_exact_range_pilot --shard-size=8 --max-ranges=2 --plan-only`
- plan-only probe 결과: seed accepted 4 / rejected 0, before unique 32, contiguous from zero 24, planned ranges 2, planned uncovered 16, first planned range 24~32, executed ranges 0, executed cases 0, after first gap 24~1152, dirty-source manifests 0, truncated manifests 0, closure blockers `coverage_gaps_present`, `full_cartesian_coverage_incomplete`, `hasExecutionEvidence=false`, `executionEvidenceClean=false`.
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` -> 1 suite / 26 cases / failed 0.
- `node .\tools\render_audit\run_internal_tests.cjs` -> 99 suites / 1056 cases / failed 0.

판단:

- AM은 산식 변경이나 HTML runner stage 변경이 아니다. 현재 `runV8ScenarioRunner()` version은 AL로 유지된다.
- AM은 full Cartesian 전수 실행을 더 쉽게 주장하는 단계가 아니라, 실행 증거를 더 좁고 정직하게 누적하기 위한 운영 장치다.
- 따라서 `full_8_2_cartesian_execution`과 `full_v8_completion`은 계속 open이다.

## 11-39. 2026-06-03 v8.0-AN V8 전체 shortcut 정밀 재감사 체크포인트

AN에서는 AM 재검토 뒤 곧바로 원래 개발 단계로 돌아가지 않고, 운동 프로필/세션 지적의 근본 원리를 V8 전체 단계에 적용해 다시 보았다.

재검토 원리:

- 파생 필드가 맞다고 해서 사용자 입력 경로가 맞는 것은 아니다.
- 테스트 fixture가 정상화로 복구된 값을 보여준다고 해서 실제 사용자가 고른 값이 올바른 것은 아니다.
- screenshot 개수, report-only 숫자, plan-only 결과, shard 계획은 각각 자기 증거 범위를 넘어서면 안 된다.
- 내부 코드 근거와 제품 정책 근거는 외부 운동생리학 근거처럼 말하면 안 된다.
- 기능은 Today 계산, Settings 기본값, Records snapshot, Recent/Coach, backup/import, 시각 QA가 함께 이어질 때만 사용자에게 실질 기능으로 본다.

확인한 내용:

- 운동 프로필/세션: 현재 소스는 `profileSession`을 숨겨진 파생 필드로 두고, 실제 사용자 선택은 `exerciseProfile + routinePlan + routine`이 소유한다. 보디빌딩 고급설정 ON은 기존 루틴 스킴을 유지하고, 보디빌딩/일반 고급설정 OFF는 기존 `REST / PUSH` 기본 운동일 경로를 유지한다. 비보디빌딩 프로필은 러닝/스트렝스/파워빌딩/혼합 전용 routine/session을 계산 입력으로 쓴다.
- 운동 입력과 `targetCal`: 프로필/세션, 강도, 웨이트 시간, 유산소 기본값은 기존 workout input 경로에 들어가므로 `targetCal`을 바꿀 수 있다. "프로필/세션은 targetCal을 바꾸면 안 된다"는 과거 P 해석은 AG에서 잘못된 계약으로 정정된 상태다.
- candidate-v2: Y preflight, Z approval decision, AA production target 적용, AB post-wiring visual QA, AH profile-owned visual evidence 정렬은 단계가 분리되어 있다. AB/AH analyzer는 `exerciseProfile=mixed`, `routinePlan=mixed_balanced`, `routine=mixed_strength_cardio`, `profileSession=mixed_strength_cardio`, `productionTargetCalApplied=true`를 함께 요구하므로 derived session만 맞는 우회 증거는 현재 통과할 수 없다.
- 8-2/8-3 증거: D category sweep, L pairwise, M/AL targeted stress, W 18개 human-review numerical gate, AC~AM shard/campaign 도구는 서로 대체물이 아니다. 80,621,568,000개 전체 조합은 계속 별도 open gate다.
- 근거 등급: D1 이후 문서는 내부 코드 근거, 제품 정책 근거, 외부 운동생리학 근거를 분리한다. maintain의 kcal 중립폭, cardio reflection, score/Coach threshold, work-adjustment fixture, campaign closure flag는 외부 생리학 근거가 아니라 내부 정책/증거 운영 기준으로 남아 있다.
- 앱 연결성: candidate-v2는 target/rate context, score/Coach, Records snapshot/signature, backup/import, user-facing QA, render audit까지 연결되었지만, full Cartesian 실행과 전체 프로필 루틴/세션의 사람 눈 UX 검토는 아직 완료가 아니다.

판단:

- AN 재감사에서 AM 외에 즉시 수정해야 할 추가 source-code shortcut은 새로 발견하지 못했다.
- 다만 이것은 V8 완료 선언이 아니다. 특히 `full_8_2_cartesian_execution`, `full_v8_completion`, 전체 프로필 routine/session 표면의 폭넓은 인간 검토는 계속 open이다.
- 다음 Codex는 이 체크포인트를 "전체 문제 없음"으로 읽으면 안 된다. 이것은 현재까지 발견된 우회성 해석을 막는 재감사 경계선이다.

## 11-40. 2026-06-03 v8.0-AO profile routine surface audit

AO에서는 AN에서 open으로 남긴 "전체 프로필 routine/session 표면의 폭넓은 검토" 중 구조 계약 부분을 먼저 검증하기 위한 report layer를 추가했다.

문제:

- AG/AH 이후 profile-owned routine/session 원칙은 코드와 일부 테스트에 들어갔지만, 모든 프로필의 plan/session/default 구조를 한 번에 훑는 V8 report layer는 없었다.
- 이 상태에서는 보디빌딩 보존, 비보디빌딩 fallback 차단, 러닝 cardio-owned default, mixed 복합 세션 coverage 같은 항목을 다음 작업자가 일부 테스트만 보고 과대해석할 위험이 있었다.

수정:

- `runV8ScenarioRunner()` version을 `8.0-AO`로 올렸다.
- `profileRoutineSurfaceAudit` report layer를 추가했다. stage는 `settings_today_profile_routine_surface_audit_v0`이다.
- audit은 모든 `EXERCISE_PROFILE_OPTIONS` profile에 대해 plan key, session value, derived profileSession, default weight duration, cardio defaults, legacy bodybuilding routine value 사용 여부를 검사한다.
- 보디빌딩은 기존 `ppl_ul / split2 / split3 / split4 / split5` 보존과 basic `REST / PUSH`만 노출되는지를 본다.
- 러닝은 training session이 weight duration을 상속하지 않고 cardio default를 갖는지 본다.
- strength/powerbuilding은 resistance session의 weight-duration default를 본다.
- mixed는 combined, cardio-focus, recovery session coverage가 있는지 본다.

검증:

- 직접 runner audit 결과: profile count 5, pass profile 5, finding count 0.
- bodybuilding legacy plan preserved true, bodybuilding basic general-only true.
- non-bodybuilding profile-owned pass 4 / 4.
- running cardio defaults owned true, mixed combined coverage present true.
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` -> 1 suite / 27 cases / failed 0.
- `node .\tools\render_audit\run_internal_tests.cjs` -> 99 suites / 1057 cases / failed 0.

판단:

- AO는 profile/session 지적을 단순히 문서로만 막지 않고, 실제 profile plan/session/default matrix를 runner evidence에 넣은 단계다.
- 그러나 AO도 사람 눈으로 모든 Settings/Today quick-edit profile surface를 보는 visual review가 아니다.
- 따라서 `full_8_2_cartesian_execution`, `full_v8_completion`, profile routine/session surface의 full human visual review는 계속 open이다.

## 11-41. 2026-06-03 v8.0-AP whole-stage evidence boundary audit

AP에서는 사용자가 지적한 profile/session 우회 문제의 근본 원리를 V8 전체 단계에 적용했다. 핵심은 "어떤 증거가 무엇을 증명하는가"와 "무엇을 절대 대체하지 않는가"를 runner 내부에서 다시 점검하게 만드는 것이다.

문제:

- AN/AO에서 파생값, fixture, planning artifact, 내부 제품 정책, runtime 관찰, visual QA, full completion을 섞어 읽지 말라는 원칙은 문서와 일부 테스트에 있었다.
- 그러나 전체 V8 report layer를 한 번에 훑어 "report-only / production-observation / visual-QA / user-path / full-completion" 경계가 아직 살아 있는지 확인하는 별도 report layer는 없었다.
- 이 상태에서는 다음 작업자가 AA/AB production 적용, W/X 18개 numeric gate, AC~AM full Cartesian tooling, AO structural profile audit 중 하나를 full V8 완료처럼 과대해석할 위험이 남는다.

수정:

- `runV8ScenarioRunner()` version을 `8.0-AP`로 올렸다.
- `wholeStageEvidenceBoundaryAudit` report layer를 추가했다. stage는 `whole_stage_evidence_boundary_audit_v0`이다.
- audit은 15개 경계 체크를 수행한다.
  - top-level runner가 report-only이고 full Cartesian 실행을 주장하지 않는지.
  - axis category sweep, pairwise, targeted stress, full Cartesian contract가 서로 다른 bounded evidence인지.
  - current formula report가 internal evidence로 남고 외부 운동생리학 근거처럼 읽히지 않는지.
  - candidate design/v0/v1/v2 comparison이 approval/application으로 읽히지 않는지.
  - maintain production goal이 profile formula/full V8 완료로 읽히지 않는지.
  - candidate-v2 contract가 internal app contract evidence이며 직접 wiring이 아닌지.
  - runtime user-facing QA가 post-wiring visual QA/full completion을 대체하지 않는지.
  - human numerical gate와 preflight가 owner signoff/production wiring으로 읽히지 않는지.
  - approval decision과 AA/AB 적용이 있더라도 full 8-2 Cartesian/full V8은 계속 open인지.
  - AO profile routine structural audit이 human visual review를 대체하지 않는지.

검증:

- 직접 runner audit 결과: check count 15, pass 15, finding count 0.
- full 8-2 Cartesian execution closed false, full V8 completion closed false.
- production application separated from full completion true.
- internal basis separated from external evidence true.
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests` -> 1 suite / 28 cases / failed 0.
- `node .\tools\render_audit\run_internal_tests.cjs` -> 99 suites / 1058 cases / failed 0.

판단:

- AP는 "전체 정밀재검토"의 첫 번째 기계적 고정 장치다. 단순 문서 메모가 아니라 runner report에 boundary audit을 넣어 다음 작업자가 증거를 과대해석하기 어렵게 했다.
- AP가 full 8-2 Cartesian 실행, full V8 completion, profile routine/session human visual review를 닫지는 않는다.
- AP가 새 산식이나 새 외부 근거를 만든 것도 아니다. 기존 단계의 증거 경계를 확인하고, 내부 코드/제품 정책 근거를 외부 생리학 근거처럼 넘기지 않도록 막는 단계다.

## 11-42. 2026-06-03 v8.0-AQ profile routine input ownership realignment

AQ에서는 Today/Settings 화면에서 드러난 profile/routine/session 입력 소유권 충돌을 수정했다. 이전 상태는 운동 프로필 카드, 루틴/세션 카드, Settings의 루틴 종류, Settings의 요일별 세션 계획이 한 화면 안에서 서로 다른 기본값을 암시할 수 있었다. 특히 Settings에 별도 기본 세션 picker를 두면 요일별 세션 계획과 충돌하고, Today에 별도 프로필 카드가 있으면 Settings가 기본 운동 프로필을 소유한다는 원칙이 흐려진다.

수정:

- Settings는 `exerciseProfile`, `routinePlan`, `routineWeekdaySchedule`을 소유한다.
- Today는 날짜별 `todayRoutinePlan`, `todayRoutineSession`을 소유한다.
- Today의 별도 운동 프로필 카드/입력은 숨겨진 compatibility 상태로만 남기고, 사용자가 보는 표면은 `<프로필 라벨> - 루틴 / 세션` 한 묶음으로 정리했다.
- Settings의 별도 default session picker는 제거했다. 기본 세션 계획은 요일별 세션 계획이 담당한다.
- `profileSession`은 snapshot/draft/report/candidate contract용 파생 필드로 유지한다. 사용자 입력을 우회하는 별도 session selector가 아니다.
- `todayRoutineExplicit`를 추가해 실제 사용자가 Today에서 루틴/세션을 고른 경우와 자동 기본값/기존 golden fixture를 분리했다. 따라서 사용자 선택은 계산 입력에 닿지만, fixture baseline은 의도 없이 broad formula/golden drift를 만들지 않는다.
- 보디빌딩 기본/고급 루틴은 기존 루틴/세션 표면을 보존한다. 비보디빌딩 프로필은 각 프로필 전용 routine plan/session을 노출하고 계산 입력으로 쓴다.

검증:

- `node .\tools\render_audit\run_internal_tests.cjs --profile=full` -> 99 suites / 1060 cases / failed 0.
- `node .\tools\render_audit\capture_render_audit.cjs` -> 65 captures.
- `node .\tools\render_audit\analyze_render_audit.cjs` -> failed 0, desktop 42, mobile 23, required mobile 23, profile routine ownership captures 10, min mobile image height 1601, min unique sample color count 547.

판단:

- AQ는 사용자가 지적한 "기준 통과용 세션 우회" 위험 중 화면 입력 소유권을 닫는 단계다.
- AQ가 full 8-2 Cartesian 실행, full V8 completion, 모든 프로필/루틴/세션 조합의 사람 눈 UX 검토를 닫지는 않는다.
- 다음 구현에서 `profileSession`이나 normalized output만 맞는다는 이유로 사용자 입력 경로 검증을 생략하면 안 된다.

## 11-43. 2026-06-03 v8.0-AQ1 Today detailed fasting weight snapshot hotfix

AQ1에서는 Today 탭의 상세 식사 추가에서 `오늘 공복 체중`을 입력했는데 Records 상세/기준 쪽에서 다른 체중이 보일 수 있는 저장 순서 문제를 수정했다.

원인:

- `saveTodayRecordStartDetailed()`가 `saveMealEntry()`를 먼저 호출했다.
- `saveMealEntry()`는 식사 저장과 동시에 `persistAutoAdherenceForDate(date, calculate())`를 호출해 goalSnapshot과 자동 점수를 만든다.
- 체크된 공복 체중을 Today 계산용 체중에 반영하는 `applyTodayRecordWeightToCalculation()`은 그 뒤에 호출되었다.
- 그래서 Records 공복 체중은 새 입력값이 될 수 있지만, 같은 상세 기록의 `goalSnapshot.weight`는 이전 Today 계산용 체중으로 남을 수 있었다.

수정:

- 상세 식사 입력에 실제 저장할 식사 내용이 있을 때만, 체크된 공복 체중을 `saveMealEntry()` 전에 Today calculation draft에 먼저 반영한다.
- 그 다음 식사 저장과 snapshot/score 계산이 같은 체중 기준으로 실행된다.
- 식사 내용이 비어 있어 저장 실패할 케이스에서는 공복 체중을 선반영하지 않아 빈 식사 검증 실패가 Today 계산용 체중을 조용히 바꾸지 않는다.
- 기존 성공 후 Records 공복 체중/메모 merge는 유지한다.

검증:

- `runTodayRecordStartTests`, `runTodayRecordConfirmationTests`, `runTodayInputActionTests`, `runRecordWeightTodayApplyPromptTests` -> 4 suites / 50 cases / failed 0.
- C3 detailed save regression now asserts Records fasting weight, Today calculation weight, and saved `goalSnapshot.weight` all equal the checked detailed fasting weight, and that no Records/Today weight mismatch remains.

## 11-44. 2026-06-03 v8.0-AR profile-aware Today training summary

AR에서는 AQ 렌더 캡처를 사람이 다시 보면서 발견한 UX 오해 가능성을 수정했다. 러닝 프로필의 Today quick-open 화면은 `러닝 - 루틴 / 세션`에서 `인터벌`을 제대로 보여주고 유산소도 35분으로 계산했지만, 상단 요약 카드가 `오늘 웨이트 / 휴식`처럼 보일 수 있었다. 사용자는 이 카드를 오늘 운동 전체 요약으로 읽을 수 있으므로, 러닝인데 오늘 운동이 휴식인 것처럼 오해할 위험이 있다.

수정:

- Today 상단 계산 summary의 해당 row를 `오늘 웨이트`에서 `오늘 훈련`으로 바꿨다.
- summary 값은 현재 `exerciseProfile + routinePlan + routine`에서 찾은 routine/session label을 사용한다.
- `weightDuration=0`이어도 routine이 `REST`가 아니면 휴식으로 표시하지 않는다. 예: 러닝 인터벌은 `오늘 훈련 / 인터벌`로 보인다.
- 하단의 유산소 소모, 웨이트 시간, 오늘 소비 기준 summary는 유지한다. 즉 계산 근거는 사라지지 않고, 상단만 운동 전체 문맥으로 바뀐다.
- render audit runtime meta에 `todayCalcSummaryText`를 추가했고, analyzer가 10개 profile routine ownership capture에서 `오늘 훈련`과 기대 session term을 확인한다.

검증:

- `runTodayQuickEditTests`, `runTodayCalculationOwnershipTests` -> 2 suites / 58 cases / failed 0.
- `capture_render_audit.cjs` -> 65 captures.
- `analyze_render_audit.cjs` -> failed 0, profile routine ownership captures 10, summary label/session checks included.

판단:

- AR은 profile routine/session surface의 사람 눈 UX 검토 중 하나를 실제 UI와 analyzer 계약에 반영한 단계다.
- AR이 모든 profile/routine/session 조합의 인간 검토를 닫지는 않는다. 다만 running/mixed처럼 웨이트 시간이 0인 session이 `휴식`으로 오해되는 대표 위험은 닫는다.

## 11-45. 2026-06-03 v8.0-AS scenario runner training summary audit

### 변경
- `runV8ScenarioRunner()` version/stage를 `8.0-AS`로 갱신했다. AR 이후 문서/HTML은 profile-aware Today training summary를 가리키는데 runner metadata가 AQ에 머무르는 혼선을 제거한다.
- `profileTrainingSummaryAudit`를 추가했다.
  - stage: `profile_aware_today_training_summary_audit_v0`
  - source: `formatTodayQuickTrainingSummary`
  - reportOnly: true
  - productionFormulaChanged: false
- 감사는 별도 문구표를 만들지 않고 현재 code routine/session definition의 label을 읽어 실제 Today summary formatter 출력과 비교한다.

### 감사 케이스
- bodybuilding split5 ARM
- powerbuilding PPL lower
- strength deload
- running interval, `weightDuration=0`
- mixed recovery, `weightDuration=0`

### 판정
- running/mixed의 zero-weight non-rest session은 `REST` summary로 collapse되면 안 된다.
- AS direct runner audit result: 5 cases / pass 5 / findings 0.
- whole-stage evidence boundary audit는 16 checks로 확장되었고, AS formatter audit이 render audit/human visual review/full completion을 대체하지 않는다는 경계를 추가했다.

### 검증
- `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
  - 1 suite / 29 cases / failed 0

### 미종결
- AS는 formatter/report metadata 동기화 단계다.
- render-audit screenshot evidence와 사람 눈 UX 검토를 대체하지 않는다.
- full 8-2 Cartesian execution, full V8 completion은 계속 open이다.

## 11-46. 2026-06-03 V8 장시간 자동 진행 방식 재감사 / 수동 재개 기준

### 배경
- 사용자가 V8 전체 13시간 작업의 진행 방식 자체에 문제가 있다고 지적했다.
- 특히 작은 stage 통과, 문서 요약, 파생 필드, 스크린샷 수, bounded shard 실행이 실제 사용자 가치나 전체 완료로 과대해석되는 위험이 반복되었다.
- 이에 따라 당시에는 추후 장시간 자동 진행 시 바로 새 micro-stage로 들어가지 않도록 별도 재개 플레이북을 작성했다.
- 2026-06-03 v8.0-AU 문서 계층 정리에서 사용자는 `/GOAL` 방식 자체를 폐기한다고 명시했다. 따라서 별도 재개 플레이북은 삭제하고, 필요한 금지선과 current/open gate 기준은 `docs/README.md`와 이 감사 ledger에 남긴다.

### 새 기준문서
- `docs/README.md`
- 이 감사 ledger의 최신 섹션

### 현재 전체 판정
- V8 전체 완료 아님.
- candidate-v2 formula selection / active runtime targetCal application / post-wiring production visual QA는 닫힘.
- `full_8_2_cartesian_execution`, `full_v8_completion`, broad profile/routine/session human UX review는 open.
- 내부 코드/제품정책 근거를 외부 운동생리학 근거처럼 읽는 문제도 신규 근거가 나올 때마다 계속 검토해야 한다.

### 직접 확인한 현재 runner 값
- `meta.version=8.0-AS`
- `meta.fullCartesianRun=false`
- `nominalFullCartesianCaseCount=80,621,568,000`
- `axisCoverageCaseCount=73`
- `humanReviewCaseCount=18`
- `profileFormulaApprovalDecision.summary.productionTargetCalApplied=true`
- `profileFormulaApprovalDecision.summary.productionVisualQaCompleted=true`
- `profileFormulaApprovalDecision.summary.fullV8Complete=false`
- `profileFormulaApprovalDecision.notApprovedYet=["full_8_2_cartesian_execution","full_v8_completion"]`
- `wholeStageEvidenceBoundaryAudit.summary.checkCount=16`, `passCount=16`, `findingCount=0`

### Full Cartesian 현재 증거
- default shard folder ledger:
  - uniqueExecutedCaseCount 32
  - contiguousCoveredFromZero 24
  - dirtySourceManifestCount 15
  - truncatedManifestCount 1
  - firstGap 24~1152
  - fullCoverageCandidate false
- clean campaign `am_an_clean_campaign_execution`:
  - after uniqueExecutedCaseCount 48
  - after contiguousCoveredFromZero 40
  - after firstGap 40~1152
  - dirtySourceManifestCount 0
  - truncatedManifestCount 0
  - fullCoverageCandidate false

### Render audit 현재 증거
- `analyze_render_audit.cjs`:
  - captureCount 65
  - desktop 42
  - mobile 23
  - postWiringProfileCandidateAppliedCaptureCount 8
  - profileRoutineOwnershipCaptureCount 10
  - failedCount 0
- 이 증거는 대표 화면과 analyzer 계약 통과를 뜻한다. 모든 profile/routine/session UX의 full human review가 아니다.

### 다음 수동 작업 강제 시작 순서
- `git status --short --branch`, `git log --oneline -10`
- `docs/README.md`, `docs/v8_운동프로필_산식_정밀설계.txt`, `docs/v8_현재산식_기준갱신_감사_2026-06-02.txt` 읽기
- `runV8ScenarioRunner()` 직접 추출로 현재 gates 확인
- `analyze_render_audit.cjs` 실행
- clean campaign state 확인
- 그 다음에야 새 구현/감사 stage를 시작

### 작업방식 강제 원칙
- 작은 report-only 증거를 전체 완료로 읽지 않는다.
- 기존 기능에 덧붙일 때는 기존 기능 원리를 먼저 파악한다.
- 사용자 소유 입력 경로를 파생 필드로 대체하지 않는다.
- 테스트 통과보다 실제 사용자 흐름이 우선이다.
- hardcoded copy table보다 actual source definition / formatter / DOM / runtime metadata를 읽는다.
- 내부근거와 외부근거를 섞지 않는다.
- 18 human-review, pairwise, targeted stress, bounded shard, render capture는 서로 대체물이 아니다.
- full V8 완료 + git clean + 사용자 명시 지시 전에는 shutdown 예약을 걸지 않는다.

## 12. 검증 메모

실행한 확인:

- index.html 코드 경로 직접 감사
- Playwright 기반 브라우저 로드 후 실제 `calculate()` 결과 확인
- 1913/196.3/180.5/45.1 케이스 재현
- Today-Records 기준갱신 시나리오 직접 확인
- 2026-06-02 보강 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-A 핫픽스 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-A Today record confirmation suite 1 suite / 9 cases / failed 0
- 2026-06-02 v8.0-A mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-A mobile Today record confirmation suite 1 suite / 9 cases / failed 0
- 2026-06-02 v8.0-A smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-A desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-A core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-A desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-B selected basis 정책 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-B Today record confirmation suite 1 suite / 10 cases / failed 0
- 2026-06-02 v8.0-B mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-B mobile Today record confirmation suite 1 suite / 10 cases / failed 0
- 2026-06-02 v8.0-B smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-B desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-B core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-B desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-C record_detail_edit 경계 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-C Today record confirmation suite 1 suite / 11 cases / failed 0
- 2026-06-02 v8.0-C mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-C mobile Today record confirmation suite 1 suite / 11 cases / failed 0
- 2026-06-02 v8.0-C smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-C desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-C core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-C desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-D 시나리오 러너 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-D scenario runner suite 1 suite / 8 cases / failed 0
- 2026-06-02 v8.0-D calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-D desktop calibration 14 suites / 116 cases / failed 0
- 2026-06-02 v8.0-D smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-D desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-D core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-D desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-D1 diff 확인: `git diff --check`
- v8.0-D1 diff check passed
- 2026-06-02 v8.0-D1 근거 등급 report 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-D1 scenario runner suite 1 suite / 8 cases / failed 0
- 2026-06-02 v8.0-D1 calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-D1 desktop calibration 14 suites / 116 cases / failed 0
- 2026-06-02 v8.0-D1 smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-D1 desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-D1 core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-D1 desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-E diff 확인: `git diff --check`
- v8.0-E diff check passed
- 2026-06-03 v8.0-E 9-3 macro audit 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-E scenario runner suite 1 suite / 9 cases / failed 0
- 2026-06-03 v8.0-E macroAudit summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().macroAudit` 추출
- v8.0-E macroAudit 91 cases = axis category fixture 73 + human-review 18, required 1913 findings 5개 확인
- 2026-06-03 v8.0-E calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-E desktop calibration 14 suites / 117 cases / failed 0
- 2026-06-03 v8.0-E smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-E desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-E core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-E desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-F diff 확인: `git diff --check`
- v8.0-F diff check passed
- 2026-06-03 v8.0-F candidateDesign 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-F scenario runner suite 1 suite / 10 cases / failed 0
- 2026-06-03 v8.0-F calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-F desktop calibration 14 suites / 118 cases / failed 0
- 2026-06-03 v8.0-F smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-F desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-F core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-F desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-G diff 확인: `git diff --check`
- v8.0-G diff check passed
- 2026-06-03 v8.0-G candidate formula comparison 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-G scenario runner suite 1 suite / 11 cases / failed 0
- 2026-06-03 v8.0-G candidateFormulaComparison summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().candidateFormulaComparison` 추출
- v8.0-G candidateFormulaComparison 91 cases, targetChangedCount 0, proteinReducedCount 77, carbsIncreasedCount 77, required 1913 candidate 1913.3kcal / 150.4g protein / 226.4g carbs / 45.1g fat 확인
- 2026-06-03 v8.0-G calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-G desktop calibration 14 suites / 119 cases / failed 0
- 2026-06-03 v8.0-G smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-G desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-G core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-G desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-H diff 확인: `git diff --check`
- v8.0-H diff check passed
- 2026-06-03 v8.0-H profileSession carbohydrate policy 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-H scenario runner suite 1 suite / 12 cases / failed 0
- 2026-06-03 v8.0-H profileSessionCarbPolicy summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().profileSessionCarbPolicy` 추출
- v8.0-H profileSessionCarbPolicy 91 cases, profileFormulaPendingCount 13, productionFallbackCount 13, currentBelowReviewFloorCount 56, candidateBelowReviewFloorCount 50, highPriorityCaseCount 3 확인
- 2026-06-03 v8.0-H calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-H desktop calibration 14 suites / 120 cases / failed 0
- 2026-06-03 v8.0-H smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-H desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-H core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-H desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-I diff 확인: `git diff --check`
- v8.0-I diff check passed
- 2026-06-03 v8.0-I maintain production contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-I scenario runner suite 1 suite / 13 cases / failed 0
- 2026-06-03 v8.0-I calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-I desktop calibration 14 suites / 121 cases / failed 0
- 2026-06-03 v8.0-I smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-I desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-I core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-I desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-J diff 확인: `git diff --check`
- v8.0-J diff check passed
- 2026-06-03 v8.0-J maintain production goal 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-J scenario runner suite 1 suite / 13 cases / failed 0
- 2026-06-03 v8.0-J impacted suite 확인:
  - `runTodayQuickEditTests` 26 cases / failed 0
  - `runAdherenceScoringTests` 44 cases / failed 0
  - `runDailyCoachRecentContextTests` 15 cases / failed 0
  - `runDataManagementBackupTests` 7 cases / failed 0
  - `runTargetMacroProductionPolicyTests` 8 cases / failed 0
  - `runMacroAllocationExplanationTests` 9 cases / failed 0
  - `runFullBackupMealsRestoreTests` 10 cases / failed 0
- 2026-06-03 v8.0-J calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-J desktop calibration 14 suites / 121 cases / failed 0
- 2026-06-03 v8.0-J smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-J desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-J core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-J desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-J UI 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=ui`
- v8.0-J desktop ui 18 suites / 144 cases / failed 0
- 2026-06-03 v8.0-J Browser 로컬 확인: `http://127.0.0.1:8765/index.html` 임시 서버로 로드해 Settings 목표 chip 확인
  - `유지` goal chip visible 1개 확인
  - `유지` 선택 후 chip class `chip active` 확인
  - 화면 text에서 `NaN` / `undefined` 없음 확인
  - 임시 서버 종료 확인
- 2026-06-03 v8.0-K diff 확인: `git diff --check`
- v8.0-K diff check passed
- 2026-06-03 v8.0-K profile macro candidate-v1 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-K scenario runner suite 1 suite / 14 cases / failed 0
- 2026-06-03 v8.0-K profile macro candidate-v1 직접 추출:
  - `profileMacroCandidateComparison.summary.caseCount=91`
  - `targetChangedCount=0`
  - `profileCarbFloorAttemptedCount=3`
  - `profileCarbFloorMetCount=0`
  - `profileCarbFloorUnresolvedCount=3`
  - `carbsIncreasedFromContextCount=0`
  - `resistanceReviewOnlyCount=79`
  - `highPriorityCaseCount=3`
  - resistance carbohydrate source가 Escobar / VanDusseldorp / Kerksick 2016으로 정정된 것 확인
- 2026-06-03 v8.0-K calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-K desktop calibration 14 suites / 122 cases / failed 0
- 2026-06-03 v8.0-K smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-K desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-K core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-K desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-K full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-K desktop full 99 suites / 1042 cases / failed 0
- 2026-06-03 v8.0-L pairwise stress 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-L scenario runner suite 1 suite / 15 cases / failed 0
- 2026-06-03 v8.0-L pairwise stress 직접 추출:
  - `pairwiseStress.summary.includedPairRequirementCount=2472`
  - `excludedPairRequirementCount=49`
  - `generatedTupleCount=50`
  - `calculatedCaseCount=50`
  - `coveredPairRequirementCount=2472`
  - `missingPairRequirementCount=0`
  - `effectiveTupleMismatchCaseCount=0`
  - `derivedTupleMismatchCaseCount=31`
  - `inactiveTupleMismatchCaseCount=13`
- 2026-06-03 v8.0-L calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-L desktop calibration 14 suites / 123 cases / failed 0
- 2026-06-03 v8.0-L full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-L desktop full 99 suites / 1043 cases / failed 0
- 2026-06-03 v8.0-M targeted stress 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-M scenario runner suite 1 suite / 16 cases / failed 0
- 2026-06-03 v8.0-M targeted stress 직접 추출:
  - `targetedStress.summary.caseCount=8`
  - `calculatedCaseCount=5`
  - `constraintOnlyCaseCount=3`
  - `derivedFfmiConflictCount=1`
  - `inactiveIntensityCaseCount=1`
  - `mixedCarbUnresolvedCaseCount=2`
  - `maintainNonBodybuildingCaseCount=2`
  - `highPriorityCaseCount=3`
- 2026-06-03 v8.0-M calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-M desktop calibration 14 suites / 124 cases / failed 0
- 2026-06-03 v8.0-M full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-M desktop full 99 suites / 1044 cases / failed 0
- 2026-06-03 v8.0-N profile macro candidate-v2 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-N scenario runner suite 1 suite / 17 cases / failed 0
- 2026-06-03 v8.0-N profile macro candidate-v2 직접 추출:
  - `profileMacroCandidateV2Comparison.summary.caseCount=96`
  - `targetChangedCount=5`
  - `profileCarbFloorAttemptedCount=5`
  - `profileCarbFloorMetCount=5`
  - `profileCarbFloorUnresolvedCount=0`
  - `targetRateContractRequiredCount=5`
  - `proteinGuardConflictCount=4`
  - pre-AG historical `targeted_mixed_carb_unresolved.targetDeltaKcal=43.72915555555528`
  - pre-AG historical `targeted_mixed_carb_unresolved.targetRateDeltaEquivalentKgPerWeek=0.03975377777777753`
- 2026-06-03 v8.0-N calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-N desktop calibration 14 suites / 125 cases / failed 0
- 2026-06-03 v8.0-N full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-N desktop full 99 suites / 1045 cases / failed 0
- 2026-06-03 v8.0-O profile macro candidate-v2 production contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-O scenario runner suite 1 suite / 18 cases / failed 0
- 2026-06-03 v8.0-O profileMacroCandidateV2Contract 직접 추출:
  - `profileMacroCandidateV2Contract.summary.contractCount=14`
  - `presentCount=3`
  - `missingContractCount=11`
  - `productionBlockerCount=12`
  - `reportOnlyGuardCount=2`
  - `settingsTodayContractMissingCount=3`
  - `snapshotFieldMissingCount=12`
  - `targetRateContractMissingCount=1`
  - `scoreCoachContractMissingCount=3`
  - `recentContextContractMissingCount=1`
  - `backupImportContractMissingCount=1`
  - `unresolvedProteinGuardConflictCount=4`
  - `productionReady=false`
- 2026-06-03 v8.0-O calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-O desktop calibration 14 suites / 126 cases / failed 0
- 2026-06-03 v8.0-O full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-O desktop full 99 suites / 1046 cases / failed 0
- 2026-06-03 v8.0-P profile/session snapshot contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-P scenario runner suite 1 suite / 19 cases / failed 0
- 2026-06-03 v8.0-P profileMacroCandidateV2Contract 직접 추출:
  - `profileMacroCandidateV2Contract.summary.contractCount=14`
  - `presentCount=9`
  - `missingContractCount=5`
  - `productionBlockerCount=6`
  - `reportOnlyGuardCount=2`
  - `settingsTodayContractMissingCount=0`
  - `snapshotFieldMissingCount=0`
  - `targetRateContractMissingCount=1`
  - `scoreCoachContractMissingCount=3`
  - `recentContextContractMissingCount=1`
  - `backupImportContractMissingCount=0`
  - `unresolvedProteinGuardConflictCount=4`
  - `productionReady=false`
- 2026-06-03 v8.0-P calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-P desktop calibration 14 suites / 127 cases / failed 0
- 2026-06-03 v8.0-P full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-P desktop full 99 suites / 1047 cases / failed 0
- 2026-06-03 v8.0-Q target-rate recentContext candidate-delta contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-Q scenario runner suite 1 suite / 19 cases / failed 0
- 2026-06-03 v8.0-Q profileMacroCandidateV2Contract 직접 추출:
  - `profileMacroCandidateV2Contract.summary.contractCount=14`
  - `presentCount=11`
  - `missingContractCount=3`
  - `productionBlockerCount=4`
  - `reportOnlyGuardCount=2`
  - `settingsTodayContractMissingCount=0`
  - `snapshotFieldMissingCount=0`
  - `targetRateContractMissingCount=0`
  - `scoreCoachContractMissingCount=3`
  - `recentContextContractMissingCount=0`
  - `backupImportContractMissingCount=0`
  - `unresolvedProteinGuardConflictCount=4`
  - `productionReady=false`
  - `targetRateProbe.appliedToTarget=false`
  - `targetRateProbe.targetCalUnchangedByProfileCandidateDelta=true`
  - `recentContextGateProbe.eligibleStatus=eligible`
  - `recentContextGateProbe.eligibleCanApplyAutomatically=false`
  - `recentContextGateProbe.blockedCoverageReasons=recent28_goal_snapshot_coverage_below_0_70`
  - `recentContextGateProbe.blockedGoalChangeReasons=recent28_goal_change_detected`
- 2026-06-03 v8.0-Q calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-Q desktop calibration 14 suites / 127 cases / failed 0
- 2026-06-03 v8.0-Q full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-Q desktop full 99 suites / 1047 cases / failed 0
- 2026-06-03 v8.0-R score Coach candidate-v2 contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-R scenario runner suite 1 suite / 19 cases / failed 0
- 2026-06-03 v8.0-R profileMacroCandidateV2Contract 직접 추출:
  - `profileMacroCandidateV2Contract.summary.contractCount=14`
  - `presentCount=14`
  - `missingContractCount=0`
  - `productionBlockerCount=1`
  - `reportOnlyGuardCount=2`
  - `settingsTodayContractMissingCount=0`
  - `snapshotFieldMissingCount=0`
  - `targetRateContractMissingCount=0`
  - `scoreCoachContractMissingCount=0`
  - `recentContextContractMissingCount=0`
  - `backupImportContractMissingCount=0`
  - `unresolvedProteinGuardConflictCount=4`
  - `productionReady=false`
  - `scoreCoachProbe.policyKey=profileCandidateV2`
  - `scoreCoachProbe.scoreProfileUsesDietProductPolicy=true`
  - `scoreCoachProbe.coachThresholdUsesDietMacroThresholds=true`
  - `scoreCoachProbe.firstSelectedDecisionType=profile_guard_conflict`
  - `scoreCoachProbe.outputFirstDecisionType=profile_guard_conflict`
  - `scoreCoachProbe.outputMentionsInternalCandidateName=false`
- 2026-06-03 v8.0-R calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-R desktop calibration 14 suites / 127 cases / failed 0
- 2026-06-03 v8.0-R full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-R desktop full 99 suites / 1047 cases / failed 0
- 2026-06-03 v8.0-S protein guard candidate-v2 resolution contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-S scenario runner suite 1 suite / 19 cases / failed 0
- 2026-06-03 v8.0-S profileMacroCandidateV2Comparison / Contract 직접 추출:
  - `profileMacroCandidateV2Comparison.summary.proteinGuardConflictCount=0`
  - `proteinGuardResolvedCount=4`
  - `proteinLegacyGuideConflictCount=4`
  - `fatGuardConflictCount=0`
  - `fatGuardResolvedCount=5`
  - `profileMacroCandidateV2Contract.summary.contractCount=14`
  - `presentCount=14`
  - `missingContractCount=0`
  - `productionBlockerCount=0`
  - `reportOnlyGuardCount=2`
  - `settingsTodayContractMissingCount=0`
  - `snapshotFieldMissingCount=0`
  - `targetRateContractMissingCount=0`
  - `scoreCoachContractMissingCount=0`
  - `recentContextContractMissingCount=0`
  - `backupImportContractMissingCount=0`
  - `unresolvedProteinGuardConflictCount=0`
  - `resolvedProteinGuardPolicyCount=4`
  - `productionReady=true`
- 2026-06-03 v8.0-S calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-S desktop calibration 14 suites / 127 cases / failed 0
- 2026-06-03 v8.0-S full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-S desktop full 99 suites / 1047 cases / failed 0
- 2026-06-03 v8.0-T runtime candidate-v2 target-delta proposal 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-T scenario runner suite 1 suite / 20 cases / failed 0
- 2026-06-03 v8.0-T calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-T desktop calibration 14 suites / 128 cases / failed 0
- 2026-06-03 v8.0-T full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-T desktop full 99 suites / 1048 cases / failed 0
- 2026-06-03 v8.0-U runtime user-facing QA 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-U scenario runner suite 1 suite / 21 cases / failed 0
- 2026-06-03 v8.0-U calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-U desktop calibration 14 suites / 129 cases / failed 0
- 2026-06-03 v8.0-U full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-U desktop full 99 suites / 1049 cases / failed 0
- 2026-06-03 v8.0-U profileFormulaUserFacingQa 직접 추출: `version=8.0-U`, `stage=9-9_profile_formula_user_facing_qa_v0`, `targetDeltaKcal=25.029155555555462`, `coachHasProfileTargetDeltaDecision=true`, `recordDetailHasProfileTargetDelta=true`, `recordDetailHasProfileTargetRateDelta=true`, `surfaceForbiddenTermCount=0`, `targetDeltaApplied=false`, `canApplyAutomatically=false`
- 2026-06-03 v8.0-V render audit capture 확인: `node .\tools\render_audit\capture_render_audit.cjs`
- v8.0-V render audit capture 47 captures 생성: desktop 33 / mobile 14
- 2026-06-03 v8.0-V render audit manifest analyzer 확인: `node .\tools\render_audit\analyze_render_audit.cjs`
- v8.0-V render audit analyzer `failedCount=0`, `minMobileImageHeight=1601`, `maxMobileImageHeight=4909`, `minUniqueSampleColorCount=547`, `minLuminanceStdDev=24.244`
- 2026-06-03 v8.0-V manual mobile screenshot spot-check: `34_mobile_today_rich.png`, `36_mobile_records.png`, `40_mobile_settings.png`
- 2026-06-03 v8.0-V full internal suite 재확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-V desktop full 99 suites / 1049 cases / failed 0
- 2026-06-03 v8.0-W human-review numerical approval gate 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-W scenario runner suite 1 suite / 22 cases / failed 0
- 2026-06-03 v8.0-W humanReviewNumericalApproval 직접 확인: `humanReviewCaseCount=18`, `numericApprovedCount=17`, `blockedCount=1`, `blockedCaseIds=["low_reliability_user"]`, `requiredRegressionNumericApproved=true`, `productionFormulaApprovalReady=false`
- 2026-06-03 v8.0-W calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-W desktop calibration 14 suites / 130 cases / failed 0
- 2026-06-03 v8.0-W full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-W desktop full 99 suites / 1050 cases / failed 0
- 2026-06-03 v8.0-X candidate-v2 fat floor consistency 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-X scenario runner suite 1 suite / 22 cases / failed 0
- 2026-06-03 v8.0-X humanReviewNumericalApproval 직접 확인: `numericApprovedCount=18`, `blockedCount=0`, `productionFormulaApprovalReady=true`, `candidateFormulaApproved=false`, `productionFormulaChanged=false`
- 2026-06-03 v8.0-X low_reliability_user 직접 확인: `targetDeltaKcal=51.44606338171707`, `fatG=55.215686054758734`, `fatPercentKcal≈15`, `carbsGPerKgBodyweight=6`
- 2026-06-03 v8.0-X calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-X desktop calibration 14 suites / 130 cases / failed 0
- 2026-06-03 v8.0-X full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-X desktop full 99 suites / 1050 cases / failed 0
- 2026-06-03 v8.0-Y profile formula approval preflight 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-Y scenario runner suite 1 suite / 23 cases / failed 0
- 2026-06-03 v8.0-Y profileFormulaApprovalPreflight 직접 확인: `checkCount=5`, `readyCheckCount=5`, `blockedCount=0`, `approvalPreflightReady=true`, `readyForExplicitFormulaApprovalDecision=true`, `candidateFormulaApproved=false`, `productionFormulaChanged=false`, `productionWiringAllowed=false`, `productionTargetCalApplied=false`, `productionVisualQaCompleted=false`
- 2026-06-03 v8.0-Y calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-Y desktop calibration 14 suites / 131 cases / failed 0
- 2026-06-03 v8.0-Y full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-Y desktop full 99 suites / 1051 cases / failed 0
- 2026-06-03 v8.0-Z profile formula approval decision 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-Z scenario runner suite 1 suite / 24 cases / failed 0
- 2026-06-03 v8.0-Z profileFormulaApprovalDecision 직접 확인: `candidateFormulaApproved=true`, `approvedCandidateModel=candidate-v8-profile-macro-v2-linked-target-v0`, `productionWiringAllowed=true`, `productionFormulaChanged=false`, `productionTargetCalApplied=false`, `productionVisualQaCompleted=false`, `fullV8Complete=false`
- 2026-06-03 v8.0-Z calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-Z desktop calibration 14 suites / 132 cases / failed 0
- 2026-06-03 v8.0-Z full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-Z desktop full 99 suites / 1052 cases / failed 0
- 2026-06-03 v8.0-AA production candidate-v2 target wiring 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AA scenario runner suite 1 suite / 24 cases / failed 0
- 2026-06-03 v8.0-AA direct runtime QA 확인: `productionTargetCal=3312.9411753385825`, `proposedCandidateTargetCal=3312.9411753385825`, `targetDeltaKcal=29.446064227471197`, `targetDeltaApplied=true`, `productionTargetCalApplied=true`, `recentGateStatus=applied`, `coachHasProfileTargetDeltaAppliedDecision=true`
- 2026-06-03 v8.0-AA sandbox restore regression 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests,runTargetCalSemanticGuardTests`
- v8.0-AA scenario + targetCal semantic guard suites 2 suites / 32 cases / failed 0
- 2026-06-03 v8.0-AA calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-AA desktop calibration 14 suites / 132 cases / failed 0
- 2026-06-03 v8.0-AA full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AA desktop full 99 suites / 1052 cases / failed 0
- 2026-06-03 v8.0-AB post-wiring render capture 확인: `node .\tools\render_audit\capture_render_audit.cjs`
- v8.0-AB render capture 55 captures = desktop 37 + mobile 18
- 2026-06-03 v8.0-AB render analyzer 확인: `node .\tools\render_audit\analyze_render_audit.cjs`
- v8.0-AB render analyzer `failedCount=0`, `postWiringProfileCandidateCaptureCount=8`, `postWiringProfileCandidateAppliedCaptureCount=8`, `minUniqueSampleColorCount=547`, `minLuminanceStdDev=22.743`
- 2026-06-03 v8.0-AB post-wiring runtime manifest 직접 추출: 8 captures all `selectedMacroBasis=profile_candidate_v2`, `productionTargetCalApplied=true`, `recentGateStatus=applied`, `targetCal=3312.9411753385825`, `targetDeltaKcal=29.446064227471197`
- 2026-06-03 v8.0-AB screenshot spot-check: `52_mobile_today_profile_candidate_v2_applied.png`, `54_mobile_records_profile_candidate_v2_detail.png`, `55_desktop_records_profile_candidate_v2_basis_open.png`, `56_mobile_records_profile_candidate_v2_basis_open.png`
- 2026-06-03 v8.0-AB scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AB scenario runner suite 1 suite / 24 cases / failed 0
- 2026-06-03 v8.0-AB full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AB desktop full 99 suites / 1052 cases / failed 0
- 2026-06-03 v8.0-AC scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AC scenario runner suite 1 suite / 25 cases / failed 0
- 2026-06-03 v8.0-AC full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AC desktop full 99 suites / 1053 cases / failed 0
- 2026-06-03 v8.0-AD scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AD scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AD coherent shard pilot: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=0 --limit=8 --max-cases=8`
- v8.0-AD coherent shard decoded 8 / calculated 8 / constraint-only 0 / fullCartesianRun=false
- 2026-06-03 v8.0-AD constraint shard pilot: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=1152 --limit=3 --max-cases=3`
- v8.0-AD constraint shard decoded 3 / calculated 0 / constraint-only 3 / fullCartesianRun=false
- 2026-06-03 v8.0-AD full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AD desktop full 99 suites / 1054 cases / failed 0
- 2026-06-03 v8.0-AE scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AE scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AE coherent shard manifest: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=0 --limit=8 --max-cases=8`
- v8.0-AE coherent manifest schema `v8_full_cartesian_shard_manifest_v1`, decoded 8 / calculated 8 / constraint-only 0 / calculation issues 0 / fullCartesianRun=false
- 2026-06-03 v8.0-AE constraint shard manifest: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --start=1152 --limit=3 --max-cases=3`
- v8.0-AE constraint manifest schema `v8_full_cartesian_shard_manifest_v1`, decoded 3 / calculated 0 / constraint-only 3 / calculation issues 0 / fullCartesianRun=false
- 2026-06-03 v8.0-AE shard-index route 확인: `node .\tools\render_audit\run_v8_full_cartesian_shard.cjs --shard-index=0 --shard-size=8 --max-cases=8`
- v8.0-AE shard-index route decoded 8 / calculated 8 / constraint-only 0 / fullCartesianRun=false
- 2026-06-03 v8.0-AE shard analyzer 확인: `node .\tools\render_audit\analyze_v8_full_cartesian_shards.cjs`
- v8.0-AE shard analyzer 2 manifests / failed 0 / decoded 11 / calculated 8 / constraint-only 3 / truncated 0
- 2026-06-03 v8.0-AE full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AE desktop full 99 suites / 1054 cases / failed 0
- 2026-06-03 v8.0-AF scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AF scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AF batch pilot: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=af_pilot`
- v8.0-AF batch pilot schema `v8_full_cartesian_batch_manifest_v1`, shard runs 2 / analyzer failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0 / fullCartesianRun=false
- 2026-06-03 v8.0-AF batch folder analyzer 확인: `node .\tools\render_audit\analyze_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_af_pilot`
- v8.0-AF batch folder analyzer 2 manifests / failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0
- 2026-06-03 v8.0-AF full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AF desktop full 99 suites / 1054 cases / failed 0
- 2026-06-03 v8.0-AH scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AH scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AH render audit capture 확인: `node .\tools\render_audit\capture_render_audit.cjs`
- v8.0-AH render audit capture 55 captures
- 2026-06-03 v8.0-AH render audit analyzer 확인: `node .\tools\render_audit\analyze_render_audit.cjs`
- v8.0-AH render audit analyzer 55 captures / 8 post-wiring candidate-v2 captures / failed 0
- 2026-06-03 v8.0-AH full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AH desktop full 99 suites / 1056 cases / failed 0
- 2026-06-03 v8.0-AI 기존 AF batch ledger 확인: `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_af_pilot`
- v8.0-AI 기존 AF batch ledger shard manifests 2 / batch summaries 1 / failed 0 / unique executed 16 / gap 2 / dirty-source manifests 2 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI ledger pilot batch 확인: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=ai_ledger_pilot`
- v8.0-AI ledger pilot batch 2 shards / analyzer failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0 / ledger unique 16 / ledger gap 2 / ledger dirty-source manifests 2 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI clean-source ledger pilot 확인: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,144 --shard-size=8 --max-cases=8 --label=ai_clean_ledger_pilot`
- v8.0-AI clean-source ledger pilot 2 shards / analyzer failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0 / ledger unique 16 / gap 2 / dirty-source manifests 0 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI ledger pilot analyzer 확인: `node .\tools\render_audit\analyze_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_ledger_pilot`
- v8.0-AI ledger pilot analyzer manifests 2 / failed 0 / decoded 16 / calculated 8 / constraint-only 8 / truncated 0
- 2026-06-03 v8.0-AI ledger pilot ledger 확인: `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_ledger_pilot`
- v8.0-AI ledger pilot ledger manifests 2 / failed 0 / unique executed 16 / contiguous from zero 8 / first gap 8~1152 / gap 2 / overlap 0 / dirty-source manifests 2 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI truncation probe batch 확인: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0 --shard-size=100000 --max-cases=8 --label=ai_truncation_probe`
- v8.0-AI truncation probe batch 1 shard / analyzer failed 0 / decoded 8 / calculated 8 / truncated 1 / ledger unique 8 / ledger gap 1 / dirty-source manifests 1 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI truncation probe ledger 확인: `node .\tools\render_audit\build_v8_full_cartesian_ledger.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_truncation_probe`
- v8.0-AI truncation probe ledger manifests 1 / failed 0 / unique executed 8 / truncated manifests 1 / first gap 8~80621568000 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI contiguous probe batch 확인: `node .\tools\render_audit\run_v8_full_cartesian_batch.cjs --shards=0,1 --shard-size=8 --max-cases=8 --label=ai_contiguous_probe_after_fix`
- v8.0-AI contiguous probe batch 2 shards / analyzer failed 0 / decoded 16 / calculated 16 / truncated 0 / ledger unique 16 / gap 1 / overlap 0 / fullCoverageCandidate false
- 2026-06-03 v8.0-AI scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AI scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AI full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AI desktop full 99 suites / 1056 cases / failed 0
- 2026-06-03 v8.0-AJ clean ledger planner 확인: `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-shards=4 --label=aj_plan_clean_pilot`
- v8.0-AJ clean ledger planner gap 2 / dirty-source manifests 0 / planned ranges 4 / planned shard indexes 4 / planned uncovered 32 / duplicate-if-batch 0 / first planned range 8~16 / full execution closed false
- 2026-06-03 v8.0-AJ truncation planner 확인: `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_truncation_probe --shard-size=100000 --max-shards=2 --label=aj_plan_truncation_probe`
- v8.0-AJ truncation planner gap 1 / dirty-source manifests 1 / truncated manifests 1 / planned ranges 2 / planned shard indexes 2 / planned uncovered 199,992 / duplicate-if-batch 8 / first planned exact range 8~100000 / exact range required true / full execution closed false
- 2026-06-03 v8.0-AJ scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AJ scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AJ full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AJ desktop full 99 suites / 1056 cases / failed 0
- 2026-06-03 v8.0-AK exact-range executor 확인: `node .\tools\render_audit\run_v8_full_cartesian_plan.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-ranges=2 --label=ak_exact_range_pilot`
- v8.0-AK exact-range executor executed ranges 2 / executed cases 16 / analyzer failed 0 / execution-folder ledger unique 16 / dirty-source manifests 2 / full execution closed false
- 2026-06-03 v8.0-AK explicit cumulative ledger 확인: selected source clean pilot + AK exact range pilot shard manifests 4
- v8.0-AK explicit cumulative ledger shard manifests 4 / batch summaries 0 / failed 0 / dirty-source manifests 2 / unique executed 32 / contiguous from zero 24 / first gap 24~1152 / fullCoverageCandidate false
- 2026-06-03 v8.0-AK zero-range guard 확인: `node .\tools\render_audit\run_v8_full_cartesian_plan.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-ranges=0 --label=ak_zero_range_guard_probe`
- v8.0-AK zero-range guard executed ranges 0 / executed cases 0 / noRangesSelected true / full execution closed false
- 2026-06-03 v8.0-AK clean-source exact-range executor 확인: `node .\tools\render_audit\run_v8_full_cartesian_plan.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-ranges=2 --label=ak_clean_exact_range_pilot`
- v8.0-AK clean-source exact-range executor executed ranges 2 / executed cases 16 / analyzer failed 0 / execution-folder ledger unique 16 / dirty-source manifests 0 / full execution closed false
- 2026-06-03 v8.0-AK clean-source explicit cumulative ledger 확인: source clean pilot `0~8`, AK clean exact `8~24`, source clean pilot `1152~1160`
- v8.0-AK clean-source explicit cumulative ledger shard manifests 4 / batch summaries 0 / failed 0 / dirty-source manifests 0 / unique executed 32 / contiguous from zero 24 / first gap 24~1152 / fullCoverageCandidate false
- 2026-06-03 v8.0-AK planner recheck 확인: `node .\tools\render_audit\plan_v8_full_cartesian_shards.cjs --dir=tools\render_audit\v8_full_cartesian_shards\batch_ai_clean_ledger_pilot --shard-size=8 --max-shards=2 --label=ak_plan_recheck`
- v8.0-AK planner recheck planned ranges 2 / planned uncovered 16 / duplicate-if-batch 0 / first planned range 8~16 / full execution closed false
- 2026-06-03 v8.0-AK scenario runner 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-AK scenario runner suite 1 suite / 26 cases / failed 0
- 2026-06-03 v8.0-AK full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-AK desktop full 99 suites / 1056 cases / failed 0
- 2026-06-03 v8.0-T runtime mixed high-load probe 직접 확인:
  - production `targetCal=3283.4951111111113`
  - proposed candidate-v2 targetCal `3308.524266666667`
  - `targetDeltaKcal=25.029155555555462`
  - `targetRateDeltaEquivalentKgPerWeek=0.022753777777777692`
  - `appliedToTarget=false`
  - `recentContext.profileCandidateV2TargetDeltaGate.canApplyAutomatically=false`

실행하지 않은 것:

- mobile screenshot audit: v8.0-V first-pass capture/analyzer evidence complete; v8.0-AB post-wiring candidate-v2 production visual QA complete
- candidate formula production approval: v8.0-Z approved candidate-v2 formula selection, AA applies active runtime target-delta cases, and AB verifies post-wiring production visual QA; this is still not full V8 completion
- profile-specific macro formula production implementation: AA production path is active for candidate-v2 target-delta cases and AB verifies Today/Records visual surfaces; broader full-axis execution remains open
- candidate-v2 target delta production formula application: v8.0-AA closes active runtime production `targetCal` application; v8.0-AB closes post-wiring visual QA
- user-facing profile formula QA after candidate-v2 contract readiness: v8.0-U runtime Coach/Records QA complete, v8.0-V first-pass screenshot audit complete, AA post-wiring runtime QA complete, AB production visual QA complete
- 80,621,568,000개 8-2 full cross-product 전수 실행
- 18개 human-review scenario의 explicit formula approval decision: v8.0-Z에서 candidate-v2 formula selection은 approved, v8.0-AA에서 active runtime production targetCal application은 적용됨, v8.0-AB에서 post-wiring visual QA는 완료됨; full V8 completion은 아직 open

최초 작성 시점에는 구현 변경이 없는 감사 산출물이었으므로 전체 테스트를 생략했다.
v8.0-A snapshotless 기준 저장 상태 분리 이후에는 관련 suite, mobile viewport suite, desktop smoke, desktop core를 실행했다.
v8.0-B selected macro basis 정책 고정 이후에는 Today record confirmation suite를 desktop/mobile viewport에서 실행했고, desktop smoke/core 회귀도 다시 실행했다.
v8.0-C record_detail_edit 보호 / 잔재 플래그 정리 이후에는 Today record confirmation suite를 desktop/mobile viewport에서 실행했고, desktop smoke/core 회귀도 다시 실행했다.
v8.0-D 시나리오 러너 1차 구현 이후에는 8-2 필수축 category sweep과 8-3 human-review 분리, 유지 축 fallback 명시, 운동 프로필별 report-only session 분리를 `runV8ScenarioRunnerTests`로 확인했고, calibration/smoke/core 회귀도 다시 실행했다.
v8.0-D1 근거 등급 재감사 이후에는 업무 보정축 active fixture가 `internal_formula_coverage_only` / `report_only_axis_coverage_case_fixture`로 남는지 `runV8ScenarioRunnerTests`에서 확인했고, calibration/smoke/core 회귀도 다시 실행했다.
v8.0-E 9-3 현재 탄단지 감사 레이어 이후에는 `macroAudit`가 report-only / productionFormulaChanged false로 남고, 필수 1913 케이스가 단백질 context mismatch와 carb/fat 내부 충돌을 모두 표시하는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-F 9-5/9-6 candidate 설계 레이어 이후에는 `candidateDesign`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, maintain key, protein context gate, fat/carb joint redistribution, profile-specific carbohydrate policy, profile session input contract가 required next decision에 포함되는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-G 9-7 context macro candidate-v0 비교 이후에는 `candidateFormulaComparison`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, 필수 1913 케이스에서 targetCal은 유지하며 protein 180.5g -> 150.4g, carbs 196.3g -> 226.4g로 바뀌되 remaining findings가 보존되는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-H 9-7 profileSession carbohydrate policy review 이후에는 `profileSessionCarbPolicy`가 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, 당시 running 세션의 cardio-carried load와 UPPER fallback을 분리하며, resistance profile에는 endurance range를 무리하게 적용하지 않는 finding이 남는지 `runV8ScenarioRunnerTests`로 확인했다. v8.0-AG 이후 현재 runner는 이 fallback을 제거하고 `productionFallbackCount=0`을 검증한다.
v8.0-I 9-6 maintain production contract review 당시에는 `maintainGoalContract`가 report-only / productionFormulaChanged false였고, maintain production approval은 의도적으로 열지 않았다. 이 단계는 `maintain -> recomp` report fallback과 `lean_bulk` silent fallback 위험을 분리하며, `GOALS.maintain` 단독 추가를 막는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-J maintain production goal 1차 이후에는 `maintainGoalContract`가 report-only / productionFormulaChanged false로 남되 `productionMaintainGoalAvailable=true`, `maintainProductionApproved=true`, `missingContractCount=0`, `currentAxisFallbackGoal=null`로 바뀌는지 확인했다. 이 승인은 maintain production goal 계약에 한정되며, profile-specific macro formula 또는 전체 v8 완료를 뜻하지 않는다.
v8.0-K profile macro candidate-v1 이후에는 `profileMacroCandidateComparison`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, running/mixed 탄수 floor를 targetCal 변경 없이 맞출 수 있는지 검토했다. 직접 추출 결과 mixed floor 시도 3건은 모두 target/protein/fat 보호선 때문에 unresolved로 남았고, 이는 candidate-v2에서 target/rate와 protein/fat guard를 함께 열어야 한다는 근거다.
v8.0-L pairwise stress 이후에는 8-2 full Cartesian을 실행했다고 주장하지 않으면서, 제약상 가능한 pair 2,472개를 50개 계산 케이스로 모두 덮는지 확인했다. 직접 제어 가능한 tuple mismatch는 0이며, FFMI 파생축과 rest-day inactive intensity는 별도 mismatch로 분리했다.
v8.0-M targeted stress 이후에는 pairwise/profile macro residual signal을 계산 가능한 5개 targeted case와 계산하면 안 되는 3개 constraint-only case로 분리했다. v8.0-M~AL 당시 AG+ profile-owned runner 기준 mixed focus는 2건이고 실제 mixed carb unresolved finding도 2건이었다. v8.0-AT 이후 current mixed carb unresolved finding은 maintain mixed 1건이며, maintain mixed는 maintain/non-bodybuilding contract risk와 mixed candidate-v1 carb-floor unresolved risk가 함께 남으므로 maintain goal production availability를 mixed formula 완료로 읽으면 안 된다. 이 단계도 full Cartesian, 18개 human-review 대체, candidate production approval이 아니다.
v8.0-N profile macro candidate-v2 이후에는 mixed carb floor를 target delta로 맞출 수 있는지 report-only로 계산했다. pre-AG targeted mixed case는 +43.729kcal, 주간 rate equivalent +0.0398kg/week로 6g/kg에 도달했다. AG~AL 당시 profile-owned mixed session 기준 직접 추출값은 +128.996kcal, 주간 rate equivalent +0.1173kg/week, 6g/kg이며, 어느 쪽도 production contract 없이 production formula가 아니다. AT 이후 general diet production protein은 별도로 복구됐으므로 이 숫자를 current production diet protein 문제의 증거로 재사용하지 않는다.
v8.0-O profile macro candidate-v2 production contract review 이후에는 candidate-v2 productionReady=false를 코드 계약으로 확인했다. Settings/Today input, target-rate delta, Records snapshot/signature, score, Coach, recentContext, backup/import 계약 11개가 missing이며, unresolved protein guard conflict까지 포함해 productionBlockerCount=12다.
v8.0-P profile/session snapshot contract 이후에는 Settings/Today exerciseProfile/profileSession derived field, Today draft persistence, Records snapshot/signature, backup/import roundtrip을 production data path로 닫았다. v8.0-AG에서 이 항목은 "profileSession 별도 입력"이 아니라 "routine/session에서 파생되는 스냅샷 필드"로 정정됐다. 그러나 target-rate delta, score, Coach, recentContext 계약 5개와 unresolved protein guard conflict가 남아 productionBlockerCount=6이며, candidate-v2는 여전히 report-only다.
v8.0-Q target-rate recentContext candidate-delta contract 이후에는 candidate-v2 target/rate delta metadata와 recent28 gate contract를 닫았다. 그러나 score/Coach 계약 3개와 unresolved protein guard conflict가 남아 productionBlockerCount=4이며, candidate-v2 target delta는 production `targetCal`에 적용되지 않는다.
v8.0-R score Coach candidate-v2 contract 이후에는 score/Coach 계약 3개도 닫혀 `missingContractCount=0`, `scoreCoachContractMissingCount=0`이 되었다. 그러나 unresolved protein/fat guard behavior가 `productionBlockerCount=1`로 남아 있고, candidate-v2 target delta는 여전히 production `targetCal`에 적용되지 않는다.
v8.0-S protein guard candidate-v2 resolution contract 이후에는 legacy 2.2g/kg BW diet guide floor와 ISSN general range를 분리해 당시 `proteinGuardConflictCount=0`, `proteinGuardResolvedCount=4`, `productionBlockerCount=0`, `productionReady=true`가 되었다. v8.0-AT 이후 production diet guide floor 자체가 복구되어 current `proteinGuardResolvedCount`와 `proteinLegacyGuideConflictCount`는 0이다. 그러나 candidate-v2 target delta는 여전히 production `targetCal`에 적용되지 않고, formula approval도 아직 아니다.
v8.0-T candidate-v2 runtime target-delta proposal wiring 이후에는 실제 `calculate()` Today 경로가 `profileMacroCandidateV2RuntimeProposal`과 targetRateContext candidate-v2 metadata를 생성한다. mixed high-load probe는 production `targetCal=3283.4951111111113`, proposed candidate target `3308.524266666667`, `targetDeltaKcal=25.029155555555462`를 보존하지만 `appliedToTarget=false`, `canApplyAutomatically=false`이므로 production targetCal 적용이나 formula approval로 읽으면 안 된다.
v8.0-U candidate-v2 runtime user-facing QA 이후에는 같은 mixed high-load runtime delta가 Coach / Records 표면에서 검토 대상으로 보존되는지 확인했다. 직접 추출 기준 `coachHasProfileTargetDeltaDecision=true`, `recordDetailHasProfileTargetDelta=true`, `recordDetailHasProfileTargetRateDelta=true`, `surfaceForbiddenTermCount=0`이지만 `targetDeltaApplied=false`, `canApplyAutomatically=false`이므로 여전히 production targetCal 적용이나 formula approval로 읽으면 안 된다.
v8.0-V mobile screenshot audit analyzer 이후에는 `capture_render_audit.cjs` 47개 캡처와 `analyze_render_audit.cjs` PNG/manifest 검증으로 first-pass mobile/desktop screenshot audit evidence gate를 닫았다. 당시에는 candidate-v2 target delta production wiring 이후의 production visual QA가 아직 실행되지 않았으므로 formula approval이나 production targetCal 적용으로 읽으면 안 됐다. 이 경계는 v8.0-AB에서 별도 post-wiring visual QA로 갱신됐다.
v8.0-W human-review numerical approval gate 이후에는 18개 human-review candidate-v2 케이스가 별도 report-only approval layer로 고정됐다. 17개는 numerical gate를 통과했지만 `low_reliability_user`가 `fat_percent_below_contest_prep_reference_floor`로 막혀 있으므로 candidate-v2 formula approval, production wiring, production `targetCal` 적용으로 읽으면 안 된다.
v8.0-X candidate-v2 fat floor linked-target consistency 이후에는 `low_reliability_user` fat-percent blocker가 report-only candidate-v2 안에서 해소되어 18개 human-review 케이스가 code numerical gate를 통과했다. 그러나 `productionFormulaApprovalReady=true`를 formula approval로 읽으면 안 되며, owner signoff / production wiring / production wiring 이후 visual QA는 여전히 별도 open gate다.
v8.0-Y profile formula approval preflight 이후에는 `profileFormulaApprovalPreflight`가 `9-10_profile_formula_approval_preflight_v0`로 추가됐다. 직접 추출 기준 `checkCount=5`, `readyCheckCount=5`, `blockedCount=0`, `approvalPreflightReady=true`, `readyForExplicitFormulaApprovalDecision=true`지만 `candidateFormulaApproved=false`, `productionFormulaChanged=false`, `productionWiringAllowed=false`, `productionTargetCalApplied=false`, `productionVisualQaCompleted=false`다. 준비 완료 check는 `candidate_v2_code_contracts_closed`, `human_review_numerical_gate_18_of_18`, `runtime_user_surfaces_review_only_ready`, `report_only_boundary_preserved`, `candidate_target_delta_not_applied`이며, 이 단계는 명시적 formula approval decision의 preflight일 뿐 production targetCal wiring이나 post-wiring production visual QA를 닫지 않는다.
v8.0-Z profile formula approval decision 이후에는 `candidate-v8-profile-macro-v2-linked-target-v0`가 production wiring을 위한 승인 산식으로 선택됐다. 그러나 `productionFormulaChanged=false`, `productionTargetCalApplied=false`, `productionVisualQaCompleted=false`, `fullV8Complete=false`이므로 실제 production targetCal 적용과 post-wiring visual QA는 다음 단계다.
v8.0-AA production candidate-v2 target wiring 이후에는 active runtime target-delta cases에서 approved candidate-v2 target/macro가 production `calculate()` 결과에 적용된다. 직접 추출 기준 `productionTargetCal=3312.9411753385825`, `targetDeltaApplied=true`, `productionTargetCalApplied=true`, `recentGateStatus=applied`, `coachHasProfileTargetDeltaAppliedDecision=true`다. scenario runner의 과거 report-only baseline은 `profileCandidateV2ProductionWiring=false`로 보호하며, sandbox 복구 누수도 수정했다. AA 자체는 post-wiring production visual QA, 80,621,568,000개 full Cartesian 실행, full V8 completion을 닫지 않았다.
v8.0-AB post-wiring production visual QA 이후에는 `profileCandidateV2` payload와 55개 render audit capture로 AA 적용 케이스를 desktop/mobile Today/Records에서 검증했다. analyzer 기준 8개 post-wiring 캡처 모두 `selectedMacroBasis=profile_candidate_v2`, `productionTargetCalApplied=true`, `recentGateStatus=applied`를 통과했고, basis-open 화면에서 `프로필 목표 보정 29 kcal`, `주간 변화 보정 0.027 kg/주`가 확인됐다. 그러나 80,621,568,000개 full Cartesian 실행과 full V8 completion은 아직 open이다.
v8.0-AH profile-owned visual QA evidence 재감사 이후에는 AB의 post-wiring capture가 단순히 derived `profileSession`만 맞춘 증거가 아니라 `exerciseProfile=mixed`, `routinePlan=mixed_balanced`, `routine=mixed_strength_cardio`를 함께 가진 사용자 소유 루틴/세션 증거인지 검사한다. 이것은 visual/runtime evidence 강화이며, full Cartesian 실행 완료나 full V8 completion이 아니다.
v8.0-AI full Cartesian shard coverage ledger 이후에는 shard/batch evidence를 `executedEndExclusive` 기준으로만 병합해 gap/overlap/dirty/truncated를 분리한다. AI pilot은 unique executed 16, first gap 8~1152, dirty-source manifests 2, fullCoverageCandidate false를 보고했고, post-commit clean-source rerun은 같은 범위에서 dirty-source manifests 0을 보고했다. truncation probe는 shard-size 100000 / max-cases 8에서도 unique executed 8과 truncated manifests 1만 인정했다. contiguous probe에서는 0~8과 8~16을 overlap 없이 unique 16으로 병합했다. 따라서 full Cartesian 실행 완료나 full V8 completion이 아니다.
v8.0-AJ full Cartesian next-shard planner 이후에는 ledger gap에서 다음 exact range와 shard-index batch 후보를 분리해 산출한다. clean pilot은 다음 4개 shard를 중복 없이 계획했고, truncation probe는 부분 실행된 shard 0에서 exact 8~100000 range와 shard-index 중복 8건을 표시했다. 계획은 실행 증거가 아니므로 full Cartesian 실행 완료나 full V8 completion이 아니다.
v8.0-AK full Cartesian exact-range plan executor 이후에는 AJ planner의 exact ranges를 실제 shard manifest evidence로 실행할 수 있다. AK pilot은 `8~16`, `16~24` 두 exact range를 실행했고, source clean pilot과 합친 explicit cumulative ledger는 contiguous from zero 24를 보고했다. 재검토 중 `--max-ranges=0`이 전체 planned ranges로 넓어질 수 있는 위험도 막아, 0범위 입력은 executed 0 / `noRangesSelected=true`로 남긴다. post-push clean-source rerun은 dirty-source manifests 0을 확인했지만, 이는 32 unique executed evidence일 뿐이며 remaining gap 24~1152와 full Cartesian incomplete blocker가 있으므로 full Cartesian 실행 완료나 full V8 completion이 아니다.
v8.0-AC full Cartesian execution contract 이후에는 `fullCartesianExecutionContract`가 20축 product `80,621,568,000`, deterministic axis stride/index decode, pilot tuple decode, `100,000` row shard / `806,216` planned shard / final `68,000` row boundary를 report한다. 이것은 전수 실행 계약일 뿐 full Cartesian 실행 완료가 아니며, `full_8_2_cartesian_execution`과 `full_v8_completion`은 아직 open이다.
v8.0-AD deterministic full Cartesian shard pilot 이후에는 `runV8FullCartesianShard()`와 `run_v8_full_cartesian_shard.cjs`가 bounded shard evidence를 생성한다. coherent pilot은 8건 계산, constraint pilot은 3건 constraint-only로 분리했다. 이것도 전체 806억 row 실행이 아니므로 `full_8_2_cartesian_execution`과 `full_v8_completion`은 아직 open이다.
v8.0-AE shard manifest integrity 이후에는 shard evidence가 `v8_full_cartesian_shard_manifest_v1` schema, report/manifest hash, source hash, decoded accounting, calculation issue count, non-claim flags를 포함하고 analyzer로 재검증된다. analyzer 기준 2 manifests / failed 0이지만, 이것도 bounded evidence integrity일 뿐 전체 806억 row 실행 완료가 아니므로 `full_8_2_cartesian_execution`과 `full_v8_completion`은 아직 open이다.
v8.0-AF shard batch orchestration 이후에는 `run_v8_full_cartesian_batch.cjs`가 여러 bounded shard manifest를 생성하고 batch folder analyzer를 통과시킨다. AF pilot은 2 shard / decoded 16 / calculated 8 / constraint-only 8 / failed 0이지만, 이것도 batch 실행 틀 검증일 뿐 전체 806억 row 실행 완료가 아니므로 `full_8_2_cartesian_execution`과 `full_v8_completion`은 아직 open이다.

## 12. 2026-06-03 V8 1단계 복구 감사

상세 문서: `docs/v8_1단계_복구감사_2026-06-03.txt`

AS2 이후 V8을 새 단계로 더 밀기 전에, 지금까지의 작업을 production 변경 / report-only 후보 / 계약 / render evidence / full Cartesian tooling으로 다시 분류했다. 결론은 "V8 자산은 유지하되, 다음 작업은 새 기능 stage가 아니라 production macro policy 복구가 우선"이다.

직접 확인:

- `git status --short --branch` 기준 clean branch: `codex/v8-formula-audit`
- `runV8ScenarioRunnerTests`: 1 suite / 29 cases / failed 0
- render audit analyzer: 65 captures / profile routine ownership 10 / post-wiring profile candidate 8 / failed 0
- default full Cartesian ledger: unique executed 32 / contiguous from zero 24 / first gap 24~1152 / dirty-source manifests 15 / truncated manifests 1 / fullCoverageCandidate false
- AT 전 대표 diet production 직접 계산: target `1908.5088kcal`, protein `180.048g`, carbs `195.8022g`, fat `45.012g`, protein `2.4g/kg`, `guideProteinRange.mid=2.4`, `profileCandidateApplied=false`

따라서 v8.0-G의 `180.5g -> 150.4g` 단백질 개선은 report-only candidate-v0 비교였고, 일반 diet production macro fix가 아니다. v8.0-S의 `proteinGuardConflictCount=0`도 candidate-v2 guard blocker 해소이지, 일반 diet 단백질 과다 해결이 아니다. v8.0-AA의 `productionTargetCalApplied=true`는 active runtime profile target-delta case에 한정되므로 general diet macro formula 완료로 읽으면 안 된다.

1단계 복구 감사에서 다음 위험을 open으로 고정한다.

- AT 전 diet production protein policy가 2.4g/kg mid를 사용했다. v8.0-AT에서 이 항목은 일반 diet production 기준 `2.0g/kg`으로 복구됐다.
- candidate-v2 production wiring은 real production change지만 scoped target-delta case에 한정된다.
- runner status의 approved/applied/complete 문구는 full V8 또는 broad macro completion으로 오해될 수 있다.
- render audit pass는 human UX full review가 아니다.
- full Cartesian tooling은 full execution이 아니다.
- `wholeStageEvidenceBoundaryAudit` pass는 "점검한 boundary가 통과했다"는 뜻이지 shortcut이 더는 없다는 보증이 아니다.

다음 작업 순서:

1. production macro policy 2단계: diet protein/fat/carb 기준을 실제 production 경로에서 재설계한다.
2. candidate-v2/scoped production status wording을 좁혀 full V8 완료와 섞이지 않게 한다.
3. profile/routine/session broad human UX review matrix를 만든다.
4. full Cartesian을 실제 전수 실행할지 owner-approved 대체 gate로 바꿀지 결정한다.

## 13. 2026-06-03 v8.0-AT 2단계 diet production macro policy recovery

1단계 복구 감사 결과를 바탕으로 기존 2단계의 우선순위를 "새 기능 추가"가 아니라 일반 diet production macro policy 복구로 좁혔다.

변경 범위:

- `GOALS.diet.proteinKg`: `2.2 -> 2.0`
- `getDualBasisProteinRange("diet")`: `{ min: 2.2, mid: 2.4, max: 2.6 } -> { min: 1.4, mid: 2.0, max: 2.0 }`
- `runV8ScenarioRunner()` version/stage: `8.0-AT`
- mode golden hash: `142adb943906d4`

근거:

- ISSN protein position stand 2017은 대부분의 운동하는 개인에 대해 `1.4~2.0g/kg/day` 범위를 제시한다.
- Helms contest-prep reference는 자연 보디빌딩 contest prep 맥락의 LBM 기준 고단백 검토 근거다. 이 근거를 일반 diet 사용자 기본값 `2.4g/kg BW` 승인 근거로 쓰지 않는다.
- 따라서 AT는 "단백질을 감각적으로 낮춘 것"이 아니라 "일반 diet production 기본값을 외부 일반 운동인 범위 안으로 복구하고, contest-prep/high-preservation context는 별도 결정으로 남기는 것"이다.

직접 추출 결과:

- required 1913 diet regression:
  - target `1913.2991519999998kcal`
  - protein `150.4166g`
  - carbs `226.376983g`
  - fat `45.124979999999994g`
  - protein `2.0g/kg BW`
  - fat `0.6g/kg BW`
  - `carbMinHit=false`
- `macroAudit.requiredRegression.findingIds`:
  - `protein_lbm_in_contest_prep_range_but_context_unverified`
  - `fat_at_guide_floor`
- 제거된 required-case finding:
  - `protein_bodyweight_above_general_issn_range`
  - `diet_default_near_2_4g_bodyweight_requires_candidate_review`
  - `carb_below_guide_min`
- `candidateFormulaComparison.summary.proteinReducedCount=0`: production current가 이미 candidate-v0 general cap을 쓰기 때문에 required case에서 더 줄일 단백질이 없다.
- `profileMacroCandidateV2Comparison.summary.proteinGuardResolvedCount=0`, `proteinLegacyGuideConflictCount=0`: legacy diet guide floor 충돌은 production range 복구로 사라졌다. 이것은 candidate-v2의 새 광범위 승인 근거가 아니라 기존 충돌이 더 이상 존재하지 않는다는 뜻이다.

해석:

- AT는 일반 diet production macro default를 고친 단계다.
- AT는 full V8 completion, full 80,621,568,000 Cartesian execution, 모든 profile/routine/session UX human review를 닫지 않는다.
- candidate-v0는 여전히 report-only comparison layer다. AT 이후 required case에서 candidate-v0가 단백질을 더 줄이지 않는 것은 production current가 같은 cap을 이미 쓰기 때문이다.
- candidate-v2는 active runtime target-delta production path와 post-wiring visual QA가 닫힌 상태지만, AT의 diet default 복구와 동일한 완료 범위가 아니다.

검증:

- `node .\tools\render_audit\run_internal_tests.cjs --profile=core --suite=runV8ScenarioRunnerTests,runTargetMacroProductionPolicyTests,runDualBasisProductionTests`
  - 3 suites / 43 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=core --suite=runModeGoldenCalculationTests`
  - 1 suite / 8 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
  - 26 suites / 370 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
  - 14 suites / 137 cases / failed 0

## 14. 2026-06-03 v8.0-AU docs hierarchy cleanup

사용자 추가 지시에 따라 `/GOAL` 장시간 자동 진행 방식은 기준 작업 방식에서 제외한다. 기존 장시간 자동 재개 문서는 삭제하고, 필요한 current/open gate 기준은 `docs/README.md`, 이 감사 ledger, `docs/v8_운동프로필_산식_정밀설계.txt`, `docs/v8_1단계_복구감사_2026-06-03.txt`에 남긴다.

정리 원칙:

- 문서를 줄이거나 과거 증거를 삭제하는 것이 목표가 아니다.
- `CURRENT_PRODUCTION`, `SCOPED_PRODUCTION`, `REPORT_ONLY`, `HISTORICAL`, `OPEN_GATE`, `DO_NOT_CLAIM_COMPLETE` 경계를 먼저 고정한다.
- AT의 general diet protein recovery는 현재 production 기준이지만, 수준별 산식 완성으로 말하지 않는다.
- candidate-v2 production application은 scoped target-delta 경로이며, 모든 사용자/운동 프로필의 전면 산식 승인으로 말하지 않는다.
- 장시간 자동 진행은 작은 stage 통과와 실제 사용자 가치 검토를 섞기 쉬우므로, 이후 V8 작업은 수동으로 범위를 작게 잡고 각 단계의 근거 등급과 사용자 경로를 먼저 확인한다.

문서 역할:

- `docs/README.md`: 최신 문서 지도와 태그 체계.
- `docs/v8_현재산식_기준갱신_감사_2026-06-02.txt`: 긴 historical/current audit ledger.
- `docs/v8_운동프로필_산식_정밀설계.txt`: V8 원래 순서와 수준별 산식 설계 의도.
- `docs/v8_1단계_복구감사_2026-06-03.txt`: AS2 시점 복구 감사 증거와 AT follow-up.

AU 당시 다음 수동 작업 우선순위:

1. candidate-v2/scoped production status wording hardening.
2. profile/routine/session broad human UX review matrix와 수준별 산식 범위 설계.
3. full Cartesian 실제 전수 실행 전략 또는 owner-approved 대체 gate 결정.

이 순서는 바로 아래 v8.0-AV follow-up에서 supersede한다. AV 이후에는 candidate-v2/scoped wording hardening을 별도 장시간 stage로 떼지 않고, 기존 3단계 preflight로 처리한다.

## 15. 2026-06-03 v8.0-AV docs cleanup completion

문서정리는 다음 기존 3단계로 넘어갈 수 있는 수준으로 닫는다. 이 결정은 "문서가 완벽하게 짧고 예뻐졌다"는 뜻이 아니라, 다음 작업자가 current/scoped/report-only/historical/open gate를 섞지 않도록 읽는 기준이 고정됐다는 뜻이다.

완료한 정리:

- `docs/README.md`가 최신 문서 지도와 태그 체계를 맡는다.
- `/GOAL` 장시간 자동 진행은 폐기했고, 별도 재개 파일은 삭제했다.
- 이 감사 ledger는 긴 historical/current 기록으로 보존한다.
- `docs/v8_운동프로필_산식_정밀설계.txt`는 원래 V8 순서와 수준별 산식 의도를 보존하는 living design ledger로 읽는다.
- AT general diet production recovery와 candidate-v2 scoped production application을 서로 다른 완료 범위로 고정했다.

다음 작업 고정:

- 다음 실제 작업은 기존 3단계로 진입한다.
- 여기서 기존 3단계는 profile/routine/session broad human UX review matrix와 수준별 산식 범위 설계를 뜻한다.
- candidate-v2/scoped production wording hardening은 3단계 preflight로 처리한다. 즉 full V8 완료처럼 보이는 표현은 좁히되, 이것만으로 새 장시간 stage를 만들지 않는다.
- full Cartesian closure는 그 다음 별도 open gate다.

금지:

- AT의 `2.0g/kg` 복구를 수준별 산식 완료로 말하지 않는다.
- candidate-v2의 active runtime target-delta 적용을 모든 profile-specific macro formula 승인으로 말하지 않는다.
- 문서정리 완료를 V8 완료로 말하지 않는다.

## 16. 2026-06-03 v8.0-AW profile routine user-path matrix

AW는 기존 3단계 진입 작업의 첫 보완이다. `runV8ScenarioRunner()`에 `profileRoutineUserPathMatrix`를 추가해 프로필/루틴/세션 경로를 코드상으로 전수 확인하되, 이것을 broad human visual review나 수준별 산식 구현 완료로 말하지 않는다.

확인한 경로는 두 종류로 분리한다.

- Settings/default Today path: Today draft가 없는 기본 경로.
- Today explicit override path: Today에서 날짜별 루틴/세션을 명시 선택한 경로.

중요한 현재 코드 사실:

- 보디빌딩 + 고급설정 OFF + Today 명시 override 없음은 단순 훈련/휴식 흐름을 유지한다. 훈련 기본 강도는 주간 보정 전 `0.70`이다.
- 보디빌딩 + 고급설정 OFF라도 Today에서 `PUSH` 같은 루틴/세션을 명시 override하면 선택 세션의 xw가 보존될 수 있다. 예: `PUSH`는 `0.80`.
- 비보디빌딩 프로필은 고급설정 OFF에서도 프로필 전용 루틴/세션 기본값을 유지한다. 고급설정 ON/OFF의 주 차이는 현재 수동 훈련강도/확장 입력의 노출 여부다.
- 이 비보디빌딩 OFF 동작은 "제품적으로 충분히 좋은가"가 남아 있는 검토대상이다. 코드 연속성 통과를 사용자 경험 승인으로 읽으면 안 된다.

AW matrix는 모든 프로필, 모든 루틴 플랜, 모든 세션, 고급설정 ON/OFF를 Today 값, `calculate()`, Records `goalSnapshot`, future level-formula input scope까지 확인한다. 다만 `performanceLevel`, 체성분 신뢰도, 최근 기록 커버리지, 최근 추세를 묶는 사용자 수준별 산식은 아직 구현하지 않았다.

검증:

- `node .\tools\render_audit\run_internal_tests.cjs --profile=core --suite=runV8ScenarioRunnerTests`
  - 1 suite / 30 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=core --suite=runTodayQuickEditTests,runTodayCalculationOwnershipTests,runTodayRecordConfirmationTests`
  - 3 suites / 70 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
  - 26 suites / 370 cases / failed 0

열린 범위:

- non-bodybuilding advanced OFF/ON product review
- profile/routine/session broad human visual review
- user-level formula design/implementation
- full 8-2 Cartesian execution
- full V8 completion

## 17. 2026-06-03 v8.0-AX exercise advanced toggle semantics recovery

AX supersedes the AW interpretation that non-bodybuilding profiles could keep detailed profile routine/session defaults visible while advanced settings are OFF.

Correct product/code rule:

- Advanced OFF is a simple user surface for every profile: visible routine/session choices are only `REST / PUSH`.
- Bodybuilding advanced OFF preserves the original basic exercise-day behavior: `PUSH` is a generic exercise-day token with base xw `0.70` before weekly adjustment. Detailed bodybuilding routine/session xw such as `PPL-UL / PUSH 0.80` is advanced-ON behavior.
- Non-bodybuilding advanced OFF still shows simple `REST / PUSH`; however, a simple training day applies the selected profile's default training session internally for calculation defaults such as xw, weight duration, and cardio defaults.
- Advanced ON is the only path that exposes detailed profile routine/session choices and manual session-intensity editing.

Implementation boundary:

- `profileSession` remains derived evidence, not a user-owned selector.
- Settings owns `exerciseProfile`, `routinePlan`, and weekday planning.
- Today owns date-specific routine/session overrides.
- The AX matrix is still report-only evidence. It does not implement the user-level formula and does not close broad human visual review, full 8-2 Cartesian execution, or full V8 completion.

Verification:

- `node .\tools\render_audit\run_internal_tests.cjs --profile=core --suite=runTodayCalculationOwnershipTests,runTodayQuickEditTests,runV8ScenarioRunnerTests`
  - 3 suites / 88 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
  - 26 suites / 370 cases / failed 0
- `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
  - 14 suites / 138 cases / failed 0

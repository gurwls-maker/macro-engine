# v8 현재 산식 및 Today-Records 기준갱신 감사 리포트

작성일: 2026-06-02
대상 브랜치: codex/v8-formula-audit
대상 구현: index.html
기준 문서: docs/v8_운동프로필_산식_정밀설계.txt 9-1

이 문서는 산식 수정 제안서가 아니다.
v8 기준문서가 지시한 1단계, 즉 현재 index.html의 산식과 Today-Records 기준갱신 판단 흐름을 감사한 결과이다.
최초 감사 리포트 작성 중에는 index.html 구현을 변경하지 않았다.
이후 v8.0-A / v8.0-B / v8.0-C 기준갱신 핫픽스, v8.0-D 시나리오 러너, v8.0-D1 근거 등급 재감사 결과는 이 문서의 진행 메모와 검증 메모에 누적한다.

## 처음 보는 작업자용 사용법

이 문서는 단독 기준문서가 아니다.
처음 이 브랜치를 여는 작업자는 아래 순서로 읽어야 한다.

1. `docs/README.md`
2. `docs/내가-바라는-앱의-성격.txt`
3. `docs/개발정책.txt`
4. `docs/v8_운동프로필_산식_정밀설계.txt`
5. 이 문서
6. `index.html`

이 문서를 읽은 뒤 바로 할 수 있는 작업은 "현재 산식 감사 결과를 바탕으로 다음 단계 설계"이다.
아직 바로 하면 안 되는 작업은 "산식 수정", "UI 수정", "점수/Coach/Recent Flow/InBody/backup schema 변경"이다.

다음 작업자는 이 문서를 다음처럼 사용한다.

- 현재 앱이 어떤 입력을 어디서 모으는지 확인하려면 1장을 본다.
- 유지 칼로리 계산식을 확인하려면 2장을 본다.
- 목표 칼로리와 energy availability floor를 확인하려면 3장을 본다.
- 단백질 180.5g 원인을 확인하려면 4장과 5장을 본다.
- Today-Records 기준갱신 문제를 확인하려면 7장과 8장을 본다.
- 다음에 무엇을 고칠지 결정하려면 9장과 10장을 본다.

이 문서만 보고 임의로 수치를 고치면 안 된다.
특히 단백질 180.5g은 "높아 보인다"는 감각으로 낮추는 대상이 아니라, 원래 의도와 부작용을 분리한 뒤 candidate formula와 scenario runner에서 비교해야 한다.

## 근거 등급 규칙

v8.0-D1부터 모든 산식 / 테스트 / 문서 근거는 아래 중 하나로 표시한다.

1. 외부 운동생리학 / 스포츠영양 근거
   - 논문, 공식 가이드, position stand처럼 외부 출처가 명시된 근거다.
   - 출처가 있다고 끝난 것이 아니라 대상군, 목표, 훈련 수준, 앱의 사용자 흐름에 맞는지까지 검토해야 한다.
2. 내부 코드 근거
   - `index.html` 경로, snapshot, 테스트, 리포트 출력처럼 현재 구현이 어떻게 동작하는지 보여주는 근거다.
   - 현재 동작을 증명할 뿐, 생리학적 타당성을 증명하지 않는다.
3. 제품 정책 근거
   - 사용자 의도, 기준문서, UX 책임, Records / Today / Coach 책임 분리처럼 앱이 어떻게 행동해야 하는지에 대한 근거다.
   - 앱 정책으로는 사용할 수 있지만 운동생리학 근거처럼 말하면 안 된다.
4. 미검증 생리학 후보
   - 그럴듯한 내부 산식 또는 의도지만 외부 근거와 앱 적합성 검토가 끝나지 않은 항목이다.

외부 운동생리학 근거라고 명시되어 있지 않은 기존 근거는 기본적으로 내부 코드 근거 또는 제품 정책 근거로 본다.
이런 항목은 후속 산식 변경에서 검토대상으로 남긴다.

## 0. 감사 결론

현재 계산은 다음 순서로 진행된다.

1. Today 계산값과 Settings 기본값을 합쳐 입력 상태를 만든다.
2. 입력 근거에 따라 내부 런타임 설정을 만든다.
3. BMR, 일상 활동, 업무 보정, 주간 웨이트 보정, 오늘 웨이트, 유산소, EPOC를 합쳐 총소비량을 만든다.
4. 목표별 주간 체중 변화율 정책과 안전선을 적용해 목표 칼로리를 만든다.
5. 1차 탄단지를 계산한다.
6. guide/activity dual basis를 다시 만들고, 선택된 basis의 탄단지를 최종 화면/저장 기준으로 사용한다.

중요한 점은 최종 탄단지가 1차 `calculateMacroTargets()` 결과가 아니라는 것이다.
최종 화면값과 goalSnapshot 저장값은 `buildDualBasisProduction()` 이후 선택된 basis 값이다.

1913kcal / 탄수 196.3g / 단백질 180.5g / 지방 45.1g 케이스는 현재 구현에서 재현된다.
직접 원인은 다음 두 가지다.

1. 목표 칼로리는 감량 deficit 결과가 아니라 energy availability floor에 의해 올라간 값이다.
2. 단백질과 지방은 guide basis의 diet 규칙인 단백질 2.4g/kg, 지방 0.6g/kg에서 나온다.

이 값은 "단백질이 남은 칼로리를 먹었다"가 아니다.
현재 guide basis는 단백질을 goal range의 mid 값으로 먼저 잡고, 지방을 min 값으로 잡은 뒤, 남은 칼로리를 탄수화물에 배정한다.

## 1. 입력값 수집 경로

확인 위치:

- `collectState()` - index.html:18980
- `getEffectiveTodayCalculationValues()` - index.html:17594
- `resolveModeFreeRuntimeConfig()` - index.html:18489
- `calculate()` - index.html:19121

`collectState()`는 계산 입력을 직접 DOM/상태에서 모으지 않고 먼저 Today 계산값을 만든다.
`getEffectiveTodayCalculationValues()`는 Settings 기본값, Today draft, 최신 InBody 자동 기본값, cardio preset 기본값을 합친다.

Today 소유 입력:

- 계산용 체중
- 체지방률
- 체지방량
- 골격근량
- 오늘 활동량
- 오늘 업무 유형
- 오늘 루틴/세션
- 오늘 훈련 강도 보정
- 오늘 웨이트 시간
- 오늘 유산소 종류/시간/속도/경사

Settings 소유 입력:

- 키
- 나이
- 성별
- 목표
- 업무 보정
- homeInbody 여부
- expertLbmAlpha
- 주간 웨이트일수
- generalAdvancedSettings
- generalLowDigestCarbs
- routinePlan

Records는 현재 계산 입력 자체를 직접 만들지 않는다.
Records의 `goalSnapshot`은 저장 당시 기준을 보관하고, Records 점수/상세 보기/Today 기준갱신 비교에서 사용된다.

내부 런타임 설정은 사용자에게 노출되는 예전 모드가 아니라 입력 근거 기반으로 만들어진다.
체성분 입력이 있으면 `bodyCompReliability`가 `partial`, `home`, `inbody` 중 하나가 되고, `appliedAlpha`가 적용된다.
체성분 입력이 있으면 BMR은 Katch-McArdle, 없으면 Mifflin-St Jeor가 된다.

주의점:

- 현재 `resolveTrainingEvidence()`는 주간 웨이트일수, 오늘 웨이트 시간, 강도 보정, 유산소 시간/속도를 함께 본다.
- 그러나 목표 rate용 `getTargetRateTrainingBand()`는 주간 웨이트일수 중심으로 beginner/intermediate/advanced를 정한다.
- v8 기준의 "주간 웨이트일수 단독 판단 금지"와 충돌 가능성이 가장 큰 지점이다.

## 2. 유지 칼로리 분해

확인 위치:

- `calcBMR()` - index.html:18592
- `calcCardio()` - index.html:18620
- `calcWeightTrainingKcal()` - index.html:18632
- `getWeeklyTrainingBaselineKcal()` - index.html:18639
- `calculate()` - index.html:19121

현재 총소비량은 다음 합이다.

```
totalBurn =
  baseDaily
  + trainingBaselineKcal
  + weightKcal
  + cardio.kcal
  + epocKcal
```

BMR:

- 체성분 입력이 있고 `appliedAlpha > 0`이면 Katch-McArdle:
  - `370 + 21.6 * lbmEffective`
- 체성분 입력이 없으면 Mifflin-St Jeor:
  - 남성: `10 * weight + 6.25 * height - 5 * age + 5`
  - 여성: `10 * weight + 6.25 * height - 5 * age - 161`

일상/업무:

```
workloadY = clamp(WORK_TYPE_Y[workType] + workAdj, 0, 1)
rawActCoeff = ACTIVITY_COEFF[activityLevel] + workloadY * 0.06
actCoeff = clamp(rawActCoeff, 1.08, 1.62)
baseDaily = bmr * actCoeff
```

주간 웨이트 보정:

```
frequencyRatio = clamp(weeklyTrainingDays / 7, 0, 1)
recoveryFactor = clamp(0.012 + frequencyRatio * 0.053, 0.015, 0.05)
trainingBaselineKcal = bmr * recoveryFactor * multiplier
```

오늘 웨이트가 있는 날은 multiplier가 0.40이다.
휴식일은 multiplier가 1이다.
즉 휴식일에도 주간 웨이트일수가 있으면 회복/유지 수요로 `trainingBaselineKcal`이 붙는다.

오늘 웨이트:

```
met = 2.5 + xw * 2.5
weightKcal = met * weight * durationHours
```

유산소:

- 트레드밀 워킹/러닝, 실외 러닝별 VO2 기반 MET 계산
- `cardio.kcal = met * weight * durationHours`
- EPOC는 MET 기준 0, 4%, 7%, 10% 중 하나

## 3. 목표 칼로리 분해

확인 위치:

- `GOAL_RATE_POLICIES` - index.html:15875
- `getTargetRateTrainingBand()` - index.html:15916
- `selectGoalWeeklyRate()` - index.html:15940
- `buildGoalTargetRateContext()` - index.html:15969
- `calculate()` - index.html:19121

현재 목표 칼로리는 단순 `totalBurn * factor`가 아니다.
실제 적용 경로는 다음과 같다.

1. cardio reflection으로 목표 계산용 소비 기준을 만든다.

```
targetBurnBase = totalBurn - cardioKcal + cardioKcal * CARDIO_TARGET_REFLECTION_RATE[goal]
```

2. 목표별 주간 체중 변화율을 선택한다.

```
selectedWeeklyRate = selectGoalWeeklyRate(goal, s, modeCfg)
weeklyBodyWeightDeltaKg = weight * selectedWeeklyRate
rawGoalDelta = weeklyBodyWeightDeltaKg * 7700 / 7
clampedGoalDelta = clamp(rawGoalDelta, policy.deltaMin, policy.deltaMax)
targetCalBeforeFloor = targetBurnBase + clampedGoalDelta
```

3. 안전선을 적용한다.

```
energyAvailabilityFloorTarget =
  30 * actualLbm + exerciseKcal

minimumTargetCal =
  max(BMR if diet/cut, energyAvailabilityFloorTarget)

targetCal =
  max(targetCalBeforeFloor, minimumTargetCal)
```

여기서 `actualLbm`은 adjusted LBM이 아니라 실제 체중/체지방률 또는 체지방량 기반 LBM이다.
이 부분은 v8 기준문서의 "안전선은 adjusted LBM이 아니라 actual LBM"과 맞는다.

위험 지점:

- `getTargetRateTrainingBand()`는 주간 웨이트일수 3/5 기준으로 intermediate/advanced를 나눈다.
- 몸 상태, 수행, 운동 프로필, 최근 기록 신뢰도 없이 목표 rate 보정에 trainingBand가 들어간다.
- 이 지점은 v8의 사용자 타입/운동 수준 판단 기준과 충돌 가능성이 높다.

## 4. 탄단지 분해

확인 위치:

- `calculateMacroTargets()` - index.html:18838
- `applyPlausibilityLayer()` - index.html:18763
- `getDualBasisProteinRange()` - index.html:54080
- `getDualBasisFatRange()` - index.html:54088
- `calculateGuideBasisMacrosWithinTarget()` - index.html:54355
- `buildDualBasisProduction()` - index.html:54519

1차 탄단지:

```
proteinBasisWeight = getProteinBasisWeight(...)
proteinRaw = GOALS[goal].proteinKg * proteinBasisWeight
defaultProteinCap = targetCal * 0.35 / 4
protein = min(proteinRaw, defaultProteinCap)
fat = fatTargetPreferredG
carbs = (targetCal - protein * 4 - fat * 9) / 4
```

그 뒤 plausibility layer가 단백질/탄수/지방 상한을 적용한다.

하지만 최종 화면값은 여기서 끝나지 않는다.
`calculate()`는 1차 결과를 `legacyA58`에 넣고, 다시 dual basis를 만든다.
그리고 `dualBasis.selected`의 target/protein/carbs/fat을 최종 `targetCal`, `protein`, `carbs`, `fat`으로 반환한다.

guide basis의 diet 범위:

```
protein: 2.2 ~ 2.6 g/kg, mid 2.4 g/kg
fat: floor/min 0.6 g/kg, mid 0.8 g/kg, max 1.0 g/kg
carbs: low_moderate load 기준 3 ~ 5 g/kg
```

guide basis within target 계산:

1. 단백질을 `protein.mid * weight`로 잡는다.
2. 지방을 `fat.min * weight`로 잡는다.
3. 남은 칼로리를 탄수화물로 채운다.
4. 목표 안에 들어가지 않으면 지방, 단백질 순서로 낮춘다.
5. 탄수화물이 range max를 넘으면 지방 flexible range로 이동한다.

현재 1913 케이스에서는 단백질과 지방만 먼저 고정되고 탄수화물이 잔여값이 된다.
그 결과 탄수화물은 guide minimum 3g/kg보다 낮은 `2.61g/kg`가 된다.
코드는 이를 `carbsBelowGuideMin: true`로 표시한다.

주의점:

- 최종 반환의 `proteinBasisWeight`는 실제 1차 protein basis가 아니라 bodyweight로 다시 기록된다.
- v8 기준문서가 요구한 `protein_g_per_kg_bodyweight`, `protein_g_per_kg_lbm`, `protein_rule_source`, cap/floor hit 같은 리포트 필드는 현재 한 번에 구조화되어 있지 않다.
- 다음 단계에서 시나리오 러너를 만들 때 이 필드는 별도 리포트 객체로 명시해야 한다.

## 5. 실제 케이스: 1913 / 196.3 / 180.5 / 45.1

브라우저에서 현재 index.html을 로드해 실제 `calculate()` 결과로 재현했다.

재현 입력:

```
goal: diet
selectedMacroBasis: guide
weight: 75.2083 kg
bodyFat: 15.2 %
skeletal: 35 kg
gender: male
height: 173 cm
age: 32
activityLevel: sedentary
workType: office
weeklyTrainingDays: 4
routine: REST
weightDuration: 0
cardioDuration: 0
cardioSpeed: 0
cardioIncline: 0
```

유지 칼로리:

```
lbmBase = 63.7766 kg
lbmEffective = 66.4249 kg
bmr = 1804.7785 kcal
baseDaily = 2057.4474 kcal
trainingBaselineKcal = 76.3146 kcal
weightKcal = 0
cardioKcal = 0
epocKcal = 0
totalBurn = 2133.7158 kcal
```

목표 칼로리:

```
selectedWeeklyRate = -0.005
rawGoalDelta = -413.6457 kcal
clampedGoalDelta = -413.6457 kcal
targetCalBeforeFloor = 1720.0701 kcal
actualLbm = 63.7766 kg
energyAvailabilityFloorTarget = 63.7766 * 30 = 1913.2992 kcal
minimumTargetCal = 1913.2992 kcal
targetCal = 1913.2992 kcal
display targetCal = 1913 kcal
```

탄단지:

```
protein = 75.2083 * 2.4 = 180.4999 g -> 180.5 g
fat = 75.2083 * 0.6 = 45.1250 g -> 45.1 g
carbs = (1913.2992 - 180.4999 * 4 - 45.1250 * 9) / 4
      = 196.2937 g -> 196.3 g
```

따라서 이 케이스의 단백질 180.5g은 다음 의미다.

- 원래 의도: diet 상태에서 근손실 방지를 위해 높은 단백질 범위의 중간값을 사용한다.
- 내부 정책상 보존할 만한 구조 후보: 단백질을 남은 칼로리 처리로 두지 않고 목표/체중 기준으로 먼저 정한다. 단, 이것은 현재 앱 산식 구조의 장점 후보이지 외부 스포츠영양 근거로 확정한 결론은 아니다.
- 과한 부분 후보: 모든 diet 사용자에게 bodyweight 2.4g/kg이 기본 중간값으로 적용된다.
- 부작용: energy availability floor로 목표 칼로리가 낮게 고정될 때 탄수화물이 guide minimum 아래로 내려간다.
- 수정 판단은 아직 금지: 현재 단계에서는 "높아 보이니 낮춘다"가 아니라, 보존할 의도와 줄여야 할 부작용을 분리해 후보안을 설계해야 한다.

비교: 1차 legacyA58 탄단지

같은 케이스에서 dual basis 적용 전 1차 macroPlan은 다음과 같다.

```
protein = 150.9275 g
carbs = 207.8161 g
fat = 53.1472 g
proteinBasisWeight = 68.6034 kg
proteinKgApplied = 2.2 g/kg
```

즉 180.5g은 1차 diet protein rule의 직접 결과가 아니라, final guide basis에서 다시 올라간 값이다.

## 6. 주간 웨이트일수 영향 확인

같은 1913 케이스에서 주간 웨이트일수를 0 / 2 / 4 / 6으로 바꿔 확인했다.

결과:

- 주 0일: targetCal 1913.0735, totalBurn 2057.4011, trainingBand beginner
- 주 2일: targetCal 1913.0735, totalBurn 2106.3869, trainingBand beginner
- 주 4일: targetCal 1913.0735, totalBurn 2133.7158, trainingBand intermediate
- 주 6일: targetCal 1913.0735, totalBurn 2147.6380, trainingBand advanced

이 케이스에서는 targetCal이 주간 웨이트일수에 따라 바뀌지 않는다.
이유는 `targetCalBeforeFloor`가 모두 energy availability floor보다 낮아서 최종 targetCal이 floor에 고정되기 때문이다.

그러나 totalBurn, trainingBaselineKcal, trainingBand는 바뀐다.
다른 체성분/활동/운동 케이스에서는 floor가 지배하지 않으면 주간 웨이트일수의 영향이 targetCal에 드러날 수 있다.
따라서 문서의 1844 / 1907 / 2025류 케이스는 별도 시나리오 그리드에서 계속 확인해야 한다.

## 7. Today-Records 기준갱신 판단 흐름

확인 위치:

- `normalizeGoalSnapshot()` - index.html:20861
- `buildGoalSnapshotFromCurrentState()` - index.html:23018
- `getGoalSnapshotSignature()` - index.html:23055
- `areGoalSnapshotsEquivalent()` - index.html:23099
- `getTodayRecordConfirmationState()` - index.html:26280
- `applyCurrentTodayBasisToTodayRecord()` - index.html:26395
- `persistAutoAdherenceForDate()` - index.html:26207

Records의 계산 기준은 `goalSnapshot`에 저장된다.
Today 기준갱신 패널은 오늘 Records가 detailed record일 때, 저장된 `goalSnapshot`과 현재 Today 기준에서 새로 만든 snapshot의 signature를 비교한다.

signature 비교 키:

```
targetCal, protein, carbs, fat,
mode, goal, weight, height, age, gender,
bodyFat, bodyFatMass, skeletal,
activityLevel, workType, weeklyTrainingDays,
routinePlan, routine, intensityOverride, weightDuration,
cardioType, cardioDuration, cardioSpeed, cardioIncline,
workAdj, homeInbody, expertLbmAlpha,
generalAdvancedSettings, generalLowDigestCarbs
```

`snapshotSource`는 signature에 들어가지 않는다.
숫자는 4자리 반올림 후 비교된다.

상태 판단:

- 오늘 기록 없음: missing
- 오늘 기록이 detailed가 아님: current
- 현재 계산 불가: stale, action none
- detailed이고 snapshot이 현재 기준과 같음: current
- detailed이고 snapshot이 현재 기준과 다름: stale, action update

수동 갱신 버튼은 다음만 갱신한다.

- `goalSnapshot`
- `snapshotSource`
- auto adherence score 관련 값

식사, 공복 체중, 메모는 유지한다.

## 8. 기준갱신 시나리오 확인 결과

브라우저에서 현재 앱을 로드해 직접 상태를 눌렀다.

기준 케이스:

```
오늘 detailed 기록 있음
goalSnapshot 있음
식사 2개 있음
공복 체중 있음
Today 기준과 snapshot 일치
```

결과:

- base current: current, 패널 숨김
- meal add only: current, 패널 숨김, snapshot 유지
- record note only: current, 패널 숨김, snapshot 유지
- Today 계산용 체중 변경: stale
- Today 계산용 체중 원복: current, 패널 숨김
- legacy snapshot에서 REST/cardio 0 필드 누락: normalize 후 current

문서 기준과 맞는 부분:

- 식사만 추가해도 기존 snapshot은 유지된다.
- 메모만 바꿔도 기준갱신은 뜨지 않는다.
- Today 값을 바꿨다가 저장 당시 값으로 되돌리면 기준갱신이 사라진다.
- REST 0, cardio 0 계열은 normalize 후 같은 값으로 취급된다.
- 수동 갱신은 식사/공복 체중/메모를 유지한다.

검토 필요한 부분:

1. snapshot 없는 detailed 오늘 기록

브라우저 확인 결과:

```
status = stale
mismatchKeys = ["unavailable"]
패널 표시 = true
```

v8 기준문서는 "저장된 오늘 Records 계산 snapshot과 현재 Today 계산 기준이 의미 있게 다를 때" 표시하라고 한다.
snapshot이 없는 detailed 기록은 비교할 저장 기준이 없으므로, "갱신 필요"보다 "당시 계산 기준 없음/기준 저장 필요"로 분리하는 편이 문서 의도에 더 가깝다.
다만 현재 일반 상세 저장 흐름은 대부분 snapshot을 생성하므로 실제 노출은 legacy/import/snapshotless edge에 가까울 수 있다.

2026-06-02 v8.0-A 반영:

- `getTodayRecordConfirmationState()`에서 snapshot 없는 detailed 오늘 기록을 `status = "basis-missing"`으로 분리했다.
- 사용자 화면 문구는 "갱신 필요"가 아니라 "계산 기준 저장 필요" / "현재 기준 저장"으로 표시한다.
- 수동 저장 액션은 기존 `applyCurrentTodayBasisToTodayRecord()`를 사용하되, 식사 / 공복 체중 / 메모를 유지하고 현재 Today 기준의 `goalSnapshot`과 자동 점수 기준만 저장한다.
- `runTodayRecordConfirmationTests`에 snapshotless 상태가 stale mismatch처럼 보이지 않는지, 저장 후 current가 되는지 확인하는 v8.0-A 회귀 테스트를 추가했다.
- desktop / mobile viewport에서 같은 suite를 통과했다.

2. guide/activity basis 토글

브라우저 확인 결과:

```
guide -> activity:
status = stale
mismatchKeys = ["carbs", "fat"]

activity -> guide 복귀:
status = current
```

targetCal은 바뀌지 않지만 selected basis의 탄수/지방이 달라지므로 goalSnapshot과 현재 Today macro basis가 달라진다.
현재 구현은 이를 기준갱신 대상으로 본다.

2026-06-02 v8.0-B 반영:

- guide/activity basis 선택은 Today target macro / remaining / score 기준에 영향을 주므로, 선택 basis가 탄수/단백질/지방 출력값을 바꾸는 경우 Today-Records 기준갱신 대상으로 유지한다.
- `selectedMacroBasis` 문자열 자체는 UI/session 상태로 남긴다. records/export/localStorage schema에는 저장하지 않는다.
- Records의 `goalSnapshot`에는 선택 basis의 결과값인 `targetCal`, `protein`, `carbs`, `fat`만 저장한다.
- 사용자가 "오늘 기준으로 기록 갱신"을 누르면 현재 선택 basis의 탄단지 산출값이 snapshot에 저장되고, 식사 / 공복 체중 / 메모는 유지된다.
- guide/activity 산출 탄단지가 우연히 같으면 저장된 계산 기준이 달라지지 않은 것이므로 stale을 띄울 이유가 없다.
- `runTodayRecordConfirmationTests`에 선택 basis 변경이 stale을 띄우되 UI-only 상태를 저장하지 않고, 수동 갱신 후 저장 basis로 돌아오면 current가 되는 v8.0-B 회귀 테스트를 추가했다.

3. `suppressTodayRecordAutoSync`

검색 결과 이 플래그는 몇몇 render 호출 주변에서 true/false로 바뀌지만, 실제 auto sync 실행 여부를 막는 조건으로는 쓰이지 않는다.
현재 기준갱신 흐름은 자동 저장보다 표시/수동 갱신 중심이라 즉시 버그로 단정하지 않는다.
하지만 이름과 역할이 어긋나 있을 가능성이 있으므로 기준갱신 핫픽스 단계에서 확인해야 한다.

2026-06-02 v8.0-C 반영:

- `render()`는 자동 Today snapshot sync를 호출하지 않는다.
- 이 플래그는 실제 분기에서 읽히지 않는 잔재 상태였으므로 제거했다.
- 관련 InBody / 기록 체중 적용 흐름은 기존처럼 `render()`를 호출한다. 산식, score formula, Coach, recentContext, InBody gate, backup schema는 바꾸지 않았다.

4. `record_detail_edit` source

`syncTodayRecordGoalSnapshot()`은 `snapshotSource === "record_detail_edit"`이면 자동 sync를 막는다.
그러나 `getTodayRecordConfirmationState()`는 해당 source여도 현재 기준과 다르면 stale 패널을 보여준다.
이건 "사용자가 Records에서 당시 기준을 직접 편집한 기록"과 "Today 기준으로 다시 덮어쓸 수 있는 기록"의 UX 경계가 더 필요할 수 있다.

2026-06-02 v8.0-C 반영:

- `record_detail_edit` snapshot은 자동 Today sync가 덮지 않는 보호 기준으로 유지한다.
- Today 기준과 다르면 stale 패널은 계속 보여준다. 이는 사용자가 명시적으로 현재 Today 기준으로 다시 저장할 수 있게 하는 경계다.
- 사용자가 기준갱신 버튼을 누르면 `today_manual_confirm`으로 snapshot source가 바뀌고, 식사 / 공복 체중 / 메모는 유지된다.
- `runTodayRecordConfirmationTests`에 자동 sync 차단과 수동 confirm 허용을 동시에 검증하는 v8.0-C 회귀 테스트를 추가했다.

## 9. 위험 지점 목록

P0 후보:

- 아직 확정 P0는 없다.
- diet targetCal > totalBurn 문제는 재현 케이스에서 확인되지 않았다.
- 1913 케이스는 targetCal 1913.3, totalBurn 2133.7로 diet targetCal <= totalBurn을 만족한다.

P1 후보:

1. 운동 수준 판단이 주간 웨이트일수 중심이다.
   - 위치: `getTargetRateTrainingBand()`
   - v8 기준의 "주간 웨이트일수 단독 기준 금지"와 직접 충돌 가능성이 있다.

2. final guide basis가 diet 단백질을 bodyweight 2.4g/kg으로 기본 적용한다.
   - 위치: `getDualBasisProteinRange()`, `calculateGuideBasisMacrosWithinTarget()`
   - 근손실 방지라는 내부 의도는 이해되지만, 외부 근거와 앱 적용범위 검토 없이 모든 diet 사용자에게 bodyweight 2.4g/kg 중간값을 확정할 수는 없다. 저칼로리 floor 지배 상황에서는 탄수화물을 guide minimum 아래로 밀 수 있으므로 미검증 생리학 후보로 남긴다.

3. snapshot 없는 detailed 오늘 기록이 "갱신 필요"로 보인다. [v8.0-A 처리 완료]
   - 위치: `getTodayRecordConfirmationState()`
   - `basis-missing` 상태와 "계산 기준 저장 필요" 문구로 분리했다.
   - desktop / mobile viewport 기준 `runTodayRecordConfirmationTests`는 통과했다.

P2 후보:

1. final result의 protein basis report가 부정확하게 읽힐 수 있다.
   - `calculate()` 최종 반환에서 `proteinBasisWeight`가 bodyweight로 덮인다.
   - v8 리포트 필드에는 bodyweight 기준과 LBM 기준을 모두 명확히 분리해야 한다.

2. selected macro basis 토글이 기준갱신을 띄우는 정책이 불명확하다. [v8.0-B 처리 완료]
   - 선택 basis가 macro 기준을 바꾸면 기준갱신 대상으로 본다.
   - `selectedMacroBasis` 문자열은 UI-only 상태로 유지하고, 저장 snapshot에는 선택 basis의 산출 macro 값만 보관한다.

3. `suppressTodayRecordAutoSync`가 실제 조건 분기에 쓰이지 않는다. [v8.0-C 처리 완료]
   - 실제 auto sync 분기가 빠진 것이 아니라, 더 이상 읽히지 않는 잔재 플래그였다.
   - 플래그를 제거하고 관련 흐름은 기존 `render()` 호출로 단순화했다.

## 10. 다음 작업 기준

현재 단계에서 하면 안 되는 것:

- 단백질 180.5g을 바로 낮추기
- guide/activity basis를 바로 제거하기
- score, Coach, Recent Flow, InBody gate, backup schema를 함께 건드리기
- 주간 웨이트일수 보정을 임의로 삭제하기
- snapshotless stale 표시를 CSS로 숨기기

다음 단계에서 해야 하는 것:

1. 현재 감사 결과를 기준으로 산식 리포트 객체를 설계한다.
2. 필수 필드를 포함한다.
   - protein_g
   - protein_g_per_kg_bodyweight
   - protein_g_per_kg_lbm
   - protein_rule_source
   - protein_soft_cap_hit
   - protein_hard_cap_hit
   - fat_g
   - fat_percent_kcal
   - fat_floor_hit
   - carb_g
   - carb_min_hit
   - redistribution_reason
3. candidate formula를 만들기 전에 현재/candidate side-by-side 시나리오 러너를 준비한다. [v8.0-D 1차 처리 완료]
   - `runV8ScenarioRunner()`는 8-2 필수축 category sweep과 8-3의 18개 human-review 케이스를 별도 레이어로 출력한다.
   - human-review 레이어에는 필수 `required_user_rest_diet_1913` 케이스를 포함한다.
   - current production과 기존 report-only `candidate-no-weekly-xw`를 나란히 보여준다.
   - `candidate-no-weekly-xw`는 비교 기준일 뿐이며, v8 승인 산식 또는 production 후보로 확정된 것이 아니다.
   - 8-2 목표 축의 `유지`는 보존한다. v8.0-D/I 당시에는 production `GOALS.maintain`이 없었으므로 유지 축 케이스를 `axisGoal=maintain`, `productionGoalFallback=recomp`로 명시했다. v8.0-J 이후 현재는 production maintain goal 1차가 들어가 `axisGoal=maintain`, `goal=maintain`, `productionGoalFallback=null`로 확인한다.
4. 사용자 타입/운동 프로필 판단은 주간 웨이트일수 단독이 아니라 체성분, SMM, FFMI, 세션 길이, 강도, 운동 프로필, cardio, 최근 기록 신뢰도까지 포함한 후보 모델로 문서화한다.
5. Today-Records 기준갱신의 사용자 노출 정책은 v8.0-A / v8.0-B에서 1차 고정했다.
   - snapshotless detailed 상태 분리는 v8.0-A에서 처리했다.
   - selected basis 토글 정책은 v8.0-B에서 처리했다.
   - `suppressTodayRecordAutoSync` 잔재 정리와 `record_detail_edit` UX 경계 확인은 v8.0-C에서 처리했다.
   - 9-4 현재/candidate side-by-side 시나리오 러너 1차본은 v8.0-D에서 처리했다.
   - 다음 큰 작업은 러너 출력으로 9-3 단백질/지방/탄수 감사를 수행하고, 그 결과를 바탕으로 9-5/9-6 candidate 설계를 좁히는 것이다.

## 11. 2026-06-02 추가 보강: 감사 한계와 다음 검증 요구

이 문서는 현재 구현을 추적한 감사 리포트이지, v8 산식이나 운동 프로필 구현 승인서가 아니다.
다음 작업자가 이 문서를 읽고 바로 산식을 고치거나 기능을 추가하면 안 된다.

오해하면 안 되는 점:

- 1913 / 196.3 / 180.5 / 45.1 케이스는 원인 재현 케이스이지 대표 검증 전체가 아니다.
- 단백질 180.5g은 "남은 칼로리를 단백질이 가져간 결과"가 아니라 guide basis의 bodyweight 2.4g/kg 규칙에서 나온 값이다.
- 주간 웨이트일수는 현재 target rate band의 입력이지만, v8 사용자 운동 수준 판단의 충분조건이 아니다.
- guide/activity basis 토글은 단순 표시 토글이 아니라 target macro / remaining / score 기준과 연결된다.
  - 2026-06-02 v8.0-B에서 이 항목은 "macro 출력값이 달라지면 기준갱신 대상, `selectedMacroBasis` 문자열은 UI-only"로 정책을 고정했다.
- snapshot 없는 detailed 기록이 "갱신 필요"로 보이는 현상은 CSS로 숨길 문제가 아니라 상태 분류와 사용자 행동 경로를 정해야 하는 문제다.
  - 2026-06-02 v8.0-A에서 이 항목은 `basis-missing` / "계산 기준 저장 필요"로 1차 처리했다.

현재 시나리오 검증의 부족한 부분:

- 브라우저에서 직접 확인한 기준갱신 시나리오는 핵심 흐름 위주다.
- v8.0-D에서 8-2 필수축 category sweep과 18개 human-review scenario 1차 그리드는 생겼지만, 전체 80,621,568,000 cross-product 실행과 각 케이스 수치의 human review / candidate formula 승인 검증은 아직 없다.
- smoke 테스트는 현재 구현의 기본 회귀 확인이지, v8 candidate 산식의 타당성 검증이 아니다.
- 현재 문서는 여성 / 고체지방 / 저체지방 운동인 / 러닝 중심 / 스트렝스 중심 / 기록 신뢰도 낮음 / 고유산소 / REST day / InBody 부재 또는 의심 기록 같은 케이스를 v8.0-D 러너에 포함했지만, 아직 수치별 해석/수정 판단을 완료하지 않았다.

v8.0-D 러너와 후속 시나리오 검증은 최소한 아래 축을 교차해야 한다.

- 목표: 유지 / diet / cut / lean_bulk / bulk
- 성별, 키, 체중, 골격근량, 체지방률, LBM 추정 신뢰도
- 활동량, 업무 유형, 주간 웨이트일수, 세션 길이, 세션 강도
- 운동 프로필: 보디빌딩, 러닝 중심, 스트렝스 중심, 혼합 운동, 운동 거의 없음
- 오늘 세션: REST / 보디빌딩 세션 / 파워빌딩 세션 / 스트렝스 세션 / 러닝 세션 / 혼합형 세션
- 유산소: 없음, 저강도, 중강도, 장시간, 고경사/고속, 웨이트와 병행
- 기록 상태: 오늘 상세 기록 있음, snapshot 없음, import/legacy, 식사만 있음, 체중만 있음, InBody 최신/과거/의심
- basis: guide / activity 선택 상태와 선택 변경 전후

v8.0-D 러너 해석 주의:

- 러너는 report-only다. 산식 / score formula / Coach / recentContext / InBody gate / backup schema를 바꾸지 않는다.
- 운동 프로필은 수치표의 축으로만 포함했다. 프로필별 세션 후보, UI 입력, snapshot 저장, Today/Records 연동은 아직 설계/구현 완료가 아니다.
- 운동 프로필별 report-only session과 current production routine fallback은 분리해서 읽어야 한다. 예를 들어 러닝 케이스의 `UPPER`는 러닝 세션이 아니라 보조 웨이트 계산 fallback이다.
- 8-2 필수축을 모두 곱한 80,621,568,000개 조합은 v8.0-D에서 전수 실행하지 않았다. v8.0-D는 모든 필수축 카테고리를 한 번씩 계산하는 category sweep과 18개 human-review 표를 분리한 1차 러너다.
- 80,621,568,000개 full cross-product는 단일 브라우저 내부 테스트로 돌릴 대상이 아니다. 1ms/케이스여도 약 933일이 걸리므로, 후속 검증은 full 축 정의를 보존한 상태에서 pairwise/orthogonal coverage, targeted stress, shard runner를 설계해야 한다.
- `유지` 목표는 8-2 필수축에서 삭제하거나 리컴프로 확정하지 않는다. v8.0-D/I는 당시 앱에 production 유지 키가 없다는 사실만 `productionGoalFallback=recomp`로 드러냈다. v8.0-J 현재는 production maintain goal 1차가 들어갔고, rate / cardio reflection / target floor / macro soft range / fat target / score / Coach / snapshot / backup-export-import 계약을 함께 닫았는지 확인한다. 단, profile-specific macro formula와 전체 v8 산식 승인은 아직 아니다.
- 업무 강도 보정축은 `office` 기준으로 검증하지 않는다.
  - 현재 산식은 `workloadY = clamp(WORK_TYPE_Y[workType] + workAdj, 0, 1)`이고 `WORK_TYPE_Y.office`는 0이라서 low 보정이 clamp에 막혀 base와 겹친다.
  - v8.0-D category sweep은 업무 보정축의 low/base/high가 앱 산식 안에서 실제로 분리되는지 보기 위해 `active` 업무 유형을 기준으로 삼는다.
  - 이 근거는 내부 코드 근거다. `WORK_TYPE_Y` 계수, 업무활동 계수, `0.06` 업무 가산폭, 실제 직업군별 활동량 모델이 외부 운동생리학적으로 타당하다는 뜻이 아니다.
  - v8.0-D1부터 report의 `axisCoverage.evidenceLevel`은 `internal_formula_coverage_only`로 표시하고, `impactScope`는 `report_only_axis_coverage_case_fixture`로 제한한다.
- candidate 열은 `candidate-no-weekly-xw` 하나만 비교한다. 이는 "주간 웨이트일수 단독 영향 제거 시 차이"를 보기 위한 기존 후보일 뿐, 다각적 v8 운동 프로필 산식 전체가 아니다.

다각적 기능 구현 기준:

- 기능을 많이 추가하는 것이 v8 목표가 아니다. 기능이 Today 계산, Records 저장, Recent 해석, InBody 근거, Settings 기본값, Coach 행동 문구 안에서 어떻게 이어지는지 먼저 정해야 한다.
- 새 기능은 입력 주체, 저장 위치, snapshot 포함 여부, Today/Records 갱신 영향, 최근 흐름 반영 여부, 사용자가 되돌릴 수 있는 경로를 함께 가져야 한다.
- 운동 프로필은 이름만 늘리면 안 된다. 프로필마다 세션 후보, 루틴 기본값, 칼로리 보정, 단백질/지방/탄수 기준, 기록 snapshot, Settings 기본값, Today 수정 흐름이 맞아야 한다.
- 충분히 설계하지 못한 프로필은 active 기능으로 열지 말고 disabled / 후속 후보로 둔다. 사용자가 세팅 단계에서 앱의 깊이를 의심하게 만드는 반쪽 기능은 금지한다.
- 사용자에게 실질적으로 도움이 되는 기준은 "화면에 기능이 있다"가 아니라 "사용자가 자기 상황을 넣었을 때 결과와 다음 행동을 믿을 수 있다"이다.

## 11-1. 2026-06-02 v8.0-D1 근거 등급 재감사

현재까지의 v8 작업을 근거 등급 기준으로 다시 보면 아래와 같다.

- v8.0-A snapshotless detailed 기록 분리
  - 근거 등급: 제품 정책 근거 + 내부 코드 근거
  - 의미: "갱신 필요"와 "저장 당시 계산 기준 없음"을 UX 상태로 분리한 것이다.
  - 외부 운동생리학 근거가 필요한 산식 변경은 아니다.
- v8.0-B selected macro basis 기준갱신 정책
  - 근거 등급: 제품 정책 근거 + 내부 코드 근거
  - 의미: 선택 basis가 실제 `targetCal/protein/carbs/fat` 저장값을 바꾸면 Records 기준갱신 대상으로 본다는 계약이다.
  - 영양학적으로 guide/activity 중 하나가 더 타당하다는 결론이 아니다.
- v8.0-C `record_detail_edit` 보호와 `suppressTodayRecordAutoSync` 잔재 정리
  - 근거 등급: 내부 코드 근거 + 제품 정책 근거
  - 의미: 자동 sync가 사용자의 Records 편집 기준을 덮지 않게 하는 저장 책임 정리다.
  - 운동 / 영양 산식 근거로 사용하지 않는다.
- v8.0-D 시나리오 러너 1차본
  - 근거 등급: 내부 코드 근거
  - 의미: 8-2 필수축 category sweep과 8-3 human-review 18개를 분리했고, 전체 80,621,568,000개 cross-product를 전수 실행하지 않았음을 명시한다.
  - 각 케이스의 수치 타당성, candidate formula 승인, profile별 세션 산식 적합성은 아직 완료되지 않았다.
- v8.0-D `유지` 목표 fallback
  - 근거 등급: 내부 코드 근거
  - 의미: v8.0-D 당시 production `GOALS`에 유지 키가 없다는 사실을 드러내기 위해 `axisGoal=maintain`, `productionGoalFallback=recomp`로 기록한 것이다.
  - 유지 목표를 리컴프로 확정한 것이 아니며, 실제 maintain 추가는 별도 production 변경이다.
  - v8.0-J 이후 이 항목은 히스토리다. 현재 구현은 maintain production goal 1차를 추가했으므로, 더 이상 현재 상태를 `maintain -> recomp`로 설명하면 안 된다.
- v8.0-D / D1 업무 강도 보정축 `active` fixture
  - 근거 등급: 내부 코드 근거
  - 의미: `office + low`가 clamp로 base와 겹치기 때문에, report-only category sweep에서 low/base/high가 실제로 분리되는지 보기 위한 fixture다.
  - `WORK_TYPE_Y` 계수나 실제 직업군별 활동량 모델을 외부적으로 검증한 근거가 아니다.
- 1913 / 196.3 / 180.5 / 45.1 단백질 회귀 케이스
  - 근거 등급: 내부 코드 근거 + 미검증 생리학 후보
  - 의미: 현재 guide basis에서 diet 단백질이 bodyweight 2.4g/kg까지 올라가는 구현을 재현한 것이다.
  - "단백질을 먼저 정하고 남은 칼로리를 탄수에 배분한다"는 구조는 내부 정책상 보존 후보지만, 모든 diet 사용자에게 2.4g/kg 기본 중간값을 적용해도 된다는 외부 근거 검토는 아직 끝나지 않았다.

재검토 결과, v8.0-A/B/C는 저장 / 기준갱신 / UX 책임 정리라서 외부 운동생리학 근거가 필요한 산식 변경으로 보지 않는다.
다만 1차 감사문서의 단백질 항목과 v8.0-D 업무 보정축 설명에는 내부 근거를 외부 타당성처럼 오해할 수 있는 표현이 남아 있었으므로 v8.0-D1에서 문서와 report metadata를 보강한다.

## 11-2. 2026-06-03 v8.0-E 9-3 현재 탄단지 감사 1차

v8.0-E에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `macroAudit` report-only 레이어를 추가했다.

근거 분리:

- ISSN protein position stand 2017
  - URL: https://jissn.biomedcentral.com/articles/10.1186/s12970-017-0177-8
  - 앱 사용: protein bodyweight g/kg이 일반 운동인 범위 상단을 넘는지 검토하는 외부 review threshold.
  - 이 근거만으로 모든 diet 사용자의 기본 단백질을 확정하지 않는다.
- Helms / Aragon / Fitschen natural bodybuilding contest prep review 2014
  - URL: https://pubmed.ncbi.nlm.nih.gov/24864135/
  - 앱 사용: 2.3~3.1g/kg LBM 단백질과 15~30% kcal 지방을 contest prep 맥락 후보로 검토한다.
  - 이 범위에 들어간다고 일반 diet 케이스가 자동 통과되는 것은 아니다.
- 현재 `index.html` macro decomposition
  - 근거 등급: 내부 코드 근거.
  - 앱 사용: 현재 산식이 어떤 값을 내는지 보여준다. 외부 생리학 타당성은 증명하지 않는다.

감사 규칙:

- `protein_bodyweight_above_general_issn_range`: 단백질이 bodyweight 2.0g/kg을 넘으면 일반 운동인 범위 상단 초과로 검토한다.
- `diet_default_near_2_4g_bodyweight_requires_candidate_review`: diet 기본값이 bodyweight 약 2.4g/kg에 가까우면 high-priority candidate review로 본다.
- `protein_lbm_in_contest_prep_range_but_context_unverified`: 단백질이 2.3~3.1g/kg LBM 범위에 들어가도, 해당 케이스가 contest prep 또는 그에 준하는 맥락인지 확인되지 않았으면 검토대상으로 둔다.
- `fat_percent_below_contest_prep_reference_floor`: 지방이 15% kcal 아래면 외부 contest-prep 하한 후보보다도 낮은 경고로 본다.
- `fat_at_guide_floor`, `carb_below_guide_min`: 내부 guide basis의 floor/min 충돌 신호다. 외부 근거가 아니라 현재 산식의 배분 순서 문제를 보여주는 내부 근거다.
- `exercise_profile_macro_formula_pending`: 운동 프로필은 report-only 축이고 아직 profile-specific macro formula가 없음을 표시한다.

v8.0-E runner 결과:

- `macroAudit` 전체 case count: 91
- 8-2 axis category fixture: 73
- 8-3 human-review case: 18
- 전체 finding count:
  - `protein_bodyweight_above_general_issn_range`: 77
  - `diet_default_near_2_4g_bodyweight_requires_candidate_review`: 77
  - `protein_lbm_in_contest_prep_range_but_context_unverified`: 76
  - `fat_at_guide_floor`: 85
  - `carb_below_guide_min`: 27
  - `fat_percent_below_contest_prep_reference_floor`: 4
  - `exercise_profile_macro_formula_pending`: 13
- human-review 18건 기준 finding count:
  - `protein_bodyweight_above_general_issn_range`: 8
  - `diet_default_near_2_4g_bodyweight_requires_candidate_review`: 8
  - `protein_lbm_in_contest_prep_range_but_context_unverified`: 8
  - `fat_at_guide_floor`: 15
  - `carb_below_guide_min`: 10
  - `fat_percent_below_contest_prep_reference_floor`: 1
  - `exercise_profile_macro_formula_pending`: 5
- 필수 `required_user_rest_diet_1913`:
  - protein: 2.4g/kg bodyweight
  - protein: 2.83g/kg LBM
  - fat: 21.23% kcal
  - findings: `protein_bodyweight_above_general_issn_range`, `diet_default_near_2_4g_bodyweight_requires_candidate_review`, `protein_lbm_in_contest_prep_range_but_context_unverified`, `fat_at_guide_floor`, `carb_below_guide_min`

해석 주의:

- 91건 count는 사용자 prevalence가 아니다. 73건은 axis category fixture라 기본 diet 케이스가 반복된다.
- 그럼에도 반복 신호가 크므로, 9-5/9-6 candidate 설계에서는 단백질 선택 기준을 목표 / 운동 프로필 / 체지방률 / LBM / 칼로리 제한 정도와 분리해야 한다.
- 현재 결론은 "단백질을 낮춰라"가 아니라 "일반 diet 기본값이 bodyweight 2.4g/kg으로 고정되는 구조를 candidate review 없이 승인하지 말라"이다.
- carb guide min 하회와 fat floor hit가 동시에 나타나는 케이스는 단백질-지방-탄수 배분 순서와 재분배 기준을 함께 설계해야 한다.

## 11-3. 2026-06-03 v8.0-F 9-5/9-6 candidate 설계 레이어 1차

v8.0-F에서는 v8.0-E의 `macroAudit` 결과를 `candidateDesign` report-only 레이어로 연결했다.

candidateDesign의 성격:

- 산식 적용이 아니다.
- candidate formula 승인도 아니다.
- 9-7 candidate 비교를 시작하기 전에 필요한 입력, 보류 조건, 외부근거 상태, production 계약을 명시하는 설계 레이어다.
- `candidateDesign.meta.productionFormulaChanged`는 false이고, `candidateFormulaApproved`도 false다.

입력 후보:

- `exerciseProfile`
  - Settings + Today report axis 소유.
  - non-bodybuilding profile은 아직 active production formula가 아니다.
- `profileSession`
  - Today 소유.
  - 러닝 세션을 `UPPER` 같은 production fallback과 섞어 말하지 않기 위한 필수 입력이다.
- `performanceLevel`
  - Settings + Today 소유.
  - 주간 웨이트일수 단독 판단을 피하기 위한 축이다.
- `bodyCompReliability`
  - Today calculation input 소유.
  - bodyweight / LBM / adjusted basis 단백질 기준을 분리하기 위해 필요하다.
- `recent14RecordCoverage`
  - Records / recentContext 소유.
  - 즉시 산식 변경이 아니라 기록 충분성 판단에 사용한다.
- `recent28Trend`
  - Records / recentContext 소유.
  - 목표 rate feedback 후보에 사용한다.

candidateDesign에서 남긴 필수 결정:

- `protein_context_gate`
  - 일반 diet 기본값이 bodyweight 2.4g/kg으로 고정되는 구조를 목표, 체지방률, LBM 신뢰도, 운동 프로필, 수행 수준, deficit 맥락으로 분리해야 한다.
- `fat_carb_joint_redistribution`
  - fat floor와 carb guide min 하회가 동시에 나타나는 케이스를 단일 영양소 수정으로 처리하지 않고 함께 재분배해야 한다.
- `profile_specific_carbohydrate_policy`
  - 러닝 / 스트렝스 / 파워빌딩 / 보디빌딩 / 혼합형은 세션별 탄수와 회복 규칙이 필요하다.
- `bodyweight_lbm_adjusted_basis_policy`
  - 체중 기준, LBM 기준, 보정 기준 단백질을 report와 candidate formula에서 분리해야 한다.
- `maintain_goal_mapping`
  - v8.0-F 당시 `maintain`은 축에는 남아 있지만 production `GOALS` key가 없으므로 리컴프로 확정하지 않았다.
  - v8.0-J 이후 `maintain`은 production key와 cross-app 계약이 함께 들어간 1차 production goal이다. 이 변경은 maintain goal mapping만 닫은 것이며, profile-specific macro formula 승인은 아니다.
- `profile_session_input_contract`
  - Today / Settings / Records snapshot / export-import에 새 profile/session 입력이 어떻게 들어가는지 정해야 한다.

미해결 production 계약:

- `maintain contract regression guard`
- Records `goalSnapshot` schema review for new profile inputs
- Today / Settings profile input ownership
- score / remaining / Coach / recentContext copy gates after formula approval
- export/import compatibility

해석 주의:

- v8.0-F는 "무슨 산식으로 바꿀지"를 확정하지 않았다.
- 대신 "무엇을 확정해야만 산식을 바꿀 수 있는지"를 report 객체로 고정했다.
- 다음 단계는 이 설계 계약을 기준으로 9-7 candidate formula 비교를 만들거나, maintain/profile input contract를 먼저 세분화하는 것이다.

## 11-4. 2026-06-03 v8.0-G 9-7 context macro candidate-v0 비교 1차

v8.0-G에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `candidateFormulaComparison` report-only 레이어를 추가했다.

candidate-v0의 성격:

- model: `candidate-v8-context-macro-v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `targetChanged`: false
- `profileSpecificCarbohydratePolicyApplied`: false
- v8.0-G 당시 `maintainGoalMappingApplied`: false
- v8.0-J 이후 `maintainGoalMappingApplied`: true
  - 의미: candidate-v0 report가 production maintain mapping을 볼 수 있다는 뜻이다.
  - 비의미: candidate-v0가 maintain macro formula를 새로 승인했거나 전체 v8 formula를 승인했다는 뜻이 아니다.

candidate-v0가 하는 일:

- current targetCal을 그대로 유지한다.
- non-contest-prep 일반 diet 맥락에서는 단백질을 ISSN 2017 general exercising-individual review upper range인 bodyweight 2.0g/kg까지 낮추는 비교를 수행한다.
- 지방은 현재값을 유지한다. 단, 외부 contest-prep lower reference보다 낮은 경우에만 report-only 비교에서 하한을 올린다.
- 남은 target kcal은 탄수로 재배분한다.

candidate-v0가 하지 않는 일:

- profile-specific carbohydrate formula를 적용하지 않는다.
- running / strength / powerbuilding / mixed 세션별 carbohydrate periodization을 적용하지 않는다.
- v8.0-G 당시에는 maintain goal production key를 만들지 않았다.
- v8.0-J 이후에는 maintain goal production key가 존재한다. 다만 candidate-v0는 current production maintain 계산을 비교에 포함할 뿐, maintain 전용 macro formula를 별도로 승인하지 않는다.
- score, remaining, Coach, recentContext, InBody gate, backup schema를 바꾸지 않는다.
- candidate-v0를 최종 v8 production formula로 승인하지 않는다.

근거 검토:

- ISSN protein 2017은 "대부분의 운동하는 개인"에 대한 1.4-2.0g/kg/day 범위를 제공한다. 따라서 v8.0-G에서는 2.0g/kg을 일반 diet 기본값의 검토 상한으로만 사용했다.
- Helms / Aragon / Fitschen 2014는 자연 보디빌딩 contest prep 맥락에서 LBM 2.3-3.1g/kg protein과 fat 15-30% kcal을 제시한다. 따라서 일반 diet 사용자에게 곧장 2.4g/kg BW를 승인하는 근거로 쓰지 않는다.
- Academy / Dietitians of Canada / ACSM 2016은 탄수화물이 training load와 session quality 맥락에서 조정되어야 한다는 근거다. v8.0-G에서는 이 근거를 profile-specific carbohydrate policy의 보류 근거로만 유지하고, candidate-v0에 적용하지 않았다.
- `currentFormulaReport`는 내부 코드 근거일 뿐 외부 운동생리학 타당성 근거가 아니다.

필수 1913 케이스 runner 결과:

- current: target 1913.3kcal / protein 180.5g / carbs 196.3g / fat 45.1g
- candidate-v0: target 1913.3kcal / protein 150.4g / carbs 226.4g / fat 45.1g
- delta: target 0 / protein -30.1g / carbs +30.1g / fat 0
- resolved findings: `protein_bodyweight_above_general_issn_range`, `diet_default_near_2_4g_bodyweight_requires_candidate_review`, `carb_below_guide_min`
- remaining findings: `protein_lbm_in_contest_prep_range_but_context_unverified`, `fat_at_guide_floor`

전체 91 케이스 runner 요약:

- targetChangedCount: 0
- proteinReducedCount: 77
- carbsIncreasedCount: 77
- candidateHighPriorityCaseCount: 0
- remainingFindingCounts에는 `fat_at_guide_floor`, `protein_lbm_in_contest_prep_range_but_context_unverified`, `carb_below_guide_min`, `exercise_profile_macro_formula_pending`이 남는다.
- newFindingCounts에 `protein_lbm_in_contest_prep_range_but_context_unverified` 6건이 생긴다. 이는 단백질을 2.0g/kg BW로 낮추어도 일부 체성분에서는 LBM 기준 contest-prep range에 들어갈 수 있음을 뜻하며, candidate-v0가 최종 승인 산식이 아니라는 추가 근거다.

해석 주의:

- v8.0-G는 "단백질을 낮추면 모든 문제가 해결된다"는 결론이 아니다.
- v8.0-G는 "현재 diet 2.4g/kg BW 기본값이 일반 사용자 기본값으로는 과한 신호를 만든다"는 현재 감사 결과를 후보 비교 수치로 확인한 단계다.
- profile-specific carbohydrate와 profile input의 Records snapshot / export-import contract는 다음 단계에서 별도로 다뤄야 한다. maintain production goal의 1차 계약은 v8.0-J에서 닫았지만, 이를 profile-specific macro formula 승인으로 읽으면 안 된다.

## 11-5. 2026-06-03 v8.0-H 9-7 profileSession carbohydrate policy review 1차

v8.0-H에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `profileSessionCarbPolicy` report-only 레이어를 추가했다.

이 레이어의 목적:

- 운동 프로필별 세션을 production routine fallback과 분리해서 본다.
- current와 `candidate-v8-context-macro-v0`의 탄수 g/kg를 profileSession별 검토범위와 비교한다.
- running / mixed의 endurance-carried load와 bodybuilding / strength / powerbuilding의 resistance load를 같은 규칙으로 뭉개지 않는다.
- profile-specific carbohydrate formula를 승인하거나 적용하지 않는다.

근거 검토:

- Academy / Dietitians of Canada / ACSM 2016 position stand는 운동 수행과 회복이 훈련/경기 시나리오별 nutrition strategy에 의해 개선될 수 있고, 식품/영양소의 양과 timing이 scenario-specific이어야 한다고 제시한다. 이 문서는 v8.0-H에서 running/mixed training-load carbohydrate review reference로만 사용한다.
- Escobar / VanDusseldorp / Kerksick 2016 BJN review는 resistance-based exercise의 carbohydrate 필요량은 endurance보다 직접 확립되어 있지 않으며, strength/power/bodybuilding 맥락에는 별도 주의가 필요하다는 근거로 사용한다.
  - v8.0-H 작성 당시 Henselmans / Schoenfeld로 표기한 것은 출처 오기다. v8.0-K에서 코드와 문서를 정정했다.
- 따라서 v8.0-H는 endurance carb range를 strength / powerbuilding / bodybuilding에 그대로 씌우지 않고, `resistance_carb_evidence_limited_do_not_overapply_endurance_range` finding을 남긴다.
- `currentFormulaReport`는 내부 구현 근거이며, production routine fallback이 actual profile session과 다르다는 사실을 드러내는 데만 쓴다.

v8.0-H runner summary:

- 전체 profileSessionCarbPolicy case count: 91
- profileFormulaPendingCount: 13
- productionFallbackCount: 13
- currentBelowReviewFloorCount: 56
- candidateBelowReviewFloorCount: 50
- highPriorityCaseCount: 3

human-review summary:

- case count: 18
- profileFormulaPendingCount: 5
- productionFallbackCount: 5
- currentBelowReviewFloorCount: 4
- candidateBelowReviewFloorCount: 3
- highPriorityCaseCount: 1

대표 확인:

- `running_focused`
  - profileSession: `running_tempo_50m_support_upper`
  - productionRoutineFallback: `UPPER`
  - current/candidate carbs: 6.83g/kg
  - review range: 5-7g/kg
  - interpretation: 탄수량 자체는 around-one-hour endurance review range 안이지만, `UPPER`는 보조 상체 fallback일 뿐 actual running session이 아니며 cardio-carried running load가 아직 macro policy에 연결되지 않았다.
  - findings: `profile_formula_pending_non_bodybuilding`, `production_routine_fallback_not_profile_session`, `running_load_carried_by_cardio_not_macro_policy`
- `strength_focused`
  - profileSession: `strength_heavy_lower_90m`
  - productionRoutineFallback: `LOWER`
  - current/candidate carbs: 5.67g/kg
  - resistance review range: 3-5g/kg
  - interpretation: high carb 자체를 오류로 단정하지 않는다. 다만 strength profile에는 endurance range를 그대로 적용하지 않고 resistance-specific policy를 별도로 설계해야 한다.
  - findings: `profile_formula_pending_non_bodybuilding`, `production_routine_fallback_not_profile_session`, `resistance_carb_evidence_limited_do_not_overapply_endurance_range`
- `low_reliability_user` mixed case
  - profileSession: `mixed_strength_cardio_70m`
  - current carbs: 5.52g/kg
  - candidate-v0 carbs: 5.88g/kg
  - mixed review floor: 6g/kg
  - interpretation: v8.0-G의 단백질 context 후보만으로는 mixed profile carbohydrate floor 문제가 해결되지 않는다.
- `required_user_rest_diet_1913`
  - rest-day case이므로 performance carbohydrate floor를 적용하지 않는다.

해석 주의:

- v8.0-H는 "profile-specific carbohydrate formula"를 구현한 것이 아니다.
- v8.0-H는 세션별 탄수 정책을 앱에 적용하기 전에, 어떤 프로필이 production routine fallback 때문에 오해될 수 있는지와 어떤 케이스가 current/candidate-v0 모두에서 여전히 세션 부하와 충돌하는지를 드러내는 감사 레이어다.
- 다음 단계에서는 이 결과를 바탕으로 maintain goal mapping 또는 profile-specific macro candidate-v1 중 어느 것을 production contract와 먼저 연결할지 결정해야 한다.

## 11-6. 2026-06-03 v8.0-I 9-6 maintain production contract review 1차
v8.0-I에서는 산식을 바꾸지 않고 `runV8ScenarioRunner()`에 `maintainGoalContract` report-only 레이어를 추가했다.

이 레이어의 목적:

- 8-2 목표 축의 `maintain`을 `recomp` fallback으로 계산해 본 사실을 유지 목표 승인으로 오해하지 않게 막는다.
- `GOALS.maintain` 하나만 추가하는 변경이 production-safe하지 않음을 코드 계약 단위로 드러낸다.
- v8.0-I 당시 scenario runner의 명시적 `maintain -> recomp` fallback과 production 함수들의 잠재적 `lean_bulk` silent fallback이 서로 다른 문제임을 분리한다.

확인된 production 계약:

- `GOALS.maintain`
- `CARDIO_TARGET_REFLECTION_RATES.maintain`
- `GOAL_RATE_POLICIES.maintain`
- `GOAL_TARGET_FLOOR_RULES.maintain`
- `GOAL_AWARE_MACRO_SOFT_RANGE_KG.maintain`
- `FAT_TARGET_PROFILES.maintain`
- `MACRO_SCORE_PROFILES.maintain`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.maintain`
- `DAILY_COACH_DECISION_PROFILES.maintain`
- `getDailyCoachGoalWording.maintain`
- `normalizeGoalSnapshot.goal=maintain`
- `Settings/Today goal chip integration`
- `backup/export/import maintain compatibility`

v8.0-I runner summary:

- `axisMaintainCaseCount`: 1
- `productionGoalExists`: false
- `missingContractCount`: 13
- `productionBlockerCount`: 13
- `silentFallbackRiskCount`: 6
- `autoExposureRiskCount`: 1
- `currentAxisFallbackGoal`: `recomp`
- `currentTargetRateFallback`: `lean_bulk`
- `currentFatTargetFallback`: `lean_bulk`
- `currentMacroScoreFallback`: `lean_bulk`
- `currentCoachDecisionFallback`: `lean_bulk`

해석:

- `maintain` 축 case는 현재 production `GOALS`에 유지 키가 없어서 `recomp`로 계산 fallback을 사용한다. 이것은 유지 목표를 리컴프로 확정했다는 뜻이 아니다.
- production에 `GOALS.maintain`만 추가하면 goal chip UI는 자동 노출될 수 있지만 rate/fat/score/Coach/copy/snapshot/export-import 계약이 비어 있어 실제 사용자는 유지 목표라고 눌렀는데 gain-phase류 fallback 안내를 볼 수 있다.
- 이 근거는 내부 코드 계약 + 제품 정책 감사 근거이며, 외부 운동생리학 근거가 아니다. 유지 목표의 실제 kcal delta, cardio 반영률, macro/score/Coach 기준을 정하려면 별도 제품 정책과 필요 시 외부 근거 검토가 필요하다.
- v8.0-I는 production 산식, score, Coach, recentContext, InBody gate, backup schema를 변경하지 않는다.

다음 production-safe 조건:

- `GOALS.maintain`을 추가하기 전에 rate, cardio reflection, target floor, macro soft range, fat target, score, Coach threshold/decision/copy, Records snapshot, export/import round-trip 테스트를 함께 준비한다.
- 유지 목표의 수치 정책이 internal-only이면 문서에 internal-only로 남기고, 외부 운동생리학 근거처럼 통과시키지 않는다.

## 11-7. 2026-06-03 v8.0-J maintain production goal 1차

v8.0-J에서는 v8.0-I에서 막아 둔 `GOALS.maintain` 단독 추가 위험을 피하기 위해 maintain production goal과 연결 계약을 함께 추가했다.

변경 범위:

- `GOALS.maintain`
- `CARDIO_TARGET_REFLECTION_RATES.maintain`
- `GOAL_RATE_POLICIES.maintain`
- `GOAL_TARGET_FLOOR_RULES.maintain`
- `GOAL_AWARE_MACRO_SOFT_RANGE_KG.maintain`
- `FAT_TARGET_PROFILES.maintain`
- `MACRO_SCORE_PROFILES.maintain`
- `DAILY_COACH_SIGNAL_THRESHOLD_PROFILES.maintain`
- `DAILY_COACH_DECISION_PROFILES.maintain`
- maintain 전용 Coach goal / decision / InBody copy
- `normalizeGoalSnapshot.goal=maintain`
- Settings / Today goal chip 자동 노출 확인
- backup / export / import maintain round-trip 확인

runner 직접 추출:

- `maintainGoalContract.meta.stage`: `9-6_maintain_production_contract_review_v1`
- `productionMaintainGoalAvailable`: true
- `maintainProductionApproved`: true
- `axisMaintainCaseCount`: 1
- `productionGoalExists`: true
- `missingContractCount`: 0
- `productionBlockerCount`: 0
- `silentFallbackRiskCount`: 0
- `autoExposureRiskCount`: 0
- `currentAxisProductionGoal`: `maintain`
- `currentAxisFallbackGoal`: null
- `currentTargetRateFallback`: `maintain`
- `currentFatTargetFallback`: `maintain`
- `currentMacroScoreFallback`: `maintain`
- `currentCoachDecisionFallback`: `maintain`

maintain axis case 직접 추출:

- `axisGoal`: `maintain`
- `goal`: `maintain`
- `goalAxisStatus`: `production_goal`
- `productionGoalFallback`: null
- `profileSession`: `bodybuilding_push`
- `selectedWeeklyRate`: 0
- `policyKind`: `maintain`
- `targetBurnBase`: 2537.7748276923076
- `finalTargetCal`: 2537.7748276923076
- `cardioTargetReflectionRate`: 0.50
- `goalTargetFloor`: 2417.7748276923076
- `goalTargetFloorApplied`: false

maintain macro 직접 추출:

- protein: 135g / 1.8g/kg BW / 21.28% kcal
- fat: 60g / 0.8g/kg BW / 21.28% kcal
- carbs: 364.44g / 4.859g/kg BW / 57.44% kcal
- macro gap: 0
- range context: protein 1.6 / 1.8 / 2.0g/kg, carbs 3 / 4 / 5g/kg low-moderate, fat floor 0.8g/kg, mid 1.2g/kg, max 1.5g/kg, flexibleMax 2.0g/kg

근거 분리:

- ISSN protein position stand 2017
  - URL: https://jissn.biomedcentral.com/articles/10.1186/s12970-017-0177-8
  - 앱 사용: maintain protein range가 일반 운동인 sports nutrition guard 밖으로 벗어나지 않는지 확인한다.
  - 비의미: maintain의 모든 macro 수치나 kcal 중립 범위를 외부 근거로 확정한다는 뜻이 아니다.
- IOM Dietary Reference Intakes Table C-5 AMDR
  - URL: https://www.ncbi.nlm.nih.gov/books/NBK208874/
  - 앱 사용: maintain macro distribution이 broad population macro distribution guard 밖으로 벗어나지 않는지 확인한다.
  - 비의미: 운동 프로필별 탄수 periodization 또는 유지 목표의 모든 세부 산식을 확정한다는 뜻이 아니다.
- maintain product policy
  - 앱 사용: 체중을 크게 움직이지 않는 중립 목표라서 `selectedWeeklyRate=0`, kcal guard ±120, target floor `totalBurn - 120`, cardio reflection 0.50, score/Coach neutral tolerance를 둔다.
  - 근거 등급: 내부 제품 정책이다. 외부 운동생리학 근거로 말하면 안 된다.

해석:

- v8.0-J는 maintain goal을 production에서 누를 수 있게 하는 1차 계약을 닫았다.
- v8.0-I의 `maintain -> recomp` / `lean_bulk` fallback 위험은 현재 상태가 아니라 히스토리와 회귀 보호 대상으로 남긴다.
- `maintainProductionApproved=true`는 maintain production goal 계약 승인이지, profile-specific macro formula 또는 전체 v8 완료가 아니다.
- 다음 작업에서 maintain 수치를 다시 조정하려면 external reference / product policy / score / Coach / Records / backup 영향을 다시 함께 검토한다.

## 11-8. 2026-06-03 v8.0-K profile macro candidate-v1 1차

v8.0-K에서는 v8.0-H가 남긴 profile-specific carbohydrate 미해결을 실제 candidate-v1 report로 연결했다.

candidate-v1의 성격:

- model: `candidate-v8-profile-macro-v1`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `targetChanged`: false
- `profileSpecificCarbohydratePolicyApplied`: true
- score / Coach / recentContext / InBody gate / backup schema 변경 없음

candidate-v1가 하는 일:

- `candidate-v8-context-macro-v0`를 출발점으로 사용한다.
- running / mixed 세션에서 profile/session 탄수 review floor를 targetCal 변경 없이 맞출 수 있는지 검토한다.
- fat은 guide/external lower guard 아래로 낮추지 않는다.
- protein은 context candidate의 보호선 아래로 더 낮추지 않는다.
- resistance-only profile에는 탄수 floor를 강제하지 않는다.

candidate-v1가 하지 않는 일:

- profile-specific macro formula를 production에 적용하지 않는다.
- target/rate를 올려서 탄수 floor를 억지로 맞추지 않는다.
- resistance profile에 endurance 탄수 range를 그대로 씌우지 않는다.
- Today / Settings profile-session 입력, Records snapshot, export/import, score/Coach 계약을 닫지 않는다.

근거 검토:

- Academy / Dietitians of Canada / ACSM 2016 position stand는 running/mixed의 training-load carbohydrate review floor 근거로만 사용한다.
- Escobar / VanDusseldorp / Kerksick 2016 BJN review는 resistance-based exercise의 carbohydrate 필요량이 endurance만큼 직접 확정되어 있지 않다는 제한적 직접성 근거로 사용한다.
- ISSN protein 2017과 Helms contest-prep 2014는 protein/fat 보호선과 candidate-v0 감사 맥락을 유지하기 위한 reference다.
- `currentFormulaReport`는 내부 코드 근거다. profile candidate-v1의 결과가 생리학적으로 승인되었다는 뜻이 아니다.

직접 추출 summary:

- `caseCount`: 91
- `targetChangedCount`: 0
- `profileCarbFloorAttemptedCount`: 3
- `profileCarbFloorMetCount`: 0
- `profileCarbFloorUnresolvedCount`: 3
- `carbsIncreasedFromContextCount`: 0
- `fatReducedFromContextCount`: 0
- `proteinReducedFromContextCount`: 0
- `resistanceReviewOnlyCount`: 79
- `highPriorityCaseCount`: 3

대표 확인:

- `running_focused`
  - profileSession: `running_tempo_50m_support_upper`
  - candidate-v0 carbs: 6.8294g/kg
  - candidate-v1 carbs: 6.8294g/kg
  - review range: 5-7g/kg
  - 해석: 탄수 floor 자체는 이미 range 안에 있으므로 재분배를 시도하지 않는다. 하지만 production `UPPER` fallback과 actual running load 분리는 여전히 production formula 전환 전 계약으로 남는다.
- `low_reliability_user` mixed case
  - profileSession: `mixed_strength_cardio_70m`
  - candidate-v0 carbs: 5.8758g/kg
  - candidate-v1 carbs: 5.8758g/kg
  - mixed review floor: 6g/kg
  - finding: `profile_carb_floor_attempted`, `profile_carb_floor_unresolved_by_target_constraints`, `profile_macro_candidate_requires_production_contract`
  - 해석: targetCal을 유지하고 protein/fat 보호선을 깨지 않으면 mixed floor를 맞출 수 없다. 이 케이스는 "탄수만 올리는 후보"로 닫을 수 없고 target/rate, protein/fat floor, Recent/Coach 계약을 함께 재설계해야 한다.
- `strength_focused`
  - profileSession: `strength_heavy_lower_90m`
  - finding: `resistance_profile_carb_floor_not_applied_limited_directness`
  - 해석: strength profile에 endurance floor를 강제하지 않는다.
- `required_user_rest_diet_1913`
  - rest-day case이므로 performance carbohydrate floor를 적용하지 않는다.

해석:

- v8.0-K는 profile-specific macro formula의 production 구현이 아니라 candidate feasibility audit이다.
- 단백질 context 후보와 maintain production goal만으로는 mixed profile 탄수 문제가 해결되지 않는다.
- v8.0-N 이후 다음 단계는 candidate-v2가 남긴 target/rate contract, protein guard conflict, score/Coach/Records 계약을 바탕으로 production 적용 가능성을 더 좁히거나, 필요하면 shard/orthogonal coverage를 추가하는 것이다. candidate-v2는 report-only feasibility audit이므로 아직 production formula가 아니다.

## 11-9. 2026-06-03 v8.0-L 8-2 pairwise / targeted stress 1차

v8.0-L에서는 80,621,568,000개 full cross-product를 실행했다고 주장하지 않으면서도, category sweep보다 강한 축 상호작용 검증을 추가했다.

pairwiseStress의 성격:

- stage: `8-2_pairwise_targeted_stress_v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `fullCartesianRun`: false
- nominal full Cartesian count: 80,621,568,000
- coverage mode: constrained pairwise covering array plus calculated stress cases

제약:

- `profileSession`은 `exerciseProfile`과 맞아야 한다. 단 `rest`는 모든 프로필의 휴식일로 허용한다.
- `rest` session은 positive `weightDuration`을 가질 수 없다.
- non-rest training session은 `weightDuration=0`을 가질 수 없다.
- `weeklyTrainingDays=0`은 non-rest training session 또는 positive `weightDuration`과 묶지 않는다.
- `performanceLevel=none`은 non-rest training session, weekly training, positive `weightDuration`과 묶지 않는다.
- running/mixed cardio session은 `cardio=none`과 묶지 않는다.

직접 추출 summary:

- `axisCount`: 20
- `includedPairRequirementCount`: 2472
- `excludedPairRequirementCount`: 49
- `generatedTupleCount`: 50
- `calculatedCaseCount`: 50
- `coveredPairRequirementCount`: 2472
- `missingPairRequirementCount`: 0
- `effectiveTupleMismatchCaseCount`: 0
- `derivedTupleMismatchCaseCount`: 31
- `inactiveTupleMismatchCaseCount`: 13
- `highPriorityJudgmentCaseCount`: 0

excluded pair 사유:

- `exercise_profile_session_mismatch`: 20
- `no_performance_with_training_session`: 5
- `training_session_without_weight_duration`: 5
- `weekly_zero_with_training_session`: 5
- `no_performance_with_weekly_training`: 3
- `no_performance_with_weight_duration`: 3
- `rest_session_with_weight_duration`: 3
- `weekly_zero_with_weight_duration`: 3
- `running_session_without_cardio_axis`: 1
- `mixed_session_without_cardio_axis`: 1

해석:

- 50개 pairwise stress case는 18개 human-review case를 대체하지 않는다.
- 2,472개 valid pair를 빠짐없이 덮지만, 80,621,568,000개 full cross-product 전체를 실행한 것은 아니다.
- `effectiveTupleMismatchCaseCount=0`이므로 직접 제어 가능한 pairwise tuple 값은 계산 입력에 보존된다.
- `derivedTupleMismatchCaseCount=31`은 FFMI가 height/weight/bodyFat과 동시에 독립적으로 맞기 어려운 파생축이라는 사실을 드러낸다. 이것은 숨길 문제가 아니라, FFMI 축을 production input으로 쓸 때 제약/우선순위를 정해야 한다는 근거다.
- `inactiveTupleMismatchCaseCount=13`은 rest day에서 intensity가 비활성화되는 것처럼 계산상 축이 꺼지는 케이스다.
- v8.0-M targeted stress는 derived FFMI conflict, rest-day inactive intensity, mixed carb unresolved, maintain + non-bodybuilding profile을 묶어 사람 눈 수치 검토를 확대하는 방향이 맞다.

## 11-10. 2026-06-03 v8.0-M targeted stress review 1차

v8.0-M에서는 v8.0-L pairwise stress가 남긴 residual signal을 그냥 메모로 두지 않고, 계산 가능한 targeted case와 계산하면 안 되는 constraint-only case로 분리했다.

targetedStress의 성격:

- stage: `8-2_targeted_stress_review_v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- `fullCartesianRun`: false
- source signals: `pairwiseStress.derivedTupleMismatches`, `pairwiseStress.inactiveTupleMismatches`, `profileMacroCandidateComparison.profileCarbFloorUnresolved`, `maintainGoalContract.plus.nonBodybuildingProfileFormulaPending`, `pairwiseStress.excludedPairRequirements`

직접 추출 summary:

- `caseCount`: 8
- `calculatedCaseCount`: 5
- `constraintOnlyCaseCount`: 3
- `derivedFfmiConflictCount`: 1
- `inactiveIntensityCaseCount`: 1
- `mixedCarbUnresolvedCaseCount`: 1
- `maintainNonBodybuildingCaseCount`: 2
- `highPriorityCaseCount`: 2

focus count:

- `derived_ffmi`: 1
- `inactive_intensity`: 1
- `mixed_carb_unresolved`: 2
- `maintain_non_bodybuilding`: 2
- `excluded_constraint`: 3

계산 케이스:

- `targeted_derived_ffmi_high_conflict`: 의도 tuple은 `ffmiBand=low`지만 height/weight/bodyFat/skeletal에서 파생된 실제 FFMI는 다르다. `derived_ffmi_axis_conflict_requires_priority_rule`로 표시한다.
- `targeted_rest_day_intensity_inactive`: rest day, `todayWeightDuration=0`에서 intensity 축은 숨은 training load가 아니며 `rest_day_intensity_axis_inactive_not_training_load`로 표시한다.
- `targeted_mixed_carb_unresolved`: mixed strength/cardio 고부하에서 candidate-v1이 `profile_carb_floor_unresolved_by_target_constraints`를 유지한다. 이는 target/rate와 protein/fat guard를 함께 여는 candidate-v2 필요 근거다.
- `targeted_maintain_running_contract`: maintain goal + running profile/session은 production maintain goal이 존재해도 running-specific macro formula와 UI/Records/Coach 계약이 별도 pending임을 표시한다.
- `targeted_maintain_mixed_contract`: maintain goal + mixed profile/session은 non-bodybuilding profile formula pending이다. 단, 이 케이스의 carb floor는 `profile_carb_floor_met_report_only`로 남았으므로 unresolved라고 과장하지 않는다.

constraint-only 케이스:

- `constraint_running_session_without_cardio_axis`: `running_session_without_cardio_axis`
- `constraint_profile_session_mismatch`: `exercise_profile_session_mismatch`
- `constraint_performance_none_training`: `no_performance_with_training_session`, `no_performance_with_weekly_training`, `no_performance_with_weight_duration`

해석:

- 8개 targeted stress는 80,621,568,000개 full Cartesian 실행이 아니다.
- 5개 calculated case는 18개 human-review case를 대체하지 않는다.
- 3개 constraint-only case는 앱이 계산해도 되는 유저 세션이 아니라, incoherent tuple을 계산하지 않도록 남기는 감사 기록이다.
- 외부 운동생리학 근거는 carbohydrate/protein/fat review threshold로만 쓰며, `currentFormulaReport`는 내부 구현 근거다.
- v8.0-M은 candidate-v8-profile-macro-v1을 production formula로 승인하지 않는다.

## 11-11. 2026-06-03 v8.0-N profile macro candidate-v2 1차

v8.0-N에서는 v8.0-M의 `targeted_mixed_carb_unresolved`를 candidate-v2 report-only 레이어로 연결했다. 핵심은 "탄수만 올리면 해결"이 아니라, target/rate delta와 protein/fat guard conflict를 같이 기록하는 것이다.

profileMacroCandidateV2Comparison의 성격:

- stage: `9-7_profile_macro_candidate_v2_linked_target_v0`
- model: `candidate-v8-profile-macro-v2-linked-target-v0`
- `reportOnly`: true
- `productionFormulaChanged`: false
- `candidateFormulaApproved`: false
- source: `candidate-v8-context-macro-v0`, `candidate-v8-profile-macro-v1`, `8-2_targeted_stress_review_v0`

직접 추출 summary:

- `caseCount`: 96
- `targetChangedCount`: 5
- `profileCarbFloorAttemptedCount`: 5
- `profileCarbFloorMetCount`: 5
- `profileCarbFloorUnresolvedCount`: 0
- `targetRateContractRequiredCount`: 5
- `proteinGuardConflictCount`: 4
- `fatGuardConflictCount`: 0
- `nonBodybuildingContractCount`: 16
- `highPriorityCaseCount`: 4

targeted calculated 5건 summary:

- `targetChangedCount`: 2
- `profileCarbFloorAttemptedCount`: 2
- `profileCarbFloorMetCount`: 2
- `profileCarbFloorUnresolvedCount`: 0
- `targetRateContractRequiredCount`: 2
- `proteinGuardConflictCount`: 1
- `nonBodybuildingContractCount`: 3
- `highPriorityCaseCount`: 1

`targeted_mixed_carb_unresolved` 직접 값:

- `targetDeltaKcal`: 43.72915555555528
- `targetRateDeltaEquivalentKgPerWeek`: 0.03975377777777753
- `carbsGPerKgBodyweight`: 6
- finding:
  - `profile_carb_floor_attempted_v2`
  - `profile_carb_floor_requires_target_rate_contract`
  - `profile_carb_floor_met_by_target_delta_report_only`
  - `profile_protein_guard_conflict_unresolved`
  - `profile_macro_candidate_v2_requires_production_contract`

해석:

- candidate-v2는 mixed carb floor feasibility를 target delta로 맞출 수 있음을 보여준다.
- 그러나 이 target delta는 diet/cut/recent28/score/Coach/Records 계약을 바꾸는 문제이므로 production 적용이 아니다.
- protein guard conflict는 모든 케이스에 무차별 적용하지 않고, profile carb floor를 실제 시도한 케이스에 한정했다. 이는 v2 목적보다 넓은 high finding을 만들지 않기 위한 수정이다.
- `profileCarbFloorUnresolvedCount=0`은 feasibility layer의 unresolved가 0이라는 뜻이지, profile-specific macro formula 전체가 끝났다는 뜻이 아니다.
- score, Coach, Settings/Today profile input, Records snapshot, export/import 계약은 아직 production 적용 전 필수 검토로 남는다.

## 12. 검증 메모

실행한 확인:

- index.html 코드 경로 직접 감사
- Playwright 기반 브라우저 로드 후 실제 `calculate()` 결과 확인
- 1913/196.3/180.5/45.1 케이스 재현
- Today-Records 기준갱신 시나리오 직접 확인
- 2026-06-02 보강 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-A 핫픽스 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-A Today record confirmation suite 1 suite / 9 cases / failed 0
- 2026-06-02 v8.0-A mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-A mobile Today record confirmation suite 1 suite / 9 cases / failed 0
- 2026-06-02 v8.0-A smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-A desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-A core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-A desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-B selected basis 정책 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-B Today record confirmation suite 1 suite / 10 cases / failed 0
- 2026-06-02 v8.0-B mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-B mobile Today record confirmation suite 1 suite / 10 cases / failed 0
- 2026-06-02 v8.0-B smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-B desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-B core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-B desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-C record_detail_edit 경계 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runTodayRecordConfirmationTests`
- v8.0-C Today record confirmation suite 1 suite / 11 cases / failed 0
- 2026-06-02 v8.0-C mobile 확인: `node .\tools\render_audit\run_internal_tests.cjs --mobile --suite=runTodayRecordConfirmationTests`
- v8.0-C mobile Today record confirmation suite 1 suite / 11 cases / failed 0
- 2026-06-02 v8.0-C smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-C desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-C core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-C desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-D 시나리오 러너 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-D scenario runner suite 1 suite / 8 cases / failed 0
- 2026-06-02 v8.0-D calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-D desktop calibration 14 suites / 116 cases / failed 0
- 2026-06-02 v8.0-D smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-D desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-D core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-D desktop core 26 suites / 364 cases / failed 0
- 2026-06-02 v8.0-D1 diff 확인: `git diff --check`
- v8.0-D1 diff check passed
- 2026-06-02 v8.0-D1 근거 등급 report 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-D1 scenario runner suite 1 suite / 8 cases / failed 0
- 2026-06-02 v8.0-D1 calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-D1 desktop calibration 14 suites / 116 cases / failed 0
- 2026-06-02 v8.0-D1 smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-D1 desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-02 v8.0-D1 core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-D1 desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-E diff 확인: `git diff --check`
- v8.0-E diff check passed
- 2026-06-03 v8.0-E 9-3 macro audit 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-E scenario runner suite 1 suite / 9 cases / failed 0
- 2026-06-03 v8.0-E macroAudit summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().macroAudit` 추출
- v8.0-E macroAudit 91 cases = axis category fixture 73 + human-review 18, required 1913 findings 5개 확인
- 2026-06-03 v8.0-E calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-E desktop calibration 14 suites / 117 cases / failed 0
- 2026-06-03 v8.0-E smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-E desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-E core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-E desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-F diff 확인: `git diff --check`
- v8.0-F diff check passed
- 2026-06-03 v8.0-F candidateDesign 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-F scenario runner suite 1 suite / 10 cases / failed 0
- 2026-06-03 v8.0-F calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-F desktop calibration 14 suites / 118 cases / failed 0
- 2026-06-03 v8.0-F smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-F desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-F core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-F desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-G diff 확인: `git diff --check`
- v8.0-G diff check passed
- 2026-06-03 v8.0-G candidate formula comparison 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-G scenario runner suite 1 suite / 11 cases / failed 0
- 2026-06-03 v8.0-G candidateFormulaComparison summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().candidateFormulaComparison` 추출
- v8.0-G candidateFormulaComparison 91 cases, targetChangedCount 0, proteinReducedCount 77, carbsIncreasedCount 77, required 1913 candidate 1913.3kcal / 150.4g protein / 226.4g carbs / 45.1g fat 확인
- 2026-06-03 v8.0-G calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-G desktop calibration 14 suites / 119 cases / failed 0
- 2026-06-03 v8.0-G smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-G desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-G core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-G desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-H diff 확인: `git diff --check`
- v8.0-H diff check passed
- 2026-06-03 v8.0-H profileSession carbohydrate policy 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-H scenario runner suite 1 suite / 12 cases / failed 0
- 2026-06-03 v8.0-H profileSessionCarbPolicy summary 직접 확인: Playwright Edge/Chrome fallback으로 `window.runV8ScenarioRunner().profileSessionCarbPolicy` 추출
- v8.0-H profileSessionCarbPolicy 91 cases, profileFormulaPendingCount 13, productionFallbackCount 13, currentBelowReviewFloorCount 56, candidateBelowReviewFloorCount 50, highPriorityCaseCount 3 확인
- 2026-06-03 v8.0-H calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-H desktop calibration 14 suites / 120 cases / failed 0
- 2026-06-03 v8.0-H smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-H desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-H core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-H desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-I diff 확인: `git diff --check`
- v8.0-I diff check passed
- 2026-06-03 v8.0-I maintain production contract 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-I scenario runner suite 1 suite / 13 cases / failed 0
- 2026-06-03 v8.0-I calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-I desktop calibration 14 suites / 121 cases / failed 0
- 2026-06-03 v8.0-I smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-I desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-I core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-I desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-J diff 확인: `git diff --check`
- v8.0-J diff check passed
- 2026-06-03 v8.0-J maintain production goal 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-J scenario runner suite 1 suite / 13 cases / failed 0
- 2026-06-03 v8.0-J impacted suite 확인:
  - `runTodayQuickEditTests` 26 cases / failed 0
  - `runAdherenceScoringTests` 44 cases / failed 0
  - `runDailyCoachRecentContextTests` 15 cases / failed 0
  - `runDataManagementBackupTests` 7 cases / failed 0
  - `runTargetMacroProductionPolicyTests` 8 cases / failed 0
  - `runMacroAllocationExplanationTests` 9 cases / failed 0
  - `runFullBackupMealsRestoreTests` 10 cases / failed 0
- 2026-06-03 v8.0-J calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-J desktop calibration 14 suites / 121 cases / failed 0
- 2026-06-03 v8.0-J smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-J desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-J core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-J desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-J UI 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=ui`
- v8.0-J desktop ui 18 suites / 144 cases / failed 0
- 2026-06-03 v8.0-J Browser 로컬 확인: `http://127.0.0.1:8765/index.html` 임시 서버로 로드해 Settings 목표 chip 확인
  - `유지` goal chip visible 1개 확인
  - `유지` 선택 후 chip class `chip active` 확인
  - 화면 text에서 `NaN` / `undefined` 없음 확인
  - 임시 서버 종료 확인
- 2026-06-03 v8.0-K diff 확인: `git diff --check`
- v8.0-K diff check passed
- 2026-06-03 v8.0-K profile macro candidate-v1 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-K scenario runner suite 1 suite / 14 cases / failed 0
- 2026-06-03 v8.0-K profile macro candidate-v1 직접 추출:
  - `profileMacroCandidateComparison.summary.caseCount=91`
  - `targetChangedCount=0`
  - `profileCarbFloorAttemptedCount=3`
  - `profileCarbFloorMetCount=0`
  - `profileCarbFloorUnresolvedCount=3`
  - `carbsIncreasedFromContextCount=0`
  - `resistanceReviewOnlyCount=79`
  - `highPriorityCaseCount=3`
  - resistance carbohydrate source가 Escobar / VanDusseldorp / Kerksick 2016으로 정정된 것 확인
- 2026-06-03 v8.0-K calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-K desktop calibration 14 suites / 122 cases / failed 0
- 2026-06-03 v8.0-K smoke 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=smoke`
- v8.0-K desktop smoke 10 suites / 121 cases / failed 0
- 2026-06-03 v8.0-K core 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=core`
- v8.0-K desktop core 26 suites / 364 cases / failed 0
- 2026-06-03 v8.0-K full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-K desktop full 99 suites / 1042 cases / failed 0
- 2026-06-03 v8.0-L pairwise stress 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-L scenario runner suite 1 suite / 15 cases / failed 0
- 2026-06-03 v8.0-L pairwise stress 직접 추출:
  - `pairwiseStress.summary.includedPairRequirementCount=2472`
  - `excludedPairRequirementCount=49`
  - `generatedTupleCount=50`
  - `calculatedCaseCount=50`
  - `coveredPairRequirementCount=2472`
  - `missingPairRequirementCount=0`
  - `effectiveTupleMismatchCaseCount=0`
  - `derivedTupleMismatchCaseCount=31`
  - `inactiveTupleMismatchCaseCount=13`
- 2026-06-03 v8.0-L calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-L desktop calibration 14 suites / 123 cases / failed 0
- 2026-06-03 v8.0-L full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-L desktop full 99 suites / 1043 cases / failed 0
- 2026-06-03 v8.0-M targeted stress 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-M scenario runner suite 1 suite / 16 cases / failed 0
- 2026-06-03 v8.0-M targeted stress 직접 추출:
  - `targetedStress.summary.caseCount=8`
  - `calculatedCaseCount=5`
  - `constraintOnlyCaseCount=3`
  - `derivedFfmiConflictCount=1`
  - `inactiveIntensityCaseCount=1`
  - `mixedCarbUnresolvedCaseCount=1`
  - `maintainNonBodybuildingCaseCount=2`
  - `highPriorityCaseCount=2`
- 2026-06-03 v8.0-M calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-M desktop calibration 14 suites / 124 cases / failed 0
- 2026-06-03 v8.0-M full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-M desktop full 99 suites / 1044 cases / failed 0
- 2026-06-03 v8.0-N profile macro candidate-v2 확인: `node .\tools\render_audit\run_internal_tests.cjs --suite=runV8ScenarioRunnerTests`
- v8.0-N scenario runner suite 1 suite / 17 cases / failed 0
- 2026-06-03 v8.0-N profile macro candidate-v2 직접 추출:
  - `profileMacroCandidateV2Comparison.summary.caseCount=96`
  - `targetChangedCount=5`
  - `profileCarbFloorAttemptedCount=5`
  - `profileCarbFloorMetCount=5`
  - `profileCarbFloorUnresolvedCount=0`
  - `targetRateContractRequiredCount=5`
  - `proteinGuardConflictCount=4`
  - `targeted_mixed_carb_unresolved.targetDeltaKcal=43.72915555555528`
  - `targeted_mixed_carb_unresolved.targetRateDeltaEquivalentKgPerWeek=0.03975377777777753`
- 2026-06-03 v8.0-N calibration profile 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=calibration`
- v8.0-N desktop calibration 14 suites / 125 cases / failed 0
- 2026-06-03 v8.0-N full internal suite 확인: `node .\tools\render_audit\run_internal_tests.cjs --profile=full`
- v8.0-N desktop full 99 suites / 1045 cases / failed 0

실행하지 않은 것:

- mobile screenshot audit
- candidate formula production approval
- profile-specific macro formula production implementation
- profile-specific UI / Records snapshot / export-import contract implementation
- 80,621,568,000개 8-2 full cross-product 전수 실행
- 18개 human-review scenario의 사람 눈 수치 승인

최초 작성 시점에는 구현 변경이 없는 감사 산출물이었으므로 전체 테스트를 생략했다.
v8.0-A snapshotless 기준 저장 상태 분리 이후에는 관련 suite, mobile viewport suite, desktop smoke, desktop core를 실행했다.
v8.0-B selected macro basis 정책 고정 이후에는 Today record confirmation suite를 desktop/mobile viewport에서 실행했고, desktop smoke/core 회귀도 다시 실행했다.
v8.0-C record_detail_edit 보호 / 잔재 플래그 정리 이후에는 Today record confirmation suite를 desktop/mobile viewport에서 실행했고, desktop smoke/core 회귀도 다시 실행했다.
v8.0-D 시나리오 러너 1차 구현 이후에는 8-2 필수축 category sweep과 8-3 human-review 분리, 유지 축 fallback 명시, 운동 프로필별 report-only session 분리를 `runV8ScenarioRunnerTests`로 확인했고, calibration/smoke/core 회귀도 다시 실행했다.
v8.0-D1 근거 등급 재감사 이후에는 업무 보정축 active fixture가 `internal_formula_coverage_only` / `report_only_axis_coverage_case_fixture`로 남는지 `runV8ScenarioRunnerTests`에서 확인했고, calibration/smoke/core 회귀도 다시 실행했다.
v8.0-E 9-3 현재 탄단지 감사 레이어 이후에는 `macroAudit`가 report-only / productionFormulaChanged false로 남고, 필수 1913 케이스가 단백질 context mismatch와 carb/fat 내부 충돌을 모두 표시하는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-F 9-5/9-6 candidate 설계 레이어 이후에는 `candidateDesign`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, maintain key, protein context gate, fat/carb joint redistribution, profile-specific carbohydrate policy, profile session input contract가 required next decision에 포함되는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-G 9-7 context macro candidate-v0 비교 이후에는 `candidateFormulaComparison`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, 필수 1913 케이스에서 targetCal은 유지하며 protein 180.5g -> 150.4g, carbs 196.3g -> 226.4g로 바뀌되 remaining findings가 보존되는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-H 9-7 profileSession carbohydrate policy review 이후에는 `profileSessionCarbPolicy`가 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, running 세션의 cardio-carried load와 UPPER fallback을 분리하며, resistance profile에는 endurance range를 무리하게 적용하지 않는 finding이 남는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-I 9-6 maintain production contract review 당시에는 `maintainGoalContract`가 report-only / productionFormulaChanged false였고, maintain production approval은 의도적으로 열지 않았다. 이 단계는 `maintain -> recomp` report fallback과 `lean_bulk` silent fallback 위험을 분리하며, `GOALS.maintain` 단독 추가를 막는지 `runV8ScenarioRunnerTests`로 확인했다.
v8.0-J maintain production goal 1차 이후에는 `maintainGoalContract`가 report-only / productionFormulaChanged false로 남되 `productionMaintainGoalAvailable=true`, `maintainProductionApproved=true`, `missingContractCount=0`, `currentAxisFallbackGoal=null`로 바뀌는지 확인했다. 이 승인은 maintain production goal 계약에 한정되며, profile-specific macro formula 또는 전체 v8 완료를 뜻하지 않는다.
v8.0-K profile macro candidate-v1 이후에는 `profileMacroCandidateComparison`이 report-only / productionFormulaChanged false / candidateFormulaApproved false로 남고, running/mixed 탄수 floor를 targetCal 변경 없이 맞출 수 있는지 검토했다. 직접 추출 결과 mixed floor 시도 3건은 모두 target/protein/fat 보호선 때문에 unresolved로 남았고, 이는 candidate-v2에서 target/rate와 protein/fat guard를 함께 열어야 한다는 근거다.
v8.0-L pairwise stress 이후에는 8-2 full Cartesian을 실행했다고 주장하지 않으면서, 제약상 가능한 pair 2,472개를 50개 계산 케이스로 모두 덮는지 확인했다. 직접 제어 가능한 tuple mismatch는 0이며, FFMI 파생축과 rest-day inactive intensity는 별도 mismatch로 분리했다.
v8.0-M targeted stress 이후에는 pairwise/profile macro residual signal을 계산 가능한 5개 targeted case와 계산하면 안 되는 3개 constraint-only case로 분리했다. mixed focus는 2건이지만 실제 unresolved finding은 1건이며, maintain mixed는 report-only carb-floor-met으로 남겨 과장하지 않았다. 이 단계도 full Cartesian, 18개 human-review 대체, candidate production approval이 아니다.
v8.0-N profile macro candidate-v2 이후에는 mixed carb floor를 target delta로 맞출 수 있는지 report-only로 계산했다. targeted mixed case는 +43.729kcal, 주간 rate equivalent +0.0398kg/week로 6g/kg에 도달하지만, protein guard conflict와 production contract pending이 남으므로 production formula가 아니다.

# v8.2 macro range candidateScorePreview sum clamp rounding test design

상태:

```text
문서 성격: candidateScorePreview sum / clamp / rounding test design docs-only
작업 범위: future test-only guard 설계
index.html 변경: 없음
package.json 변경: 없음
tools/render_audit 변경: 없음
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 구현: 없음
candidateScorePreview production field 숫자화: 없음
comparison.candidateScorePreview 숫자화: 없음
component pointsPreview production field 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
candidate score storage 구현: 없음
goalSnapshot schema 변경: 없음
record schema 변경: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
DailyCoach action 변경: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_candidate_score_preview_sum_clamp_rounding_decision_2026-07-06.md` 이후 test design이다.

목적:

```text
future candidateScorePreview sum / clamp / rounding formula를 테스트로 고정하기 전에 필요한 fixture, exact expected value, invalid payload rejection, no-leak 금지선을 문서로 닫는다.
아직 index.html에 suite를 구현하지 않고, rangeScoreCandidateSummary.candidateScorePreview에 숫자를 넣지 않는다.
```

핵심 결론:

```text
다음 test-only suite 후보는 runMacroRangeCandidateScorePreviewSumClampRoundingDecisionTests다.
테스트 설계의 첫 목적은 production score 계산이 아니라, candidateScorePreview total formula v0 후보와 금지선을 고정하는 것이다.
candidateScorePreview 후보 formula는 clamp(100 + sum(kcal/protein/fat/carbExchange pointsPreview), 0, 100)이다.
dataQuality는 합산 제외다.
fractional input은 rounding하지 않고 invalid로 reject한다.
positive component point는 upper clamp로 흡수하지 않고 invalid로 reject한다.
comparison.candidateScorePreview와 scoreDeltaPreview는 계속 null이다.
storage / backup / Recent / UI / DailyCoach 연결은 계속 금지다.
```

## 1. current baseline

이미 닫힌 field decision:

```text
candidateScorePreview field name 예약
allowed location 후보: rangeScoreCandidateSummary.candidateScorePreview
default: null
record / goalSnapshot / backup / Recent / UI / DailyCoach 금지
```

이미 닫힌 component formula decision:

```text
component formula: macro_range_component_points_preview_formula_v0
component pointsPreview value domain: integer <= 0 또는 null
score component: kcal, protein, fat, carbExchange
dataQuality: null-only eligibility gate
positive bonus 금지
fractional points 금지
blocked sentinel zero 금지
```

이미 닫힌 candidate formula decision:

```text
candidate formula: macro_range_candidate_score_preview_sum_v0
basis label: macro_range_candidate_score_preview_transient
candidateScorePreview 후보 = clamp(100 + sum(score component pointsPreview), 0, 100)
rounding policy: identity / no-op integer
fractional input은 invalid
```

이미 test-only로 고정된 guard:

```text
production componentPreviews.*.pointsPreview null
production candidateScorePreview null
production scoreDeltaPreview null
testLocalNumericPreview nested-only
old fixed Records no-mutation
backup / Recent / UI / DailyCoach no-leak
macro-policy / core profile registration coverage
```

중요:

```text
candidate formula decision 완료 != candidateScorePreview production 숫자화
candidate formula test design 완료 != candidateScorePreview implementation readiness
candidate formula test design 완료 != comparison candidate score readiness
candidate formula test design 완료 != scoreDeltaPreview readiness
```

## 2. 이번 test design이 정하는 것

정하는 것:

```text
future suite 이름 후보
fixture category
assertion group
source component inventory
dataQuality exclusion assertion
exact sum expected values
lower clamp boundary fixture
upper clamp defensive-only assertion
fractional input rejection
positive input rejection
blocked / invalid null behavior
old fixed Records no-mutation guard
storage / backup / Recent / UI / DailyCoach no-leak guard
```

정하지 않는 것:

```text
actual rangeScoreCandidateSummary.candidateScorePreview number 구현
comparison.candidateScorePreview number 구현
comparison.scoreDeltaPreview number 구현
production component pointsPreview number 구현
candidate score storage schema
backup roundtrip
Recent aggregation
UI visibility copy
DailyCoach action
getDailyAdherenceScore replacement
ADHERENCE_SCORING_VERSION bump
production macro range
```

따라서 이번 문서 이후에도 production-facing 값은 다음과 같다.

```text
rangeScoreCandidateSummary.candidateScorePreview: null
comparison.candidateScorePreview: null
comparison.scoreDeltaPreview: null
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview: null
```

## 3. future suite identity

suite 후보:

```text
runMacroRangeCandidateScorePreviewSumClampRoundingDecisionTests
```

suite 성격:

```text
test-only guard
suite 내부 formula candidate helper
suite 내부 approved component point fixture
suite 내부 invalid payload rejection
suite 내부 promoted payload rejection
before/after side-effect comparison
production helper 변경 없음
```

등록은 실제 구현 단계에서만 한다.

## 4. fixture category

필수 fixture category:

```text
formula_inventory
source_component_inventory
eligible_all_neutral_sum
eligible_mixed_penalty_sum
eligible_strong_penalty_sum
lower_clamp_boundary
upper_clamp_defensive_only
dataQuality_excluded
missing_component_points
blocked_data_quality
positive_component_payload
fractional_component_payload
non_finite_component_payload
fixed_score_fallback_payload
test_local_promotion_payload
old_fixed_detailed_sufficient
old_fixed_simple_or_weight_only
storage_backup_recent_ui_dailyCoach_payload
promoted_candidateScorePreview_payload
```

fixture 원칙:

```text
approved component point fixture는 suite 내부 formula candidate fixture일 뿐 production output이 아니다.
production buildMacroRangeScoreCandidateReportOnly output은 계속 candidateScorePreview null / component pointsPreview null이어야 한다.
```

## 5. source component inventory assertion

합산 대상:

```text
kcal
protein
fat
carbExchange
```

합산 제외:

```text
dataQuality
```

assertion:

```text
score component inventory는 정확히 4개다.
dataQuality는 score component inventory에 들어가면 안 된다.
unknown component가 들어오면 candidateScorePreview null / violation이다.
missing score component가 있으면 candidateScorePreview null이다.
```

금지 source:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
fixedScoreSummary.score
saved adherencePercent
saved componentScores
getDailyAdherenceScore percent
backup payload
Recent aggregate
UI state
DailyCoach payload
```

## 6. exact sum assertion

formula:

```text
rawCandidateScorePreview = 100 + kcal + protein + fat + carbExchange
candidateScorePreview = clamp(rawCandidateScorePreview, 0, 100)
```

expected exact values:

```text
[0, 0, 0, 0] -> raw 100 -> candidate 100
[-10, 0, -5, 0] -> raw 85 -> candidate 85
[-16, -24, -12, -8] -> raw 40 -> candidate 40
[-10, -16, 0, -6] -> raw 68 -> candidate 68
[-16, 0, -5, -6] -> raw 73 -> candidate 73
```

assertion:

```text
exact expected value를 검증한다.
range label이나 severity label만으로 값을 추론하지 않는다.
candidateScorePreview는 integer 0..100이어야 한다.
```

## 7. lower clamp assertion

lower clamp fixture:

```text
[-80, -40, -30, -10] -> raw -60 -> candidate 0
[-120, 0, 0, 0] -> raw -20 -> candidate 0
```

assertion:

```text
rawCandidateScorePreview < 0이면 candidateScorePreview 후보는 0이다.
단, blocked / invalid / insufficient 상태를 0으로 표현하면 안 된다.
lower clamp 0은 eligible numeric path에서만 의미가 있다.
```

## 8. upper clamp defensive-only assertion

정상 component formula v0에서는 component points가 모두 <= 0이므로 raw candidate가 100을 초과할 수 없다.

따라서 test design은 다음을 구분한다.

```text
defensive clamp boundary: clamp(104, 0, 100) 자체는 100일 수 있음
approved formula path: positive component point가 있으면 candidateScorePreview null / violation
```

future suite가 검증할 것:

```text
positive component pointsPreview는 invalid payload로 reject
positive payload를 100으로 clamp해서 accepted candidate로 만들지 않음
upper clamp는 positive bonus 허용 장치가 아님
```

negative fixture:

```text
[4, 0, 0, 0] -> invalid positive component point
[0, 0, 2, 0] -> invalid positive component point
```

expected:

```text
candidateScorePreview null
violation includes positive_component_point
rangeScoreCandidateSummary.candidateScorePreview remains null
```

## 9. rounding rejection assertion

rounding policy:

```text
identity / no-op integer
```

normal path:

```text
integer component points -> integer raw total -> integer candidateScorePreview
```

negative fixture:

```text
[-10.5, 0, 0, 0]
[-10, -16.25, 0, 0]
[0, 0, -5.5, 0]
```

expected:

```text
candidateScorePreview null
violation includes fractional_component_point
no Math.round / floor / ceil recovery
no silent rounding
```

non-finite fixture:

```text
NaN
Infinity
-Infinity
string numeric value
```

expected:

```text
candidateScorePreview null
violation includes non_finite_or_non_numeric_component_point
```

## 10. blocked and null assertion

blocked fixture:

```text
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
unsupported_range_basis
invalid_range_basis
range_policy_missing
```

component point fixture:

```text
kcal null
protein null
fat null
carbExchange null
dataQuality null
```

expected:

```text
candidateScorePreview null
comparison.candidateScorePreview null
comparison.scoreDeltaPreview null
blocked candidateScorePreview is not 0
insufficient candidateScorePreview is not 0
invalid candidateScorePreview is not clamp 0
```

## 11. dataQuality exclusion assertion

dataQuality fixture:

```text
dataQuality.pointsPreview null
dataQuality.status full_day_candidate
dataQuality.status one_meal_pending
dataQuality.status insufficient_record
synthetic dataQuality.pointsPreview 0 payload
synthetic dataQuality.pointsPreview -5 payload
```

expected:

```text
dataQuality is excluded from sum
dataQuality.pointsPreview must remain null-only
dataQuality sentinel 0 is rejected
dataQuality negative score contribution is rejected
dataQuality blocked state keeps candidateScorePreview null
```

## 12. fixed score fallback rejection

negative fixture:

```text
fixedScoreSummary.score = 81
saved adherencePercent = 81
saved componentScores present
component points missing
candidateScorePreview attempted from fixed score
```

expected:

```text
candidateScorePreview null
violation includes fixed_score_fallback
saved adherencePercent unchanged
fixedScoreSummary.score remains comparison source only
```

금지:

```text
candidateScorePreview = fixedScoreSummary.score
candidateScorePreview = saved adherencePercent
candidateScorePreview = getDailyAdherenceScore percent
candidateScorePreview = saved component score average
```

## 13. test-local promotion rejection

현재 허용된 nested evidence:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
testLocalNumericPreview.testLocalCandidateScorePreviewVersion
testLocalNumericPreview.testLocalFixedScoreBasisLabel
testLocalNumericPreview.testLocalCandidateScoreBasisLabel
```

negative fixture:

```text
rangeScoreCandidateSummary.candidateScorePreview copied from testLocalNumericPreview.testLocalCandidateScorePreview
componentPreviews.*.pointsPreview copied from testLocalNumericPreview.testLocalComponentPoints
comparison.candidateScorePreview copied from testLocalNumericPreview
```

expected:

```text
promoted test-local payload rejected
production candidateScorePreview remains null
production componentPreviews.*.pointsPreview remains null
comparison.candidateScorePreview remains null
scoreDeltaPreview remains null
```

## 14. old fixed Records no-mutation assertion

old fixed fixture:

```text
old fixed detailed sufficient Record
old fixed simple Record
old fixed weight-only Record
old fixed missing snapshot Record
```

허용:

```text
sufficient detailed old fixed Record를 transient candidate formula eligibility 후보로 평가
```

금지:

```text
stored adherencePercent overwrite
stored adherenceScoringVersion overwrite
saved componentScores clone
old fixed record object mutation
record.candidateScorePreview 저장
goalSnapshot.candidateScorePreview 저장
backup candidateScorePreview 저장
Records UI candidate score 표시
Recent aggregation 사용
DailyCoach action 사용
```

expected:

```text
old fixed record JSON unchanged
old fixed saved score unchanged
old fixed saved scoring version unchanged
candidateScorePreview storage no-leak
```

## 15. promoted payload rejection

promoted payload keys:

```text
top-level candidateScorePreview
top-level score
top-level adherenceScore
top-level finalScore
rangeScoreCandidateSummary.candidateScorePreview number
rangeScoreCandidateSummary.candidateScorePreviewStatus computed
comparison.candidateScorePreview number
comparison.scoreDeltaPreview number
record.candidateScorePreview
goalSnapshot.candidateScorePreview
backupPayloadFragment candidateScorePreview
Recent payload candidateScorePreview
UI state candidateScorePreview
DailyCoach payload candidateScorePreview
```

expected:

```text
all promoted candidate score payloads rejected
allowed report-only candidate has no production-facing numeric preview shape
```

## 16. production consumer side-effect assertion

future suite는 candidate formula fixture 생성 전후로 다음을 비교한다.

```text
getDailyAdherenceScore percent
getDailyAdherenceScore detail.scoringVersion
buildFullBackupPayload
getRecentContextComponentCounts
getRenderedBodyText
generateDailyCoachOutput
recordsState.items
ADHERENCE_SCORING_VERSION
```

expected:

```text
production score unchanged
scoring version unchanged
backup unchanged
Recent unchanged
UI rendered text unchanged
DailyCoach unchanged
Records state unchanged
backup string에 candidateScorePreview / pointsPreview / scoreDeltaPreview 없음
```

## 17. assertion groups

future suite assertion groups:

```text
formula_identity
basis_label
source_component_inventory
dataQuality_exclusion
exact_sum_values
lower_clamp_boundary
upper_clamp_defensive_only
integer_no_op_rounding
fractional_input_rejection
positive_input_rejection
non_finite_input_rejection
blocked_null_behavior
fixed_score_fallback_rejection
test_local_promotion_rejection
old_fixed_records_no_mutation
promoted_payload_rejection
storage_backup_recent_ui_dailyCoach_no_leak
ADHERENCE_SCORING_VERSION_unchanged
```

## 18. expected future test cases

future suite test case 후보:

```text
v8.2 candidateScorePreview sum clamp rounding inventory stays closed
v8.2 candidateScorePreview sum clamp rounding exact values match formula table
v8.2 candidateScorePreview sum clamp rounding applies lower clamp only on eligible numeric path
v8.2 candidateScorePreview sum clamp rounding rejects invalid numeric inputs
v8.2 candidateScorePreview sum clamp rounding keeps blocked and unresolved candidates null
v8.2 candidateScorePreview sum clamp rounding rejects fallback and test-local promotion
v8.2 candidateScorePreview sum clamp rounding preserves old fixed Records
v8.2 candidateScorePreview sum clamp rounding has zero production consumer side effects
```

예상 케이스 수:

```text
8 cases
```

케이스 수는 구현 단계에서 fixture 중복 제거에 따라 조정 가능하다.

## 19. implementation registration candidate

실제 구현 단계에서 필요한 등록:

```text
index.html: runMacroRangeCandidateScorePreviewSumClampRoundingDecisionTests
index.html: window.runMacroRangeCandidateScorePreviewSumClampRoundingDecisionTests
package.json: test:macro-policy suite 목록
tools/render_audit/run_internal_tests.cjs: core profile
```

구현 단계에서 함께 확인할 것:

```text
단독 suite 실행
npm run test:macro-policy
npm run test:core
git diff --check
```

단:

```text
이번 docs-only test design 단계에서는 위 등록을 하지 않는다.
```

## 20. acceptance criteria

이번 docs-only 단계 통과 조건:

```text
index.html diff 없음
package.json diff 없음
tools/render_audit diff 없음
production code diff 없음
README 등록
candidateScorePreview sum / clamp / rounding decision 후속 기록 추가
future suite 후보 명시
fixture category 명시
assertion group 명시
exact sum table 명시
dataQuality exclusion 명시
lower clamp boundary 명시
upper clamp defensive-only 명시
fractional input rejection 명시
positive input rejection 명시
fixed-score fallback rejection 명시
test-local promotion rejection 명시
candidateScorePreview / comparison / scoreDelta production field null 유지
storage / backup / Recent / UI / DailyCoach no-leak 유지
git diff --check 통과
```

앱 테스트:

```text
docs-only 변경이므로 생략 가능
```

단:

```text
index.html / package.json / tools/render_audit를 변경했다면 test:macro-policy 실행이 필요하다.
```

## 21. next candidates

이 test design 이후 가능한 안전 후보:

```text
1. runMacroRangeCandidateScorePreviewSumClampRoundingDecisionTests test-only 구현
2. scoreDeltaPreview numeric formula decision docs-only
3. line hold / 보류
```

다음 구현 후보를 열더라도:

```text
production score 구현이 아니다.
production candidateScorePreview 저장이 아니다.
comparison.candidateScorePreview 숫자화가 아니다.
production scoreDeltaPreview 숫자화가 아니다.
storage/schema 변경이 아니다.
UI / Recent / DailyCoach 변경이 아니다.
```

## 22. final judgement

현재 결론:

```text
candidateScorePreview sum / clamp / rounding test design: 닫힘
candidateScorePreview formula readiness: docs-only 후보 닫힘
candidateScorePreview implementation readiness: 아직 아님
comparison.candidateScorePreview readiness: 아직 아님
scoreDeltaPreview readiness: 아직 아님
storage/schema readiness: 아직 아님
production score readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
이번 단계는 candidateScorePreview 숫자 구현이 아니라, future suite가 100 + score component points sum / lower clamp / invalid rounding rejection / no-leak 금지선을 어떻게 고정해야 하는지 닫는 docs-only test design이다.
```

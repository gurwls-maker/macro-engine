# v8.2 macro range candidateScorePreview numeric policy tests implementation

상태:

```text
작업 성격: candidateScorePreview numeric policy test-only guard 구현
index.html 변경: 테스트 suite 추가만 있음
package.json 변경: test:macro-policy suite 연결
tools/render_audit core profile 변경: suite 연결
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
candidateScorePreview production field 승격: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
storage/schema 구현: 없음
backup / Recent / UI / DailyCoach production 연결: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_candidate_score_preview_numeric_policy_test_design_2026-07-06.md`의 후속 test-only 구현 기록이다.

후속 decision 기록:

```text
v8.2_macro_range_score_delta_preview_policy_decision_2026-07-06.md
```

목적은 `candidateScorePreview` 숫자화를 여는 것이 아니라, `testLocalNumericPreview.testLocalCandidateScorePreview`와 production-facing `rangeScoreCandidateSummary.candidateScorePreview` 사이의 경계를 테스트로 고정하는 것이다.

## 1. 구현 요약

추가:

```text
runMacroRangeCandidateScorePreviewNumericPolicyDesignTests
```

등록:

```text
window.runMacroRangeCandidateScorePreviewNumericPolicyDesignTests
npm run test:macro-policy suite 목록
tools/render_audit/run_internal_tests.cjs core profile
```

suite 성격:

```text
test-only guard
suite 내부 fixture
suite 내부 promoted payload rejection
before/after side-effect comparison
production helper 변경 없음
```

## 2. 테스트 고정 범위

이번 suite는 다음을 고정한다.

```text
candidateScorePreview null
candidateScorePreviewStatus not computed
comparison.candidateScorePreview null
comparison.scoreDeltaPreview null
componentPreviews.*.pointsPreview null
testLocalNumericPreview nested-only
fixed score fallback 금지
old fixed Records no-mutation
record / goalSnapshot / backup no candidateScorePreview
Recent no-aggregation
UI no-visibility
DailyCoach no-action
promoted candidateScorePreview payload rejection
scoreDeltaPreview last-gate 유지
ADHERENCE_SCORING_VERSION unchanged
```

## 3. 테스트 케이스

추가된 케이스:

```text
v8.2 candidateScorePreview numeric policy test design inventory stays closed
v8.2 candidateScorePreview numeric policy keeps eligible test-local evidence nested only
v8.2 candidateScorePreview numeric policy blocks incomplete and impossible records without sentinel scores
v8.2 candidateScorePreview numeric policy preserves old fixed Records without fallback scores
v8.2 candidateScorePreview numeric policy rejects promoted production-facing payloads
v8.2 candidateScorePreview numeric policy has zero production consumer side effects
```

총 6개 케이스다.

## 4. test-local evidence separation

허용:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
testLocalNumericPreview.testLocalCandidateScorePreviewVersion
testLocalNumericPreview.testLocalFixedScoreBasisLabel
testLocalNumericPreview.testLocalCandidateScoreBasisLabel
```

계속 금지:

```text
rangeScoreCandidateSummary.candidateScorePreview 숫자값
comparison.candidateScorePreview 숫자값
comparison.scoreDeltaPreview 숫자값
componentPreviews.*.pointsPreview 숫자값
top-level candidateScorePreview
record.candidateScorePreview
goalSnapshot.candidateScorePreview
backupPayloadFragment candidateScorePreview
```

즉 nested test-local score evidence가 있어도 production `candidateScorePreview` 숫자화 승인으로 해석하지 않는다.

## 5. eligible / unresolved guard

eligible fixture:

```text
eligible_full_day_all_neutral
eligible_full_day_anchor_penalty_candidate
eligible_full_day_range_evidence_penalty_candidate
eligible_full_day_mixed_penalty_candidate
range_policy_missing
```

확인:

```text
eligible full-day는 nested test-local evidence를 가질 수 있음
range policy missing은 production candidateScorePreview null
candidate_score_numeric_policy decision 유지
fixed score fallback 없음
scoreDeltaPreview null
component pointsPreview null
```

## 6. blocked guard

blocked fixture:

```text
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
```

확인:

```text
production candidateScorePreview null
comparison.candidateScorePreview null
scoreDeltaPreview null
nested testLocalCandidateScorePreview null
nested testLocalComponentPoints null
blocked 상태를 sentinel 0으로 저장하지 않음
dataQuality.pointsPreview null-only
```

## 7. old fixed Records guard

old fixed detailed / simple / weight-only fixture를 둔다.

확인:

```text
stored adherencePercent 유지
stored adherenceScoringVersion 유지
fixed score summary 유지
old fixed record object no-mutation
simple record object no-mutation
weight-only record object no-mutation
candidateScorePreview fixed-score fallback 없음
records에 candidateScorePreview / scoreDeltaPreview / pointsPreview / rangeScoreCandidate 추가 없음
```

즉 sufficient detailed old fixed Record는 transient eligibility 후보가 될 수 있지만, 저장 score basis는 fixed score basis로 유지한다.

## 8. promoted payload rejection

suite 내부에서 금지 payload를 만든다.

포함:

```text
top-level candidateScorePreview
top-level candidateScorePreviewStatus: computed
top-level scoreDeltaPreview
top-level pointsPreview
rangeScoreCandidateSummary.candidateScorePreview
rangeScoreCandidateSummary.candidateScorePreviewStatus: computed
componentPreviews.kcal.pointsPreview
comparison.candidateScorePreview
comparison.scoreDeltaPreview
record.candidateScorePreview
goalSnapshot.candidateScorePreview
backupPayloadFragment.candidateScorePreview
backupPayloadFragment.scoreDeltaPreview
backupPayloadFragment.pointsPreview
```

확인:

```text
promoted production-facing candidate score shape는 guard violation으로 잡는다.
```

## 9. side-effect guard

candidate 생성 전후로 비교한다.

확인:

```text
getDailyAdherenceScore percent unchanged
scoringVersion unchanged
buildFullBackupPayload unchanged
backup string에 candidateScorePreview / scoreDeltaPreview / pointsPreview 없음
getRecentContextComponentCounts unchanged
rendered body text unchanged
generateDailyCoachOutput unchanged
recordsState.items unchanged
ADHERENCE_SCORING_VERSION unchanged
```

## 10. 계속 금지

이번 작업에서 계속 금지:

```text
production candidateScorePreview 계산
candidateScorePreview = sum(component pointsPreview)
candidateScorePreview fixed-score fallback
component pointsPreview 숫자화
scoreDeltaPreview 숫자화
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
fixed score weighting 변경
DailyCoach decision matrix 변경
Today score UI 변경
Records score display 변경
Recent aggregation 변경
backup / restore payload 변경
goalSnapshot에 score candidate 저장
macroRangeContract 저장
old fixed Records saved score overwrite
old fixed Records backup/schema migration
```

## 11. 검증 기준

필수:

```text
node tools/render_audit/run_internal_tests.cjs --suite=runMacroRangeCandidateScorePreviewNumericPolicyDesignTests
npm run test:macro-policy
npm run test:core
git diff --check
```

## 12. 최종 판단

현재 결론:

```text
candidateScorePreview numeric policy test-only guard: 구현 완료
candidateScorePreview numeric readiness: 아직 아님
component pointsPreview numeric readiness: 아직 아님
scoreDeltaPreview readiness: 아직 아님
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
candidateScorePreview numeric policy는 아직 production 숫자화가 아니라, test-local evidence / null production field / no-leak / no-mutation 금지선을 test-only로 고정한 상태다.
```

# v8.2 macro range score weight / severity decision tests implementation

상태:

```text
작업 성격: score weight / severity decision test-only guard
index.html 변경: 테스트 suite 추가만 있음
package.json 변경: test:macro-policy suite 연결
runner 변경: core profile suite 연결
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
storage/schema 구현: 없음
backup / Recent / UI / DailyCoach 연결: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_score_numeric_preview_weight_severity_decision_2026-07-05.md`의 후속 test-only 구현 기록이다.

목적은 `candidateScorePreview`, component `pointsPreview`, `scoreDeltaPreview`를 숫자로 만드는 것이 아니라, weight / severity decision lane이 숫자화 없이 고정되는지 테스트하는 것이다.

2026-07-05 latest-policy correction:

```text
old fixed Records no-reinterpret guard는 저장값 no-mutation guard다.
충분한 detailed old fixed Record를 latest-policy preview 후보에서 자동 제외한다는 뜻이 아니다.
```

## 1. 추가 suite

추가:

```text
runMacroRangeScoreWeightSeverityDecisionDesignTests
```

등록:

```text
window.runMacroRangeScoreWeightSeverityDecisionDesignTests
tools/render_audit/run_internal_tests.cjs core profile
package.json test:macro-policy
```

## 2. 테스트가 고정한 것

이번 suite는 다음을 테스트한다.

```text
anchor lane: kcal / protein
range evidence lane: fat / carbExchange
eligibility lane: dataQuality
각 lane numericWeight null
각 lane bonusAllowed false
blocked / strong / material / observation / neutral severity lane inventory
inside_recommended no bonus
exchange_explained no bonus
full_day_candidate no numeric preview
one_meal_pending / insufficient_record / snapshotless_debug_only / blocked_by_impossible_state blocked
candidateScorePreview null 유지
pointsPreview null 유지
scoreDeltaPreview null 유지
old fixed Records saved-value no-mutation guard 유지
production score / scoring version / snapshot signature / backup / Recent / UI / DailyCoach no-impact
```

## 3. weight lane

테스트에서 고정한 lane:

```text
anchor:
- kcal
- protein

range_evidence:
- fat
- carbExchange

eligibility:
- dataQuality
```

이 lane은 숫자 weight가 아니다.

유지되는 금지선:

```text
numericWeight: null
bonusAllowed: false
component pointsPreview: null
```

## 4. severity lane

테스트에서 고정한 lane:

```text
blocked:
- one_meal_pending
- insufficient_record
- snapshotless_debug_only
- blocked_by_impossible_state

strongPenaltyCandidate:
- protein low
- fat below_action_floor
- fat above_flexible

materialPenaltyCandidate:
- kcal high
- kcal low
- carb below_training_support
- carb above_candidate_range

observationCandidate:
- fat below_recommended
- fat above_recommended

neutralExplanationCandidate:
- kcal matched
- protein satisfied
- fat inside_recommended
- carb exchange_explained
- carb inside_candidate_range
- full_day_candidate
```

이 lane은 점수 감점 폭이 아니다.

## 5. 계속 금지

이번 작업에서 계속 금지:

```text
numeric candidate score helper 구현
component point 계산
candidateScorePreview 숫자화
pointsPreview 숫자화
scoreDeltaPreview 숫자화
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
fixed score weighting 변경
DailyCoach decision matrix 변경
Today score UI 변경
Records score display 변경
Recent aggregation 변경
backup / restore payload 변경
goalSnapshot에 score candidate 저장
macroRangeContract 저장
macroRangeBasisSignature 저장
old fixed Records saved score overwrite
old fixed Records backup/schema migration
```

## 6. 다음 단계

가능한 다음 단계:

```text
1. 외부 감사
2. old/new Records score basis decision docs-only
3. candidate score version naming docs-only
4. macro range line hold
```

아직 numeric score formula readiness / production score readiness / storage schema readiness / production macro range readiness는 아니다.

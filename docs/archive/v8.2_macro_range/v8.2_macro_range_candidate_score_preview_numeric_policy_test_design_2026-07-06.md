# v8.2 macro range candidateScorePreview numeric policy test design

상태:

```text
문서 성격: candidateScorePreview numeric policy test design docs-only
작업 범위: future test-only guard 설계
index.html 변경: 없음
package.json 변경: 없음
tools/render_audit 변경: 없음
production score 변경: 없음
getDailyAdherenceScore 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
candidateScorePreview 숫자화: 없음
pointsPreview 숫자화: 없음
scoreDeltaPreview 숫자화: 없음
candidateScorePreview production field 승격: 없음
candidateScorePreview persistence: 없음
goalSnapshot schema 변경: 없음
record schema 변경: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
DailyCoach action 변경: 없음
production macro range 구현: 없음
```

이 문서는 `v8.2_macro_range_component_points_preview_numeric_policy_tests_implementation_2026-07-06.md` 이후 gate design이다.

후속 구현 기록:

```text
v8.2_macro_range_candidate_score_preview_numeric_policy_tests_implementation_2026-07-06.md
```

목적:

```text
future candidateScorePreview numeric policy를 구현하기 전에 필요한 test-only guard의 범위, fixture, assertion, 금지선을 문서로 닫는다.
아직 rangeScoreCandidateSummary.candidateScorePreview 숫자값, component pointsPreview 합산, scoreDeltaPreview 비교, production score 연결은 열지 않는다.
```

핵심 결론:

```text
다음 test-only suite 후보는 runMacroRangeCandidateScorePreviewNumericPolicyDesignTests다.
테스트 설계의 첫 목적은 candidateScorePreview를 계산하는 것이 아니라, production-facing candidateScorePreview 숫자화 조건이 아직 닫혀 있음을 증명하는 것이다.
rangeScoreCandidateSummary.candidateScorePreview는 계속 null이다.
comparison.candidateScorePreview는 계속 null이다.
comparison.scoreDeltaPreview는 계속 null이다.
componentPreviews.*.pointsPreview는 계속 null이다.
testLocalNumericPreview.testLocalCandidateScorePreview는 nested test-local evidence일 뿐 production field가 아니다.
old fixed Records 저장값은 절대 덮어쓰지 않는다.
storage / backup / Recent / UI / DailyCoach 연결은 계속 금지다.
```

## 1. current baseline

이미 닫힌 field decision:

```text
candidateScorePreview field name 예약
allowed location 후보: rangeScoreCandidateSummary.candidateScorePreview
default: null
record / goalSnapshot / backup / Recent / DailyCoach / UI state로 복사 금지
scoreDeltaPreview 별도 gate
pointsPreview 별도 gate
```

이미 test-only로 열린 것:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
testLocalNumericPreview.testLocalCandidateScorePreviewVersion
testLocalNumericPreview.testLocalFixedScoreBasisLabel
testLocalNumericPreview.testLocalCandidateScoreBasisLabel
```

이미 고정한 no-leak:

```text
testLocalNumericPreview nested-only
consumer no-leak
component pointsPreview null
component no-bonus
old fixed Records no-mutation
macro-policy / core profile registration coverage
```

중요:

```text
testLocalNumericPreview 숫자 존재 != candidateScorePreview production field 숫자화 승인
component pointsPreview test guard 통과 != candidateScorePreview total formula readiness
consumer no-leak 통과 != production consumer 연결 승인
```

## 2. 이번 test design이 정하는 것

정하는 것:

```text
future test-only suite 이름 후보
candidateScorePreview numeric policy fixture category
candidateScorePreview assertion group
eligible / blocked / unsupported 구분
old fixed Records no-overwrite guard
test-local evidence와 production-facing field 분리 guard
scoreDeltaPreview last-gate guard
storage / backup / Recent / UI / DailyCoach no-leak guard
promoted candidateScorePreview payload rejection
```

정하지 않는 것:

```text
actual candidateScorePreview number
candidateScorePreview = sum(componentPreviews.*.pointsPreview)
candidateScorePreview fallback from fixed score
candidate score clamp / rounding policy
candidate score version name for production
ADHERENCE_SCORING_VERSION bump
scoreDeltaPreview formula
UI display copy
DailyCoach action
Recent aggregation
storage schema
backup roundtrip
```

따라서 이번 문서 이후에도 유지되는 값:

```text
rangeScoreCandidateSummary.candidateScorePreview: null
comparison.candidateScorePreview: null
comparison.scoreDeltaPreview: null
rangeScoreCandidateSummary.componentPreviews.*.pointsPreview: null
```

## 3. future suite candidate

suite 후보:

```text
runMacroRangeCandidateScorePreviewNumericPolicyDesignTests
```

suite 성격:

```text
test-only guard
production candidate score helper 아님
production scoring helper 아님
UI render test 아님
backup schema test 아님
Recent aggregation test 아님
DailyCoach action test 아님
```

실제 구현 단계 등록 후보:

```text
index.html internal test suite
window.runMacroRangeCandidateScorePreviewNumericPolicyDesignTests
package.json test:macro-policy
tools/render_audit core profile
```

주의:

```text
이번 docs-only 단계에서는 위 등록을 하지 않는다.
실제 suite 구현 단계에서만 index.html / package.json / tools/render_audit를 변경한다.
```

## 4. fixture categories

future test-only suite가 다뤄야 할 fixture category:

```text
eligible_full_day_all_neutral
eligible_full_day_anchor_penalty_candidate
eligible_full_day_range_evidence_penalty_candidate
eligible_full_day_mixed_penalty_candidate
old_fixed_detailed_sufficient
old_fixed_simple_record
old_fixed_weight_only_record
snapshotless_debug_only
one_meal_pending
insufficient_record
unsupported_range_basis
invalid_range_basis
range_policy_missing
component_points_policy_unresolved
storage_policy_unapproved
ui_visibility_unapproved
scoreDelta_policy_unresolved
leaky_promoted_candidateScorePreview_payload
```

fixture가 검증해야 하는 공통값:

```text
candidateScorePreview
candidateScorePreviewStatus
comparison.candidateScorePreview
comparison.scoreDeltaPreview
componentPreviews.*.pointsPreview
testLocalNumericPreview presence / nested-only
fixedScoreSummary source / version / score
guards.productionScoreUnchanged
guards.scoringVersionUnchanged
guards.backupRestoreUnaffected
guards.recentUnaffected
guards.uiUnaffected
guards.dailyCoachUnaffected
```

fixture가 검증하지 않는 값:

```text
actual candidate score
actual component point sum
actual score delta
actual user-facing score copy
actual DailyCoach action
actual Recent trend
```

## 5. candidateScorePreview status policy

현재 기본값:

```text
candidateScorePreview: null
candidateScorePreviewStatus: "not_computed_policy_unresolved"
```

blocked 후보:

```text
one_meal_pending
insufficient_record
snapshotless_debug_only
blocked_by_impossible_state
simple_record_not_numeric_candidate
weight_only_record_not_numeric_candidate
unsupported_range_basis
invalid_range_basis
range_policy_missing
score_persistence_unapproved
ui_visibility_unapproved
```

policy unresolved 후보:

```text
component weight unresolved
component pointsPreview numeric policy unresolved
candidate score sum policy unresolved
candidate score version policy unresolved
old fixed Records display policy unresolved
scoreDelta policy unresolved
```

중요:

```text
blocked와 unresolved 모두 production-facing candidateScorePreview는 null이다.
blocked를 0점으로 저장하지 않는다.
unresolved를 fixed score fallback으로 채우지 않는다.
```

## 6. assertion groups

future suite assertion group:

```text
1. candidateScorePreview field remains null
2. candidateScorePreviewStatus unresolved / blocked assertion
3. comparison.candidateScorePreview null assertion
4. comparison.scoreDeltaPreview null assertion
5. component pointsPreview null assertion
6. testLocalNumericPreview nested-only assertion
7. no fixed-score fallback assertion
8. old fixed Records no-mutation assertion
9. no record / goalSnapshot / backup candidateScorePreview assertion
10. Recent no-aggregation assertion
11. UI no-visibility assertion
12. DailyCoach no-action assertion
13. promoted candidateScorePreview payload rejection assertion
14. scoreDeltaPreview last-gate assertion
```

`computed` 관련:

```text
candidateScorePreviewStatus: "computed"는 future numeric policy가 별도 문서와 테스트로 닫힌 뒤에만 허용한다.
이번 test design에서는 computed를 expected output으로 두지 않는다.
```

## 7. old fixed Records policy

old fixed Records test design:

```text
sufficient detailed old fixed Record -> transient eligibility candidate로 분류 가능
simple old fixed Record -> candidateScorePreview numeric policy 대상 아님
weight_only old fixed Record -> candidateScorePreview numeric policy 대상 아님
snapshotless old fixed Record -> blocked / debug-only
insufficient old fixed Record -> blocked
```

계속 금지:

```text
saved adherencePercent overwrite
saved adherenceScoringVersion overwrite
saved score basis 변경
backup payload에 candidateScorePreview 추가
Records UI에 candidateScorePreview 표시
Recent aggregation에 candidateScorePreview 사용
DailyCoach reason/action에 candidateScorePreview 사용
```

검증 방향:

```text
old fixed Records는 latest-policy candidate score eligibility 후보가 될 수 있지만, 저장 score basis는 fixed로 유지한다.
```

## 8. test-local evidence separation

허용된 test-local evidence:

```text
testLocalNumericPreview.testLocalCandidateScorePreview
testLocalNumericPreview.testLocalComponentPoints
testLocalNumericPreview.testLocalCandidateScorePreviewVersion
testLocalNumericPreview.testLocalFixedScoreBasisLabel
testLocalNumericPreview.testLocalCandidateScoreBasisLabel
```

계속 닫힌 production-facing field:

```text
rangeScoreCandidateSummary.candidateScorePreview
comparison.candidateScorePreview
comparison.scoreDeltaPreview
componentPreviews.*.pointsPreview
top-level candidateScorePreview
record.candidateScorePreview
goalSnapshot.candidateScorePreview
backup.data.records[].candidateScorePreview
```

정책:

```text
testLocalNumericPreview는 evidence source다.
candidateScorePreview는 future production-facing candidate summary field다.
둘은 이름, 위치, persistence, visibility가 다르다.
```

## 9. relationship with pointsPreview

이번 test design으로 열지 않는 것:

```text
candidateScorePreview = sum(componentPreviews.*.pointsPreview)
component pointsPreview computed_candidate
component point sum formula
component point persistence
```

관계:

```text
component pointsPreview guard는 candidateScorePreview total formula의 선행 보호막이다.
candidateScorePreview numeric policy는 component pointsPreview shape / policy가 안전하더라도 별도 gate다.
```

따라서:

```text
component pointsPreview numeric policy test 통과 != candidateScorePreview numeric readiness
```

## 10. relationship with scoreDeltaPreview

이번 test design으로 열지 않는 것:

```text
comparison.scoreDeltaPreview
top-level scoreDeltaPreview
record scoreDeltaPreview
UI score delta copy
Recent improvement trend
DailyCoach score delta action
```

정책:

```text
scoreDeltaPreview는 fixed score와 candidateScorePreview의 직접 비교다.
candidateScorePreview production field numeric policy가 닫힌 뒤에도 scoreDeltaPreview는 별도 last gate로 남긴다.
```

따라서:

```text
scoreDeltaPreview는 계속 null이다.
```

## 11. promoted payload rejection design

future suite는 아래 payload를 reject해야 한다:

```text
{
  candidateScorePreview: 92,
  rangeScoreCandidateSummary: {
    candidateScorePreview: 92,
    candidateScorePreviewStatus: "computed",
    componentPreviews: {
      kcal: { pointsPreview: 0 }
    }
  },
  comparison: {
    candidateScorePreview: 92,
    scoreDeltaPreview: 11
  },
  goalSnapshot: {
    candidateScorePreview: 92
  },
  backupPayloadFragment: {
    candidateScorePreview: 92
  }
}
```

기대 assertion:

```text
top_level:candidateScorePreview rejected
rangeScoreCandidateSummary.candidateScorePreview rejected
rangeScoreCandidateSummary.candidateScorePreviewStatus:computed rejected
componentPreviews.kcal.pointsPreview rejected
comparison.candidateScorePreview rejected
comparison.scoreDeltaPreview rejected
goalSnapshot.candidateScorePreview rejected
backupPayloadFragment.candidateScorePreview rejected
```

주의:

```text
reject는 production sanitizer 구현 지시가 아니다.
test-local guard에서 promoted payload가 금지선 위반임을 명시하는 것이다.
```

## 12. storage / backup / Recent / UI / DailyCoach no-leak

테스트 설계상 계속 닫힌 consumer:

```text
record state
goalSnapshot
backup export
backup import
records-only import
Recent context
Recent summary
Today UI state
Records UI state
DailyCoach payload
DailyCoach action
```

금지 payload:

```text
candidateScorePreview
scoreDeltaPreview
pointsPreview
rangeScoreCandidate
rangeScoreCandidateSummary
macroRangeContract.scoring.candidateScorePreview
```

허용 payload:

```text
testLocalNumericPreview source-only object
```

단:

```text
testLocalNumericPreview도 backup / Recent / UI / DailyCoach consumer로 새면 안 된다.
```

## 13. acceptance criteria

이번 docs-only 단계 통과 조건:

```text
index.html diff 없음
package.json diff 없음
tools/render_audit diff 없음
production code diff 없음
README 등록
component pointsPreview numeric policy tests implementation 후속 기록 추가
future suite 후보 명시
fixture category 명시
assertion group 명시
candidateScorePreview null 유지
comparison.candidateScorePreview null 유지
scoreDeltaPreview null 유지
pointsPreview null 유지
testLocalNumericPreview nested-only 유지
old fixed Records no-overwrite 유지
storage / backup / Recent / UI / DailyCoach no-leak 유지
git diff --check 통과
```

앱 테스트:

```text
docs-only 변경이므로 생략 가능
```

단:

```text
index.html / package.json / tools/render_audit를 변경했다면 test:macro-policy 실행이 필요하다.
```

## 14. next candidates

이 test design 이후 가능한 안전 후보:

```text
1. runMacroRangeCandidateScorePreviewNumericPolicyDesignTests test-only 구현
2. scoreDeltaPreview policy decision docs-only
3. range score UI visibility decision docs-only
4. score persistence gate design docs-only
5. Recent candidate score summary decision docs-only
6. line hold / 보류
```

다음 구현 후보를 열더라도:

```text
production score 구현이 아니다.
production candidateScorePreview 숫자화가 아니다.
production pointsPreview 숫자화가 아니다.
scoreDeltaPreview 숫자화가 아니다.
storage/schema 변경이 아니다.
UI / Recent / DailyCoach 변경이 아니다.
```

## 15. final judgement

현재 결론:

```text
candidateScorePreview numeric policy test design: 닫힘
candidateScorePreview numeric readiness: 아직 아님
component pointsPreview numeric readiness: 아직 아님
scoreDeltaPreview readiness: 아직 아님
UI visibility readiness: 아직 아님
Recent readiness: 아직 아님
DailyCoach readiness: 아직 아님
score persistence readiness: 아직 아님
production score readiness: 아직 아님
storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

한 줄 요약:

```text
이번 단계는 candidateScorePreview production 숫자화를 여는 단계가 아니라, test-local numeric evidence와 production-facing candidateScorePreview field 사이의 경계를 future test-only suite가 어떻게 고정해야 하는지 닫는 docs-only gate다.
```

# v8.2 macro range phase gate closeout

상태:

```text
문서 성격: macro range report-only / test-local / storage-design phase gate closeout
작업 범위: docs-only
구현 변경: 없음
index.html 변경: 없음
테스트 코드 추가: 없음
production 산식 변경: 없음
production score 변경: 없음
ADHERENCE_SCORING_VERSION 변경: 없음
goalSnapshot schema 변경: 없음
macroRangeContract 저장: 없음
macroRangeBasisSignature 저장: 없음
backup / restore schema 변경: 없음
Recent aggregation 변경: 없음
Today / Records / Recent UI 변경: 없음
production macro range 구현: 금지 유지
```

이 문서는 `v8.2_macro_range_contract_storage_design_2026-07-03.md` 이후 phase gate closeout이다.

목적:

```text
macro range 라인의 report-only / test-local / storage-design 단계가 무엇을 닫았는지 정리한다.
동시에 다음 단계에서 production storage/schema 구현으로 오해하지 않도록 금지선을 재고정한다.
```

최종 판단:

```text
macro range report-only/test-local evidence phase: 완료 가능
known macroRangeContract storage design docs-only: 완료 가능
production storage/schema readiness: 아직 아님
production macro range readiness: 아직 아님
```

## 1. closeout 범위

이번 closeout에 포함되는 라인:

```text
macro range feasibility audit
macro range policy design
report-only helper design / implementation
score candidate report-only design / implementation
snapshot compatibility design / tests
explicit normalizer simulation design / tests
signature decision design / tests
backup/restore old-new fixture design / tests
Recent mixed-basis handling design / tests
storage compatibility checkpoint
known macroRangeContract storage design
```

이번 closeout에 포함되지 않는 것:

```text
production macro range formula 구현
production score version 변경
goalSnapshot schema migration
macroRangeContract persistence
macroRangeBasisSignature persistence
backup / restore schema 변경
Recent range-aware aggregation
Today / Records / Recent UI range 표시
DailyCoach range action
old fixed Records range 재해석
```

## 2. 승인된 것

현재까지 승인 가능한 것:

```text
macro range가 실제 제품 문제라는 판단
copy-only로 해결 불가능하다는 판단
fixed target production 계약 보존
range 후보 field no-leak guard
test-local simulation / detector / fixture 기반 위험 관찰
known macroRangeContract 후보 shape 문서화
storage decision 영향표 문서화
```

문서상 승인된 작업 성격:

```text
docs-only 설계
report-only helper
test-local helper
fixture / assertion
no-impact guard
external audit / review
```

## 3. 승인되지 않은 것

아직 승인되지 않은 것:

```text
macroRangeContract 저장
macroRangeBasisSignature 저장
range-aware score production 적용
Recent range-aware production aggregation
backup/restore known range roundtrip
restore conflict preview basisConflict 표시
smart restore range basis conflict UX
Today range UI
Records range basis display
DailyCoach range action
```

특히 아래 오해를 금지한다.

```text
test-local valid shape == production schema 승인
storage design == storage implementation 준비 완료
candidateScorePreview 숫자화 == score production readiness
macroRangeContract 저장 == range score production 적용
```

## 4. 현재 production 계약

현재 source of truth:

```text
fixed target goalSnapshot
targetCal
targetProtein
targetCarbs
targetFat
ADHERENCE_SCORING_VERSION = v6.1_alcohol_penalty_v2
GOAL_SNAPSHOT_SIGNATURE_KEYS fixed-only
backup / restore fixed snapshot normalize path
Recent fixed target component counts
```

현재 정상 동작:

```text
unknown macroRangeContract는 prune된다.
unknown macroRangeBasisSignature는 저장되지 않는다.
candidateScorePreview는 저장 / backup / restore / Recent / UI에 남지 않는다.
Recent는 range 후보를 fixed component count로 조용히 확장하지 않는다.
DailyCoach는 range basis action을 만들지 않는다.
```

이 상태는 range 미구현의 결함이 아니라 현재 phase gate의 의도된 안전 계약이다.

## 5. 고정된 guard

고정된 guard:

```text
targetCal unchanged
fixed macro target unchanged
fixed score unchanged
ADHERENCE_SCORING_VERSION unchanged
goalSnapshot fixed signature unchanged
backup payload no range candidate leak
records-only import no range candidate leak
smart restore meals-only keeps local snapshot basis
Recent counts unchanged
DailyCoach output unchanged
UI text unchanged
```

guard가 막는 실패:

```text
range 후보가 production 저장 경로로 새는 것
candidate score가 user-facing score처럼 저장되는 것
old fixed Records가 range basis로 조용히 재해석되는 것
smart restore meals-only가 snapshot basis까지 바꾸는 것
Recent가 fixed/range basis를 같은 component count로 섞는 것
```

## 6. 금지선

다음은 계속 금지다.

```text
calculateMacroTargets 변경
buildExternalMacroActiveProductionApplication 변경
getDailyAdherenceScore 변경
ADHERENCE_SCORING_VERSION 변경
candidateScorePreview 숫자화
scoreDeltaPreview 숫자화
goalSnapshot schema 변경
macroRangeContract 저장
macroRangeBasisSignature 저장
normalizeGoalSnapshot range-aware 변경
normalizeMacroRangeContractForSnapshot production 구현
GOAL_SNAPSHOT_SIGNATURE_KEYS 변경
Records 무변경 저장 로직 변경
backup schema 변경
restore policy 변경
smart restore range-aware policy 변경
Recent aggregation 변경
Recent UI 변경
DailyCoach action 변경
Today / Records / Recent UI range 표시
old fixed Records를 range 기준으로 조용히 재해석
```

이 금지선의 의미:

```text
영구 금지가 아니다.
production gate가 닫히기 전까지 열지 않는 순서 통제다.
```

## 7. 현재 문서별 상태

| 문서 | 상태 | production 영향 |
| --- | --- | --- |
| `v8.2_macro_range_rebalance_feasibility_audit_2026-07-03.md` | 통과 | 없음 |
| `v8.2_macro_range_policy_design_2026-07-03.md` | 통과 | 없음 |
| `v8.2_macro_range_report_only_helper_design_2026-07-03.md` | 통과 | 없음 |
| `v8.2_macro_range_report_only_helper_implementation_2026-07-03.md` | test/report-only 통과 | production 연결 없음 |
| `v8.2_macro_range_score_candidate_report_only_design_2026-07-03.md` | 통과 | 없음 |
| `v8.2_macro_range_score_candidate_report_only_implementation_2026-07-03.md` | test/report-only 통과 | production score 변경 없음 |
| `v8.2_macro_range_snapshot_compatibility_design_tests_implementation_2026-07-03.md` | 통과 | schema 변경 없음 |
| `v8.2_macro_range_explicit_normalizer_simulation_tests_implementation_2026-07-03.md` | 통과 | production normalizer 없음 |
| `v8.2_macro_range_signature_decision_tests_implementation_2026-07-03.md` | 통과 | signature 변경 없음 |
| `v8.2_macro_range_backup_restore_fixture_tests_implementation_2026-07-03.md` | 통과 | backup/restore 변경 없음 |
| `v8.2_macro_range_recent_mixed_basis_tests_implementation_2026-07-03.md` | 통과 | Recent 변경 없음 |
| `v8.2_macro_range_storage_compatibility_checkpoint_2026-07-03.md` | 통과 | 없음 |
| `v8.2_macro_range_contract_storage_design_2026-07-03.md` | 통과 | storage 구현 없음 |

## 8. production gate가 열리려면

production storage/schema 구현을 열려면 최소한 아래가 별도 승인되어야 한다.

```text
1. macroRangeContract supported version / shape 확정
2. production normalizer design과 tests 확정
3. invalid / unsupported contract 처리 확정
4. signature policy 확정
5. backup/restore roundtrip policy 확정
6. smart restore basis conflict policy 확정
7. Recent fixed/range handling policy 확정
8. score/Coach와 storage 분리 여부 확정
9. old fixed Records no-migration 원칙 확정
10. user-facing UI 노출 여부 별도 결정
```

하나라도 빠지면 production storage 구현을 열지 않는다.

## 9. 다음 단계 후보

이 closeout 이후 가능한 안전 후보:

```text
1. macro range line hold / 보류
2. contract normalizer production design docs-only
3. range score severity / policy design docs-only
4. storage gate 외부 재검수
5. 다른 v8.2 후보로 전환
```

후속 normalizer design 문서:

```text
v8.2_macro_range_contract_normalizer_design_2026-07-03.md
```

다음 단계에서 여전히 금지:

```text
index.html production normalize 변경
goalSnapshot 저장 field 추가
backup payload field 추가
Recent aggregation 변경
score version 변경
production macro range 구현
```

## 10. closeout 판단

최종 판단:

```text
phase gate closeout: 가능
hotfix 필요: 없음
문서 보강 필요: 없음
앱 테스트 필요: 없음, docs-only
production storage 구현: 아직 금지
production macro range 구현: 계속 금지
```

이번 phase의 성과:

```text
macro range 문제를 제품 정책 / 저장 계약 / 점수 / Recent / backup 위험으로 분해했다.
test-local evidence와 production readiness를 분리했다.
future storage 후보 shape를 문서화하되 승인된 schema로 오해하지 않게 막았다.
기존 fixed target production 동작을 보존했다.
```

이번 phase의 종료 조건:

```text
더 많은 report-only/test-local guard를 쌓는 것보다,
다음에는 production gate를 열지 말지의 제품 판단이 필요하다.
```

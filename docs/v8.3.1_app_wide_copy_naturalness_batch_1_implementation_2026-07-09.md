MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-07-v1
- source_ledger_checked: not needed; this is copy implementation, not score formula work
- superseded_docs_checked: docs/v8.3.1_app_wide_user_facing_copy_inventory_decision_2026-07-09.md
- external_anchor_checked: not needed; no numeric scoring or physiology policy changed

DOCUMENT ROLE
- implementation_log

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview mainline
- preview field storage/UI/Recent/DailyCoach exposure

# v8.3.1 app-wide copy naturalness batch 1 implementation

Date: 2026-07-09

This is a narrow user-facing copy implementation.

It implements the first batch from `docs/v8.3.1_app_wide_user_facing_copy_inventory_decision_2026-07-09.md`.
It does not change score formula, range calculation, adaptive target logic, UI layout, storage/schema, tooltip/glossary behavior, scoreDeltaPreview, old records cleanup, package version, or the v8.3 tag.

## PROMPT_SCOPE_AUDIT

Requested next step:
- Apply the GPT audit only as reference, re-check repo/current truth, and execute without narrowing the work incorrectly.

Repo/current truth next step:
- `v8.3.1 app-wide copy naturalness batch 1 implementation`.

Mismatch or risk:
- The audit says "batch 1", but the user also warned against grep-only fixes and against tests forcing awkward AI-like copy.
- The work could drift into app-wide rewrite, tooltip implementation, score tuning, UI layout, or storage/schema if the inventory is read too broadly.

Chosen action:
- Update only batch 1 user-facing copy and exact wording tests that fossilized awkward phrases.
- Preserve meaning guards, source guards, alcohol/scoring guards, and internal-term exclusion.

Why not broader:
- Broad DailyCoach tone pass, tooltip/glossary spec, InBody phrase-set rewrite, and Settings broad copy polish are separate gates.
- This branch should remove obvious first-batch awkward copy without becoming a full copy rewrite.

## Batch 1 Selection

Implemented:
- A04 Today target extreme note.
- B01/B02 Today score/source visibility notes.
- C01/C02 DailyCoach leftover "재료" family copy.
- D03 low-digest setting copy.
- E03 backup warning tone.
- G02 InBody `체지방 쪽 변화` exact copy and test expectation.
- G04 InBody help tone.
- H01/H02 validation spacing/tone.
- I02 Settings hero "오늘값" copy.

Held:
- D02 `오늘 목표 자동 조정` explanation.
  - Reason: this needs a small tooltip/glossary/spec decision because bad wording can make adaptive target feel like "먹은 대로 목표가 따라간다".
- C03 broad DailyCoach "흐름" tone pass.
  - Reason: too broad for batch 1.
- G03 full InBody trend phrase-set rewrite.
  - Reason: `체지방 쪽 변화` was small and test-locked, but the whole trend phrase set needs a separate pass.
- Settings broad copy polish.
  - Reason: only the hero and low-digest copy were first-batch candidates.

## Production Copy Changes

Today target extreme note:
- Before: `목표 칼로리가 높아 탄단지는 현실 범위까지만 표시합니다. 식단은 g 기준을 우선 보세요.`
- After: `목표가 높아 탄단지는 무리 없는 g으로 잡았습니다.`

Macro allocation KPI note:
- Before: `목표 칼로리가 높아 탄단지는 현실적인 범위까지만 배분했습니다. 식단은 아래 탄단지 g을 우선 기준으로 보세요.`
- After: `목표가 높아 탄단지는 무리 없는 g으로 잡았습니다. 아래 탄단지를 기준으로 보세요.`

Source visibility notes:
- Before: `오늘 총소비량 기준이 없어, 목표 대비 섭취량 중심으로 점수를 해석합니다.`
- After: `오늘 소비 칼로리 기준이 없어, 먹은 양과 목표 차이를 중심으로 봅니다.`
- Before: `체중 기준이 없어, 단백질 목표에서 추정한 체중 기준으로 일부 항목을 해석합니다.`
- After: `체중 입력이 없어, 단백질 목표로 추정한 체중을 참고합니다.`

DailyCoach:
- Before: `회복을 위해 기본 재료는 필요합니다.`
- After: `회복하려면 단백질과 먹기 쉬운 탄수화물은 필요합니다.`
- Before: `작은 흑자를 만들 재료가 부족하면 회복과 성장 흐름이 약해집니다.`
- After: `린매스업은 먹은 양이 너무 낮으면 운동 회복과 증량이 흔들립니다.`

Settings:
- Before: `Today는 오늘값, 기본 설정은 시작 기준과 백업을 관리합니다.`
- After: `Today는 오늘 계산, 기본 설정은 평소 기준과 백업을 관리합니다.`
- Before: `소화 부담이 큰 편이면 일반 벌크의 탄수화물 상한을 보수적으로 낮춥니다.`
- After: `탄수화물을 많이 먹기 힘들면 벌크 목표의 탄수 범위를 조금 낮춥니다.`

Backup warning:
- Before: `백업 쪽 식사 수가 더 적은 날짜 ... 부분 입력 백업일 수 있습니다.`
- After: `백업 파일의 식사 수가 더 적은 날짜 ... 일부 식사가 빠졌을 수 있습니다.`

InBody:
- Before: `체지방 쪽 변화가 더 큽니다.`
- After: `체지방 변화가 더 큽니다.`
- Before: `측정 조건 차이가 있을 수 있는 InBody 값은 조금 보수적으로 반영합니다.`
- After: `측정 조건이 달라질 수 있어 InBody 영향은 작게 봅니다.`

Validation:
- Before: `확인해주세요`, `입력해주세요`
- After: `확인해 주세요`, `입력해 주세요`

## Test Expectation Policy

Tests must protect meaning, not awkward legacy wording.

Updated exact wording tests where they were locking awkward copy:
- source visibility note expectations now check the new user-facing source wording.
- Settings header/helper expectation now checks the new compact copy.
- InBody expectations now check `체지방 변화`, not `체지방 쪽 변화`.
- macro allocation note expectation now checks `무리 없는 g`, not the older "현실적인 범위" phrase.

Preserved meaning guards:
- source visibility notes still exclude `TDEE`, `scoringContext`, `penalty`, `anchor`, `curve`, `source visibility`, and `targetProteinFallback`.
- InBody copy still labels source with `인바디 분석 결과`.
- medium confidence InBody text still does not become goal-specific action.
- DailyCoach copy still excludes `회복 재료`, `회복재료`, and `성장 재료`.

## Minimal Surface vs Complete Scope

Minimal surface:
- Only user-facing strings and exact copy test expectations changed in `index.html`.
- No score formula, range calculation, adaptive target, layout, storage/schema, package, tag, or backup version change.

Complete batch scope:
- The first-batch COPY_FIX_NEXT / TEST_EXPECTATION_UPDATE_NEXT items were handled, except D02 and broader copy passes that require separate gates.

## Result

`v8.3.1 app-wide copy naturalness batch 1 implementation` is implemented.

Next candidate:
- If continuing copy work: `v8.3.1 tooltip/glossary and adaptive-target help copy spec` or `v8.3.1 app-wide copy batch 2 decision`.
- If no new copy issue is raised: monitor; do not open broad rewrite by default.

Still forbidden:
- score formula / numeric tuning implementation
- UI layout redesign
- tooltip/glossary implementation without spec
- UI/storage/schema expansion
- scoreDeltaPreview product path
- old records cleanup/reset/fallback implementation

MANDATORY PRE-READ
- docs/00_current_truth/00_READ_FIRST.txt
- docs/00_current_truth/02_macro_range_current_truth.txt
- docs/00_current_truth/04_document_status_index.txt

READ RESULT
- read_before_writing: yes
- current_truth_version: 2026-07-07-v1
- source_ledger_checked: not required; this implementation does not change scoring or nutrition anchors.
- superseded_docs_checked: yes; the archived v8.2 exercise-only onboarding note was treated as historical evidence, not an instruction.
- external_anchor_checked: not applicable; this implementation changes first-run ownership and UI/storage flow, not physiological coefficients.

DOCUMENT ROLE
- implementation_log

FORBIDDEN WITHOUT EXPLICIT SUPERSEDING DECISION
- v8.2 fixed penalty table as production body
- stepwise score cap
- hard zero threshold as primary scoring policy
- exercise bonus
- v6.1 alcoholImpactPenalty post-score subtraction
- scoreDeltaPreview mainline
- preview field storage/UI/Recent/DailyCoach exposure

# v8.3.1 onboarding / first-run flow implementation

Date: 2026-07-10

## Plain explanation

새 브라우저에서는 예시 프로필로 개인 목표를 먼저 계산하지 않는다. 사용자가 계산에 필요한 기본정보를 확인하기 전에는 목표 kcal·탄단지·점수를 placeholder로 두고, 3단계 설정을 끝낸 뒤에만 계산한다. 예전 데이터가 있어도 그 사실만으로 예시 Settings가 본인 정보라고 간주하지 않는다.

## 코드 몰라도 보는 완료 보존 핫픽스

### 어떤 문제가 있었나

`초기 설정 다시 하기`에서 체중이나 목표만 고치고 완료해도, 온보딩에서 묻지 않은 값까지 처음 쓰는 사람의 기본값으로 돌아갔다. 예를 들어 사용자가 단백질 목표를 `높게`, 오늘 목표 자동 조정을 `OFF`, 오늘 운동을 PUSH 70분으로 쓰고 있어도 완료 뒤에는 `중간`, `ON`, 휴식 0분으로 바뀔 수 있었다.

### 이제 어떻게 동작하나

| 상황 | 바뀌는 값 | 그대로 두는 값 |
|---|---|---|
| 앱을 정말 처음 설정함 | 온보딩에서 입력한 프로필과 생활 기준 | 첫 기본값으로 단백질 `중간`, 자동 조정 `ON`, 오늘 운동 `휴식/0분` 적용 |
| 설정에서 다시 입력함 | 성별, 나이, 키, 체중, 목표, 평소 활동/업무/시간, 운동 관리 방식처럼 폼에서 확인한 값 | 단백질 단계, 자동 조정, 고급 설정, 루틴 계획, 오늘 운동·유산소, Today 초안, Records, InBody, 프리셋과 템플릿 |
| 예전 데이터는 있지만 프로필이 미확인임 | 이번에 확인한 필수 프로필 | 사용자가 만든 설정과 오늘 데이터. 단, 손대지 않은 옛 예시 운동값만 휴식/0분으로 정리 |

운동 프로필을 바꿨는데 기존 루틴 이름이 새 프로필에서 쓸 수 없는 경우에는 그 루틴 계획과 세션만 새 프로필의 정상값으로 바꾼다. 운동시간, 유산소, 단백질 단계, 자동 조정, 기록처럼 관계없는 값은 초기화하지 않는다.

## 코드 몰라도 보는 전체 백업 스냅샷 재설정 핫픽스

### 어떤 문제가 추가로 확인됐나

프로필 확인을 끝내지 않은 상태 A에서 다른 전체 백업 B를 복원하면 화면과 저장소 대부분은 B로 바뀌지만, 온보딩 완료 때 되돌려 쓸 Settings/Today 보존본만 A가 남을 수 있었다. 그 상태에서 온보딩을 완료하면 프로필·Records·InBody·초안·프리셋은 B인데 단백질 단계, 자동 조정, 루틴, 운동시간, 유산소 같은 일부 값만 A로 돌아가는 혼합 상태가 됐다.

완료된 전체 백업을 복원하는 경우에도 오래된 보존본이 메모리에 남아 있었다. 즉시 화면을 바꾸지는 않더라도, 전체복원이 현재 상태를 완전히 교체한다는 계약과 맞지 않았다.

### 이제 어떻게 동작하나

- 전체 백업 B의 Settings와 분류를 적용한 뒤, 완료/미완료 처리를 나누기 전에 과거 상태의 온보딩 보존본을 버린다.
- B가 프로필 미확인 백업이면, B의 Settings/Today를 적용한 직후 B 기준 보존본을 새로 만든다. 이후 온보딩을 완료해도 B의 값만 복원된다.
- B가 온보딩 완료 백업이면 온보딩을 열지 않고 보존본도 비어 있는 상태로 둔다.
- Records-only 가져오기와 스마트복원의 기록 추가 경로는 전체 상태 교체가 아니므로 기존 보존본을 그대로 둔다.
- 전체 초기화는 기존 계약대로 보존본을 비운다.

예를 들어 A가 단백질 `높게`·자동 조정 `OFF`·PULL 70분이고, B가 단백질 `낮게`·자동 조정 `ON`·PUSH 44분이면, B 복원 후 온보딩을 마쳤을 때 Settings, 화면 입력값, 브라우저 저장소, 재시작 뒤 상태가 모두 B의 `낮게/ON/PUSH 44분`으로 유지된다. Records, InBody, Today 초안, 유산소 프리셋, 식사 템플릿, 요일별 루틴과 시간 기본값도 B와 일치하며 A 표식은 남지 않는다.

### 정책·산식과 보류 범위

칼로리·탄단지·점수·탄수/지방 전환·단백질 정책과 백업 파일 형식은 바꾸지 않았다. 사용자 화면에 새 기능이나 문구를 추가한 작업도 아니다. 이번 수정은 전체 백업이 메모리·화면·저장소·재시작 뒤 상태를 한 출처 B로 맞추도록 온보딩 보존본의 수명만 바로잡는다. Today Coach, 탄수/지방 전환 설명, 점수 카드, tone/persona 후보는 아래 후속 판단 순서를 유지하며 구현하지 않았다.

### 계산 정책은 바뀌었나

아니다. 칼로리·탄단지 산식, 점수 곡선, 탄수·지방 자동 전환 정책, 단백질 정책은 바꾸지 않았다. 이번 수정은 사용자가 이미 정한 값을 온보딩 완료가 지우지 않도록 저장 소유권만 바로잡은 것이다.

### 사용자가 큰 틀에서 확인할 점

- 첫 설정은 지금처럼 안전한 중립 상태에서 시작하는가.
- 설정을 다시 해도 온보딩에서 묻지 않은 옵션과 오늘 기록이 그대로인가.
- 운동 종목을 바꿀 때 맞지 않는 루틴만 바뀌고 나머지는 유지되는가.
- 예전 가상 예시 운동값과 실제 사용자 운동값이 구분되는가.

## PROMPT_SCOPE_AUDIT

- requested next step: 추가 decision 없이 onboarding / first-run flow를 구현하고 전체 회귀 검증 후 조건부 merge/push한다.
- repo/current truth next step: onboarding decision이 next substantive gate로 complete implementation을 지정하고 있었고, 실제 fresh-state 저장/계산 경로도 그 문제를 재현했다.
- root problem in plain language: `DEFAULT_STATE -> loadState -> render -> saveState`가 역사적 예시 프로필과 오늘 운동값을 사용자 소유 값처럼 표시·저장했다.
- evidence checked outside next-gate text: raw localStorage, initialization order, Settings/Today ownership, validation, body-status materialization, BMR/protein context, full backup, old backup, records-only import, reset, Chromium desktop/mobile 화면을 확인했다.
- alternatives considered: 현 상태 유지, UI modal만 추가, exercise-only 질문, 3상태 migration, 4상태 truthful gate.
- chosen action: four-state raw-storage classification plus complete truthful setup gate.
- why not broader: score formula, nutrition anchors, DailyCoach, old record scores, app/version/tag surface는 이 문제의 원인이 아니다.
- what would prove this wrong: setup 전 개인 목표가 다시 노출되거나, data-only 상태가 자동 완료되거나, missing body composition이 15%/35kg으로 계산되거나, 기존 데이터/backup이 손실되는 경우.

## Full-backup snapshot hotfix PROMPT_SCOPE_AUDIT

- requested next step: 독립 감사에서 재현한 전체 백업 A/B 혼합 위험을 최소 코드로 고치되, 전체 상태 전이와 회귀 범위는 완전하게 검증하고 같은 구현 결과로그를 갱신한 뒤 조건부 merge/push한다.
- repo/current truth next step: 문서는 completion ownership hotfix를 완료로 적었지만, latest `origin/master`의 실제 `restoreFullBackupData()`에는 full-state replacement 시 기존 `completionOwnershipSnapshot`을 폐기하는 경계가 없었다. 문서의 완료 표기보다 코드와 동적 재현을 우선했다.
- root problem in plain language: 전체 백업 B가 앱 데이터는 교체해도 온보딩 완료용 보존본 A를 교체하지 않아, 나중에 A와 B가 섞였다.
- evidence checked outside prompt wording: incomplete A snapshot 생성, records-only import 후 보존, incomplete full backup B 복원·완료, completed full backup 복원, memory/DOM/localStorage/reload, Records/InBody/Today drafts/cardio presets/meal templates/schedules/defaults를 실제 Chromium 내부 테스트에서 확인했다.
- alternatives considered: 현 상태 유지, 복원 뒤 한 필드만 직접 덮기, snapshot helper/restore framework 재설계, full-state replacement 경계에서 snapshot을 비우고 기존 incomplete 준비 경로가 B를 다시 캡처하게 하기.
- chosen action: `restoreFullBackupData()`가 B Settings와 분류를 적용한 뒤 완료/미완료로 갈라지기 직전에 snapshot을 `null`로 만든다. 미완료 경로는 기존 `prepareIncompleteOnboardingState()`가 B를 다시 캡처하고, 완료 경로는 null을 유지한다.
- minimal surface: production 변경은 snapshot 수명 경계 1줄이다.
- complete scope: 두 restore 분기, records-only/smart-restore 비회귀, reset, memory/DOM/storage/reload, 모든 백업 collection의 deep equality, A 표식 부재, docs-policy와 전체 profile까지 검증한다.
- why not broader: helper 추상화나 restore 재설계는 이 결함을 푸는 데 필요하지 않고, score/macro/Coach/UI/schema/tag는 원인과 무관하다.
- what would prove this wrong: 미완료 B 복원 후 snapshot이 B와 다르거나, 완료 B 복원 후 snapshot이 남거나, 완료·저장·reload 과정에서 A 표식이 돌아오거나 B collection이 달라지는 경우.

## State model

`ONBOARDING_VERSION = 1` and `onboardingCompletedVersion` are implemented with four classifications:

| Classification | Raw evidence | Result |
|---|---|---|
| `fresh` | no completion marker and no user evidence; exact historical defaults alone also count as fresh | show full setup and calculation placeholders |
| `completed` | completion version is current | load the normal app |
| `existing_profile_confirmed` | customized required profile Settings provide trusted pre-onboarding profile evidence | preserve state and write the completion marker once |
| `existing_data_profile_unconfirmed` | Records/InBody/Today draft/presets/templates or non-profile Settings exist, but required profile ownership is unconfirmed | preserve data, keep placeholders, and require profile confirmation |

Classification runs from raw storage before the first render/save cycle. `hasMeaningfulPreOnboardingUserState` can report either profile or data evidence, but only trusted profile evidence may auto-complete setup.

Exact historical defaults plus Records or InBody are `existing_data_profile_unconfirmed`. Records-only import does not complete setup. This closes the gap where user data could otherwise legitimize the fictional 75kg/173cm/32-year-old male lean-bulk profile.

## Implemented flow

- Added a full-screen setup gate with an entry surface, backup restore access, a three-step form, and a completion-only handoff to the app.
- Step 1 collects gender, age, height, weight, and goal.
- Step 2 collects activity, work type, sleep hours, work hours, and lifestyle hours.
- Step 3 requires general/exercise mode; exercise mode additionally requires profile and planned weekly days.
- Required values reject empty, invalid, and out-of-range input. A requested time total above 24 hours keeps the existing explicit warning/automatic-adjustment contract visible.
- `proteinTargetLevel` remains `medium`; onboarding does not ask for it.
- `adaptiveMacroTargetsEnabled` remains ON; onboarding does not ask for it.
- Settings now exposes `초기 설정 다시 하기`. Re-entry is prefilled and cancelable. Submit updates only the collected onboarding fields, preserves non-onboarding Settings and Today ownership, and normalizes only routine values that are incompatible with a changed exercise profile.
- The Settings header keeps `초기 설정 다시 하기` and `입력 초기화` on equal, stable action tracks: 148x42px on desktop and equal-width halves on the narrow layout.
- Full reset clears completion and returns to setup.
- Focus is trapped inside the open setup surface; desktop uses a centered dialog and the 390px layout uses a full-height one-column surface with a stable footer.

## Calculation and persistence gate

- Pending setup renders neutral placeholders for target kcal, protein, carbs, fat, and score-facing personalized output.
- Pending render returns before `calculate()` and before Settings persistence.
- Records remain writable/importable while setup is pending, but their presence does not unlock personalized output.
- `saveState()` continues to preserve Records and refuses to persist the fictional Settings snapshot until onboarding is complete.
- Fresh completion writes user-owned Settings and starts Today at `REST`, zero weight-training minutes, and zero cardio minutes.
- Re-entry and `existing_data_profile_unconfirmed` completion preserve protein level, adaptive target setting, advanced settings, compatible routine ownership, Today exercise/cardio values, drafts, Records, InBody, presets, and templates. Untouched historical fictional Today defaults remain the only neutralized existing-data case.
- Completion does not create a Today draft merely to copy Settings.
- Existing users with a genuinely customized required profile are migrated once. Historical PUSH/60-minute weights/30-minute cardio are neutralized only in the specific untouched historical Today context.

An attempted global change to make every Settings-to-Today handoff neutral caused eight established smoke regressions. That change was too broad and was reverted. Today neutrality is limited to first-run completion and the exact historical migration case, preserving existing normal ownership behavior.

## Body-composition authority

- Added `bodyCompositionConfirmed` as a separate authority signal.
- Body fat and skeletal muscle are optional; required numeric validation no longer forces them.
- Fresh completion leaves both values empty unless trusted user-owned body composition already exists.
- Historical 15% body fat and 35kg skeletal muscle values do not become trusted merely because they were stored by the old default flow.
- Missing/unconfirmed body composition uses the no-body-composition reliability path and Mifflin-St Jeor BMR instead of silently entering body-composition protein/physiology adjustments.
- Explicit Settings body composition, a user source, or an applied InBody source can establish body-composition authority; InBody records alone do not confirm the whole Settings profile.
- Source text now distinguishes confirmed Settings values from `체성분 입력 없음`.

## Backup and migration

- New full backups include optional `onboardingCompletedVersion` without changing `FULL_BACKUP_VERSION`.
- A new backup with the completion marker restores as `completed`.
- An old backup with customized required profile Settings may auto-migrate as `existing_profile_confirmed`.
- An old backup with exact historical Settings plus Records/InBody restores data as `existing_data_profile_unconfirmed` and keeps target placeholders.
- Records-only import preserves imported Records and does not complete onboarding.
- Full backup restore is the full-state replacement boundary: it clears any pre-restore completion ownership snapshot after applying restored Settings/classification. An incomplete B backup immediately recaptures B through the existing incomplete-onboarding path; a completed B backup leaves the snapshot null.
- Records-only import and smart-restore record-add paths preserve the current ownership snapshot because they do not replace Settings/Today ownership. Full reset and completed full restore leave it null.
- Current backup regression fixtures explicitly model the new completion field; the old-backup fixture intentionally omits it.

## Minimal surface

Not implemented:

- No score formula change.
- No scoring-curve or nutrition-anchor change.
- No DailyCoach rewrite.
- No old record regrade, recompute, or schema migration.
- No scoreDeltaPreview work.
- No broad Settings redesign.
- No app title/header/STAGE/package version change.
- No `FULL_BACKUP_VERSION` bump.
- No v8.3 tag movement.

## Complete feature scope

Completed together rather than shipping a UI-only shell:

- pre-render raw classification;
- four-state migration;
- calculation/persistence placeholder gate;
- three-step setup and conditional exercise inputs;
- truthful optional body-composition path;
- neutral first Today;
- full/new/old backup and records-only import behavior;
- full reset and cancelable, ownership-preserving Settings re-entry;
- desktop/mobile/focus behavior;
- dedicated onboarding and existing regression coverage.

## Regression coverage

Added `runOnboardingFirstRunFlowTests` to smoke, core, UI, and mobile profiles. Its 19 cases cover:

1. clean storage;
2. exact historical defaults;
3. exact defaults plus Records;
4. exact defaults plus InBody;
5. customized required profile Settings migration;
6. completion without body composition;
7. exercise setup with neutral Today;
8. records-only import;
9. old full backup with unconfirmed profile;
10. Settings re-entry cancel;
11. explicit time-ownership warning;
12. full reset;
13. horizontal overflow.
14. Settings re-entry submit preserves non-onboarding Settings, Today values, Records, InBody, schedules, and drafts;
15. exercise-profile change normalizes only incompatible routine ownership;
16. existing-data profile confirmation preserves user-owned Settings and Today data;
17. existing-data profile confirmation neutralizes only untouched historical Today defaults.
18. incomplete full backup restore discards stale A ownership, recaptures B, and keeps B deep-equal across memory, DOM, localStorage, reload, Records, InBody, Today drafts, cardio presets, meal templates, schedules, and defaults;
19. completed full backup restore closes onboarding and clears a stale ownership snapshot.

Existing InBody, Today ownership, backup/restore, score, macro policy, DailyCoach, UI, and mobile suites remain in the regression set. Copy assertions changed only where an older expectation encoded the wrong source meaning; natural wording was not bent to satisfy an AI-like exact-string test.

The first full run exposed four failures. They were audited before changing anything:

- Three failures came from old Settings tests that treated calculation weight as a permanently hidden Today-only mirror. The onboarding contract makes it an explicit user-owned starting profile value, and existing copy guidance already names calculation weight as a Settings/Today candidate. Tests now verify that it belongs to the visible Settings profile group while actual body-composition/cardio fields remain in the hidden compatibility mirror.
- One calibration fixture entered body fat and skeletal muscle but did not establish the new body-composition authority or pass its collected state into `getEffectiveModeConfig`. The fixture now explicitly confirms its supplied body data and resolves runtime config from that same state. Production macro policy and expected numeric ranges were not changed.
- The four affected suites then passed 38/38, followed by a clean full run.

## Browser QA

- Desktop 1280x900 fresh flow: entry, all three steps, exercise mode, completion, Settings re-entry, and cancel verified.
- Completed example: female, age 34, 165cm, 62kg, diet, running, 4 days; target rendered after completion while body fat/skeletal stayed empty and Today stayed at rest with no cardio.
- Mobile 390x844 fresh flow: full-height layout, sticky footer, no horizontal overflow, and focus wrap verified.
- Desktop/mobile document and dialog overflow checks passed.
- Settings header actions measured 148x42px each at the desktop viewport and 162.5x42px each at the narrow viewport; both checks confirmed equal dimensions, no overlap, no text clipping, and no horizontal overflow.
- Browser console errors: 0.

## Additional requested product audit: not implemented

The following three ideas were inspected against the current code and copy policy, but no related UI, score, Coach, tooltip, storage, or onboarding behavior was implemented in this branch.

### 1. DailyCoach, help ownership, and selectable tone

Observed:

- The current Coach already separates selected decisions into `state`, `reason`, and `action`, and keeps `copyIntent`/goal context in the output path.
- The actual wording is still spread across large goal/decision maps, so adding three complete copies of every branch would multiply drift and contradictory advice.
- The same Coach items currently feed both `오늘의 코치` details and the score card's `추가 확인`, which creates duplicated ownership.
- Existing copy truth already says compact cards own numbers, DailyCoach owns interpretation/action, and tooltip/glossary needs a small spec. The new request is consistent with that direction and makes the routing boundary more concrete.

Recommended surface contract:

| Information type | Surface |
|---|---|
| stable concept, persistent setting, or definition the user may choose to read | context-local `?` tooltip or Settings help |
| today's detected state, priority, reason, and next action | `오늘의 코치` |
| blocking validation, destructive warning, or immediate safety condition | inline beside the control or in the confirm dialog; never hide it in Coach/tooltip |
| score/source limitation that changes how the visible number can be trusted | compact note beside the score, with optional help for the stable definition |

Do not keep explanatory prose permanently visible across the app merely because it is globally true. Put the short fact where the user encounters the concept and move the fuller explanation behind help. Conversely, do not move a day-specific warning into a static tooltip; it belongs in today's Coach.

Selectable Coach tone is a valid product feature, but it is not a copy-only toggle:

- Implement it as a semantic-message layer: decision priority, facts, numbers, safety strength, and action must remain identical; only phrasing changes.
- Generate canonical message intent first, then apply a tone renderer at the final copy boundary. Do not fork the decision engine or maintain three unrelated copies of every rule.
- A persistent `coachTone`-style setting would require Settings, save/load, full backup/restore, defaults, tests, and copy fixtures.
- In onboarding it must be optional and non-blocking, placed after or alongside the final step with a clear default. It must never become part of the calculation-truthfulness gate.
- Tooltip definitions, units, warnings, and destructive confirmations stay canonical. The selected tone may affect a short conversational wrapper, but cannot rewrite the factual core.
- `열정형` and `냉철형` are clear prototypes. `AI형` is fun as an experiment, but it conflicts with the project's explicit effort to remove AI-like copy; compare it against a `차분형`/`기본형` candidate before fixing product labels.

### 2. Diet score labels, component display, and extra checks

Observed current behavior:

- Overall score bands are `매우 우수` (90+), `우수` (80+), `보통` (70+), `보완 필요` (50+), and `많이 부족`.
- A high clean score then adds `안정적` below `매우 우수`, which usually repeats the same conclusion without adding an action or exception.
- The four compact cards expose production contribution values as `감점 없음` or a negative point value. That is an implementation-facing view of a 100-minus-penalties engine, not the easiest user-facing view.
- The production score has kcal, protein, fat, carb/exchange, and alcohol-physiology contributions, while the compact grid permanently shows only kcal/protein/carbs/fat. Therefore four apparent 100-point component cards cannot be presented as an average or complete decomposition of the final score.
- `추가 확인` exists both under the score and under Coach, often from the same Coach items. Some entries are actions/context, while source reliability is genuinely score-specific.

Recommended score-surface decision:

- Keep one score-band label if it helps scan the number. Remove the generic second line when it only says `안정적`/`대체로 안정` again.
- Reuse that second-line space only for a high-signal exception, such as a high score with mixed over/under items or a non-card factor that materially explains the total. Otherwise render no line and keep the card shorter.
- Prefer 0-100 display for the four compact components. The least distorting candidate is a display-only retained score such as `clamp(100 + componentContribution, 0, 100)`, with legacy component scores normalized to the same scale. It must not feed calculation, storage, Records regrading, or score tuning.
- Before implementation, prove that kcal's combined target/TDEE axis and carbs' combined exercise/exchange axis remain understandable, and explicitly handle the alcohol contribution that is not one of the four permanent cards.
- A small static help text may say that component values show each area separately and the total reflects them together; it should not expose penalty tables or imply that the four numbers are averaged.
- Remove the generic score-card `추가 확인` disclosure by default. Move day-specific action/context to Coach, keep only compact score-validity/source notes beside the score, and delete low-value entries rather than moving everything wholesale.
- Do not add a permanent fifth card merely to explain rare conditions; use the meaningful secondary line or Coach when such a condition is present so the panel does not grow vertically.

### 3. Carb/fat adaptive-target explanation in DailyCoach

Plain-language judgment:

- The calculation policy is already more detailed than a simple `3 days` / `7 days` switch. Protein is reserved first, carbs and fat move inside the same kcal budget, and recent excess magnitude plus recency continuously reduce how far the target may move.
- The current Coach uses the adjusted target when judging carbs/fat, but it does not receive a dedicated explanation signal saying which direction moved, by how much, whether recent history limited it, or whether a joint range boundary stopped it.
- Therefore adding only example sentences now would be unsafe. The Coach could claim `3 days in a row` when the engine actually saw nonconsecutive records, or say adjustment was impossible when the continuous limiter merely made it small.

The user's example is directionally useful but not product truth as written:

- `누적되면 좋지 않다` is too broad. Repeated high-carb intake is not automatically physiologically bad; the current product policy says repeated skew reduces how much the app will keep moving the target.
- Current `3-day` and `7-day` evidence is a recency-weighted pressure window, not a hard consecutive-day bucket.
- `7일 연속이라 조절할 수 없다` is valid only if a future explanation contract proves both true consecutiveness and an effectively zero adjustment. Current data does neither explicitly.

Future semantic states to decide before copy implementation:

| State | Coach behavior |
|---|---|
| no adjustment or tiny movement | stay silent; do not narrate every small arithmetic change |
| carb-led adjustment | say carbs moved up and fat allowance moved down within the same kcal budget, then give the remaining-meal action |
| fat-led adjustment | symmetric explanation and action |
| history-limited adjustment | say recent records reduced today's adjustment only when enough eligible history evidence exists |
| joint-boundary limited | say little adjustment room remains without exposing internal exchange arithmetic |
| carbs and fat both high / kcal over | do not present auto-adjustment as permission; calorie/fat/alcohol priority remains above the adjustment explanation |
| adaptive setting OFF or insufficient records | do not claim an automatic adjustment or history pattern |

Evidence required from a future semantic signal:

- direction and actual carb/fat deltas from base target to adaptive target;
- meaningful-movement threshold so tiny changes stay silent;
- carb/fat pressure and multiplier, eligible record count/dates, and history confidence;
- explicit consecutiveness only if it is actually calculated;
- whether the joint range boundary, recent pressure, mixed overage, or another higher-priority risk limited the move;
- symmetric high-fat fixtures, mixed high-carb/high-fat fixtures, gaps/missing-day fixtures, adaptive OFF, low-data, goal/training, alcohol, and kcal-over tests.

Surface ownership remains:

- Settings/help explains the stable meaning of `오늘 목표 자동 조정`.
- DailyCoach explains what happened today and what to do next.
- The macro card keeps compact numbers.
- No surface should expose `skew`, multiplier, pressure formula, or 4:9 exchange arithmetic.

Decision: accept this as a future candidate inside the joint `Today score and guidance surface ownership inventory / decision`, not as an immediate copy hotfix. After that inventory and the narrow score-card ownership cleanup, implement `DailyCoach semantic v2 phase 1` with the adaptive-allocation signal and natural action copy. Keep selectable tone/persona as a later phase so tone does not hide missing semantics.

### Recommended order

1. Finish the onboarding completion-ownership hotfix without any Coach or score implementation mixed into it.
2. Run one joint docs-only `Today score and guidance surface ownership inventory / decision`. Inventory every score band/secondary state, every final contribution axis, all `추가 확인` producers, tooltip candidates, adaptive-allocation evidence/states, and representative Coach intents.
3. Implement the narrow score-card semantic cleanup first: 0-100 component display, redundant secondary-line removal, and score-vs-Coach extra ownership. Keep the production score formula unchanged.
4. Implement `DailyCoach semantic v2 phase 1`: canonical intents, adaptive carb/fat allocation signal, priority/gating, natural state/reason/action copy, and regression coverage. Do not add tone selection yet.
5. Implement selectable Coach tone/persona only after semantic phase 1 is stable, with Settings/storage/backup and optional onboarding behavior as a separate complete feature.
6. Only after those implementations should broad tooltip/glossary or app-wide copy batch 2 be reconsidered. Do not start with a broad tooltip rollout or a global string rewrite.

Reason for this order: the score cleanup first establishes which facts remain beside the score and which actions belong to Coach. The new adaptive-target request also shows that semantic evidence must exist before tone variants. Splitting semantic Coach behavior from persona avoids producing three polished versions of an explanation the engine cannot yet prove.

## Initial implementation verification

- `docs-policy`: passed.
- `runOnboardingFirstRunFlowTests`: 13 cases / 0 failed.
- `npm test` equivalent smoke profile: 21 suites / 206 cases / 0 failed.
- `npm run test:core` equivalent: 64 suites / 752 cases / 0 failed.
- `npm run test:ui` equivalent: 26 suites / 196 cases / 0 failed.
- `npm run test:mobile` equivalent: 20 suites / 158 cases / 0 failed.
- macro-policy: 30 suites / 240 cases / 0 failed.
- DailyCoach focused: 2 suites / 98 cases / 0 failed.
- onboarding + backup/restore focused: 6 suites / 57 cases / 0 failed.
- Settings ownership + calibration correction check: 4 suites / 38 cases / 0 failed.
- full profile: 140 suites / 1,414 cases / 0 failed; console errors 0; page errors 0.
- Browser QA: desktop 1280x900 and mobile 390x844 passed with no overflow or console errors.
- Settings action sizing hotfix: onboarding 13/13, UI 196/196, and mobile 158/158 passed after the equal-size assertion was added.
- `git diff --check`: passed; only repository line-ending conversion warnings were reported.

## Completion ownership hotfix verification

- `docs-policy`: passed after replacing one stale exact-text expectation with the new semantic-phase/tone-phase boundary.
- onboarding focused: 1 suite / 17 cases / 0 failed.
- smoke: 21 suites / 210 cases / 0 failed.
- onboarding + backup/restore focused: 6 suites / 61 cases / 0 failed.
- UI: 26 suites / 200 cases / 0 failed.
- mobile 390x844: 20 suites / 162 cases / 0 failed.
- macro-policy: 30 suites / 240 cases / 0 failed.
- core: 64 suites / 756 cases / 0 failed.
- full: 140 suites / 1,418 cases / 0 failed; console errors 0; page errors 0.
- `git diff --check`: passed; only repository LF-to-CRLF conversion warnings were reported.

Test-failure review in plain language:

- An InBody test sample omitted body-fat mass, which real storage requires. The fixture was corrected; production validation was not weakened.
- A test claimed `PULL` should survive while advanced routine mode was OFF, even though the existing normalizer only permits `REST`/`PUSH` there. The fixture now enables the valid advanced routine mode; production routine rules were not loosened.
- Today render may add source metadata to a saved draft. Comparing the entire JSON object treated harmless added metadata as data loss. The test now proves that every user-owned date and field remains present and unchanged while allowing additive metadata.
- These corrections follow the project's meaning-first test policy. No test was changed merely to hide a product-state loss.

## Full-backup snapshot rebase hotfix verification

- test-first reproduction before production change: onboarding 19 cases / 2 failed. The two failures independently proved stale A ownership after incomplete B restore and a stale snapshot after completed B restore.
- focused after the one-line restore-boundary change: onboarding 19/19 passed.
- onboarding + smart restore + backup/restore focused: 6 suites / 63 cases / 0 failed; console errors 0; page errors 0.
- incomplete restore fixture verifies B ownership in memory, DOM, localStorage, and a save/reload cycle; all B Records/InBody/Today drafts/cardio presets/meal templates/schedules/defaults remain deep-equal and A sentinels are absent.
- records-only import remains snapshot-preserving, completed full restore leaves snapshot null, and existing reset behavior remains null-clearing.
- `docs-policy`: passed, including the restore-body order guard that requires B Settings application -> snapshot clear -> incomplete B recapture.
- smoke: 21 suites / 212 cases / 0 failed.
- core: 64 suites / 758 cases / 0 failed.
- UI: 26 suites / 202 cases / 0 failed.
- mobile 390x844: 20 suites / 164 cases / 0 failed.
- macro-policy: 30 suites / 240 cases / 0 failed.
- DailyCoach focused: 1 suite / 83 cases / 0 failed.
- full: 140 suites / 1,420 cases / 0 failed; console errors 0; page errors 0.

## Result log

Read basis:
- `docs/00_current_truth/00_READ_FIRST.txt`
- `docs/00_current_truth/02_macro_range_current_truth.txt`
- `docs/00_current_truth/04_document_status_index.txt`
- `docs/v8.3.1_onboarding_first_run_flow_decision_2026-07-10.md`
- `docs/v8.3.1_carb_fat_exchange_range_consistency_audit_design_2026-07-10.md`
- `docs/v8.3.1_carb_fat_exchange_joint_allocation_model_decision_2026-07-10.md`
- `docs/v8.3.1_carb_fat_joint_allocation_model_implementation_2026-07-10.md`
- `docs/v8.3.1_carb_fat_joint_allocation_model_qa_closeout_2026-07-10.md`
- `docs/v8.3.1_user_facing_range_explanation_copy_decision_2026-07-09.md`
- `docs/v8.3_dailycoach_copy_tone_decision_2026-07-08.md`
- initialization/storage/calculation/backup/reset/test paths in `index.html`
- joint allocation, recent skew limiter, adaptive target, and DailyCoach signal/decision paths in `index.html`

Accepted:
- onboarding as a calculation-truthfulness gate;
- four-state classification;
- required Settings ownership before personalized output;
- optional body composition with explicit authority;
- complete backup/reset/re-entry/mobile/focus scope;
- equal and stable Settings header actions without changing their commands or copy;
- explicit fresh/re-entry/existing-data completion ownership;
- future adaptive-allocation Coach explanation as a semantic-signal feature, not an immediate copy patch.
- full backup restore as a Settings/Today ownership replacement boundary that must rebase an incomplete B snapshot and clear a completed B snapshot.

Rejected:
- data existence as profile confirmation;
- UI-only modal over fictional defaults;
- global rewriting of normal Today ownership;
- unconditional medium/adaptive ON/REST reset on every onboarding submit;
- silently retaining 15%/35kg;
- changing product copy merely to satisfy an exact-string test;
- treating recent 3-day/7-day pressure as a hard consecutive-day bucket;
- saying repeated high carbs are inherently bad or that seven days always means adjustment is impossible.
- trusting the existing "hotfix complete" document label without replaying the A -> B state transition;
- broad restore-framework/helper redesign for a one-boundary lifetime defect.

Integrated:
- existing Settings/Today ownership model where it remains valid;
- existing low-authority BMR/body context;
- existing backup version with an optional backward-compatible field;
- existing warning/clamp contract for time totals;
- existing routine/profile normalizers for incompatible profile changes only;
- existing protein-reserved same-kcal joint allocation and continuous recency-weighted excess pressure as future Coach evidence.
- existing `prepareIncompleteOnboardingState()` recapture path, records-only ownership behavior, full reset behavior, and completed-restore classification.

Deferred:
- joint Today score/guidance surface ownership inventory;
- score-card semantic cleanup, DailyCoach semantic v2 phase 1, selectable tone/persona, tooltip/glossary, and app-wide copy batch 2;
- score tuning and adaptive limiter work;
- old record cleanup/reset/fallback.

Forbidden:
- score/macro anchor changes;
- DailyCoach/adaptive-target copy implementation in this hotfix;
- scoreDeltaPreview;
- hidden old-record mutation;
- version/tag movement as part of feature code.
- backup schema/version change, onboarding UI change, and new audit/decision/closeout documents for this narrow hotfix.

Next:
- docs-policy와 모든 required regression profile은 통과했다. commit 직전 `origin/master`가 audited base `3842fbc`와 같은 것도 재확인했으므로 branch push와 조건부 no-ff master merge/push로 이어간다;
- after onboarding is closed, keep the ordered joint score/guidance audit above as the next planning candidate rather than implementing the score, adaptive Coach, tone, or tooltip ideas from this branch.
